# services/mcp-server/.env.example
# Copy to .env and fill in a real token minted from services/api/scripts/mint_ingest_token.py.
# The .env file is gitignored — never commit real tokens.

AGENTRISE_INGEST_TOKEN=hrn_pat_replace_me_with_a_real_token_64_hex_chars
AGENTRISE_INGEST_BASE_URL=http://127.0.0.1:8000
