### session

```yaml
produced_by: [AUTH-01, AUTH-05, AUTH-06, AUTH-07]
consumed_by: [AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, SHP-01, SHP-02, SHP-03]
shape:
  mechanism: "FastAPI-owned Keycloak OIDC code exchange (Authlib's starlette_client.OAuth) with bearer-JWT bridging — no shared-origin session cookie. Routes: GET /auth/login (redirect to Keycloak's authorization endpoint), GET /auth/callback?code=&state= (code exchange), POST /auth/refresh {refresh_token} (refresh grant), POST /auth/dev-bypass {role?, email?, programs?} (registered only for an allow-listed ENVIRONMENT, see dev_bypass below). Login/callback/refresh return Keycloak's token-endpoint response verbatim as a JSON body `{access_token, refresh_token, expires_in}` — never a Set-Cookie header. `redirect_uri` (used identically by both /auth/login's authorization request and /auth/callback's token exchange, per OAuth's exact-match requirement) is configuration-driven via `OIDC_REDIRECT_URI` (unset falls back to the API's own callback URL, this story's original default); AUTH-05 sets it to the frontend's own callback route, so Keycloak redirects the browser to the frontend first, which relays `code`/`state` server-to-server to this same GET /auth/callback to complete the exchange — no client-side page ever parses the callback response."
  token_origin: "the JWT is Keycloak's own signed access_token from its token endpoint (role/groups claims as issued by the IdP); FastAPI does not mint its own session token. Realm: Apexon (https://lab.apexonlab.com/apexonlogin/realms/Apexon)."
  frontend_storage: "frontend holds access_token + refresh_token server-side, in a Next.js Route Handler/server-action-owned store (httpOnly cookie scoped to the frontend's own origin, or in-memory on the Node server) — never localStorage, never a client-JS-readable cookie or storage API"
  transport: "frontend attaches `Authorization: Bearer <access_token>` on every request to the FastAPI API origin; a shared FastAPI dependency (`app.core.auth.get_current_user`) validates the JWT signature per request against Keycloak's JWKS (fetched/cached by `app.auth.jwks`, 3600s TTL, fetch-once-per-unrecognized-kid on a verification failure — no server-side session store) and derives `user_id=sub, email=email, role=<realm/client role claim>, groups=groups` from its claims. CORS (`app.main`) allow-lists `settings.cors_origins` explicitly with `allow_credentials=False`, `allow_methods=[GET,POST,OPTIONS]`, `allow_headers=[Authorization,Content-Type]` — no credentials/cookie mode, since auth rides the Authorization header."
  refresh: "access_token lifetime is realm-driven — implementation reads `expires_in` from Keycloak's response and never hardcodes it (Apexon realm's documented default is 300s, a test-fixture value only). The frontend's server-side store calls POST /auth/refresh with the refresh_token proactively at a 60s-remaining skew or reactively on a 401 from any API call; FastAPI exchanges it via Keycloak's refresh_token grant and returns a new access/refresh pair (200). Any non-2xx Keycloak response (expired/revoked/generic error) maps to 401, not a passthrough of Keycloak's raw status, so the frontend uniformly redirects through the Keycloak login flow (see 'Session expiry' UX case, PRD §Persona Flows)."
  fields: { user_id: str, email: str, role: str, name: str | None, roles: [str, ...], groups: [str, ...], programs: [str, ...] }   # `groups` is the raw IdP claim verbatim, whatever Keycloak sends (empty when unsent, and absent entirely on a dev-bypass token); the retired "program-<slug>" convention was AUTH-01's prefix encoding, deleted by AUTH-06. `name`/`roles` FILLED AT PLAN TIME (AUTH-07, docs/features/AUTH-07/PLAN.md T-06) -- both additive, per `display_name_note`/`role_selection_note` below: `name` is `claims.get("name")` -> (`given_name`+" "+`family_name`, both present+non-empty) -> `preferred_username` -> `None`; `roles` is every surviving `realm_access.roles` entry (post `_is_keycloak_system_role` filtering) in the token's ORIGINAL order, for audit only -- `role` (singular) stays the precedence-selected winner every existing consumer already reads.
  program_membership_source: "AUTH-06 (shipped): `session.programs` is the distinct set of `program_id` values matched from `program_roster` (ING-10, `program-roster-schema`) by the session's `email` -- the roster's primary address or any `aliases[]` row, `removed_at IS NULL` only. `get_current_user` (app/core/auth.py) resolves it via `app.core.program_roster_resolver.ProgramRosterResolver`: one query, per-process-cached 300s (`time.monotonic()`-keyed), `asyncio.Lock`-guarded double-checked re-read bounding concurrent cache-miss callers for the same email to a single query, `asyncio.wait_for(timeout=3.0)`-bounded. `program_roster` is the ONLY table read on this path -- `user_roles` and `program_members` are never consulted (AC-9). Keycloak/OIDC authenticates the user and resolves their persona; it does NOT decide which programs they belong to, and onboarding a program requires no Keycloak group or role. Cache invalidation is TTL expiry only -- no push-invalidation from ING-10's `POST /api/ingest/manifest` in this story's scope (documented limitation, research Risk #2); a roster re-push is visible after the next per-worker/per-process 300s expiry, or immediately after an app restart."
  program_group_parsing: "DELETED by AUTH-06 -- `_parse_programs`, `Settings.program_group_prefix`/`PROGRAM_GROUP_PREFIX`, and the `groups` entry in `OIDC_SCOPE`'s default are removed from the codebase, and README.md's Keycloak `groups` client-scope + Group Membership mapper guidance is removed with them. Kept here only as historical record of what AUTH-01 originally shipped and AUTH-06 retired: each string in the groups claim starting with `program_group_prefix` (default \"program-\") contributed its remainder as a program-membership entry (e.g. \"program-alpha\" -> \"alpha\"); non-matching groups were dropped; an absent/empty groups claim yielded an empty list, never an error. The `groups` field in `fields` above stays on the session (still the raw, verbatim IdP claim, populated whenever Keycloak sends it) -- it simply no longer feeds `programs`, and `OIDC_SCOPE`'s default no longer requests it (a deployment relying on `groups` for a purpose other than program-membership scoping must re-add it to its own `OIDC_SCOPE` value)."
  dev_bypass_programs: "AUTH-06: `POST /auth/dev-bypass` mints `programs` as its own top-level JWT claim (`payload.programs` verbatim, default `[]`) instead of prefix-encoding it into `groups` -- a dev-bypass token no longer carries a `groups` claim at all. `get_current_user` reads this claim directly, with ZERO `program_roster` query, if and only if `kid == DEV_BYPASS_KID` -- the same discriminator `_claims_options` already branches on (`app/core/auth.py:172`). Never on the mere presence of a `programs` claim (a Keycloak-issued token forging one is still resolved from the roster, never from its own claim) and never on `payload.email` (caller-supplied on the dev-bypass request body, forgeable) -- widening this check to either rejected alternative is a security regression, not a refactor."
  unchanged_by_auth06: "the open-aggregate RBAC model (RTM Decisions 2026-08-26) is NOT reversed -- only the SOURCE of the program list moves. `program_visibility` remains a veto gate passing for any authenticated session; `GET /api/overview/program-detail/{program_id}` remains unscoped; `individual_usage_visibility`/`governance_visibility` remain the real gates. `rbac-checks` and its 16 consumers need no change."
  config_completeness_gate: "if any of oidc_client_id/oidc_client_secret/oidc_issuer is unset, /auth/login and /auth/callback return 501 via the standard error envelope at request time — app startup never crashes on incomplete OIDC config (the unset-triple is also the feature flag: set them to go live, unset to back out without a redeploy)"
  dev_bypass: "POST /auth/dev-bypass issues a token through the same {access_token, refresh_token, expires_in} Bearer shape without contacting Keycloak. Gated fail-closed by allow-list membership, not a `!= \"production\"` deny-check: the router is registered in `app.main` only when `settings.environment` (lowercased at Settings load) is a member of the pinned non-production set {local, development, dev, test, ci} — every other value, including \"production\", \"prod\", \"staging\", and any typo, leaves the route unregistered (404), not merely unauthorized. Dev-bypass never emits the dashboard_login (or any audit-log) event — the logging call is skipped on that code path entirely, not filtered downstream."
  observability: "a successful /auth/callback or /auth/refresh (never dev-bypass) emits one structlog JSON `dashboard_login` event carrying `user_id` only — no email, name, or token values, ever."
  frontend_ownership_note: "AUTH-01 shipped only the backend half of this contract (routes, JWT validation, CORS allow-list, dev-bypass, observability). `frontend_storage`, `refresh` (the client-triggering side: proactive 60s-skew + reactive 401), and the client-forwarding side of `transport` (attaching `Authorization: Bearer <access_token>` on every FastAPI call) are produced by AUTH-05 — apps/web had zero auth code until then. AUTH-05 also consumes this contract: it is built against AUTH-01's already-shipped /auth/login, /auth/callback, /auth/refresh, /auth/dev-bypass routes. For `frontend_storage`, AUTH-05 resolves the contract's either/or to the httpOnly-cookie option only (survives Node restarts, no shared state needed across frontend instances) — the in-memory alternative this field also names is not what AUTH-05 builds. AUTH-05's only services/api touch is a config value, not code: `OIDC_REDIRECT_URI` (Settings field + blank `.env.example` entry already shipped under AUTH-01, D-11/AF-08 — `app/auth/oidc.py::_resolve_redirect_uri` already reads it) gets set to the frontend's callback route; AUTH-05 also documents the resulting additional Keycloak valid-redirect-URI requirement and this env var in README.md (both currently silent on it)."
  display_name_note: "CONFIRMED by user 2026-09-10 (no realm or mapper change). AUTH-07 (docs/prd/cio-shell-logout-and-persona-resolution.md) adds `name: str | None` to `CurrentUser` -- and hence to the `fields` set above, which is left verbatim here and updated by AUTH-07 when it plans, per the same do-not-rewrite-contract-field-text precedent. The signed-in user's display name, parsed from the same verified claims `role`/`groups` already come from (`name` -> `given_name` + `family_name` -> `preferred_username` -> None; never composed from `email`, never a placeholder). Additive and fail-soft, matching `_parse_role`/`_parse_groups`' existing no-exception-on-absent-claim discipline: nothing today reads `name`, so no consumer changes. `OIDC_SCOPE`'s shipped default already requests `profile`, so no realm, client-scope, or mapper change is required -- and none is wanted: AUTH-06 deliberately removed the last realm-administration dependency and this must not reintroduce one. A dev-bypass token carries no profile claims, so `name` is None there by construction. Exposed to the frontend by `session-identity-api` (api.md) and nowhere else; `observability`'s dashboard_login PII invariant is unchanged -- `name` must never enter a log line."
  role_selection_note: "AUTH-07 (same source, merged scope) REDEFINES how `role` is chosen when the token carries several. SUPERSEDES `transport`'s 'role=<realm/client role claim>' clause above (left verbatim per the 2026-09-04 do-not-rewrite-contract-field-text precedent) and `app/core/auth.py::_parse_role`'s shipped 'the first surviving entry, in original list order' rule, which Keycloak does not guarantee the order of -- observed live 2026-09-10 on a real token carrying `realm_access.roles = ['Architect','Developer','developer']`, i.e. the same user could resolve to a different persona, and therefore a different dashboard, between logins with no account change. After AUTH-07: `roles: list[str]` joins the `fields` set above -- again left verbatim here, filled in by AUTH-07 when it plans (every surviving entry after `_is_keycloak_system_role` filtering, verbatim order preserved for audit) -- and `role` becomes the single WINNER of an explicit precedence rule evaluated over all of them -- never a positional accident. `role` stays a `str` and stays one of the token's own roles, so `rbac-checks` and its 16 consumers are untouched (they take `CurrentUser` and resolve persona through persona-resolver; only the resolver call sites read `.role`). AUTH-01's shipped ACs correctly describe what was built and are NOT retroactively rewritten -- this note is what supersedes them (same discipline as the 2026-09-08 groups-claim supersession)."
  logout_note: "CONFIRMED by user 2026-09-10 as FULL RP-INITIATED END-SESSION -- not a local cookie clear. AUTH-07 (same source, merged scope) adds session TERMINATION, the half this contract has never had: users authenticate through Keycloak and today have no way to end the session (no occurrence of logout/signout anywhere in apps/web/src or services/api/app). Route-only, no new UI -- DECIDED by user 2026-09-10, because no mockup's brand bar contains an `<a>`, a `<button>`, or a `cursor:pointer` (the signed-in identity is display-only) and CLAUDE.md forbids inventing UI. App-wide by construction, not a per-page or per-persona feature: `dashboard_session` is written at `path: '/'` by apps/web/src/lib/tokenStore.ts and is the single session for every route. Shape: a new additive FastAPI `GET /auth/logout` 302s to `{issuer}/protocol/openid-connect/logout` (a new `_LOGOUT_PATH` constant alongside the shipped `_AUTHORIZE_PATH`/`_TOKEN_PATH`, carrying `client_id` + `post_logout_redirect_uri`, NOT `id_token_hint` -- `/auth/callback` returns `{access_token, refresh_token, expires_in}` and must not start returning `id_token`, which would reopen AUTH-01's shipped response shape); a new Next.js `GET /logout` Route Handler clears the cookie via tokenStore's existing `clearSession()` then relays that redirect to the browser, mirroring `/login`'s shipped server-to-server `redirect: 'manual'` relay verbatim. No shipped route is modified. Known and accepted limitation to state as an acceptance criterion, not to engineer around: clearing the cookie plus ending the Keycloak SSO session invalidates the refresh token, but an already-issued access token stays valid until its own expiry (realm default 300s) -- inherent to stateless bearer JWTs with no server-side session store (`transport`, above). WHY full end-session and not just `clearSession()`: the user's requirement is that logout 'redirect to login page', and with the Keycloak SSO session left alive `/login` silently signs the user straight back in -- a local cookie clear therefore does not satisfy the requirement, it only hides the session from this one browser profile for one hop. `post_logout_redirect_uri` is the frontend's own `/login` and MUST be registered under the Keycloak client's **Valid post logout redirect URIs** -- a README deliverable carried by AUTH-07, sitting alongside the Audience-mapper note being added to the same README section out-of-pipeline."
```

### persona-resolver

```yaml
produced_by: [AUTH-02, AUTH-07]
consumed_by: [AUTH-03, AUTH-07, SHP-01]
shape:
  mechanism: "PersonaResolver instance on app.state.persona_resolver, constructed synchronously in create_app() (mirrors JwksCache, AUTH-01 D-07). Per resolve() call: Tier-1 env-JSON dict (Settings.persona_role_map, parsed once at Settings load from PERSONA_ROLE_MAP; unparseable -> logged warning + treated as empty, falls through) -> Tier-2 YAML (services/api/config/persona_role_map.yaml, loaded once at PersonaResolver.__init__; missing/malformed file is a startup error that propagates uncaught through create_app(), aborting Uvicorn boot -- fail-fast, no lifespan wrapper) -> Tier-3 Postgres persona_config table (role PK point lookup via an injectable session_factory defaulting to app.core.db.SessionLocal, 3.0s asyncio.wait_for timeout). All 3 tiers empty raises PersonaNotFoundError(role) -- fail-closed, never a default persona."
  interface: "async def resolve(self, role: str) -> str  (raises PersonaNotFoundError | PersonaResolutionError)"
  input: "role: str -- the session contract's role field (docs/requirements/auth.md#session, CurrentUser.role)"
  output: "persona: str -- typically one of cio | architect | developer | product-manager | engineering-manager, but the resolver is fully data-driven: any string an ops-configured tier maps a role to is returned verbatim, no hardcoded persona enum or exec-role branch"
  cache: "per-role in-process dict {role: (persona, expiry_ts)}, 300s TTL, asyncio.Lock-guarded read+miss+write critical section (no threading.Lock -- no synchronous call path exists in this story's or its consumers' scope). Per-worker/per-process, no cross-worker coherence; Postgres is the source of truth; an app restart is the ops-level hard-refresh lever."
  errors: "PersonaResolutionError(role, reason) is the base class (Tier-3 timeout or connectivity failure); PersonaNotFoundError(role) is the fail-closed subclass raised when all 3 tiers miss. Callers (AUTH-03) must catch and decide fail-request vs. deny-and-log."
  observability: "every resolve() call (cache hit or miss) emits logger.info('persona_mapping_loaded', extra={role, persona, tier, timestamp}) with tier in {tier-1-env, tier-2-yaml, tier-3-postgres}; a FRESH tier-3 query additionally carries tier3_latency_ms (the measured query time, rounded to 3dp) -- a warm cache hit whose stored tier is tier-3-postgres deliberately omits it, since no query ran and re-emitting the original measurement would dilute the p95 the 200ms alert is built on; presence of the field is therefore the reliable signal that a query actually executed (AUTH-02 D-11). No user_id/email/groups/session context is ever included (PII invariant, unit-tested)."
  note: "config-driven additional executive roles (FR-SH-20) resolve to `cio` via any of the 3 sources, no code change -- the resolver contains zero `if role in {...}` branches, verified by a dedicated test case (AUTH-02-TC-07) using a custom slug, not a hardcoded exec-role example."
  case_insensitivity_note: "AUTH-07 (docs/prd/cio-shell-logout-and-persona-resolution.md, merged scope) makes tier lookup CASE-INSENSITIVE across all three tiers and the cache key. SUPERSEDES the implicit exact-match semantics of `mechanism` above (left verbatim per the 2026-09-04 precedent). Observed live 2026-09-10: `Architect` does not match the shipped Tier-2 `architect: architect` entry, so a correctly-provisioned user fails CLOSED -- and the realm genuinely holds both `Developer` and `developer` as separate roles, so near-duplicate names are a live condition, not hypothetical. Normalization is LOOKUP-SIDE ONLY: the incoming role and each tier's keys are casefolded for comparison, and no stored value is rewritten -- `PERSONA_ROLE_MAP`, `persona_role_map.yaml`, and the `persona_config` table keep their contents byte-identical, and the returned persona is still the tier's own value verbatim. BACKWARD COMPATIBLE by construction: every mapping that resolves today still resolves to the same persona; the only behavioural delta is that STRICTLY MORE roles now match, so no working configuration can break. Two consequences worth pinning at plan time: a tier whose keys differ only by case is now ambiguous and must fail loudly rather than pick one silently, and the cache key must be the normalized form or the same user alternates between a hit and a miss."
  deterministic_selection_note: "AUTH-07 also makes selection deterministic when a token carries several mappable roles -- the compounding half of the same defect (auth.md#session role_selection_note). Note the ORDER these two fixes must ship in, which is why they were never separable: making matching case-insensitive ALONE is strictly worse than today, because `['Architect','Developer','developer']` currently fails closed (visible, if indistinguishably so) whereas all three would then map and claim order would silently decide which dashboard the user sees. CONFIRMED by user 2026-09-10 (RTM Decisions, same date): resolve every surviving role and pick the winner by an EXPLICIT persona precedence -- default `cio > architect > product-manager > engineering-manager > developer` -- carried through the SAME 3-tier config mechanism this resolver already owns, so ops can change it without code. Rejected alternatives, all deterministic but wrong for a different reason: alphabetical order (stable but arbitrary -- encodes no intent), least-privilege-wins (not well-ordered here, since `governance_visibility` excludes `cio` -- personas are views, not a privilege ladder), and fail-closed-on-ambiguity (would lock out the real, correctly-provisioned account this defect was found on). EXPLICITLY REJECTED BY THE USER, and the reason AUTH-07's resolver scope must not be re-scoped later: resolving a role list to a persona SET, so a multi-role user could reach every dashboard their roles allow. ONE PERSONA DECIDES ONE LANDING PAGE. `resolve()` keeps returning a single `str` (`interface`, above), `CurrentUser.role` stays a single `str`, and no consumer gains a set/list persona concept -- do not model one. **RESOLVED 2026-09-10 (user decision, taken at the /arh-research AUTH-07 gate) -- previously flagged here as an open, must-settle-before-planning item:** the concern was that this precedence order could REDUCE access for a multi-role user, deterministically, because `governance_visibility` (rbac-checks) allows `architect | product-manager | developer` and EXCLUDES `cio`, so a user holding both a `cio`-mapped and a `developer`-mapped role resolves to `cio`. It does not, and no mitigation is required: the allowed-persona set MIRRORS the per-persona page layout rather than encoding a privilege ladder. `Compliance & guardrails` and `Organization Constitution` appear on exactly the Architect, Developer and Product Manager mockups and on neither the CIO Portfolio nor the Engineering Manager mockup -- the same split as `_GOVERNANCE_PERSONAS` -- so a `cio`-resolved user lands on a page that carries no governance panel to lose. Consequently `_GOVERNANCE_PERSONAS` is NOT widened, `governance_visibility` does NOT become `CurrentUser.roles`-aware, and `rbac-checks` and its 16 consumers still need NO change (consistent with `unchanged_by_auth06` above). A deployment that wants a dual-role user landing on an Architect/Developer page re-orders precedence through this resolver's own 3-tier config -- an ops change, no code (AUTH-07 AC-20). Verified against the six mockups, not inferred (RTM Decisions 2026-09-10, resolution entry same date)."
  unmapped_role_observability_note: "AUTH-07 closes the third half of the same live finding: an INDISTINGUISHABLE failure. The observed result was `rbac_check_org_access outcome=denied` with no `persona` field and no `persona_mapping_loaded` line at all -- byte-identical to a genuinely unmapped role, with nothing anywhere saying 'a role was present but matched no tier'. `observability` above emits `persona_mapping_loaded` only on a SUCCESSFUL resolve, so the miss path is silent by omission. AUTH-07 adds a distinct event on the all-three-tiers-miss path carrying the attempted role(s) and the tiers consulted. The PII invariant is unchanged and binding: role slugs and tier names only -- never user_id, email, name, or groups."
```

### rbac-checks

```yaml
produced_by: AUTH-03
consumed_by: [AUTH-04, OVW-01, OVW-02, OVW-03, OVW-04, PGD-01, PGD-02, PGD-03, PGD-04, PGD-05, PGD-06, SHP-02, SHP-03, SHP-04, SHP-05, SHP-06]
shape:
  module: "app.core.rbac (services/api/app/core/rbac.py) — pure in-process functions, no route surface of its own; each of the 16 consumers imports directly, e.g. `from app.core.rbac import org_access`"
  checks:
    - org_access: "cio only; org-wide /api/overview/* endpoints"
    - program_visibility: "open-aggregate — any authenticated session; program id not used for gating (A-004)"
    - individual_usage_visibility: "self always; else cio only"
    - member_in_program_visibility: "program_visibility AND (self OR cio)"
    - governance_visibility: "architect | product-manager | developer only (cio, engineering-manager excluded); includes developer from day one per FR-DV-05, no later scope-reversal story needed"
  signatures: |
    async def org_access(current_user: CurrentUser) -> None
    async def program_visibility(current_user: CurrentUser, program_id: str) -> None
    async def individual_usage_visibility(current_user: CurrentUser, target_user_id: str) -> None
    async def member_in_program_visibility(current_user: CurrentUser, program_id: str, target_member_id: str) -> None
    async def governance_visibility(current_user: CurrentUser, program_id: str | None = None) -> None
    # all five: async, current_user is always the first positional argument (AUTH-03-FR-5,
    # AUTH-03-TC-25 asserts this via inspect.iscoroutinefunction + inspect.signature).
    # Names, parameter order, and count are locked from AUTH-03's Product Gate forward — a
    # post-hoc rename requires a coordinated migration across all 16 consumers, not a silent
    # signature change.
  behavior: "each check either returns None (authorized) or raises fastapi.HTTPException(status_code=403) (denied) — never a bool return, never a 5xx for a denial. PersonaResolutionError and PersonaNotFoundError (both from app.core.persona_resolver, AUTH-02) are caught at every call site that resolves persona and converted to HTTPException(403); neither ever propagates to the caller (AUTH-03-FR-1, fail-closed, zero default-permit)."
  cascades: "member_in_program_visibility calls program_visibility(current_user, program_id) first — a denial there short-circuits immediately, without evaluating self-or-cio (AUTH-03-FR-4). governance_visibility, when given a program_id, evaluates the persona gate (AC6) first, then calls program_visibility(current_user, program_id) only if that passed — both must pass (AUTH-03-FR-4)."
  logging: "every check outcome logged (rbac_check_org_access, rbac_check_governance_visibility, individual_view_denied, member_view_denied); the two rbac_check_* events record both authorized and denied outcomes, the two *_view_denied events record denials only. rbac_check_governance_visibility extends NFR-011's set per AUTH-03 Decision log 2026-08-31. Exact field allowlist per event and outcome semantics: AUTH-03-FR-2 (docs/features/AUTH-03/REQUIREMENTS.md). program_visibility emits no event of its own (no denial branch to log)."
```

### ingest-token-auth

```yaml
produced_by: ING-01
consumed_by: [ING-02, ING-03, ING-07]
shape:
  token_format: "hrn_pat_ + 32 CSPRNG bytes hex (64 hex chars, secrets.token_hex(32)), printed once, never stored raw"
  storage: "ingest_tokens.token_hash = hashlib.sha256(raw).hexdigest(); label, user_email, allowed_program_ids (array; wildcard is the single element \"*\"; empty array = allow-all); expires_at/revoked_at null at mint"
  auth_check_function: "app.core.ingest_auth.get_ingest_token(program_id: str, credentials: HTTPAuthorizationCredentials | None = Depends(HTTPBearer(auto_error=False)), session: AsyncSession = Depends(get_db)) -> IngestToken"
  auth_check_behavior: >
    SHA-256-hashes the bearer token, looks up ingest_tokens.token_hash (unique index).
    401 reason=missing (no header or non-Bearer scheme) | reason=unknown (no matching hash) |
    reason=revoked (revoked_at is not null) | reason=expired (expires_at is not null and <= now()).
    403 reason=scope when the row is active but ING-01-FR-3's check fails. On pass, returns the
    resolved IngestToken row. program_id is a caller-supplied parameter of the dependency itself
    (resolved by FastAPI the same way any route parameter of that name is) -- never read off the
    token row (ING-01-FR-6 / DECISIONS.md D-01).
  scope_semantics: "empty allowed_program_ids == allow-all (unscoped, ADR-0006 accepted default); [\"*\"] == explicit wildcard; otherwise exact-string membership test (no UUID-specific parsing)"
  lifetime: "expires_at defaults to null (no expiry); revocation via revoked_at is the containment mechanism"
  mint_surface: "uv run python scripts/mint_ingest_token.py --label <str> --user-email <str> [--program-ids <comma-separated ids or \"*\">] (stdlib argparse, no new dependency); --label/--user-email required, --program-ids optional. Each comma-separated element is whitespace-trimmed and empty elements dropped (\"a, b\" -> [\"a\", \"b\"]; \"*\" -> [\"*\"]); allowed_program_ids=[] (allow-all) is reachable ONLY by omitting --program-ids entirely (DECISIONS.md D-04, D-05a) -- a supplied value that collapses to zero usable elements after trimming (e.g. \"\", \" \", \",\") is a usage error, never allow-all. Exits 0 with one raw-token stdout line and one committed row on success; exits non-zero with no DB write and no token printed on any argparse failure, the input-validation usage-error case above, or a DB failure"
  log_event: "ingest_token_auth_failed, emitted from app.core.ingest_auth, INFO level, once per denial (never on success); required fields exactly {token_id, reason, program_id, timestamp}, optional {} -- token_id is null when no row was resolved (reason missing|unknown), else ingest_tokens.id; reason in {missing, unknown, revoked, expired, scope}; never user_email, raw token, or token_hash"
  authority: "ADR-0006 - supersedes story AC-1's 24-byte figure per security-baseline CSPRNG >= 32 bytes; DECISIONS.md D-01..D-05a (docs/features/ING-01/DECISIONS.md) settle the implementation-surface choices ADR-0006 leaves open"
```
