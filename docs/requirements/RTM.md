# Requirements Traceability Matrix
Source hash: b3cd0523ae59

| ID | Title | Type | Parent | Pri | Size | Depends-on | Contract | Source | Tracker | Status |
|----|-------|------|--------|-----|------|------------|----------|--------|---------|--------|
| BED | Backend Foundation & Data Model | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#1 | — |
| BED-01 | Data model & Alembic migrations (18-table shape) | Story | BED | P1 | L | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#11 | validated |
| BED-02 | Shared API conventions: range validation, pagination, derived-value computation, formatting | Story | BED | P1 | M | BED-01 | db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#12 | validated |
| BED-03 | Rollup rebuild engine (idempotent upsert + full rebuild) | Story | BED | P1 | L | BED-01 | db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#13 | validated |
| BED-04 | Ingestion freshness accessor | Story | BED | P1 | S | BED-01 | db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#14 | validated |
| AUTH | Authentication & RBAC | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#2 | — |
| AUTH-01 | Keycloak OIDC sign-in, bearer-JWT session bridging, dev-bypass | Story | AUTH | P1 | L | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#15 | validated |
| AUTH-02 | Persona resolver (3-tier, cached) | Story | AUTH | P1 | M | AUTH-01, BED-01 | session, db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#16 | validated |
| AUTH-03 | RBAC check library (org-access, program-visibility, individual-usage, member-in-program, governance) | Story | AUTH | P1 | M | AUTH-01, AUTH-02 | session, persona-resolver | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#17 | validated |
| AUTH-04 | GET /api/programs persona-scoped list | Story | AUTH | P1 | S | AUTH-01, AUTH-03, BED-01 | session, rbac-checks, db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#18 | validated |
| AUTH-05 | Frontend session/token layer (server-side store, bearer forwarding, refresh) | Story | AUTH | P1 | M | AUTH-01 | session | raw text | pratikpawar009/Dashboard#204 | validated |
| OVW | CIO Adoption Overview | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#3 | — |
| OVW-01 | Org summary cards + adoption indicator | Story | OVW | P1 | M | BED-01, AUTH-03, BED-02, BED-04 | db-schema, rbac-checks, api-conventions, freshness-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#19 | validated |
| OVW-02 | Org token trend chart (12-month) | Story | OVW | P1 | M | BED-01, AUTH-03, BED-02 | db-schema, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#20 | validated |
| OVW-03 | Org MAU-by-role stacked chart (12-month) | Story | OVW | P1 | M | BED-01, AUTH-03, BED-02 | db-schema, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#21 | validated |
| OVW-04 | Program board | Story | OVW | P1 | M | BED-01, AUTH-03, BED-02 | db-schema, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#22 | validated |
| PGD | CIO / EM Program Detail | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#4 | — |
| PGD-01 | Program Detail page shell: header, summary cards, switch/back nav | Story | PGD | P1 | M | BED-01, AUTH-03, AUTH-04, BED-02 | db-schema, rbac-checks, programs-api, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#23 | validated |
| PGD-02 | Daily program token trend chart | Story | PGD | P1 | M | BED-01, AUTH-03, BED-02 | db-schema, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#24 | validated |
| PGD-03 | Releases list (paginated) | Story | PGD | P1 | M | BED-01, AUTH-03, BED-02 | db-schema, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#25 | validated |
| PGD-04 | Program command activity | Story | PGD | P1 | S | BED-01, AUTH-03, BED-02 | db-schema, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#26 | validated |
| PGD-05 | Project team table + per-member usage popup (FR-AUTH-08) | Story | PGD | P1 | L | BED-01, AUTH-03, BED-02, SHP-02 | db-schema, rbac-checks, api-conventions, personal-usage-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#27 | validated |
| PGD-06 | Daily session-time chart w/ member filter | Story | PGD | P1 | M | BED-01, AUTH-03, BED-02 | db-schema, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#28 | validated |
| SHP | Shared Persona Components | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#5 | — |
| SHP-01 | Persona header/context shell | Story | SHP | P1 | S | AUTH-01, AUTH-02 | session, persona-resolver | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#29 | validated |
| SHP-02 | Personal usage panel: cards + daily token chart + session-time chart + commands | Story | SHP | P1 | L | BED-01, AUTH-01, AUTH-03, BED-02 | db-schema, session, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#30 | validated |
| SHP-03 | Personal session-wise usage list (paginated) | Story | SHP | P1 | M | BED-01, AUTH-01, AUTH-03, BED-02 | db-schema, session, rbac-checks, api-conventions | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#31 | validated |
| SHP-04 | Artifacts generated panel | Story | SHP | P1 | S | BED-01, AUTH-03 | db-schema, rbac-checks | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#32 | validated |
| SHP-05 | Compliance & guardrails panel | Story | SHP | P1 | S | BED-01, AUTH-03 | db-schema, rbac-checks | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#33 | validated |
| SHP-06 | Organization Constitution panel | Story | SHP | P1 | S | BED-01, AUTH-03 | db-schema, rbac-checks | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#34 | validated |
| SHP-07 | Extended role/team taxonomy beyond 4 roles | Story | SHP | P3 | M | BED-01, OVW-03, PGD-05 | db-schema, overview-mau-series-api, program-team-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#35 | validated |
| ARC | Architect Dashboard | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#6 | — |
| ARC-01 | Compose Architect Dashboard | Story | ARC | P1 | L | SHP-01, SHP-02, SHP-03, PGD-01, SHP-04, PGD-03, PGD-05, PGD-04, SHP-05, SHP-06, BED-04 | persona-shell, personal-usage-api, personal-sessions-api, program-detail-api, artifacts-api, program-releases-api, program-team-api, program-commands-api, guardrails-api, constitution-api, freshness-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#36 | validated |
| DEV | Developer Dashboard | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#7 | — |
| DEV-01 | Compose Developer Dashboard (full-fidelity governance panels) | Story | DEV | P1 | L | SHP-01, SHP-02, SHP-03, PGD-01, SHP-04, PGD-03, PGD-05, PGD-04, SHP-05, SHP-06, BED-04 | persona-shell, personal-usage-api, personal-sessions-api, program-detail-api, artifacts-api, program-releases-api, program-team-api, program-commands-api, guardrails-api, constitution-api, freshness-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#37 | validated |
| PMD | Product Manager Dashboard | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#8 | — |
| PMD-01 | Compose Product Manager Dashboard | Story | PMD | P1 | L | SHP-01, SHP-02, SHP-03, PGD-01, SHP-04, PGD-03, PGD-05, PGD-04, SHP-05, SHP-06, BED-04 | persona-shell, personal-usage-api, personal-sessions-api, program-detail-api, artifacts-api, program-releases-api, program-team-api, program-commands-api, guardrails-api, constitution-api, freshness-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#38 | validated |
| EMD | Engineering Manager Dashboard | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#9 | — |
| EMD-01 | Compose Engineering Manager Dashboard (program-level, no governance) | Story | EMD | P1 | L | SHP-01, AUTH-04, PGD-01, PGD-02, PGD-03, PGD-04, PGD-05, PGD-06, BED-04 | persona-shell, programs-api, program-detail-api, program-token-trend-api, program-releases-api, program-commands-api, program-team-api, program-session-series-api, freshness-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#39 | validated |
| ING | Activity & Artifact Ingestion | Epic | — | — | — | — | — | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#10 | — |
| ING-01 | Ingest token minting + bearer auth | Story | ING | P1 | M | BED-01 | db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#40 | validated |
| ING-02 | POST /api/ingest/files — activity ingest | Story | ING | P1 | L | ING-01, BED-01, BED-03 | ingest-token-auth, db-schema, rollup-rebuild | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#41 | validated |
| ING-03 | POST /api/ingest/artifacts | Story | ING | P1 | M | ING-01, BED-01 | ingest-token-auth, db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#42 | validated |
| ING-04 | MCP server tool exposure (push_activity / push_artifacts) | Story | ING | P1 | S | ING-02, ING-03 | ingest-files-api, ingest-artifacts-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#43 | validated |
| ING-05 | Copilot Chat activity-hook bridge | Story | ING | P1 | S | ING-04 | mcp-tools | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#44 | validated |
| ING-06 | Manual CLI ingester (dogfooding, self-referential program) | Story | ING | P1 | M | ING-02, BED-03 | ingest-files-api, rollup-rebuild | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#45 | validated |
| ING-07 | Admin GitHub repo-scan endpoint | Story | ING | P1 | M | ING-01, BED-01 | ingest-token-auth, db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#46 | validated |
| ING-08 | Role-sync CLI (Keycloak → reference/audit table) | Story | ING | P2 | M | BED-01 | db-schema | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#47 | validated |
| ING-09 | Scheduled ingestion (cron) | Story | ING | P3 | M | ING-02 | ingest-files-api | docs/prd/ai-sdlc-adoption-dashboards.md | pratikpawar009/Dashboard#48 | validated |

## Decisions

- 2026-08-26 DEV/PM/EM dashboard priority conflict: PRD §4.2 (C-006/007/008) labels these "Should Have" but §6.1 places FR-DV-*/FR-PM-*/FR-EM-* textually inside "Must Have — Launch Blockers". Resolved DEV-01/PMD-01/EMD-01 as P1, following the more granular FR-section placement over the capability-table label. CONFIRMED by user 2026-08-26 (no change).
- 2026-08-26 (reconciled) Auth-bridging topology (Q-006/A-006, PRD §9.1/§9.1 design note): CONFIRMED by user as **bearer-JWT bridging**, overriding the original shared-origin-cookie assumption — frontend (Next.js) and backend (FastAPI) are on separate origins with no shared reverse-proxy. FastAPI still owns the Keycloak OIDC code exchange, but instead of setting a session cookie it hands Keycloak's token-endpoint response (access/refresh JWTs) to the frontend, which stores them server-side (not `localStorage`, not a JS-readable cookie) and forwards `Authorization: Bearer <access_token>` on every FastAPI call. Rewrote the `session` contract in `docs/requirements/auth.md` accordingly (mechanism, transport, refresh flow) and retitled AUTH-01. Checked AUTH-02, AUTH-03, AUTH-04, SHP-01, SHP-02, SHP-03 for cookie-specific assumptions: none of their contract shapes name a cookie or same-origin transport directly (they consume `session`'s decoded fields — `user_id, email, role, groups` — which are unchanged in shape), so only the `session` contract itself and AUTH-01's title needed edits.
- 2026-08-26 RBAC scope (Q-001/A-004, PRD §9.1/§6): CONFIRMED by user as the **open-aggregate model** — any signed-in user may view any program's aggregate data; only individual usage and governance data are gated. Shapes the `rbac-checks` contract's `program_visibility` check (AUTH-03). No change from the original assumption.
- 2026-08-26 FR-DV-05 (developer full-fidelity governance) folded into AUTH-03's `governance_visibility` check as a day-one allowed-persona member (`architect | product-manager | developer`) rather than modeled as a later scope-reversal story — the reference implementation's DEV-02 history doesn't need reproducing in a greenfield build.
- 2026-08-26 FR-SH-11/13/14/15 (program-summary/releases/team/commands reused verbatim on persona dashboards) folded as acceptance criteria into PGD-01/PGD-03/PGD-05/PGD-04 respectively instead of separate SHP stories — same contract, same mechanism, no new interface; ARC-01/DEV-01/PMD-01 depend on the PGD contracts directly.
- 2026-08-26 Splitting rationale (vertical-slice test, not a false split): PGD-01..06 and OVW-01..04 are six/four distinct backend resources, each independently buildable/testable against its own query surface and rollup table, each with its own independent range-toggle refresh (FR-PD-07/11/13/15 each refresh only their own widget, not the whole page) — a real split, not a lifecycle-state peel-out of one endpoint.
- 2026-08-26 Retention policy (Q-003/NFR-014/R-001): CONFIRMED by user — no retention/archival mechanism in this build; `usage_events` stays unbounded per the reference implementation, gap carried forward pending a Data Engineering/Legal decision.
- 2026-08-26 Ingest token capability model (Q-004): CONFIRMED by user — kept to program-id allowlist + `"*"` wildcard only (A-003); no additional scope/capability dimension modeled.
- 2026-08-26 Role taxonomy (Q-005): CONFIRMED by user — current 4-role MAU/team taxonomy kept as the v1 default; extension modeled as deferred P3 story SHP-07 (requires an explicit Alembic migration), not built into the v1 schema.
- 2026-08-26 (reconciled) BED-01 table-count mismatch: PRD prose (§Overview, §8, §8.4 lead-in, §Traceability) says "17-table/model shape" in four places, but PRD §8.4's own itemized enumeration lists 10 rollup + 3 governance + 5 ingestion/auth/system = **18** named tables verbatim (counted directly, matches `db-schema`'s existing itemized list in `docs/requirements/data.md` table-for-table). Per R-007, the itemization — not the prose label — is the acceptance spec, so the "17" prose is the stale figure, not the contract's list. Fixed `db-schema`'s `mechanism` line to say 18 and added a note explaining the prose/itemization split; retitled `BED-01`'s RTM row to "(18-table shape)". PRD source file itself is left untouched (not this pipeline's artifact to edit) — the mismatch is called out in the `acceptance_spec` note instead. `BED-01`'s status reverted `validated` → `draft`: its already-authored story file (`docs/stories/BED-01.md`) states the old "17-table" figure and needs re-authoring against the corrected count.
- 2026-08-26 (reconciled) FR-AUTH-08 (per-member drill-down popup) coverage gap: vertical-slice test applied — does a standalone "popup" story own a distinct mechanism, or is it the same mechanism a sibling already owns? It reuses `personal-usage-api` (SHP-02, `GET /api/personal-usage/{user_id}`, already parametrized by `user_id`) verbatim for its data, and `rbac-checks`' `member_in_program_visibility` check (already defined under AUTH-03, already logged as `member_view_denied`) verbatim for its gate — no new interface is produced either way. Folded as additional acceptance criteria into `PGD-05` (same precedent as the FR-SH-11/13/14/15 fold above: same contract, same mechanism, no new interface) rather than a new `PGD-07` — a standalone story here would produce zero new contract shape, which is the phantom-story smell this schema's `produced_by` rule exists to catch. Retitled `PGD-05` to "Project team table + per-member usage popup (FR-AUTH-08)", added `SHP-02` / `personal-usage-api` to its `Depends-on`/`Contract` (index-aligned 4th pair), resized `M` → `L` for the added cross-epic integration surface, added `PGD-05` to `personal-usage-api`'s `consumed_by` in `docs/requirements/api.md` with an `authz_note` distinguishing its `member_in_program_visibility` gate from SHP-02's own `individual_usage_visibility` gate. `PGD-05`'s status reverted `validated` → `draft`: its already-authored story file (`docs/stories/PGD-05.md`) doesn't cover FR-AUTH-08 and needs re-authoring.
- 2026-09-04 AUTH-05 (frontend session/token layer) added — single story, not split. Vertical-slice test: server-side token store, `Authorization: Bearer` forwarding, proactive (60s-skew) + reactive (401) refresh, and non-2xx→Keycloak-login redirect all share one mechanism (the frontend token lifecycle); happy-path/expiry/error are states of that one lifecycle, not independently buildable slices — same fold precedent as PGD/OVW's non-split of range-toggle states. Source: raw text (this intake run), hashed independently of the header's PRD hash — see note below on why the header `Source hash` line is left untouched.
- 2026-09-04 `session` contract (`docs/requirements/auth.md`) is now multi-producer: `produced_by: [AUTH-01, AUTH-05]`. AUTH-01 shipped the backend routes/validation/CORS half (`mechanism`, `token_origin`, `dev_bypass`, `observability`, `config_completeness_gate`, the server-side half of `transport`); the three fields the task named as unbuilt — `frontend_storage`, `refresh` (client-triggering side), and the client-forwarding half of `transport` — are produced by AUTH-05, which also consumes `session` (added to `consumed_by`) since it's built against AUTH-01's already-shipped routes. Field text itself is unchanged verbatim per the task's derive-don't-invent instruction; only the ownership wiring and one clarifying `frontend_ownership_note` were added.
- 2026-09-04 (OPEN QUESTION, best judgment applied) Login/callback hand-off mechanism: `/auth/callback` returns a JSON body directly to whoever calls it — never a redirect, never a `Set-Cookie` (README, session contract) — and README's Keycloak-client section pins the registered redirect URI to the API's own origin (`http://localhost:8000/auth/callback`), not a frontend URL. Taken literally, a raw browser navigation ends the OAuth dance by rendering JSON, never reaching the frontend's server. Best judgment (not settled by any of the three named sources, so not decided silently): AUTH-05 implements the exchange via Next.js Route Handlers — `/login` calls FastAPI's `GET /auth/login` server-to-server with `redirect: manual` and relays the captured Keycloak authorization URL to the browser; `/callback` is the actual browser-facing landing point and itself calls FastAPI's `/auth/callback` server-to-server (forwarding `code`/`state`) before setting the httpOnly cookie and redirecting the browser onward — never a client-side page parsing the callback response directly. This likely requires the Keycloak client to register the frontend's own callback URL as an additional valid redirect URI (an IdP/ops config change, not application code) — flagged under OPEN QUESTIONS for confirmation, since a different answer (e.g. keeping only the API-origin redirect URI and having FastAPI itself 302 onward, which would be a backend change out of this story's stated scope) changes AUTH-05's mechanism materially.
- 2026-09-04 Carry-forward closure verified per-record, not assumed: `frontend-auth-token-gap` (PGD-01 `pending_carry_forward`, `resolved_at: null`) is genuinely open and is what AUTH-05 closes, by retrofitting `fetchProgramDetail`/`fetchPrograms` (`apps/web/src/lib/programDetailApi.ts`, D-08's additive `opts` seam) to attach the bearer token. `SHP-01-state-browser-binding` (AUTH-01 `pending_carry_forward`) is NOT open — its own record shows `resolved_at: 2026-08-28` with evidence D-14 (PKCE S256) already closed the browser-binding gap at the protocol level without needing frontend participation, contrary to the item's superseded 2026-08-28 (D-13-era) note that closing it "needs the frontend to" participate. AUTH-05 does not address it and does not claim to.
- 2026-09-04 RTM header `Source hash` line left unchanged (still the PRD's hash, covering the pre-existing 38 rows). This run's input is `raw text`, a different source than the PRD; the schema's single header-hash line can't drift-check two distinct sources at once without misrepresenting one of them, and overwriting it would falsely invalidate already-recorded `rtm_source_sha` pins in shipped features' `state.json` (e.g. PGD-01, AUTH-01) for content that hasn't changed. Only the new `AUTH-05` row's own `Source` column is set to `raw text`, matching this run's actual origin.
- 2026-09-04 (RESOLVED, supersedes the "Login/callback hand-off mechanism" open question above) CONFIRMED by user: frontend-callback + backend `redirect_uri` parameterization. Keycloak redirects the browser to a Next.js Route Handler on the frontend origin; that handler relays `code`/`state` server-to-server to FastAPI's existing `GET /auth/callback` (same query-param signature, no method/route change), gets the JSON `{access_token, refresh_token, expires_in}`, sets the httpOnly cookie, then 302s the browser onward — no client-side page ever parses the callback response. Verified against shipped code, not assumed: `services/api/app/auth/oidc.py::_resolve_redirect_uri` (D-11/AF-08) **already** reads `settings.oidc_redirect_uri` when set, used identically by both `/auth/login`'s authorization request and `/auth/callback`'s token exchange; the `Settings.oidc_redirect_uri` field (`config.py:55`) and a blank `OIDC_REDIRECT_URI=` line (`.env.example:30`) already ship under AUTH-01. **Correction to the initial framing**: no `services/api` source change is needed — this is a config value (set `OIDC_REDIRECT_URI` to the frontend's callback URL), not "a small backend change" to make it configuration-driven; that already happened. AUTH-05's non-`apps/web` scope is therefore: (1) set that env var/config value in deployment, (2) document the resulting additional Keycloak valid-redirect-URI requirement in `README.md` § "Keycloak client requirements" (currently silent on it), (3) add an `OIDC_REDIRECT_URI` row to `README.md`'s env-var table (the var itself is not new — it exists in `config.py`/`.env.example` already — only its README documentation and its first real value are new) — recorded here as required AUTH-05 deliverables for the authoring step, not performed in this decomposition pass (out of this step's `docs/requirements/*` write scope). D-13/D-14 reasoning checked against shipped code and holds: `app/auth/state_store.py`'s `state` + PKCE `code_verifier` live in FastAPI's own per-process, in-memory store keyed by the opaque `state` string (`OAuthStateStore`), entirely independent of which origin the browser's HTTP requests touch — so `state_store.consume(state)` and the token exchange still complete correctly when the frontend relays the browser's `code`/`state` server-to-server, exactly as they do today when the browser calls `/auth/callback` directly. Non-blocking. `AUTH-05`'s RTM row (`Pri/Size/Depends-on/Contract`) is unchanged by this: no code dependency edge is added, and the added scope is a config value + two documentation edits, not enough to change `Size` from `M`. Unrelated, not fixed here (surgical-changes): the `session` contract's `mechanism` field also names "Authlib's starlette_client.OAuth", which `app/auth/oidc.py`'s own module docstring says was deliberately NOT used (SessionMiddleware/cookie conflict) — a pre-existing doc/code drift, carried forward for a future doc pass, not touched by this edit.
- 2026-09-04 (RESOLVED) Token storage: CONFIRMED by user as httpOnly cookie scoped to the frontend's own origin, not in-memory — survives Node restarts and needs no shared state across multiple frontend instances. `docs/requirements/auth.md`'s `frontend_storage` field text is left as-is (still correctly states the general either/or); the choice is recorded in that section's `frontend_ownership_note` addendum instead, scoped to what AUTH-05 actually builds, per the "don't rewrite the contract's own field text" instruction.
- 2026-09-04 Correction acknowledged, not new: the coordinator's relay of the user's prior statement that AUTH-05 would close AUTH-01's `SHP-01-state-browser-binding` carry-forward was itself corrected by the coordinator after independently re-checking the record. No change to the 2026-09-04 "Carry-forward closure verified per-record" entry above — it already had this right: that item's `resolved_at: 2026-08-28` / D-14 (PKCE) evidence stands, and AUTH-05 closes only `frontend-auth-token-gap` (PGD-01).
