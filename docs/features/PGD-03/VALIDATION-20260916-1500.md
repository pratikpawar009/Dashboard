# Validation report — PGD-03

- Date: 2026-09-16T15:00:00+00:00
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, invoked from `/arh-implement` Validate ∥ Review gate)
- Total: 22   Passed: 19   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 3
- Duration: ~35 minutes wall-clock (preflight + full suites + stack smoke + live curl checks)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Integration  |  18   |  18  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Manual (e2e) |   3   |   —  |   —  |   —   |   —   |

No contract-conformance findings (Phase 3b, `program-releases-api`) — shipped `ProgramReleasesResponse`/`ProgramReleaseItem` (`services/api/app/schemas/program_releases.py`) match `docs/requirements/api.md#program-releases-api` field-for-field, including `relTotal`/`stories`/`prs` as strings, `tagColor`/`tagBg` hoisted top-level only, and the closed 3-entry status vocabulary. README API table row (T-16) also matches the shipped shape.

Full regression suites (not just this story's TCs) also run clean: backend `740 passed, 7 deselected` (perf tests excluded by default `addopts`), frontend `227 passed` — no drift introduced elsewhere in the codebase.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/migrations/versions/007_program_releases_date_index.py` | ✓ | n/a | PASS |
| T-02 | `services/api/app/models/rollup.py` (`Index("ix_program_releases_program_id_date", ...)`), `services/api/tests/fixtures/prd_8_4_schema.json` (`"index(program_id, date)"` constraint) | ✓ | ✓ | PASS |
| T-03 | `services/api/app/schemas/program_releases.py` | ✓ | n/a | PASS |
| T-04 | `services/api/app/services/program_releases.py` | ✓ | n/a | PASS |
| T-05 | `services/api/app/api/overview.py` (new `/releases` subroute) | ✓ | ✓ (route registered, confirmed live via curl) | PASS |
| T-06 | `services/api/tests/unit/test_program_releases_route.py` | ✓ | n/a | PASS |
| T-07 | `services/api/tests/unit/test_program_releases_service.py` | ✓ | n/a | PASS |
| T-08 | `services/api/tests/perf/test_program_releases_perf.py` | ✓ | n/a | PASS |
| T-09 | `apps/web/src/types/programReleases.ts` | ✓ | n/a | PASS |
| T-10 | `apps/web/src/lib/programDetailApi.ts` (`fetchProgramReleases`) | ✓ | ✓ | PASS |
| T-11 | `apps/web/src/app/api/proxy/program-detail/[program_id]/releases/route.ts` + `.test.ts` | ✓ | n/a | PASS |
| T-12 | `apps/web/src/lib/programDetailApi.client.ts` (`fetchProgramReleases`) | ✓ | ✓ | PASS |
| T-13 | `apps/web/src/components/ReleasesList.tsx`, `.module.css` | ✓ | n/a | PASS |
| T-14 | `apps/web/src/components/ReleasesList.test.tsx` | ✓ | n/a | PASS |
| T-15 | `apps/web/src/components/ProgramDetailView.tsx` (`<ReleasesList programId={programId} />` mounted) | ✓ | ✓ (grep confirms mount at line 207, import at line 23) | PASS |
| T-16 | `README.md` (API table row for `/api/overview/program-detail/{program_id}/releases`) | ✓ | ✓ | PASS |

All 16 tasks: **PASS**. No blocked or skipped tasks in `tasks.json`.

### Passed

| TC id | Title |
|-------|-------|
| PGD-03-TC-01 | Default query params return 30d window, offset 0, limit 20 with full row shape |
| PGD-03-TC-02 | tagColor/tagBg hoisted to response top level, not per-row |
| PGD-03-TC-03 | Range param filters rows to the requested window (7d/30d/90d) |
| PGD-03-TC-04 | offset/limit pagination returns correct page of rows |
| PGD-03-TC-05 | limit above 50 is clamped to 50, not rejected |
| PGD-03-TC-06 | limit exactly 50 is accepted unclamped |
| PGD-03-TC-07 | Invalid range value returns explicit 400, not FastAPI default 422 |
| PGD-03-TC-08 | Cross-persona response is byte-identical for the same program and range |
| PGD-03-TC-09 | Any authenticated persona succeeds for any program_id (no per-program gating) |
| PGD-03-TC-10 | Unauthenticated request returns 401 |
| PGD-03-TC-11 | Unknown program_id returns 404 with JSON error body |
| PGD-03-TC-15 | Release-kind label/dot mapping matches the closed 3-entry vocabulary |
| PGD-03-TC-16 | A release type outside the closed vocabulary raises a data-integrity error |
| PGD-03-TC-17 | relTotal uses the same date window as the returned rows for each range value |
| PGD-03-TC-18 | relTotal reflects full window count even when limit truncates rows |
| PGD-03-TC-19 | date field is pre-formatted without year |
| PGD-03-TC-20 | Range/filter refresh completes within the 2s NFR budget |
| PGD-03-TC-21 | program_visibility RBAC check applied server-side with no dedicated audit event |
| PGD-03-TC-22 | Structured request log emitted with required fields |

Additionally verified live (not just via pytest's ASGI transport): started the real `uvicorn` server against the real `dashboard-dev5442` Postgres, ran `alembic upgrade head` (clean, applies migration 007), acquired a `dev-bypass` token, and curled the running endpoint — confirmed live 404 (`program not found`) for an unknown program and live 401 (`invalid_token`) with no bearer token, corroborating TC-10/TC-11's pytest results against a genuinely running process, not just the in-process ASGI transport.

### Flaky tests

None.

### Performance budget breaches

None.

### PGD-03-TC-20 — Range/filter refresh completes within the 2s NFR budget  (NFR-002)

- Target: p95 < 2000ms @ representative scale (5000+ seeded releases, 50 requests)
- Measured: observed per-request latencies (from structured logs during the run) ranged 2–11ms; p95 well under 2000ms
- `budget_pass: true`
- `EXPLAIN ANALYZE` assertion (second test in the file) confirms `ix_program_releases_program_id_date` index usage, not a sequential scan
- Command: `cd services/api && uv run pytest tests/perf/test_program_releases_perf.py -q -m perf` → `2 passed`

### Regression coverage

None — no `regression-<id>`-tagged TCs exist in this manifest (first validation pass for this story).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

These TCs require human verification (no e2e framework configured — `test_e2e` unset in `docs/config/project-commands.yaml`, per ADR-0001; Playwright browser binaries not installed, AF-04):

- PGD-03-TC-12 — Release list scrolls without layout breakage when content exceeds visible area
- PGD-03-TC-13 — Independent range switcher on the panel does not affect PGD-02's chart switcher
- PGD-03-TC-14 — Six-row loading placeholder shown while releases are fetching

All three have an automated component-level stand-in in `apps/web/src/components/ReleasesList.test.tsx` (part of the 227-test passing vitest run) per PLAN.md § 7: 6-row placeholder rendering, empty state, range-switcher independent refetch, and top-level tagColor/tagBg application are all asserted there — but the real-browser visual/layout assertion (CSS `max-height:296px; overflow:auto` behavior, actual scroll containment) remains unautomated pending a Playwright wiring decision, which is a pre-existing project-wide gap (not new to this story).

**GATE MODE note**: per the mode-conditional carry-forward rule, these three manual-TC carry-forward entries are **returned to the orchestrator below**, not written to `state.json`.

### Budget-unparseable NFRs

None — the NFR-002 budget (`p95 < 2s`) parsed cleanly.

### Proof of run

```
### Proof of run

- session_id:        95ab9a5d-b38c-4740-9f95-d6b62fc33732
- bash_invocations:  ~28
- test_runner_runs:  6 (pytest×5: route+service subset, full backend suite, perf subset with -s, perf subset; vitest×1: full frontend suite)
- stack_smoke_runs:  2 (api on :8000 via curl /health = 200; web on :3000 via curl / = 307 redirect, expected unauthenticated behavior)
- duration_seconds:  ~2100
- timestamp:         2026-09-16T15:00:00+00:00
```
