# Validation report — AUTH-03

- Date: 2026-08-31T08:20:27Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, `/arh-implement` Validate ∥ Review gate, round 1 of max 3)
- Total: 28   Passed: 28   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~600s (this validation session; excludes Step 1's own impl_evidence run, relied on not re-run)
- Source snapshot: `services/api/app/core/rbac.py` (new), `services/api/tests/unit/test_rbac.py` (new), `services/api/tests/perf/test_rbac_perf.py` (new), `services/api/app/main.py` (modified), `services/api/README.md` (modified) — matches `tasks.json` `file_plan` F-01..F-05 exactly, no extra files in `git status --porcelain -- services/api apps/web`.

AUTH-03 is a backend-only RBAC check library with no route surface and no UI — all 28 TCs are `unit`/`security`/`contract`/`performance`, executed directly via `pytest` (no flow-generation, no E2E layer per PLAN.md § 7).

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |  20   |  20  |   0  |   0   |   0   |
| Security     |   7   |   7  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

Layer counts follow `docs/test-cases/AUTH-03.json`'s per-TC `type` (unit: TC-01..16,24,28; security: TC-17..23,27; contract: TC-25; performance: TC-26), matching PLAN.md § 7's declared mapping exactly. All 28 TCs map to real, executing pytest node ids in `services/api/tests/unit/test_rbac.py` (37 node ids collected, incl. 8 parametrize expansions of TC-11/12 and TC-25) and `services/api/tests/perf/test_rbac_perf.py` (5 node ids) — 42 total collected test functions/parametrizations, 42 passed, 0 failed.

**Stack smoke**: relied on `docs/features/AUTH-03/state.json` `.impl_evidence.checks.runtime` (Step 1's receipt, session ended `2026-08-31T08:08:14Z`, same implementation round) per this validation's own invocation instructions — not re-run, per `/arh-implement`'s "must NOT regenerate" rule. That receipt: `fastapi-2` (api, port 8123) PASS, clean boot log, `GET /health` → 200, plus an explicit extra check that `app.core.rbac._persona_resolver is app.state.persona_resolver` (D-06 wiring proven live against a real running process, not just a unit-test stub); `nextjs` (web, port 3123) PASS, boot-only (no E2E tooling wired). Corroborating evidence gathered independently in this validation session: a fresh-process `uv run python -c "import app.main"` (Phase 1 preflight, this session) completed with exit 0, exercising `create_app()` — including the new `rbac.configure(...)` line — a second time outside Step 1's own run; and the full 377-test backend suite (this session) includes `tests/unit/test_auth_*.py`, whose fixtures build the app via `create_app(settings_override=...)` + `httpx.ASGITransport` per request, so every one of those runs exercised the new wiring line unconditionally, all green. Methodology note: port 8000 was found squatted by a pre-existing long-running `uvicorn app.main:app --reload --port 8000` dev process (PID 21566, started before this session) that answered `GET /health` → 200; this validation did not spin up a second competing instance on that port, relying instead on Step 1's isolated-port receipt (8123/3123) plus the in-process ASGI evidence above.

**Read-only compliance note**: running the mandated Phase-1 preflight `pnpm -C apps/web install` step (needed to reach the `[ERR_PNPM_IGNORED_BUILDS]` finding below) had a side effect of auto-creating `apps/web/pnpm-workspace.yaml` (pnpm's own generated stub for the ignored-build approval gate) — not a deliberate edit. Detected via a post-hoc `git status --porcelain -- services/api apps/web` diff check against the 5-file AUTH-03 scope, and removed immediately to restore the GATE MODE read-only invariant. Final source diff re-confirmed as exactly the 5 files listed above.

## Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/core/rbac.py`, `services/api/tests/unit/test_rbac.py`, `services/api/app/main.py` (scaffold `org_access`+`program_visibility`, shared helpers, `configure()`, F-05 wiring) | ✓ | ✓ (both checks present, `configure()`/`_resolver()`/`_resolve_persona_or_deny`/`_log_event` all present; `main.py` calls `rbac.configure(app.state.persona_resolver)` immediately after `PersonaResolver(cfg)`) | PASS |
| T-02 | `services/api/app/core/rbac.py`, `services/api/tests/unit/test_rbac.py` (`individual_usage_visibility`) | ✓ | ✓ (function present, self-path short-circuit + TC-05/06/07/22 all pass) | PASS |
| T-03 | `services/api/app/core/rbac.py`, `services/api/tests/unit/test_rbac.py` (`member_in_program_visibility`) | ✓ | ✓ (function present, `program_visibility` called first per FR-4, TC-08/09/10/23 all pass) | PASS |
| T-04 | `services/api/app/core/rbac.py`, `services/api/tests/unit/test_rbac.py` (`governance_visibility`) | ✓ | ✓ (function present, persona gate before cascade per FR-4, TC-11..16,19,21 all pass) | PASS |
| T-05 | `services/api/tests/unit/test_rbac.py` (contract test, TC-25) | ✓ | ✓ (`test_rbac_check_signatures_match_locked_contract_tc25` + `test_governance_visibility_program_id_defaults_to_none_tc25`, both pass) | PASS |
| T-06 | `services/api/tests/perf/test_rbac_perf.py` (perf test, TC-26) | ✓ | ✓ (5 tests, all p95 < 5ms, independently re-measured this session) | PASS |
| T-07 | `services/api/README.md` ("RBAC checks" section) | ✓ | ✓ (`## RBAC checks` present at `README.md:86`, directly after `## Auth` (line 46) and before `## Persona resolution` (line 152) — matches task notes' placement requirement exactly) | PASS |

All 7/7 tasks verified PASS. No `blocked` or `skipped` tasks in `tasks.json`.

## Contract conformance (Phase 3b)

`docs/requirements/auth.md` § `rbac-checks` (`produced_by: AUTH-03`) checked field-for-field against the shipped module:

- `signatures`: all five async signatures match `_CONTRACT_PARAMS` in `test_rbac.py` and the live `rbac.py` source exactly (name, param order, param count, `current_user` first, `governance_visibility`'s `program_id` defaults to `None`).
- `cascades`: `member_in_program_visibility` → `program_visibility` first (verified in source at `rbac.py:175` and by call-order spy TC-10); `governance_visibility` → persona gate first, then conditional `program_visibility` (verified in source at `rbac.py:224-235` and by ordered-spy TC-16).
- `logging`: all four event names, field allowlists, and outcome semantics match `AUTH-03-FR-2` and the contract's `logging` field verbatim.
- `checks` behavioural descriptions match the implementation for all five functions.

No conformance findings. **PASS.**

## Requirement verification (beyond TC pass/fail)

- **FR-4 cascade order, non-vacuous check**: `program_visibility` currently never denies (D-03 open-aggregate), which would make an *outcome-only* order test vacuous. Confirmed TC-10/13/14/15/16 avoid this trap: they patch `program_visibility` with a `_ProgramVisibilitySpy` that **can** deny (`deny=True`) or that appends to a **shared ordered call-recorder** (TC-16: `order == ["persona-check", "program-visibility"]`), so the assertions prove genuine call ORDER and short-circuit behavior, not merely an end-state that the current (never-denying) implementation would also produce. TC-10's proof is outcome-based but non-vacuous by construction: the self-path can never raise on its own, so the observed `HTTPException(403)` only reachable if `program_visibility` (patched to deny) ran before self-or-cio — same conclusion, different mechanism.
- **FR-1 except-clause order**: confirmed in source (`rbac.py:284-303`) — `except PersonaNotFoundError` precedes `except PersonaResolutionError` (its own base class), matching the module docstring's explicit warning that reversing the order makes the INFO branch dead code. Confirmed a regression WOULD be caught: if reversed, TC-18 (`PersonaNotFoundError` case) would be caught by the (now-first) `except PersonaResolutionError` clause and log at `ERROR` instead of `INFO` — TC-18 asserts `events[0].levelno == logging.INFO`, which would fail.
- **FR-2 field allowlists / literal outcome strings**: `_EVENT_REQUIRED_FIELDS`/`_EVENT_OPTIONAL_FIELDS` + `Literal["authorized", "denied"]` typing in `_log_event` (`rbac.py:315-365`) enforced structurally (raises `AssertionError` on a mismatched field set); TC-20..23 assert the exact JSON-formatted payload key sets. Confirmed passing.
- **FR-5 locked signatures**: see Contract conformance above.
- **TC-26 perf methodology**: confirmed `individual_usage_visibility` and `member_in_program_visibility` are measured on their **non-self** paths (`target_user_id="u-target-1" != user_id="u-cio-2"`; `target_member_id="u-target-2" != user_id="u-cio-3"`) — the self path returns before any persona resolution and would understate real cost. Confirmed `production_logging` fixture (`configure_logging()` + forced `INFO` root level) is applied via `@pytest.mark.usefixtures` on all 5 perf tests, so real JSON-formatting/stdout-write cost is inside the timed window where a log call fires (`org_access`, `governance_visibility`). Independently re-measured this session with a standalone throwaway script (outside the repo's own test suite, not reusing its fixtures) — see Proof of run: all five p95 well under budget (0.0002ms–0.0254ms), consistent with the suite's own passing assertions.

## Reliability NFR coverage gap (carry-forward, does not fail this gate)

PLAN.md § 7 asserts TC-17/TC-18/TC-19 "substantively cover" the Reliability NFR ("fail-closed on every resolver error path... zero default-permit outcomes across all five checks"). Verified independently: **this claim is only partially true.**

- TC-17/TC-18 exercise `org_access`'s resolver-error paths (`PersonaResolutionError` → ERROR/403, `PersonaNotFoundError` → INFO/403).
- TC-19 exercises `governance_visibility`'s resolver-error path (`PersonaResolutionError` → ERROR/403).
- **No TC exercises `individual_usage_visibility`'s or `member_in_program_visibility`'s non-self resolver-error path** (both call the same shared `_resolve_persona_or_deny` helper, but via their own call sites with their own `event_name`/`extra_fields`). Confirmed by exhaustive grep of `test_rbac.py` for `individual_usage_visibility`/`member_in_program_visibility` combined with `raises=`/`PersonaResolutionError`/`PersonaNotFoundError` — zero matches.

Risk is genuinely low (all four persona-resolving checks share one helper, already proven correct at 2 of 4 call sites, and the field-allowlist/level logic is identical regardless of which check invokes it) — but the gap is real, not hypothetical: a future edit that special-cased one call site (e.g. an inline try/except added directly in `individual_usage_visibility` instead of reusing the helper) would ship with zero test coverage to catch it. Recorded as carry-forward, consistent with how PLAN.md itself flagged this as a named risk rather than silently claiming full coverage.

## Passed

| TC id | Title | Test node id(s) |
|-------|-------|------------------|
| AUTH-03-TC-01 | org_access denies non-CIO, logs denied | `test_org_access_denies_non_cio_session_tc01` |
| AUTH-03-TC-02 | org_access passes CIO, logs authorized | `test_org_access_passes_cio_session_tc02` |
| AUTH-03-TC-03 | program_visibility passes any session | `test_program_visibility_passes_for_any_authenticated_session_tc03` |
| AUTH-03-TC-04 | program_visibility invariant across program_id | `test_program_visibility_outcome_invariant_across_program_ids_tc04` |
| AUTH-03-TC-05 | individual_usage_visibility passes self | `test_individual_usage_visibility_passes_for_self_tc05` |
| AUTH-03-TC-06 | individual_usage_visibility passes CIO | `test_individual_usage_visibility_passes_for_cio_tc06` |
| AUTH-03-TC-07 | individual_usage_visibility denies non-self/non-cio | `test_individual_usage_visibility_denies_non_self_non_cio_tc07` |
| AUTH-03-TC-08 | member_in_program_visibility passes self | `test_member_in_program_visibility_passes_for_self_tc08` |
| AUTH-03-TC-09 | member_in_program_visibility denies non-member | `test_member_in_program_visibility_denies_non_member_non_cio_tc09` |
| AUTH-03-TC-10 | FR-4 cascade short-circuit (member) | `test_member_in_program_visibility_program_denial_short_circuits_tc10` |
| AUTH-03-TC-11 | governance_visibility passes arch/pm/dev | `test_governance_visibility_enumerates_persona_set_tc11_tc12[architect/product-manager/developer-True]` |
| AUTH-03-TC-12 | governance_visibility denies cio/em | `test_governance_visibility_enumerates_persona_set_tc11_tc12[cio/engineering-manager-False]` |
| AUTH-03-TC-13 | governance+program_id passes both | `test_governance_visibility_with_program_id_passes_when_both_pass_tc13` |
| AUTH-03-TC-14 | governance+program_id denies on cascade | `test_governance_visibility_with_program_id_denies_when_program_denies_tc14` |
| AUTH-03-TC-15 | governance persona-denial short-circuits | `test_governance_visibility_persona_denial_short_circuits_before_program_tc15` |
| AUTH-03-TC-16 | FR-4 call order (governance) | `test_governance_visibility_call_order_persona_then_program_tc16` |
| AUTH-03-TC-17 | PersonaResolutionError → 403/ERROR | `test_org_access_persona_resolution_error_denies_and_logs_at_error_tc17` |
| AUTH-03-TC-18 | PersonaNotFoundError → 403/INFO | `test_org_access_persona_not_found_error_denies_and_logs_at_info_tc18` |
| AUTH-03-TC-19 | fail-closed at governance_visibility too | `test_governance_visibility_persona_resolution_error_denies_tc19` |
| AUTH-03-TC-20 | org_access payload allowlist | `test_rbac_check_org_access_payload_allowlist_tc20` |
| AUTH-03-TC-21 | governance payload allowlist | `test_rbac_check_governance_visibility_payload_allowlist_tc21` |
| AUTH-03-TC-22 | individual_view_denied allowlist | `test_individual_view_denied_payload_allowlist_tc22` |
| AUTH-03-TC-23 | member_view_denied allowlist | `test_member_view_denied_payload_allowlist_tc23` |
| AUTH-03-TC-24 | hardcoded governance tuple | `test_governance_personas_is_hardcoded_constant_tc24` |
| AUTH-03-TC-25 | locked contract signatures | `test_rbac_check_signatures_match_locked_contract_tc25[×5]` + `test_governance_visibility_program_id_defaults_to_none_tc25` |
| AUTH-03-TC-26 | p95 < 5ms per check | 5 tests in `test_rbac_perf.py` (see budget below) |
| AUTH-03-TC-27 | program_visibility never reads .programs | `test_program_visibility_never_reads_programs_tc27` |
| AUTH-03-TC-28 | program_visibility emits no log event | `test_program_visibility_emits_no_log_event_tc28` |

## Flaky tests

None. Every TC passed on first attempt (0 retries triggered).

## Performance budget (V3)

### AUTH-03-TC-26 — each rbac check p95 < 5ms (NFR-performance)

- Target: `p95 < 5ms per check, in-process, no I/O`
- Measured (this session's suite run, plus an independent standalone re-measurement — 100 iterations/check, non-self paths for `individual_usage_visibility`/`member_in_program_visibility`, `program_id` supplied for `governance_visibility`):

| Check | p95 (ms) | max (ms) | budget_pass |
|---|---|---|---|
| `org_access` | 0.0254 | 0.1432 | true |
| `program_visibility` | 0.0002 | 0.0006 | true |
| `individual_usage_visibility` | 0.0004 | 0.0013 | true |
| `member_in_program_visibility` | 0.0005 | 0.0012 | true |
| `governance_visibility` | 0.0178 | 0.0205 | true |

`budget_pass: true` for all five. No breach.

## Regression coverage (V4)

No `regression-<id>`-tagged TCs exist in `docs/test-cases/AUTH-03.json` (first validation pass for this story, no prior fix-loop).

## Failed (bug blocks)

None.

## Errored

None.

## Manual follow-up

None — all 28 TCs are `automatable: true`.

## Budget-unparseable NFRs

None — TC-26's budget parsed cleanly.

## Carry-forward (returned to orchestrator — GATE MODE, not self-written)

```json
[
  {
    "item_id": "AUTH-03-NFR-reliability-coverage-gap",
    "kind": "coverage_gap",
    "reason": "individual_usage_visibility's and member_in_program_visibility's non-self persona-resolution-error paths (PersonaResolutionError/PersonaNotFoundError via the shared _resolve_persona_or_deny helper) have no direct executing test -- only org_access (TC-17/18) and governance_visibility (TC-19) test the fail-closed resolver-error behavior. Low risk (shared helper, already proven at 2 of 4 call sites) but the Reliability NFR ('zero default-permit outcomes across all five checks') is not independently verified for all four persona-resolving checks. PLAN.md Section 7 claims TC-17/18/19 'substantively cover' this NFR; verified only partially true.",
    "owner": "Pratik Pawar (PO)",
    "added_at": "2026-08-31T08:20:27Z",
    "added_by": "validate-feature/gate-round-1 (reliability NFR audit)",
    "resolved_at": null,
    "evidence": "docs/features/AUTH-03/VALIDATION-20260831-0820.md § Reliability NFR coverage gap"
  },
  {
    "item_id": "env-pnpm-ignored-builds-apps-web",
    "kind": "environment",
    "reason": "pnpm -C apps/web install (preflight) exits non-zero with [ERR_PNPM_IGNORED_BUILDS]: unrs-resolver@1.12.2's build script is blocked by pnpm's supply-chain approval gate ('pnpm approve-builds'). Packages themselves install successfully (390 resolved/added); this is a pnpm policy gate, not a missing/broken dependency, and AUTH-03 touches zero files under apps/web so this is pre-existing environment drift unrelated to this story's diff -- but it will cause every future story's Phase-1 preflight to report a non-zero exit on this step until resolved (run 'pnpm approve-builds' once, interactively, or pin unrs-resolver's build permission in apps/web/package.json's pnpm.onlyBuiltDependencies).",
    "owner": "unassigned",
    "added_at": "2026-08-31T08:20:27Z",
    "added_by": "validate-feature/gate-round-1 (Phase 1 preflight)",
    "resolved_at": null,
    "evidence": "pnpm -C apps/web install output, this session: '[ERR_PNPM_IGNORED_BUILDS] Ignored build scripts: unrs-resolver@1.12.2'"
  }
]
```

### Proof of run

- session_id:        b9248a37-6871-4695-b27c-8b86888dca4b
- bash_invocations:  45 (this validation session)
- test_runner_runs:  3 (`uv run pytest` — full suite once [377 passed], `test_rbac.py`+`test_rbac_perf.py` targeted twice [42 passed each, verbose + `--durations=0`]); plus 1 standalone non-pytest independent-verification script (`rbac_perf_probe.py`, outside the repo's test suite, re-measuring TC-26's five p95 values)
- stack_smoke_runs:  0 fresh spawns by this session (relied on Step 1's `impl_evidence.checks.runtime`, 2 stacks PASS, same implementation round, per this validation's own "do not regenerate" instruction); 1 corroborating healthcheck against a pre-existing dev-server process (`GET http://127.0.0.1:8000/health` → 200)
- duration_seconds:  ~600 (approximate, this validation session only)
- timestamp:         2026-08-31T08:20:27Z
