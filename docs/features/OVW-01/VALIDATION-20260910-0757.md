# Validation report — OVW-01

- Date: 2026-09-10T07:57:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; invoked from `/arh-implement` Validate ∥ Review gate)
- Total: 8   Passed: 8   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~640s (wall-clock, this validation session; see Proof of run)

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   0*  |   —  |   —  |   —   |   —   |
| Integration  |   5   |   5  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

\* No `unit`-typed TC exists by design (test-case-generation skill scope rule — unit coverage lives in `tasks.json`, not the TC manifest). The story's own new-module unit tests all pass: backend `pytest` full suite **514 passed** (507 pre-OVW-01 baseline + 7 new in `test_overview.py`, matches BED-05's recorded floor per project memory); frontend `vitest` full suite **24 files / 122 tests passed**, of which 29 tests across 5 files (`OrgSummaryCards.test.tsx`, `AdoptionIndicator.test.tsx`, `OverviewErrorPanel.test.tsx`, `AdoptionOverview.test.tsx`, `PersonaDashboardShell.test.tsx`) are this story's.

Stack smoke: `fastapi-2` and `nextjs` both PASS (see Phase 2b detail below). No `(external — …)`/`[NEEDS CLARIFICATION]` deps skipped; `postgres`'s own stack-smoke row is folded into `fastapi-2`'s `Deps:` bullet per `docs/config/stack-smoke.md`.

Contract conformance (Phase 3b): `overview-summary-api` (`docs/requirements/api.md`) checked field-by-field against the live endpoint — **no findings**. See § Contract conformance.

## Environment (Phase 1)

- Tools not on the non-interactive PATH, per project memory — resolved: `uv=/Users/pratik.pawar/.local/bin/uv`, `pnpm=/usr/local/bin/pnpm`.
- Preflight (`docs/config/project-commands.yaml`): **6/6 commands PASS** — `pnpm -C apps/web install` (0), `uv sync` (0, 47 packages audited), `python -c "import app.main"` (0), `import authlib` → `0.15.6`, `import respx` → `0.23.1`, `import yaml` → `6.0.3`.
- Live Postgres confirmed via `settings.database_url` (never `.env` directly, per governance deny set): `postgresql+psycopg://postgres:postgres@localhost:5442/dashboard` — port **5442**, not the documented 5432 (`dashboard-dev5442` container). Not touched: created/dropped my own ephemeral database (`ovw01_smoke_val`) for stack smoke, and a suffixed database (`HARNESS_TEST_DB_SUFFIX`) for the pytest run.
- Canonical ports 8000/3000 both free before use; both released after (`pkill` + `lsof` re-check clean).

## Stack smoke (Phase 2b)

| Stack | Status | Sub-step | Detail |
|---|---|---|---|
| `fastapi-2` | PASS | — | `docker exec ... CREATE DATABASE ovw01_smoke_val` → `alembic upgrade head` from **empty** → reached `004_rollup_query_indexes` (matches the orchestrator's stated head) → `uvicorn` boot on :8000 → `GET /health` → `200 {"status":"ok"}` |
| `nextjs` | PASS | — | `pnpm build` (`ƒ /overview` now in the route table, confirming the 404 is fixed) → `pnpm dev --port 3000` boot → `GET /overview` unauthenticated → **307 → /login** (independently reproduced, matches the orchestrator's claim) |

Both processes killed and ports re-verified free after use; smoke DB dropped after use.

## Live AC re-derivation (independent of the pytest/vitest suites)

Since the directive asked several claims be re-derived rather than trusted, I exercised the running server directly (dev-bypass tokens, real Postgres, real migrations) in addition to running the automated suites:

| AC | Live check | Result |
|---|---|---|
| AC-1/AC-5 | Seeded `org_summary_rollup` (count=7,total=10,tokens=12.5M,LOC=850K,releases=42,repos=18/25) → `GET /api/overview/summary` as CIO | `200`; 5 cards, **4 fields each** (`glyph,value,label,sub`); card 1 `"7 / 10"` sub `"70% adoption"`; cards 2–4 `"12.5M"/"850.0K"/"42"` sub `null`; card 5 plain `"18"` sub `null`; `programs_using_ai={count:7,total:10,adoption_percent:70.0}`; no color/style key anywhere |
| AC-2 | Rollup absent, `system_metadata` ingestion row present | `200`; card 1 `"0 / 0"`, cards 2–5 `"0"`; `programs_using_ai={count:0,total:0,adoption_percent:null}` (JSON `null`, not `0.0`) |
| AC-7 / the AC-2↔AC-7 collision | **Both** rows absent (genuinely fresh DB) | `500 {"error":{"message":"ingestion job may not have run yet"}}` — the 500 wins over the all-zero 200, confirming `OVW-01-FR-3`/`OVW-01-TC-04`'s claim directly against a live, never-migrated-then-freshly-migrated database, not just the test's mocked spy |
| AC-3/AC-8 | `developer`-persona token → same endpoint | `403 {"error":{...}}` — **no** `cards`/`programs_using_ai` key in the body; app log shows `rbac_check_org_access` firing on **both** the prior CIO call (`outcome: authorized, persona: cio`) and this call (`outcome: denied, persona: developer`), each with `user_id`/`persona`/`timestamp` |
| AC-6 | No freshness field in any of the above response bodies | Confirmed backend-only, as required — not treated as a gap per the directive |

All five independently reproduce the values `docs/test-cases/OVW-01.json` TC-01/02/04/05 assert, and the `docs/features/OVW-01/DECISIONS.md` D-02 correction (ratio+sub on card 1, plain count on card 5) is what actually shipped — no stale artefact found anywhere in the repo asserting the pre-correction rule (checked `docs/requirements/api.md`, `README.md`, `docs/test-cases/OVW-01.json`, `tasks.json`, `DECISIONS.md`; the only two hits are `tasks.json` T-02's own historical "found and fixed" note and `DECISIONS.md` D-02's own quoted-for-the-record error string — both intentional history, not live claims).

## Contract conformance (Phase 3b)

`docs/requirements/api.md#overview-summary-api` (`produced_by: OVW-01`) checked against the live `GET /api/overview/summary`:

| Recorded shape | Shipped | Match |
|---|---|---|
| `{cards: list[OrgSummaryCard], programs_using_ai: ProgramsUsingAi}`, two structures not one | confirmed | ✓ |
| Card = 4 fields `{glyph,value,label,sub}`, 5 entries, fixed mockup order | confirmed | ✓ |
| Card 1 ratio + `"{pct}% adoption"` sub; cards 2–5 `format_number()`, `sub: null`; card 5 plain count | confirmed | ✓ |
| `programs_using_ai.adoption_percent` nullable, `None` not `0.0` when `total==0` | confirmed (both missing-row and all-absent cases) | ✓ |
| Freshness read unconditioned by rollup lookup, before it, 500 even on all-zero case | confirmed | ✓ |
| RBAC: cio-only, 403 no body, `rbac_check_org_access` both outcomes | confirmed | ✓ |
| Errors: 403/500/401 | confirmed (401 seen on no-token call, 403/500 above) | ✓ |

**No conformance findings.** Verdict unaffected by this phase.

## Passed

| TC id | Title | Type | Runner |
|---|---|---|---|
| OVW-01-TC-01 | Populated 5-card + adoption payload, format_number, no inline CSS, freshness silent | contract | pytest |
| OVW-01-TC-02 | Missing rollup → all-zero 200, `adoption_percent` null not 0 | integration | pytest |
| OVW-01-TC-03 | Missing `system_metadata` ingestion row → 500 "ingestion job may not have run yet" | integration | pytest |
| OVW-01-TC-04 | Fully-fresh DB (both rows absent) → still 500, freshness call unconditioned by rollup | integration | pytest |
| OVW-01-TC-05 | Non-CIO 403 no body (4 personas) + `rbac_check_org_access` logged both outcomes | security | pytest |
| OVW-01-TC-06 | `/overview` renders 5 cards + adoption headline/bar/legend, client-derived colors | integration | vitest |
| OVW-01-TC-07 | Zero-programs `0/0` headline, flat single-color bar, zero legend counts | integration | vitest |
| OVW-01-TC-08 | Single indexed-row read, no joins, handler < 500ms budget | performance | pytest |

All 8/8 automatable TCs PASS on first attempt — no retries triggered.

## Performance budget (V3)

### OVW-01-TC-08 — Org summary single-query budget (NFR-performance)

- Target: exactly 1 SELECT against `org_summary_rollup` (`ORG_SUMMARY_QUERY_COUNT_BUDGET=1`, no joins); handler duration < `ORG_SUMMARY_HANDLER_DURATION_BUDGET_MS=500.0`ms (backend proxy for NFR-001's ≤3s page-render budget — frontend/network legs are out of a backend test's reach, `test_e2e` empty).
- Measured: PASS on both assertions in my own re-run against a freshly-migrated ephemeral DB (query-count == 1; elapsed < 500ms). Live figure not printed at this pytest verbosity (repo convention — the PASS/FAIL is the evidence, per project memory); `tasks.json` T-07's own prior measurement recorded 29.84ms for a full call, ~16.8x headroom.
- `budget_pass: true`. No budget breach.

## Regression coverage (V4)

None — no TC in this manifest carries a `regression-<id>` tag. This is OVW-01's first validation pass; no prior fix-loop has run against it.

## Flaky tests (V1)

None — every TC passed on its first attempt.

## Task completion verification (MANDATORY)

Source: `docs/features/OVW-01/tasks.json` `tasks[]` (15 tasks, `file_plan` F-01..F-24) — read directly, not from `PLAN.md` §5 (its one-line pointer). All 15 tasks report `status: "done"`.

| Task | Target file(s) | Existence | Section match | Verdict |
|------|-----------------|-----------|---------------|---------|
| T-01 | `services/api/app/schemas/org_summary.py` (OrgSummaryCard/ProgramsUsingAi/OrgSummaryResponse) | ✓ | ✓ (`class OrgSummaryCard`, `sub:` field, `class ProgramsUsingAi`, `class OrgSummaryResponse` all present) | PASS |
| T-02 | `docs/requirements/api.md` (`#overview-summary-api` filled) | ✓ | ✓ (section present, matches D-01/D-02 corrected shape) | PASS |
| T-03 | `services/api/app/api/overview.py` (route) | ✓ | ✓ (`GET /api/overview/summary` live, behavior verified) | PASS |
| T-04 | `services/api/tests/unit/test_overview.py` (TC-01/TC-02) | ✓ | ✓ (both tests present and PASS) | PASS |
| T-05 | `services/api/tests/unit/test_overview.py` (TC-03/TC-04) | ✓ | ✓ (both tests present and PASS) | PASS |
| T-06 | `services/api/tests/unit/test_overview.py` (TC-05) | ✓ | ✓ (test present and PASS) | PASS |
| T-07 | `services/api/tests/perf/test_overview_perf.py` (TC-08) | ✓ | ✓ (appended to existing file per AF-06, test present and PASS) | PASS |
| T-08 | `apps/web/src/components/PersonaDashboardShell.tsx` + `.test.tsx` (optional `program`, `children`) | ✓ | ✓ (D-04 cases present: program-omitted, children-render) | PASS |
| T-09 | `apps/web/src/types/overview.ts`, `apps/web/src/lib/overviewApi.ts` | ✓ | ✓ (types + `fetchOverviewSummary` present) | PASS |
| T-10 | `OrgSummaryCards.tsx/.module.css/.test.tsx` | ✓ | ✓ (5-card render, sub-omission, loading-placeholder tests present) | PASS |
| T-11 | `AdoptionIndicator.tsx/.module.css/.test.tsx`, `docs/design/tokens.md` | ✓ | ✓ (`#c3c9d2`/`#dfe3e9` present in tokens.md Color table) | PASS |
| T-12 | `OverviewErrorPanel.tsx/.module.css/.test.tsx` | ✓ | ✓ (D-07 single-message test present) | PASS |
| T-13 | `AdoptionOverview.tsx/.module.css/.test.tsx` | ✓ | ✓ (TC-06/TC-07 present and PASS) | PASS |
| T-14 | `apps/web/src/app/overview/page.tsx` | ✓ | ✓ (route live, 307→/login confirmed) | PASS |
| T-15 | `README.md` (API table row) | ✓ | ✓ (`GET /api/overview/summary` row present, matches shipped shape) | PASS |

**15/15 tasks PASS.** No `blocked` or `skipped` tasks in this manifest — no exempted rows.

## Findings for the reviewer (not new agent flags — pre-existing, already triaged)

Per the directive, these are confirmed rather than re-reported as new:

- **AF-04 (accepted)** — `get_freshness_accessor` dependency confirmed present in `services/api/app/api/overview.py` (`Depends(get_freshness_accessor)`, overridable), consistent with my fresh read of the snapshot after this edit landed.
- **AF-07 / `design_check: N/A`** — confirmed `design_check` is empty in `docs/config/project-commands.yaml`; no automated a11y tool wired. Accepted gap, not re-flagged as new.
- **D-02 correction** — confirmed shipped everywhere (live endpoint, `api.md`, `README.md`, `tasks.json`, test-cases JSON all agree); no stale artefact found.

## One naming-precision note (new, minor — does not affect verdict)

`docs/test-cases/OVW-01.json` TC-05's `test_data.expected_log_fields` lists `"authorized"` as a field name (implying a boolean `authorized` key), inherited from the story `OVW-01.md` AC-8 prose ("...user id, persona, authorized (bool), and timestamp"). The actual shipped/tested field — both in `app/core/rbac.py` and in `test_overview.py`'s own TC-05 assertions — is `outcome: "authorized"|"denied"` (a string enum), which is the pre-existing, correct `rbac-checks` (AUTH-03) contract convention (`docs/requirements/auth.md`'s `rbac_check_org_access` entry: "record both authorized and denied outcomes"). The underlying requirement (both outcomes logged, with `user_id`/`persona`/`timestamp`) is fully satisfied and independently reproduced live above — this is a wording mismatch in the TC/story prose against the established field name, not a shipped defect. Worth a one-line fix in the TC manifest at the next touch; does not block PASS.

## Manual follow-up

None — all 8 TCs are `automatable: true`. No `.pending_carry_forward` entries generated by this run (GATE MODE — none to return; see § Carry-forward below).

## Carry-forward (returned to orchestrator, not self-written — GATE MODE)

No **new** carry-forward entries from this validation run (no manual TCs pending, no new contract-conformance finding, no new task failure). The following already exist in `docs/features/OVW-01/state.json` `.pending_carry_forward` from earlier phases and are unaffected by this run — listed here only for the orchestrator's join, not re-added:

- `auth-03-security-review-open-caveat` (risk, from plan-implementation) — unaffected.
- `AF-02-carry` (flaky perf-test-under-parallel-load convention gap) — unaffected; my perf re-run was in isolation and passed.
- `AF-06-carry` (plan-validation rubric should stat `file_plan` actions against reality) — unaffected.
- `AF-07-carry` (no a11y tool wired project-wide) — unaffected, matches this run's `design_check: N/A`.

## Proof of run

```
- session_id:        0c26fdcc-d9a3-4106-b486-3b51ee89af95
- bash_invocations:  38
- test_runner_runs:  4   (pytest full-suite ×1, pytest targeted test_overview.py+test_overview_perf.py ×1, vitest full-suite ×1, vitest targeted 5-file run ×1)
- stack_smoke_runs:  2   (fastapi-2 migrate+boot+healthcheck, nextjs build+boot+healthcheck)
- duration_seconds:  ~640  (approximate — derived from observed command timings: pytest full suite 164.42s, pnpm build 12.44s, pnpm test 6.94s, 2× alembic migrate ~3s, uvicorn/next boot waits ~8s, targeted pytest 4.09s, targeted vitest ~2s, plus curl/grep/read overhead; no separate session-start timestamp was logged, so this is a sum-of-observed-durations estimate, not a stopwatch figure)
- timestamp:         2026-09-10T07:57:14Z
```

## Return to orchestrator

```
V:            PASS
P/TOTAL:      8/8
Report:       docs/features/OVW-01/VALIDATION-20260910-0757.md
Per-AC:       AC-1 PASS · AC-2 PASS · AC-3 PASS · AC-4 PASS · AC-5 PASS · AC-6 PASS (backend-only, confirmed absent from every response body) · AC-7 PASS · AC-8 PASS
Tasks:        15/15 done, 15/15 verified PASS
Carry-forward (new): none
Contradicts tasks.json reason fields: none found — every checked claim (D-02 shape, AF-04 dependency injection, T-07's file_plan correction, T-15's README row, T-08/T-13's test counts) reproduced as stated.
Minor note for review: TC-05 test_data field name "authorized" vs shipped "outcome" (see above) — cosmetic, not a defect.
```
