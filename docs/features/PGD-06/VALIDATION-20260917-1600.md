# Validation report — PGD-06

- Date: 2026-09-17T16:00:00+00:00
- Verdict: PASS
- Mode: full (GATE MODE — report-only)
- Total: 17   Passed: 17   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~28 minutes (preflight through report write)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   9   |   9  |   0  |   0   |   0   |
| Integration  |   9   |   9  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Security     |   2   |   2  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

(Layer counts reflect each TC's declared `type`; TC-01/04/05/06/07/13 = integration/positive-boundary but backed by service-level pytest "unit" files per this repo's convention — counted here by declared JSON `type`, not by which physical file houses them. TC-02 is `type: contract`.)

**Contract conformance (Phase 3b):** `program-session-series-api` (`docs/requirements/api.md:221-239`, `produced_by: PGD-06`) checked field-by-field against the shipped route/schema/service. No drift found — endpoint, response model, field types (raw ints), day-bucket query shape, no-404 empty behaviour, gate ordering, error codes (400/401, no 404), and the `program_drilldown` observability event all match exactly.

**Stack smoke:** both `fastapi-2` and `nextjs` PASS. Postgres already running as `dashboard-dev5442` (host port **5442**, not the 5432 default recorded in `docs/config/stack-smoke.md` — confirmed via `docker ps`, per prior memory of this project's real port mapping). `alembic upgrade head` against `postgresql+psycopg://postgres:postgres@localhost:5442/dashboard` completed with no pending revisions. `uvicorn` → `GET /health` returned 200. `pnpm dev --port 3000` → `GET /` returned 307 to `/overview` (expected unauthenticated-redirect behaviour, not a failure). Both processes killed cleanly after smoke; ports 8000/3000 confirmed free afterward.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/services/program_session_series.py`, `services/api/app/schemas/program_session_series.py` | ✓ | n/a (feat) | PASS |
| T-02 | `services/api/app/api/overview.py` (register 8th sibling route) | ✓ | ✓ (`get_program_session_series` route registered, gate ordering confirmed in body) | PASS |
| T-03 | `services/api/tests/unit/test_program_session_series_service.py` | ✓ | n/a (test) | PASS |
| T-04 | `services/api/tests/unit/test_program_session_series_route.py` | ✓ | n/a (test) | PASS |
| T-05 | `services/api/tests/perf/test_program_session_series_perf.py` | ✓ | n/a (test) | PASS |
| T-06 | `docs/requirements/api.md` (`program-session-series-api` contract section) | ✓ | ✓ (section present, matches shipped surface exactly — see Contract conformance above) | PASS |
| T-07 | `README.md` (add session-time-series endpoint row) | ✓ | ✓ (row present at line 46, intro sentence at line 29 also updated) | PASS |
| T-08 | `apps/web/src/types/programSessionSeries.ts` | ✓ | n/a (feat) | PASS |
| T-09 | `apps/web/src/lib/programSessionSeriesApi.ts` | ✓ | n/a (feat) | PASS |
| T-10 | `apps/web/src/app/api/proxy/program-detail/[program_id]/session-time-series/route.ts`, `route.test.ts` | ✓ | n/a (feat) | PASS |

All 10 tasks: `status: "done"` confirmed genuine — every resolved file exists, is non-empty, and its content matches what the task notes and PRD describe. No `blocked`/`skipped` tasks in this manifest.

### Passed

| TC id | Title |
|-------|-------|
| PGD-06-TC-01 | Unfiltered request SUMs session_time_seconds across all members per day (no null-member_id read) |
| PGD-06-TC-02 | Response fields are raw ints, never formatted strings |
| PGD-06-TC-03 | Unknown program_id returns 200 with an all-zero zero-padded series, never 404 |
| PGD-06-TC-04 | points[] has exactly 7/30/90 entries for the resolved range, quiet days zero-padded not omitted |
| PGD-06-TC-05 | Program with zero session_series rows across the entire range returns a full all-zero series |
| PGD-06-TC-06 | avg_seconds_per_day divides by the fixed range day-count, not the count of days with data |
| PGD-06-TC-07 | Same underlying data yields a different avg_seconds_per_day for 7d vs 30d (differential-range) |
| PGD-06-TC-08 | Non-self, non-cio member_id filter is denied 403 before any session_series query runs |
| PGD-06-TC-09 | Self-filtered member_id request is authorized via open-aggregate program_visibility |
| PGD-06-TC-10 | Unfiltered request is authorized for any authenticated session regardless of program membership |
| PGD-06-TC-11 | Invalid range value returns 400, not FastAPI default 422 |
| PGD-06-TC-12 | Unauthenticated request returns 401 |
| PGD-06-TC-13 | Soft-deleted roster member (removed_at populated) still appears in historical session_series data |
| PGD-06-TC-14 | Omitted range query param defaults to 30d |
| PGD-06-TC-15 | Each 7D/30D/90D range returns its own window and series length |
| PGD-06-TC-16 | Structured log emitted with method, path, program_id, range, status, latency on successful fetch |
| PGD-06-TC-17 | Range/member-filter refresh completes within 2s performance budget |

### Flaky tests

None.

### Performance budget breaches

None. TC-17 (NFR-002, target `p95 < 2000ms`, single aggregate query per request):

- range=7d unfiltered: 7.10ms, 1 query
- range=30d unfiltered: 4.80ms, 1 query
- range=90d unfiltered: 7.03ms, 1 query
- range=30d self-filtered: 5.06ms, 1 query

All comfortably within budget; `budget_pass: true`.

### Regression coverage

None declared in this manifest — PGD-06 is a new story with no prior fix-loop regression tags.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None. All 17 TCs are `automatable: true`; none marked `manual`.

### Budget-unparseable NFRs

None — NFR-performance's `p95 < 2000ms` budget parsed cleanly.

### AF-01 / AF-02 post-triage verification

- **AF-01 (frontend 403 handling, accepted):** confirmed correctly and consistently shipped across all three layers:
  - `apps/web/src/types/programSessionSeries.ts` — `SessionSeriesResult` carries `{status:"denied"}`, no `not_found` member (the type's own comment documents its deliberate absence, since this endpoint never 404s).
  - `apps/web/src/lib/programSessionSeriesApi.ts` — `fetchProgramSessionSeries()` maps upstream `403 → {status:"denied"}` before the generic non-ok branch.
  - `apps/web/src/app/api/proxy/program-detail/[program_id]/session-time-series/route.ts` — maps `denied → 403 {error:"denied"}`, distinct from `error → 502 upstream_error`, mirroring PGD-05's `personal-usage` proxy precedent exactly.
  - `route.test.ts` — includes a dedicated test (`maps status 'denied' to 403 {error: 'denied'} — a denied non-self member_id stays a denial, never a 502`) and asserts `callWithAuth` does not retry on `denied`. Grepped the full frontend surface for stale `not_found` handling on this feature — the only two hits are prose in the type/route doc-comments explaining why `not_found` is deliberately absent, not dead code.
- **AF-02 (PLAN.md F-06 → F-03 correction, accepted):** confirmed — `docs/features/PGD-06/PLAN.md:131` cites F-03 (`app/api/overview.py`, the actual entry-registration file for the new route) as the Wiring dimension's evidence. No lingering F-06 reference in that context.
- `docs/features/PGD-06/FLAGS.md` confirms both AF-01 and AF-02 are `status=accept`, triaged 2026-09-17 (AF-03/AF-04 rejected, not in scope of this validation).

### FR-1 and FR-6 — explicit statement

- **FR-1 (cross-member SUM, not a null-member_id read): genuinely satisfied.** `services/api/app/services/program_session_series.py::fetch_program_session_series` issues `SELECT date_trunc('day', date), SUM(session_time_seconds) FROM session_series WHERE program_id=:pid AND date>=:range_start GROUP BY date_trunc('day', date)` when `member_id is None` — no `member_id IS NULL` predicate appears anywhere in the module. `PGD-06-TC-01` seeds two distinct members' same-day rows (1800s + 2400s) and asserts the unfiltered response's point equals 4200 — a null-member_id read would return 0 here since the rollup producer (`app/services/rollup_rebuild.py::_build_session_series`) never writes a null `member_id`. Test passed. The module's own docstring documents this as a corrected premise relative to the story's original AC-1 wording, matching `docs/requirements/api.md`'s `org_wide_semantics` field verbatim.
- **FR-6 (gate-before-query): genuinely satisfied.** In `app/api/overview.py::get_program_session_series`, `await member_in_program_visibility(current_user, program_id, member_id)` is called and awaited (raising `HTTPException(403)` on denial) strictly before `await fetch_program_session_series(...)` is invoked in the function body — structurally impossible for a query to fire on the denied path since the exception propagates before that line executes. `PGD-06-TC-08` installs a query-execution spy on the `session_series` query path and asserts zero invocations plus a `member_view_denied` log entry on a non-self, non-cio `member_id` request; both assertions passed. The 403 response body was confirmed to carry no `points`/`period_total_seconds`/`avg_seconds_per_day` fields.

### Proof of run

```
### Proof of run

- session_id:        7cf21480-15ec-454f-ab78-0f63cde2139e
- bash_invocations:  33
- test_runner_runs:  7  (pytest x5: scoped unit, scoped perf, full suite; vitest x2: scoped proxy test, full web suite)
- stack_smoke_runs:  2  (fastapi-2 start+health, nextjs start+check)
- duration_seconds:  ~1700
- timestamp:         2026-09-17T16:00:00+00:00
```
