# Validation report — SHP-02

- Date: 2026-09-08T03:17:53Z
- Verdict: **PASS**
- Mode: full (gate-mode invocation from `/arh-implement`'s Validate ∥ Review gate; no `--rerun`/`--rerun-failed` flag given)
- Total: 2   Passed: 2   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~240s measured command execution (pytest + stack-smoke + live-HTTP checks); full wall-clock including agent reasoning time not separately instrumented

## Summary

| Layer                                    | Total | Pass | Fail | Error | Flaky |
|-------------------------------------------|-------|------|------|-------|-------|
| Stack smoke (fastapi-2, nextjs)            |   2   |   2  |   0  |   0   |   0   |
| Contract (SHP-02-TC-01)                    |   1   |   1  |   0  |   0   |   0   |
| Security (SHP-02-TC-02)                    |   1   |   1  |   0  |   0   |   0   |
| Performance (task-level, T-10; no TC id)   |   3   |   3  |   0  |   0   |   0   |
| Task-owned regression suite (pytest)       |  97   |  97  |   0  |   0   |   0   |

Contract conformance (Phase 3b): PASS, no findings — see below.

## Phase 1 — Environment check

- Preflight (`docs/config/project-commands.yaml` `preflight:`): all 6 commands passed — `pnpm -C apps/web install` (already up to date), `uv sync` (resolved 49 packages), smoke-imports of `app.main`, `authlib` (0.15.6), `respx` (0.23.1), `pyyaml` (6.0.3).
- Endpoints: `GET http://127.0.0.1:8000/health` → `{"status":"ok"}`. `GET /api/personal-usage/{id}` (no token) → `401`, confirming route registration.
- No missing env vars surfaced.

## Phase 2b — Stack smoke

| Stack | Sub-step | Status | Duration | Note |
|---|---|---|---|---|
| fastapi-2 | migrate + healthcheck | PASS | ~1s (migration) | DB was at `001_initial_schema`; ran `uv run alembic upgrade head` → `002_personal_usage_indexes` applied cleanly (additive index-only). Post-migration `/health` re-checked: 200. |
| nextjs | start + healthcheck | PASS | ~35s boot (elevated — shared-machine load, matches the disclosed ~87-load-avg context) | Started `pnpm dev --port 3000` fresh (was not already running); `GET /` returned 200 (5s response under load, not a defect). Torn down after check (`kill`), log cleared per PASS-cleanup rule. SHP-02 touches no `apps/web` file — checked anyway per "every runnable stack" rule. |

Both stacks PASS. Phase 3 proceeded for both.

## Phase 3 — Run all flows

No canonical `flow_path` files exist for SHP-02-TC-01/TC-02 (no `docs/test-cases/SHP-02.json` `flow_path` field set). The TCs' executable bodies are implemented verbatim as the two named pytest functions in `services/api/tests/unit/test_personal_usage.py` (per `tasks.json` T-08/T-09 notes: "SHP-02-TC-01 verbatim" / "SHP-02-TC-02 verbatim") — this is the flow file for both TCs.

**Confirmed two ways, per the "real API, no mocks" directive:**

1. **pytest, in-process against the real migrated Postgres** (no DB/HTTP mocks):
   - `test_personal_usage_full_envelope_contract_tc01` — PASS (call 3.51s / setup 2.62s / teardown 1.13s).
   - `test_personal_usage_cross_user_denied_bare_403_tc02` — PASS (setup 3.16s / call 2.03s / teardown 2.48s), including the log-capturing-handler assertion (exactly one `individual_view_denied` record, no PII/no usage-data fields).
   - Full task-owned suite re-run independently: `test_personal_usage.py` (5), `test_personal_usage_perf.py` (3), `test_migrations.py` (12), `test_format.py` (31), `test_models.py` (46) → **97 passed**, 0 failed, in 94.74s.

2. **Live HTTP against the already-running server (port 8000)** — genuine no-mocks re-verification, independent of the recorded evidence pass:
   - Minted a dev-bypass token, decoded its `sub`, called `GET /api/personal-usage/{sub}?range=30d` self-view (fresh random user, zero sessions): **200**, `cards` = exactly 4 order-locked entries (`Sessions`/`Total time`/`Total tokens`/`Avg tokens / session`), each with exactly the 5 keys (`glyph,value,label,iconBg,iconColor`), no `delta` key anywhere in the body, no `program_id` anywhere, `daily_tokens.points` = 30 entries, `commands` = `{total_runs:"0", items:[]}` (zero-session branch, FR-5).
   - Minted a second token and called the first user's endpoint from it (cross-user): **403**, body exactly `{"error":{"code":"http_403","message":"Forbidden","details":null}}` — byte-identical to TC-02's expected envelope.
   - `range=1d` (invalid) → **400** `{"error":{"code":"http_400","message":"invalid_range","details":null}}`. Omitted `range` → **200** (defaults to 30d). No token → **401**.
   - **Known gap, disclosed honestly**: could not re-seed TC-01's exact fixture rows (7 `user_sessions` + `usage_events`) against the live server to re-derive the literal `18.0K`/`2.6K`/`12.0K`/`400`/`60` figures live, because (a) `app/api/ingest.py` is a persistence-skeleton that does not write to `user_sessions`/`usage_events`, so there is no HTTP-reachable seeding path, and (b) direct DB seeding would require `services/api/.env`'s `DATABASE_URL` credentials, and reading `.env` is in the governance deny-set — the local Postgres on 5432 does not accept the `.env.example` placeholder credentials (auth failure), confirming it is not the default. This exact-figure assertion rests on the pytest-level confirmation in (1), which is itself a real end-to-end run against the actual migrated database, not a mock.
   - Similarly, the TC-02 log-content assertion (PII-free, exactly-one-record) rests on the pytest-level confirmation — no tail access to the already-running server's stdout was available to re-observe it live.

## Phase 3b — Contract conformance

One contract produced by SHP-02: `personal-usage-api` (`docs/requirements/api.md:186-201`). Compared recorded `shape` against the shipped surface:

| Field | Recorded shape | Shipped | Match |
|---|---|---|---|
| `cards` | 4 order-locked `{glyph,value,label,iconBg,iconColor}`, to-date | `app/schemas/personal_usage.py::PersonalUsageCard` + `_build_cards` (4-tuple, to-date `fetch_card_totals`) | ✓ |
| `daily_tokens` | `{points:[{date,value}], period_total, avg_per_day}`, range-scoped, zero-padded | `DailyTokenSeries` + `fetch_daily_token_series`/`_zero_padded_points` | ✓ |
| `commands` | `{total_runs, items:[{command,count:int,barStyle}]}`, max-of-range | `CommandsPanel`/`CommandEntry` + `_build_commands_panel` using `bar_style_for_share` | ✓ |
| `scope` (no `program_id`) | none anywhere | confirmed absent in schemas + live response body | ✓ |
| `rbac` | `individual_usage_visibility`, bare 403 | router's bare call, `app/core/rbac.py::individual_usage_visibility` | ✓ |
| `errors` | 400 `invalid_range` via shared `validate_range` | `_range_with_default` delegates to `app.dependencies.range.validate_range`, unedited | ✓ |

No drift. **PASS.**

## Task completion verification

| Task | Target file(s) | Existence | Section/content match | Verdict |
|------|-----------------|-----------|------------------------|---------|
| T-01 | `services/api/migrations/versions/002_personal_usage_indexes.py` (F-05) | ✓ | ✓ (`upgrade`/`downgrade` both create/drop the 2 named indexes) | PASS |
| T-02 | `services/api/app/models/rollup.py`, `app/models/ingestion.py` (F-06, F-07) | ✓ | ✓ (`Index("ix_user_sessions_user_id_started_at", ...)` on `UserSessions`; `Index("ix_usage_events_user_ts", ...)` appended to `UsageEvent`'s existing 4-index tuple) | PASS |
| T-03 | `services/api/app/utils/format.py` (F-08) | ✓ | ✓ (`bar_style_for_share` present, doctest matches ADR/FR-3 formula) | PASS |
| T-04 | `services/api/app/schemas/personal_usage.py` (F-01) | ✓ | ✓ (`PersonalUsageCard`/`DailyTokenPoint`/`DailyTokenSeries`/`CommandEntry`/`CommandsPanel`/`PersonalUsageResponse`, exact field/alias set) | PASS |
| T-05 | `services/api/app/services/personal_usage.py` (F-02) | ✓ | ✓ (3 bounded SELECT functions, `compute_average` zero-session branch, max-of-range `barStyle`) | PASS |
| T-06 | `services/api/app/api/personal_usage.py` (F-03) | ✓ | ✓ (`_range_with_default`, bare `individual_usage_visibility` call, route wiring) | PASS |
| T-07 | `services/api/app/main.py` (F-04) | ✓ | ✓ (`app.include_router(personal_usage_router)` immediately after `overview_router`) | PASS |
| T-08 | `services/api/tests/unit/test_personal_usage.py` (F-09) | ✓ | ✓ (`test_personal_usage_full_envelope_contract_tc01` + zero-session FR-5 test present, both PASS) | PASS |
| T-09 | `services/api/tests/unit/test_personal_usage.py` (F-09) | ✓ | ✓ (`test_personal_usage_cross_user_denied_bare_403_tc02` + AC-5/FR-6 range tests present, all PASS) | PASS |
| T-10 | `services/api/tests/perf/test_personal_usage_perf.py` (F-10) | ✓ | ✓ (3/3 parametrized ranges pass, query-count + duration budget) | PASS |
| T-11 | `README.md` (F-11) | ✓ | ✓ (API table row for `GET /api/personal-usage/{user_id}` present, matches ADR-0009 shape) | PASS |
| T-12 | `tests/test_migrations.py`, `docs/test-cases/SHP-02.json`, `tests/fixtures/prd_8_4_schema.json`, `tests/unit/test_format.py` (F-12..F-15) | ✓ | ✓ (`HEAD_REVISION` now derived via `ScriptDirectory.get_current_head()`; TC-01 assertions read `18.0K`/`2.6K`; fixture declares both new indexes; `test_format.py` carries `bar_style_for_share` boundary + max-vs-share regression tests) | PASS |

**12/12 tasks PASS.** No `blocked`/`skipped` statuses in `tasks.json`.

## Passed

| TC id | Title |
|---|---|
| SHP-02-TC-01 | Full personal-usage envelope: to-date cards + ranged daily token series + ranged commands panel |
| SHP-02-TC-02 | Cross-user request denied with bare 403 and PII-free individual_view_denied log |

## Performance (task-level, T-10 — SHP-02-NFR-performance)

Not TC-anchored (disclosed uncovered in `docs/test-cases/SHP-02.json` `coverage_audit` — explicit 2-TC cap for this run), but the required task-level perf test exists and passes comfortably:

| Range | Measured handler duration | Budget (NFR-002) | Result |
|---|---|---|---|
| 7d  | 968.66ms | ≤ 2000ms | PASS |
| 30d | 48.85ms  | ≤ 2000ms | PASS |
| 90d | 113.21ms | ≤ 2000ms | PASS |

Query-count budget (3 bounded SELECTs) held in all 3 cases. `budget_pass: true` for all.

## Disclosed coverage gaps (not defects)

Per `docs/features/SHP-02/REQUIREMENTS.md` § Approvals and `coverage_audit.uncovered`, an explicit user instruction capped this run at 2 TCs. `SHP-02-AC-5`/`FR-6` (range default/400) and `SHP-02-FR-5` (zero-session average) have no TC id, but both are exercised as regression tests inside T-09/T-08 (`test_personal_usage_omitted_range_defaults_to_30d_ac5`, `test_personal_usage_invalid_range_returns_400_fr6`, `test_personal_usage_zero_sessions_average_is_zero_not_none`) — all 3 confirmed PASS in this run, and the zero-session case was additionally re-confirmed live (see Phase 3). `SHP-02-NFR-performance` is likewise TC-uncovered but task-covered (T-10, above). This is disclosed, not a silent gap.

## Regression coverage

N/A — no TC in `docs/test-cases/SHP-02.json` carries a `regression-<id>` tag this run.

## Flaky tests

None.

## Manual follow-up

None — both TCs are `automatable: true`, no `last_run: null` entries.

## Known context (does not affect this verdict)

`state.json` `impl_evidence.verdict` is `BLOCKED` with a recorded, user-authorized override (`impl_evidence.override`, authorized 2026-09-08T02:18:06Z by pawar.pratik0903@gmail.com). The FAIL is confined to 6 perf-budget test files in `services/api/tests/perf/` that are **not** owned by SHP-02 (not in its `file_plan`) and were proven code-independent via a stash-out A/B under shared-machine load (~87-load-avg on 8 cores at override time). Independently re-confirmed in this validation: every file SHP-02 *does* own (`test_personal_usage.py`, `test_personal_usage_perf.py`, `test_migrations.py`, `test_format.py`, `test_models.py` — 97 tests) is green. Per the invocation's explicit instruction, these 6 unrelated files do not drive this validation verdict.

## Proof of run

```
### Proof of run

- session_id:        c071245a-4886-45a0-a3f6-878d3089f32a
- bash_invocations:  ~55
- test_runner_runs:  6 (pytest: 5-file suite; test_personal_usage.py -v; perf -v -s x2; TC-01 solo; TC-02 solo)
- stack_smoke_runs:  2 (fastapi-2: migrate+healthcheck; nextjs: start+healthcheck+teardown)
- duration_seconds:  ~240 (summed measured command execution; full wall-clock not separately instrumented)
- timestamp:         2026-09-08T03:17:53Z
```
