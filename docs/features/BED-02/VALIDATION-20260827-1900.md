# Validation report — BED-02

- Date: 2026-08-27T19:00:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, invoked by `/arh-implement` Validate ∥ Review gate)
- Total: 20   Passed: 20   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~28 minutes (environment provisioning + full review)

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   1   |   1  |   0  |   0   |   0   |
| Unit         |  14   |  14  |   0  |   0   |   0   |
| Integration  |   5   |   5  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

Stack smoke: `fastapi-2` (the only stack with a runnable, `services/api/**`-scoped server per `docs/config/stack-smoke.md`) — `alembic upgrade head` against the disposable Postgres (`bed02-evidence-pg`, port 5446) then `uvicorn app.main:app --port 8011`, polled `/health` → `200`. `next`/`nextjs` and `postgres` sections are datastore/frontend-only for this backend-only story and are not exercised by BED-02's scope (no `apps/web` or route-level changes). `pytest`/`alembic`/`typescript`/`pydantic` sections are `(n/a — ...)` tool-only entries, not separate processes.

Full backend suite (`cd services/api && uv run pytest`, `DATABASE_URL`/`TEST_DATABASE_URL` pointed at the disposable Postgres): **120 passed**, matching the pre-dispatch baseline of `120 passed in 2.21s` exactly (observed twice: `120 passed in 2.30s` and `120 passed in 3.00s`). No unexplained delta. This includes 61 BED-02 tests (`tests/unit/` + `tests/perf/`) plus the pre-existing 59 (`test_smoke.py`, `test_models.py`, `test_migrations.py`).

Order-dependence check (per dispatch instruction — two BED-02 tests were reported flaky under a specific run order earlier this session): ran `tests/test_migrations.py` immediately followed by `tests/unit/test_range_validation.py` (the exact adverse order that previously triggered AF-08-carry's vacuous-log-capture bug, since `migrations/env.py`'s `logging.config.fileConfig(disable_existing_loggers=True)` disables every already-existing logger process-wide) — all 28 tests in that combined run passed, including `test_invalid_range_logs_only_documented_fields` (TC-19) and `test_invalid_range_end_to_end_json_log_line` (TC-20). The `_isolated_range_logger` fixture forces `.disabled = False` regardless of ambient state, so this is no longer order-dependent.

**TC-19 vacuous-test check (explicit ask):** empirically verified TC-19's assertion is capable of failing. Reproduced the pre-fix condition (`logger.disabled = True`, simulating alembic's `fileConfig` side effect) in an isolated script against the real `app.dependencies.range` logger: `logger.warning(...)` produced **0** captured records, and the test's `assert len(range_logger_records) == 1` genuinely raised `AssertionError: 0 != 1`. The test is real, not vacuous.

**TC-15 regression coupling check (explicit ask):** `JSONFormatter.format` (`services/api/app/core/logging.py:55-57`) only surfaces `extra` fields because of the explicit `for key, value in record.__dict__.items(): if key not in _RESERVED_LOGRECORD_ATTRS and key not in payload: payload[key] = value` loop added for FR-3. Removing that loop (reverting to the fixed-key-set payload the docstring describes as the pre-fix behaviour) would make TC-15 fail — genuine coupling, not a tautology.

**TC-14 cross-router check (explicit ask):** `test_two_consumer_routers_return_identical_rejection` issues two separate HTTP requests — `GET /consumer-a/items` and `GET /consumer-b/reports` — against two independently-mounted `APIRouter` instances (`consumer_a`, `consumer_b`), each declaring its own `range: str = Depends(validate_range)`. Both responses are asserted independently and then cross-compared (`resp_a.json() == resp_b.json() == EXPECTED_ERROR_BODY`). Genuinely two consumers, not one assertion duplicated.

**TC-03/TC-04 400-vs-422 check (explicit ask):** `test_invalid_range_returns_400_not_422` asserts both `resp.status_code == 400` **and** `resp.status_code != 422` explicitly (not merely "not 2xx") across 5 parametrized invalid values including the case-sensitivity variant `"7D"` and empty string `""`. `validate_range()` performs the membership check in the function body (`app/dependencies/range.py:52`) ahead of any Pydantic coercion, raising `HTTPException(400, "invalid_range")` explicitly — the 422 path (FastAPI's default type-coercion error) is structurally unreachable for this parameter since `range: str` never fails to coerce from a query string. TC-04 separately asserts the full envelope byte-for-byte (`{"error": {"code": "http_400", "message": "invalid_range", "details": null}}`).

**TC-18 NFR-002 honesty note (explicit ask, already-accepted flag):** `tests/perf/test_range_pagination_perf.py` passes trivially (p95 well under 60ms across 50 requests) because its test route touches **no database** — `compute_guardrail_summary` runs over in-memory `ProgramGuardrail` instances, not real Postgres rows. This is documented in the test file's own module docstring and matches the already-triaged flag `AF-06` (`docs/features/BED-02/FLAGS.md`: `status=defer → AF-06-carry`). **NFR-002's real 2s end-to-end budget is NOT verified by this test** — it only proves the shared-contract layer itself (dependency resolution, range validation, pagination clamp, compute-function iteration) adds negligible overhead. Real verification requires a DB-backed consumer-story test (OVW/PGD/SHP). Not raised as a new finding; already carried forward.

## Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/dependencies/range.py` (validate_range Depends(), range_to_start) | ✓ | ✓ (Depends()-not-middleware, HTTPException(400,"invalid_range"), logger.warning route/param/rejected_value) | PASS |
| T-02 | `services/api/app/dependencies/pagination.py` (offset/limit max 50, page/page_size max 100) | ✓ | ✓ (`min(value, MAX)` clamp, `Query(..., ge=)` only, `MAX_PAGE_SIZE=100`) | PASS |
| T-03 | `services/api/app/dependencies/__init__.py` (barrel export) | ✓ | ✓ (`validate_range`, `range_to_start`, `get_offset_limit`, `get_page_params`, `ALLOWED_RANGES` in `__all__`) | PASS |
| T-04 | `services/api/app/core/logging.py` (JSONFormatter extras merge) | ✓ | ✓ (`_RESERVED_LOGRECORD_ATTRS` frozenset + merge loop, lines 13-39, 55-57) | PASS |
| T-05 | `services/api/app/services/rollup_compute.py` (adoption_percent, period_delta, average) | ✓ | ✓ (formulas in docstrings verbatim, matches TC-17) | PASS |
| T-06 | `services/api/app/services/guardrail_compute.py` (X/Y passing) | ✓ | ✓ (`PASSING_STATUS = "Enforced"`, formula docstring) | PASS |
| T-07 | `services/api/app/services/__init__.py` (barrel export) | ✓ | ✓ (`compute_adoption_percent`, `compute_average`, `compute_guardrail_summary`, `compute_period_delta` in `__all__`) | PASS |
| T-08 | `services/api/app/utils/format.py` (M/K numeric, h/m duration) | ✓ | ✓ (`format_number`, `format_duration`, promotion-on-rounding logic, D-09 boundary contract) | PASS |
| T-09 | `services/api/app/utils/__init__.py` (barrel export) | ✓ | ✓ (`format_duration`, `format_number` in `__all__`) | PASS |
| T-10 | `services/api/tests/unit/__init__.py` (package marker) | ✓ | — (chore, existence-only) | PASS |
| T-11 | `services/api/tests/unit/test_range_validation.py` (TC-01,02,03,04,14,19,20) | ✓ | ✓ (18 tests present, all pass; two consumer routers for TC-14, isolated logger fixtures for TC-19/20) | PASS |
| T-12 | `services/api/tests/unit/test_pagination.py` (TC-05,06,07,08) | ✓ | ✓ (6 tests present, all pass) | PASS |
| T-13 | `services/api/tests/unit/test_derived_values.py` (TC-09,10,17) | ✓ | ✓ (14 tests present, all pass) | PASS |
| T-14 | `services/api/tests/unit/test_format.py` (TC-11,12,13) | ✓ | ✓ (17 tests present, all pass) | PASS |
| T-15 | `services/api/tests/unit/test_logging.py` (TC-15,16) | ✓ | ✓ (4 tests present, all pass) | PASS |
| T-16 | `services/api/tests/perf/__init__.py`, `services/api/tests/perf/test_range_pagination_perf.py` (TC-18) | ✓ | ✓ (p95 assertion present and passes) | PASS |
| T-17 | `services/api/README.md` (shared-modules convention) | ✓ | ✓ ("app/dependencies/, app/services/, app/utils/ are the shared layer..." section present, lines 23-26) | PASS |
| T-18 | `docs/requirements/api.md` (api-conventions contract reconciliation) | ✓ | ✓ (`### api-conventions`, `produced_by: BED-02`, `shape:` block matches shipped signatures exactly — see Contract conformance below) | PASS |

**18/18 tasks PASS.** No `blocked` or `skipped` tasks in `tasks.json`.

## Contract conformance (Phase 3b)

`docs/requirements/api.md` `### api-conventions` (`produced_by: BED-02`) checked field-by-field against the shipped code:

| Contract field | Recorded shape | Shipped code | Match |
|---|---|---|---|
| `range.dependency` | `validate_range(request, range: str = Query(...)) -> str` | `app/dependencies/range.py:45` | ✓ |
| `range.allowed_values` | `{7d, 30d, 90d}` | `ALLOWED_RANGES` frozenset | ✓ |
| `range.rejection` | `HTTPException(400, 'invalid_range')` → documented envelope | `app/dependencies/range.py:61` + `app/core/errors.py` | ✓ |
| `range.window_helper` | `range_to_start(range_value, now=None) -> datetime`, naive `now` raises `ValueError` | `app/dependencies/range.py:65-81` | ✓ |
| `pagination.offset_limit` | clamps to 50, `MAX_OFFSET_LIMIT` not re-exported on barrel | `app/dependencies/pagination.py` + `__init__.py` | ✓ |
| `pagination.page_size` | clamps to 100, matches `activities.MAX_PAGE_SIZE` | `app/dependencies/pagination.py:14` + TC-08 | ✓ |
| `derived_values.adoption_percent` | formula + `None` on `programs_total==0` | `app/services/rollup_compute.py:17-39` | ✓ |
| `derived_values.guardrail_summary` | formula + `"0/0 passing"` on empty | `app/services/guardrail_compute.py:23-51` | ✓ |
| `formatting.numeric` | promotion-on-rounding, 1 decimal, M largest bucket | `app/utils/format.py:59-90` | ✓ |
| `formatting.duration` | h/m split, negative raises `ValueError` | `app/utils/format.py:93-114` | ✓ |

No drift found. **No contract-conformance findings.**

### Passed

| TC id | Title |
|---|---|
| BED-02-TC-01 | validate_range accepts 7d, 30d, 90d and scopes the query |
| BED-02-TC-02 | Accepted range maps to correct lookback-window boundary |
| BED-02-TC-03 | Invalid range returns HTTP 400, never 422 |
| BED-02-TC-04 | Invalid range rejection returns exact error_body() envelope |
| BED-02-TC-05 | offset/limit clamps limit to 50 when omitted/over-max |
| BED-02-TC-06 | offset/limit passes through within-bounds values unchanged |
| BED-02-TC-07 | page/page_size clamps page_size to 100 when omitted/over-max |
| BED-02-TC-08 | Shared page/page_size max matches activities.py's MAX_PAGE_SIZE |
| BED-02-TC-09 | adoption_percent computed server-side by services function |
| BED-02-TC-10 | "X/Y passing" computed server-side by services function |
| BED-02-TC-11 | M/K numeric formatter consistent across magnitude boundaries |
| BED-02-TC-12 | h/m time formatter consistent across duration boundaries |
| BED-02-TC-13 | No duplicate frontend M/K or h/m formatting utility exists |
| BED-02-TC-14 | Two routers on shared validate_range() return identical 400 |
| BED-02-TC-15 | JSONFormatter surfaces extra={route,param,rejected_value} fields |
| BED-02-TC-16 | JSONFormatter extras merge excludes reserved LogRecord attrs |
| BED-02-TC-17 | Every derived-value services function documents its formula |
| BED-02-TC-18 | Range/filter-change refresh under 2s budget (shared-contract overhead only — see honesty note above; does not verify real NFR-002) |
| BED-02-TC-19 | Invalid-range rejection logs only route/param/rejected_value, no PII |
| BED-02-TC-20 | validate_range() rejection triggers structured warning log (real dependency, end-to-end) |

### Flaky tests

None.

### Performance budget breaches

None. TC-18's own assertion passes (`p95 < 60ms` vs `2000ms` budget) — see honesty note above for why this does not constitute end-to-end NFR-002 verification.

### Regression coverage

None — no `regression-<id>`-tagged TCs in this manifest (BED-02 is a first-pass implementation, no prior fix-loop).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — all 20 TCs are `automatable: true`.

### Budget-unparseable NFRs

None — NFR-002's `p95 < 2000ms` budget parsed and was enforced (see TC-18).

## Observations (not failures)

- **TC-09/TC-10 deviate from their documented `preconditions`/`steps`.** The TC JSON specifies "Disposable test database migrated to head" and "Seed org_summary_rollup/program_guardrails with test_data counts" — but `tests/unit/test_derived_values.py` builds unattached `OrgSummaryRollup`/`ProgramGuardrail` ORM instances directly in Python, never touching Postgres. This is a deliberate, documented consequence of decision D-02 (`docs/features/BED-02/DECISIONS.md`): every derived-value function is DB-session-free by design (takes pre-fetched rows, does no querying itself), so a live DB adds no coverage for this AC. The behavioural assertion (`adoption_percent == 75.0` etc.) is still genuinely exercised. Not a defect — flagging so the TC JSON's `preconditions`/`steps` can be reconciled to match the DB-free design on a future touch of this manifest.

## Proof of run

```
### Proof of run

- session_id:        cafee0d3-bd1a-4d4d-a955-b604497448cd
- bash_invocations:  27
- test_runner_runs:  6  (uv run pytest -q ×3 full suite; uv run pytest tests/test_migrations.py tests/unit/test_range_validation.py -v ×1 order-check; uv run pytest tests/unit tests/perf -v --durations=0 ×1; uv run python -c "..." ×1 TC-19 empirical repro)
- stack_smoke_runs:  1  (uvicorn app.main:app --port 8011 start + poll http://127.0.0.1:8011/health -> 200, then killed)
- duration_seconds:  ~1680
- timestamp:         2026-08-27T19:00:00Z
```

Environment: disposable Postgres `bed02-evidence-pg` (port 5446, `dashboard` + `dashboard_test`), `DATABASE_URL`/`TEST_DATABASE_URL` exported for every DB-touching command, pytest run from `services/api`. Baseline `120 passed in 2.21s` reproduced as `120 passed` (2.30s, then 3.00s) across two independent full-suite runs — no delta from baseline, no new failures introduced.
