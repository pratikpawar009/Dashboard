# Security review — BED-04

- Date: 2026-09-15T13:15:00Z
- Verdict: PASS
- Governance profile: standard
- Compliance overlays applied: none
- Total findings: 1  (Critical: 0, High: 0, Medium: 0, Low: 1)
- Duration: ~4 min

## Inputs

- Story/PRD: `docs/features/BED-04/REQUIREMENTS.md`
- PLAN / DECISIONS / DATA-DESIGN: `docs/features/BED-04/{PLAN,DECISIONS,DATA-DESIGN}.md`
- REVIEW (round 2): `docs/features/BED-04/REVIEW.md` — verdict PASS, 0 CRIT/HIGH/MED
- Merged commit: `f5f91b2` — `feat(api): ingestion freshness accessor (BED-04) (#186)`
- Diff scope (source): `services/api/app/services/freshness.py` (new), `services/api/app/services/__init__.py` (barrel), `services/api/tests/unit/test_freshness.py` (new), `services/api/tests/perf/test_freshness_perf.py` (new)
- Docs touched: `services/api/README.md`, `docs/requirements/api.md#freshness-api`, artefact set under `docs/features/BED-04/`
- Governance source: `harness.yaml § governance.profile: [standard]`

## Summary

| Source          | Critical | High | Medium | Low |
|-----------------|----------|------|--------|-----|
| SAST (grep)     |    0     |  0   |   0    |  0  |
| Deps scan       |   N/A    | N/A  |  N/A   | N/A |
| OWASP checklist |    0     |  0   |   0    |  0  |
| Stack patterns  |    0     |  0   |   0    |  0  |
| Compliance      |    0     |  0   |   0    |  0  |
| Carry-forward   |    0     |  0   |   0    |  1  |

## Dependency vulnerability scan

Skipped — no dependency manifest touched by the diff. `git diff-tree` on `f5f91b2` shows the only `docs/requirements/api.md` match against the manifest regex is a documentation file (the `freshness-api` contract), not a Python/JS dependency manifest. `pyproject.toml`, `uv.lock`, `package.json`, `pnpm-lock.yaml` are all untouched, so no `pip-audit` / `pnpm audit` invocation is required.

## SAST grep pass

Ran the full `security-review-checklist § Detectable patterns` regex table against the BED-04 source diff (`git show f5f91b2 -- services/api/app/services/freshness.py services/api/app/services/__init__.py services/api/tests/unit/test_freshness.py services/api/tests/perf/test_freshness_perf.py`, 883 lines) plus a supplementary sweep for `print(...)`/`logger.*` PII strings and suppressor comments (`# noqa`, `# type: ignore`, `# SECURITY-OK`).

- Arbitrary code execution (`eval`, `exec`, `subprocess(shell=True)`, `os.system`): 0 hits.
- SQL injection (string-built SQL, f-string SELECT, `+ <var>`): 0 hits. The one SELECT is parameterised via SQLAlchemy Core (`select(SystemMetadata).where(SystemMetadata.key == "ingestion").limit(1)`, `freshness.py:150`), no user input.
- XSS sinks (`dangerouslySetInnerHTML`, `innerHTML=`, `document.write`): 0 hits. No frontend in this diff.
- JWT `algorithm: "none"`, `rejectUnauthorized: false`, `verify=False`: 0 hits.
- Weak hashes / ciphers (`MD5`, `SHA1`, `DES`, `ECB`): 0 hits.
- Hardcoded secrets (`(api_key|password|secret|token)\s*[:=]\s*"..."`): 0 hits.
- PII in logs (`email`, `password`, `token`, `ssn`, `phone` in `print()` / `console.log` / `logger.*`): 0 hits. The two `logger.warning()` sites (`freshness.py:157`, `freshness.py:164`) carry only `extra={"reason": "<internal code>"}` — no user identifier, no request content, matching REQUIREMENTS.md § NFR Security.
- Suppressor comments (`# noqa`, `# SECURITY-OK`, `// eslint-disable`): 0 hits. Nothing is silencing a rule.

## Manual OWASP checklist

| Category | Applies to diff? | Result |
|---|---|---|
| Input | Yes | Accessor takes no caller-supplied input — no query params, no body, no path variable. `WHERE key = 'ingestion'` is a literal. Nothing to validate. |
| Authentication / authorization | Yes | Deliberately unauthenticated + unauthorised — REQUIREMENTS.md § NFR Security records the rationale: freshness timestamp is a public dashboard artefact rendered on every persona, so RBAC gating is out of scope. DATA-DESIGN.md § 3 confirms no per-resource ownership check applies (singleton row, no caller-supplied id, no mutation). Consistent with the design; not a finding. Downstream consumers (OVW-01, ARC-01, DEV-01, PMD-01, EMD-01) own their own route-level authn/authz. |
| Data | Yes | Two non-PII columns read (`key`, `last_successful_run_at`); returned datetime is operational metadata. No PII in payload, no PII in logs (see SAST). No new field classification. |
| Crypto | No | No crypto operation introduced or modified. |
| Dependencies | No | No manifest change — see dependency scan section. |
| Logs and errors | Yes | Two `logger.warning()` sites use PII-free `extra` reason codes with a pinned key set enforced by TC-02's assertion `set(payload.keys()) == {"timestamp", "level", "logger", "message", "reason"}` (`test_freshness.py:145-147`). `HTTPException` `detail` strings ("ingestion job may not have run yet", "ingestion freshness query timed out") leak no internal state, path, id, or stack. Consistent with `security-review-checklist § Logs and errors`. |

## Stack-pattern pass (fastapi + typescript + nextjs + postgres + pytest + alembic + pydantic)

Per skill G15 caveat: every `<stack>-patterns/SKILL.md` body in this repo is an unfilled scaffold (`fastapi-patterns` etc. carry the "fill body with team conventions" TODO). Falling back to each framework's real, well-known idioms per `.claude/rules/pattern-consistency.md`:

- `fastapi`: `Depends()`-based auth / `response_model` / OAuth2 scopes — **N/A**. This story introduces no route (REQUIREMENTS.md § Scope explicitly excludes any HTTP surface). The accessor is a service class consumed via constructor injection by future stories, not a router endpoint. Nothing to enforce.
- `postgres`: parameterised queries only, no `text()` string concat — **satisfied**. See SAST §.
- `pydantic`: sensitive-field exclusion, strict schemas — **N/A**. No Pydantic model added or changed by this diff.
- `pytest` / `alembic` / `next` / `typescript`: no relevant surface introduced by this diff.

No stack-pattern findings.

## Compliance overlay

Governance profile is `standard` (list-of-one in `harness.yaml § governance.profile`), no `hipaa` / `pci` / `sox` / `gdpr` overlay active. No `.claude/rules/governance/<profile>/` directory to apply beyond baseline. Compliance section is `N/A — profile: standard`.

## Cross-reference with REVIEW.md

REVIEW.md (round 2, verdict PASS) already asserted the security-adjacent claims this pass would otherwise re-derive — not re-flagged here (double-flagging is prohibited by skill `security-assessment § Inputs`):

- Read-only, two non-PII columns, parameterised `select()`, both warnings PII-free with an assertion pinning the exact key set — confirmed by REVIEW.md § Dimension verdicts → Safety & security.
- `_QUERY_TIMEOUT_MESSAGE` and `_NOT_RUN_MESSAGE` remain module-private, not re-exported on the barrel — confirmed by REVIEW.md § Answers to the gate's specific questions → 4 (Barrel hygiene).
- Contract drift is additive, not breaking — confirmed by REVIEW.md § 6.

## Compliance carry-forward

`state.json § pending_carry_forward` was walked; no entry carries a `hipaa` / `phi` / `pci` / `sox` / `gdpr` / `audit-logging` tag, and the active profile is `standard`. The RC3 rule ("carry-forward tagged with the active profile forces BLOCKED regardless of counts") does not fire. `CF-BED-04-03` (repo-wide unbounded DB timeouts) and `CF-BED-04-04` (mid-statement cancel behaviour) touch reliability/DoS risk but are not compliance-tagged and are correctly out of BED-04's scope — see Low findings below.

## Findings

### Low

#### F-1 — carry-forward: `CF-BED-04-03` repo-wide DB reads remain unbounded

- Source: `carry-forward/CF-BED-04-03` (state.json § pending_carry_forward)
- Severity: Low (informational — does not block this PR)
- File: `services/api/app/core/db.py:17`
- Issue: `create_async_engine(settings.database_url)` sets no `connect_args`, no `command_timeout`, no statement timeout. Every merged story other than BED-04 issues DB reads without an explicit timeout — a stalled connection can hang the calling request and, via pool exhaustion, adjacent requests too. This is a latent DoS / reliability surface identified by both the code review and validation agents; BED-04's own accessor is fine (D-04's `asyncio.wait_for` closes the local risk), but the surface remains open across AUTH-01..04, BED-01..03, ING-01.
- Applies to BED-04 diff? No — deliberately deferred. D-04 records the scope decision; fixing engine-wide would need its own ADR + story (per `.claude/rules/pattern-consistency.md`, a one-off local fix would reduce consistency).
- Verdict impact: none. Carry-forward is not compliance-tagged; recorded here so the successor story cannot claim absence of a known issue.
- Fix: address once, repo-wide, in a follow-up story with its own ADR.

## Tool-missing report

- `pip-audit` (fastapi/uv): not on `PATH`. Not blocking — dependency scan was skipped for this diff (no manifest change), so the missing tool did not gate anything. Install in CI before a manifest-changing story reaches this gate.

## Action plan

1. No Critical / High findings to fix — nothing blocks merge.
2. No Medium findings.
3. Track `F-1` under its existing carry-forward (`CF-BED-04-03`); do not create a duplicate item.
4. Install `pip-audit` in CI before the next story that edits `pyproject.toml` / `uv.lock` reaches `/arh-security-review`.

## Verdict

**PASS** — 0 Critical, 0 High, 0 Medium; 1 Low (carry-forward-only, non-compliance-tagged); dependency scan not required; SAST clean; OWASP walk clean; stack patterns N/A or satisfied; no compliance-tagged carry-forward blocks.
