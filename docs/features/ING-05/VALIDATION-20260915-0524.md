# Validation report — ING-05

- Date: 2026-09-15T05:24:05Z
- Verdict: **PARTIAL**
- Mode: full (GATE MODE — report-only; state-write deferred to `/arh-implement` orchestrator)
- Total automatable TCs: 3   Passed: 3   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Node unit-test blocks: 23   Passed: 23   Failed: 0
- Duration: ~46 s (unit-test wall-clock 41.4 s + preflight + report write)
- Branch: `feature/ING-05` @ `5a5d820404355949a67ceb25383eab7e0e3c844f`
- Snapshot SHA (source-scoped): `13a86279eddf4f5516529661d74cc72980c03b58818536e72ef62c1359b4cd42` (matches expected)

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   0   |   0  |   0  |   0   |   0   |
| Unit         |  23   |  23  |   0  |   0   |   0   |
| Integration  |   0   |   0  |   0  |   0   |   0   |
| E2E          |   0   |   0  |   0  |   0   |   0   |
| Performance  |   0   |   0  |   0  |   0   |   0   |

Verdict justification — PARTIAL because: (a) all 3 automatable TCs green; (b) all 5 FRs, 5 ACs, and 4 NFRs traced to evidence (see § FR/NFR/AC evidence trace below); (c) task-completion check flags **T-07** with two `## Verify` and `## Manual E2E walkthrough` sections missing from [docs/how-to/copilot-chat-setup.md](docs/how-to/copilot-chat-setup.md) — plan-doc gap, not a source bug. No regressions. Stack-smoke is `n/a` (no runnable stack owned by this feature per PLAN §3 — the hook talks over loopback to the already-live ING-04 MCP server; no new port bound).

## TC results

| TC id | Type | Title | Status | Verdict | Attempts | Duration | Anchor test(s) |
|---|---|---|---|---|---|---|---|
| ING-05-TC-01 | unit | program_id env-wins / yaml-fallback / unresolved-skips-write | PASS | PASS | 1 | ~1.2 s (4 blocks) | `copilot-activity.test.mjs` blocks 1–4 |
| ING-05-TC-02 | unit | MCP push aborts by TIMEOUT_MS; parent hook non-blocking | PASS | PASS | 1 | ~10.7 s | `harness-mcp-push.test.mjs` block (c)+(d); `copilot-activity.test.mjs` block 13 (fire-and-forget spawn) |
| ING-05-TC-03 | unit | Push failures write exactly one log line and exit 0 | PASS | PASS | 1 | ~10.6 s | `harness-mcp-push.test.mjs` blocks (e)+(f)+(g)+(h) |

Full node-test log: [docs/features/ING-05/validation/node-test.log](docs/features/ING-05/validation/node-test.log).

## Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | [.github/hooks/copilot-activity.mjs](.github/hooks/copilot-activity.mjs) (F-01: `JOURNAL_CODEC_VERSION`, `resolveProgramId`, feature regex, structured logs, `source:"copilot"`) | ✓ | ✓ (all identifiers grep-present at lines 49, 88, 254, 517, 543) | PASS |
| T-02 | [.github/hooks/harness-mcp-push.mjs](.github/hooks/harness-mcp-push.mjs) (F-02: `TIMEOUT_MS = 10_000`, `writeLog` + rotation, `mcp_url_non_loopback` refuse, always `exit(0)`) | ✓ | ✓ (`TIMEOUT_MS = 10_000` line 41; `writeLog` line 62; loopback refuse lines 195/201; `process.exit(0)` lines 198/202/241) | PASS |
| T-03 | [.github/hooks/\_\_tests\_\_/fixtures/chat-session.jsonl](.github/hooks/__tests__/fixtures/chat-session.jsonl) (F-05), [transcript.jsonl](.github/hooks/__tests__/fixtures/transcript.jsonl) (F-06), [program.yaml](.github/hooks/__tests__/fixtures/program.yaml) (F-07) | ✓ ✓ ✓ | ✓ | PASS |
| T-04 | [.github/hooks/\_\_tests\_\_/copilot-activity.test.mjs](.github/hooks/__tests__/copilot-activity.test.mjs) (F-03) | ✓ | ✓ (13 blocks, all green) | PASS |
| T-05 | [.github/hooks/\_\_tests\_\_/harness-mcp-push.test.mjs](.github/hooks/__tests__/harness-mcp-push.test.mjs) (F-04) | ✓ | ✓ (10 blocks a–j, all green) | PASS |
| T-06 | [docs/adr/0015-program-id-sourcing-precedence.md](docs/adr/0015-program-id-sourcing-precedence.md) (F-09); [docs/adr/README.md](docs/adr/README.md) index row (F-10) | ✓ ✓ | ✓ (index row line 19) | PASS |
| T-07 | [docs/how-to/copilot-chat-setup.md](docs/how-to/copilot-chat-setup.md) (F-08: 6 required sections per PLAN — Prerequisites / Wire the hook / Environment variables / **Verify** / Troubleshooting / **Manual E2E walkthrough**) | ✓ | **MISSING: `## Verify` header + `## Manual E2E walkthrough` section** — file has §4 "What gets recorded" (row-shape reference, not step-by-step verify) and §§ 6–7 "Backward compatibility" + "Reference" instead. Walkthrough content that closes AC-5 Manual + research C-7 is not present as a step list. | **FAIL** |
| T-08 | (manual execution — `files: []`; walkthrough documented by T-07 F-08) | — | — | PARTIAL (manual — no artefact recorded; cascades from T-07 gap) |

Every task's `status` is `done` in tasks.json ✓. Every non-empty `files_touched[]` path exists on disk ✓. T-07 is the sole task-completion FAIL; drops report verdict to PARTIAL minimum per validation-execution § Verdict rule.

## FR / NFR / AC evidence trace

| Requirement | Evidence type | Anchor |
|---|---|---|
| ING-05-FR-1 (`program_id` per row + skip on unresolved) | Unit — 4 sub-tests | `copilot-activity.test.mjs` blocks 1–4 (env-wins, yaml `program_id`, yaml legacy `programId`, unresolved-skip) |
| ING-05-FR-2 (`JOURNAL_CODEC_VERSION` pin + unknown-kind skip-and-log-once) | Unit — 1 sub-test | `copilot-activity.test.mjs` block 5 (kinds `[0, 99, 1, 2, 99]` → 1 row, 1 log line) |
| ING-05-FR-3 (`cache_read=0` / `cache_write=0`) | Unit — 1 sub-test | `copilot-activity.test.mjs` block 6 |
| ING-05-FR-4 (feature bare-token positional regex) | Unit — 6 parametrised sub-tests | `copilot-activity.test.mjs` blocks 7–12 |
| ING-05-FR-5 (single-attempt, no retry, `TIMEOUT_MS = 10_000`) | Unit — 2 sub-tests | `harness-mcp-push.test.mjs` blocks (c)+(d) — abort by ≤10.5 s, `fetch` called exactly once |
| ING-05-AC-1 (row emit + upsert) | Unit — 4 sub-tests | `copilot-activity.test.mjs` blocks 1, 2, 3, 6 |
| ING-05-AC-2 (fire-and-forget spawn) | Unit — 1 sub-test | `copilot-activity.test.mjs` block 13 (`detached:true`, `stdio:'ignore'`, `child.unref()` called, parent returns without awaiting child) |
| ING-05-AC-3 (MCP RPC sequence order) | Unit — 1 sub-test | `harness-mcp-push.test.mjs` block (b) — `initialize → notifications/initialized → tools/call push_activity → DELETE` |
| ING-05-AC-4 (IPv4-loopback default `http://127.0.0.1:3010/mcp`) | Unit — 2 sub-tests | `harness-mcp-push.test.mjs` blocks (a) + (i) (non-loopback refused with `mcp_url_non_loopback` log) |
| ING-05-AC-5 (silent-on-failure, exit(0)) | Unit — 3 sub-tests | `harness-mcp-push.test.mjs` blocks (e)+(f)+(g); PARTIAL for the manual-live-run half — T-08 not executed |
| ING-05-NFR-performance | Unit + evidence log | TC-02 abort-≤10.5 s ceiling; [evidence/runtime.log](docs/features/ING-05/evidence/runtime.log) records real-run 0.171 s + 0.224 s exits |
| ING-05-NFR-security (loopback-only, no PII in log) | Unit — 2 sub-tests | `harness-mcp-push.test.mjs` blocks (h) allowlist + (i) non-loopback refuse; hook grep confirms no `Authorization` / bearer read |
| ING-05-NFR-accessibility | N/A | background Node hook, no UI surface (PRD § Non-functional requirements) |
| ING-05-NFR-observability (100-line / 32 KB `.mcp-push.log` rotation, event vocabulary) | Unit — 1 sub-test | `harness-mcp-push.test.mjs` block (j) — head-drop rotation after cap breach |

All 5 FRs, 5 ACs, and 3 applicable NFRs (NFR-accessibility is N/A) have a direct evidence anchor. AC-5's manual live-run half is only partially covered — see § Follow-ups (manual). The 7 uncovered IDs from `docs/test-cases/ING-05.json coverage_audit.uncovered` (D-04 Product-Gate-approved cap) are exercised inline per PLAN §7 and traced above.

## PLAN completeness

- Task DAG: ✓ acyclic (per PLAN §5 validation); all 8 tasks `status: done`.
- `file_plan` F-01..F-10: ✓ every file exists on disk (see § Task completion verification).
- Task-file cross-check: ✓ no task references a file that wasn't produced.
- Section-match gap: ✗ T-07 F-08 missing `## Verify` and `## Manual E2E walkthrough` sections required by PLAN §5 F-08 spec and PRD § Documentation requirements. Content in §4 "What gets recorded" and §5 "Troubleshooting" partially substitutes for these but does not include the step-by-step walkthrough closing research C-7 + AC-5 Manual.

`plan_completeness: GAP: T-07 how-to missing 'Verify' + 'Manual E2E walkthrough' sections (PLAN §5 F-08 section-list; PRD § Documentation requirements)`

## Contract conformance

N/A — ING-05 produces zero shared contracts (per REQUIREMENTS.md and PLAN §4 — pure consumer of the frozen `mcp-tools → push_activity` contract owned by ING-04 and the `ActivityRowIn` schema owned by ING-02). No `produced_by: ING-05` sections in `docs/requirements/*.md`.

## Failed (bug blocks)

None. Zero source-file regressions surfaced.

## Follow-ups (manual)

- **T-08 execution** — the manual E2E walkthrough (real Copilot Chat sessionEnd → `activity.jsonl` row → `.mcp-push.log` success → dashboard visibility → 20-session p95 timing) has NOT been executed. `tasks.json` records T-08 as `status: done` with `files_touched: []` and a note that execution is by a human engineer post-merge. Evidence is a tracker comment which cannot be verified by this agent. This is the manual half of AC-5 and closes research C-7.
- **T-07 doc gap** — [docs/how-to/copilot-chat-setup.md](docs/how-to/copilot-chat-setup.md) is missing the `## Verify` (step-by-step: trigger command → tail `activity.jsonl` → tail `.mcp-push.log` → dashboard check) and `## Manual E2E walkthrough` (closes C-7 + AC-5 Manual, 20-session p95 timing) sections that PLAN §5 F-08 and PRD § Documentation requirements pin. Neither is a source bug; both are doc completeness. Fix is a doc-only edit — append §§ Verify + Manual E2E walkthrough with the PRD-listed content.

## Proof of run

```
- session_id:        vscode:55114403-6754-4abb-aa5c-a4daab977f06
- bash_invocations:  ~9 (git rev-parse, node --check, node --test, file listing, greps, timestamps)
- test_runner_runs:  1 (node --test .github/hooks/__tests__/copilot-activity.test.mjs .github/hooks/__tests__/harness-mcp-push.test.mjs — 23 blocks, all pass)
- stack_smoke_runs:  0 (no runnable stack owned by this feature — n/a)
- duration_seconds:  ~46 (gate start 2026-09-15T05:21:18Z → gate end 2026-09-15T05:24:05Z minus report authoring)
- timestamp:         2026-09-15T05:24:05Z
- snapshot_sha:      13a86279eddf4f5516529661d74cc72980c03b58818536e72ef62c1359b4cd42  (source-scoped `git status --porcelain -- .github/hooks docs/features/ING-05 docs/how-to/copilot-chat-setup.md`)
- node_version:      v22.17.1
- head_commit:       5a5d820404355949a67ceb25383eab7e0e3c844f
- branch:            feature/ING-05
- test_summary:      ℹ tests 23 · pass 23 · fail 0 · skipped 0 · duration_ms 41431.221
```
