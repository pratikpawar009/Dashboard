# Validation report — PGD-07

- Date: 2026-09-16T12:50:00Z
- Verdict: PASS
- Mode: full (GATE MODE — report-only)
- Total: 21   Passed: 21   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~35 minutes wall-clock (env setup + smoke + full suite)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |   9   |   9  |   0  |   0   |   0   |
| Integration  |  10   |  10  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |

Stack smoke: both declared runnable stacks (`nextjs`, `fastapi`/`fastapi-2`) checked. `nextjs`: `pnpm dev --port 3000` → `GET /` 307 → `/login` (expected, unauthenticated), `GET /login` 200, `GET /programs/prog-123` 307 (expected redirect). `fastapi`: Postgres 16 (ephemeral smoke container, host port 5433 — host 5432 was occupied by a pre-existing, unrelated local Postgres process outside this validation's control; verified via `lsof`/`netstat`, no dev DB touched), `alembic upgrade head` clean through revision `006_usage_events_source_credits`, `uvicorn --port 8000` → `GET /health` 200. Contract conformance (Phase 3b): N/A — PGD-07 produces no contract (`docs/requirements/*.md` grep for `produced_by: PGD-07` returns nothing; REQUIREMENTS.md confirms "Downstream: none").

Additional gates run beyond the 21 TCs: `tsc --noEmit` clean; `eslint .` clean; backend `pytest` 700 passed / 5 deselected; backend `mypy`/`ruff check` clean. These aren't TCs in the manifest but are the project's own required commands (`docs/config/project-commands.yaml`) and confirm the change set introduces no regression elsewhere in the two touched stacks.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `apps/web/src/app/programs/[program_id]/page.tsx` (F-01: concurrent `GET /api/me` fetch, sentinel, C-4 comment) | ✓ | ✓ (`Promise.all`, `PERSONA_RESOLUTION_ERROR` sentinel, comment references `tokenStore.refreshPromise` at line 62) | PASS |
| T-02 | `apps/web/src/components/ProgramDetailView.tsx` (F-02: wrap in `PersonaDashboardShell`, `program: undefined`) | ✓ | ✓ (`PersonaDashboardShell program={undefined}` with FR-2 comment) | PASS |
| T-03 | `apps/web/src/components/ProgramDetailView.test.tsx` (F-03: shell-wrap + `program:undefined` enforcement tests) | ✓ | ✓ (`describe` block "PersonaDashboardShell program:undefined enforcement (PGD-07-TC-02 / PGD-07-FR-2-TC-01)") | PASS |
| T-04 | `apps/web/src/components/ProgramDetailView.test.tsx` (F-03: Condition C-3 persona-parameterized test) | ✓ | ✓ (`it.each`-style block "renders jobTitle and avatar colour matching PERSONA_DISPLAY[...]" across 5 personas) | PASS |
| T-05 | `apps/web/src/components/ProgramDetailView.authFlow.test.tsx` (F-04: fix selectors for new shell wrapper) | ✓ | N/A — task's own record states no change was required (persona undefined in every fixture here suppresses all persona-gated regions, so existing queries still resolve); file is untouched in `git status`, consistent with that claim; 6/6 existing tests still pass in the full run | PASS |
| T-06 | `apps/web/src/lib/composeSignedInUser.test.ts` (F-07: Condition C-1 decoy-jobTitle fixture, non-cio persona) | ✓ | ✓ (test titled "...for a non-cio persona... (PGD-07-TC-C1)") | PASS |
| T-07 | `apps/web/src/components/PersonaDashboardShell.test.tsx` (F-06: Condition C-2 sign-out-in-both-branches) | ✓ | ✓ (test titled "PGD-07-TC-C2: renders the sign-out control in both the populated and neutral-fallback identity branches...") | PASS |
| T-08 | `apps/web/src/app/programs/[program_id]/page.test.tsx` (F-05: new page-level composition harness) | ✓ | ✓ (all 5 mapped `describe` blocks present: FR-1-TC-01, NFR-performance-TC-01, TC-C4, TC-09, FR-3-TC-01, TC-05) | PASS |

All 8/8 tasks verified PASS. No `blocked`/`skipped` status in `tasks.json`.

### Passed

| TC id | Title |
|-------|-------|
| PGD-07-TC-01 | Brand bar region renders above ProgramDetailHeader |
| PGD-07-TC-02 | Shell receives program: undefined explicitly, no header double-render |
| PGD-07-FR-2-TC-01 | program: undefined enforcement — caller obligation |
| PGD-07-TC-03 | Identity block sourced from GET /api/me — cio persona |
| PGD-07-TC-04 | Identity block renders correctly for all five personas |
| PGD-07-TC-05 | Loading state — only static brand bar shows |
| PGD-07-TC-06 | 403 persona-resolution failure — neutral badge and aria-live announcement |
| PGD-07-TC-07 | Unrecognized persona value — neutral badge and aria-live announcement |
| PGD-07-FR-3-TC-01 | Persona-resolution-error sentinel passed to shell, not omitted persona |
| PGD-07-TC-08 | signedInUser.name null — neutral gray circle fallback |
| PGD-07-TC-09 | 401 from GET /api/me — existing unauthorized handling applies |
| PGD-07-TC-10 | Sign-out control renders on Program Detail identity block |
| PGD-07-TC-11 | Sign-out control is the same PersonaDashboardShell control, not a fork |
| PGD-07-FR-1-TC-01 | Promise.all coordinates /api/me and program-detail fetch |
| PGD-07-NFR-performance-TC-01 | One GET /api/me call per render, concurrent, ≤3s render budget |
| PGD-07-NFR-security-TC-01 | Identity block source is bearer session only |
| PGD-07-NFR-accessibility-TC-01 | Persona-unavailable aria-live announcement visually hidden and assertive |
| PGD-07-TC-C1 | composeSignedInUser omits decoy jobTitle from wire response |
| PGD-07-TC-C2 | Sign-out control renders in BOTH populated and neutral-fallback branches |
| PGD-07-TC-C3 | jobTitle/avatar colour derive from PERSONA_DISPLAY across all 5 personas |
| PGD-07-TC-C4 | SessionExpiredError from either fetch triggers coordinated redirect |

Evidence for each: `pnpm -C apps/web test` → 184/184 tests pass, 31 files, including `apps/web/src/app/programs/[program_id]/page.test.tsx` (new, 319 lines, T-08) and the modified `ProgramDetailView.test.tsx`, `PersonaDashboardShell.test.tsx`, `composeSignedInUser.test.ts`. Source-level cross-check performed for every AC/FR against the real diff (`git diff origin/main`) rather than trusting task claims alone — see AC-5 note below.

**AC-5 / AF-01 specifically verified against source**, not just test pass/fail: `PersonaDashboardShell.tsx` diff adds the "Persona unavailable" badge + `aria-live="assertive"` block inside the identity region, guarded on `personaColor === null && program === undefined && pageTitle === undefined` — this guard is provably scoped to routes (Program Detail) where neither of the shell's own header regions mounts, since both existing header blocks are gated on `program !== undefined` / `pageTitle !== undefined` respectively. `personaColor` resolves to `null` for the `PERSONA_RESOLUTION_ERROR` sentinel via the same `PersonaTagError`-catch mechanism already used elsewhere in the file (`personaAvatarColor` throws for a non-enum persona, caught, returns `null`). The regression risk AF-01 flagged (double-render on `/overview`/org-header) is covered by the existing suite (`SHP-01-TC-02`, OVW-05 `AC-8/AC-9/AC-10`) and confirmed still green in the same 184/184 run.

### Flaky tests

None.

### Performance budget breaches

None. `PGD-07-NFR-performance-TC-01` is assertion-based (call-count + Promise.all concurrency ordering, not a wall-clock k6/perf run) per the project's existing precedent (OVW-05) for this TC type in a vitest-only stack — `docs/config/project-commands.yaml` `test_e2e`/dedicated perf runner is unset. `budget_pass: true` recorded on the concurrency/call-count assertions the test actually makes; no numeric latency budget is enforced by tooling in this repo today.

### Regression coverage

No TC in this manifest carries a `regression-<id>` tag — all four Research-Condition TCs (`PGD-07-TC-C1`..`C4`) are net-new coverage for conditions raised at plan time, not fixes for a prior validation failure. The AF-01 fix (`PersonaDashboardShell.tsx`) is itself covered by inverted assertions in `page.test.tsx`'s `PGD-07-FR-3-TC-01` block (the two assertions that originally documented the bug — badge absent, no `aria-live` — were flipped to pin the corrected behaviour, per FLAGS.md). No dedicated `regression-AF-01` tag exists in the TC manifest; not required since the affected TCs (`PGD-07-TC-06`/`TC-07`, listed in FLAGS.md `Affects:`) already exist and now pass against the fixed code.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None. Story's own Test mapping states "Manual: NA — covered by unit tests; no e2e framework to add manual-only coverage for." All 21 TCs are `automatable: true`.

### Budget-unparseable NFRs

None.

### Carry-forward (returned to orchestrator — GATE MODE, not self-written)

No new carry-forward items from this validation run. Two pre-existing items from FLAGS.md remain open and are restated here for the orchestrator's awareness, not newly raised:

1. **AF-01 carry-forward (for OVW-05's owner)**: `PersonaDashboardShell.tsx` now has two badge render paths (header-region-owned, and the new identity-block fallback added here). If the header regions are ever refactored, the identity-block fallback's guard (`personaColor === null && program === undefined && pageTitle === undefined`) must be revisited alongside them.
2. **`DQA-1-preauth-contrast` (PLAN.md § Risks accepted)**: the inherited `#8a93a1` brand-bar tagline/jobTitle grey is 3.10:1 contrast on white, failing WCAG AA's 4.5:1 for normal text. This story reproduces the existing OVW-05 defect on a fifth surface (`/programs/<program_id>`). Accepted, not fixed here — remains OVW-05's carry-forward per that story's Decision log.
3. **AF-02 / AF-03 (evidence-na, both pre-existing)**: `design_check` unwired (no a11y/visual tool configured project-wide) and `apps/web` has no E2E/browser tooling (`test_e2e` empty), so `render_check` evidence is boot-only. Neither blocks this verdict — both are documented, pre-existing repo-wide gaps, not PGD-07-introduced defects.

No `.pending_carry_forward` entries generated for manual TCs (none exist in this manifest).

### Proof of run

```
### Proof of run

- session_id:        e54287ac-16c3-4448-beba-2a7a561bac99
- bash_invocations:  ~28
- test_runner_runs:  4 (vitest x1 full suite [184 tests/31 files], tsc --noEmit x1, eslint x1, pytest x1 [700 passed/5 deselected], mypy x1, ruff check x1)
- stack_smoke_runs:  2 (nextjs: dev server start + / , /login, /programs/prog-123 checks; fastapi: postgres(ephemeral smoke container, port 5433) + alembic upgrade head + uvicorn start + /health check)
- duration_seconds:  ~2100
- timestamp:         2026-09-16T12:50:00Z
```
