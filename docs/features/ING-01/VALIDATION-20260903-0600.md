# Validation report — ING-01

- Date: 2026-09-03T06:00:53Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, invoked from `/arh-implement` Step 2 Validate ∥ Review gate; source snapshot `3e00f83db836`)
- Total: 24   Passed: 24   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: 575s

## Summary

| Layer                          | Total | Pass | Fail | Error | Flaky |
|---------------------------------|-------|------|------|-------|-------|
| Stack smoke (fastapi-2)         |   1   |   1  |   0  |   0   |   0   |
| TC type: integration             |  18   |  18  |   0  |   0   |   0   |
| TC type: security                 |   5   |   5  |   0  |   0   |   0   |
| TC type: performance              |   1   |   1  |   0  |   0   |   0   |
| Full backend suite (cross-check) | 419   | 419  |   0  |   0   |   0   |

- **Stack smoke scoping**: ING-01 ships no HTTP route and no frontend surface (`REQUIREMENTS.md` § Scope / § Visual spec — `design_mode = none`, no `apps/web` files in `tasks.json` `file_plan`). Only the `fastapi-2` stack was smoked; `nextjs` was not, as no consumer of this story's changes exists there. `fastapi-2` smoke: migrated a disposable test DB (`localhost:5443/dashboard_test`, container `bed03-manual`) with `alembic upgrade head` (exit 0), booted `uvicorn` on port **8099** (own PID 15726, confirmed via `lsof` before and after — distinct from the pre-existing `:8000` squatter, PID 21566, left untouched), `GET /health` → `200 {"status":"ok"}`, then killed the process and confirmed the port was released.
- **Full backend suite cross-check**: `cd services/api && uv run pytest -q` → `419 passed`, matching the orchestrator's pre-measured figure exactly, no disagreement. `uv run mypy .` → `Success: no issues found in 88 source files` (matches). `uv run ruff check .` → `All checks passed` (matches).
- **TC-22 (performance) cross-check**: my own measured run: **p95 = 0.7100ms** (min 0.3287ms, max 1.7164ms) across 100 calls, vs the orchestrator's pre-measured **0.78ms** — both comfortably under the 10ms budget; the ~0.07ms delta is normal microbenchmark run-to-run variance, not a disagreement worth flagging.
- **Preflight**: backend preflight (`uv sync`, `import app.main`, `import authlib` 0.15.6, `import respx` 0.23.1, `import yaml` 6.0.3) all exit 0. `pnpm -C apps/web install` exits **1** (`ERR_PNPM_IGNORED_BUILDS` on `unrs-resolver@1.12.2`) — pre-existing, unrelated to this backend-only story (already triaged and deferred as `AF-03` in `docs/features/ING-01/FLAGS.md`/`state.json`). Does not block this story's validation.

## Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/core/ingest_auth.py` (`get_ingest_token()`, scope check, denial logging) | ✓ | n/a (feat) | PASS |
| T-02 | `services/api/scripts/mint_ingest_token.py` (argparse mint CLI) | ✓ | n/a (feat) | PASS |
| T-03 | `services/api/tests/unit/test_mint_ingest_token.py` (TC-01,02,03,04,05,23) | ✓ | n/a (test) | PASS |
| T-04 | `services/api/tests/unit/test_ingest_token_auth.py` (TC-06..20,24) | ✓ | n/a (test) | PASS |
| T-05 | `services/api/tests/unit/test_ingest_token_isolation.py` (TC-21) | ✓ | n/a (test) | PASS |
| T-06 | `services/api/tests/perf/test_ingest_token_auth_perf.py` (TC-22) | ✓ | n/a (test) | PASS |
| T-07 | `docs/requirements/auth.md` § `ingest-token-auth` (fill contract shape) | ✓ | ✓ — independently re-verified line-by-line against shipped `app/core/ingest_auth.py`/`scripts/mint_ingest_token.py`: function signature, 401/403 branch table, scope semantics, mint flags, `ingest_token_auth_failed` field set all match exactly | PASS |
| T-08 | `services/api/README.md` § `## Ingest token auth` (mint invocation + bearer contract + scope/lifetime defaults) | ✓ | ✓ — section present at line 86, mint example with all 3 flags, `--program-ids` optionality/allow-all note, `expires_at` null default, ADR-0006 §3/§4 pointers, AF-01 dev-DB warning | PASS |

8/8 tasks PASS. `files_touched` claims in `tasks.json` all resolve to real, non-empty files (T-07's empty `files_touched=[]` is correct — the task's own note says no edit was required because the contract section was already filled at plan time; independently confirmed true above, not merely trusted).

## Independent coverage verification

Every one of the 24 `ING-01-TC-*` ids in `docs/test-cases/ING-01.json` was matched, by grep, to exactly one test function across the four implementing files — no orphan TC, no orphan test function, no manifest/test drift:

| TC range | File | Test functions found |
|---|---|---|
| TC-01,02,03,04,05,23 | `tests/unit/test_mint_ingest_token.py` | 6/6 |
| TC-06..20,24 | `tests/unit/test_ingest_token_auth.py` | 16/16 |
| TC-21 | `tests/unit/test_ingest_token_isolation.py` | 1/1 |
| TC-22 | `tests/perf/test_ingest_token_auth_perf.py` | 1/1 |

`coverage_audit.uncovered = []` confirmed accurate: every story AC (1-5), every PRD delta FR (FR-1..FR-6), and every NFR topic (performance, security, observability) resolves to a real TC with a matching `requirement_id`.

**Test-quality note (non-blocking, advisory — not a bug block, does not affect verdict):** `ING-01-TC-11` ("A non-Bearer Authorization scheme raises 401 reason=missing") is coded as `credentials=None` passed directly to `get_ingest_token()` — identical to TC-10's input. The test file's own docstring explains this is deliberate (`HTTPBearer(auto_error=False)` is asserted, not exercised, to collapse a non-Bearer scheme to `None`), but as written TC-11 never actually round-trips a real `Authorization: Basic ...` header through `HTTPBearer` the way `test_ingest_token_isolation.py`'s dual-dependency harness does for the Bearer path. The reasoning is sound and the test passes, but the specific claim in its title is verified by code-reading, not by execution. Recommend strengthening TC-11 to send a literal `Basic ...` header through a live route in a follow-up, non-blocking pass.

## Dev database safety (CRITICAL check)

`ingest_tokens` row count on the live native dev DB (`Settings.database_url` default, `localhost:5432/dashboard`), read via the app's own `Settings` object (never by displaying `.env` contents — reading `.env` directly is governance-denied):

- **Before this run: 0**
- **After this run: 0**

No test in this run connects to the dev DB: `test_mint_ingest_token.py` force-overrides `DATABASE_URL` to `test_database_url` on every subprocess call (module docstring cites `AF-01` explicitly — a prior worker minted a real row into the dev DB exactly this way during T-02's implementation, which is why the override is now unconditional); all other TCs seed rows directly through `test_session`/`migrated_db`, bound to the disposable container `bed03-manual` (`localhost:5443/dashboard_test`); `test_ingest_token_isolation.py` explicitly overrides FastAPI's `get_db` dependency to the same disposable session. `AF-01` itself (the dev-DB-default-write risk) is already triaged and deferred in `docs/features/ING-01/state.json`/`FLAGS.md` — this run's 0→0 result is consistent with, and independently re-confirms, that triage.

## Contract conformance (Phase 3b)

`docs/requirements/auth.md` § `ingest-token-auth` (`produced_by: ING-01`) checked field-by-field against the shipped surface:

| Field | Recorded (auth.md) | Shipped | Match |
|---|---|---|---|
| `token_format` | `hrn_pat_` + 64 hex chars, `secrets.token_hex(32)` | `mint_ingest_token.py::_mint` | ✓ |
| `storage` | `token_hash`=sha256 hex; label/user_email/allowed_program_ids; expires_at/revoked_at null | same | ✓ |
| `auth_check_function` | `get_ingest_token(program_id, credentials=Depends(HTTPBearer(auto_error=False)), session=Depends(get_db)) -> IngestToken` | `app/core/ingest_auth.py` signature (via `_http_bearer` instance) | ✓ |
| `auth_check_behavior` | 401 missing/unknown/revoked/expired; 403 scope; caller-supplied `program_id` | matches branch-for-branch | ✓ |
| `scope_semantics` | empty=allow-all; `["*"]`=wildcard; else exact-string membership | `_check_program_scope` | ✓ |
| `lifetime` | `expires_at` null default; `revoked_at` containment | matches | ✓ |
| `mint_surface` | argparse flags, required/optional, exit codes | `mint_ingest_token.py::_parse_args`/`_run` | ✓ |
| `log_event` | `ingest_token_auth_failed`, exact 4-field allowlist, denial-only | `_log_ingest_token_auth_failed` | ✓ |

No conformance findings. Verdict not affected.

### Passed

| TC id | Title |
|---|---|
| ING-01-TC-01 | Mint prints a raw token matching `hrn_pat_` + 64 lowercase hex chars exactly once |
| ING-01-TC-02 | Mint script writes exactly one `ingest_tokens` row on valid input and exits 0 |
| ING-01-TC-03 | Mint script exits non-zero with no DB write and no token printed on a missing argument |
| ING-01-TC-04 | Minted row persists `token_hash`, label, user_email, `allowed_program_ids`, null expiry/revocation |
| ING-01-TC-05 | No `ingest_tokens` column or emitted log line contains the raw token value |
| ING-01-TC-06 | Empty `allowed_program_ids` passes the scope check (allow-all) |
| ING-01-TC-07 | Wildcard-array `["*"]` passes the scope check |
| ING-01-TC-08 | Program-id membership hit in a specific-ids array passes the scope check |
| ING-01-TC-09 | Program-id membership miss on a non-wildcard array raises 403 |
| ING-01-TC-10 | Missing Authorization header raises 401 `reason=missing` |
| ING-01-TC-11 | A non-Bearer Authorization scheme raises 401 `reason=missing` (see coverage note above) |
| ING-01-TC-12 | An unknown token hash (no matching row) raises 401 `reason=unknown` |
| ING-01-TC-13 | A revoked token raises 401 `reason=revoked` |
| ING-01-TC-14 | An expired token raises 401 `reason=expired` |
| ING-01-TC-15 | `expires_at` exactly `now()` classifies as expired; +1s does not |
| ING-01-TC-16 | A valid, active, in-scope token resolves and authorization succeeds |
| ING-01-TC-17 | Scope check uses the caller-supplied `program_id`, never the token record |
| ING-01-TC-18 | `ingest_token_auth_failed` payload key set equals exactly the required allowlist |
| ING-01-TC-19 | `reason` takes each of the five literal values across the five denial branches |
| ING-01-TC-20 | No `ingest_token_auth_failed` event is emitted on a successful auth check |
| ING-01-TC-21 | `get_ingest_token` and `get_current_user` resolve independently on a dual-dependency route |
| ING-01-TC-22 | Token verification adds under 10ms p95 to request latency |
| ING-01-TC-23 | Two consecutive mints produce cryptographically distinct raw tokens and hashes |
| ING-01-TC-24 | Scope check compares `program_id` as an opaque string (UUID or slug, no canonicalization) |

### Flaky tests

None — all 24 TCs passed on first attempt, no retries triggered.

### Performance budget breaches

None. `ING-01-TC-22` — target `p95 < 10ms`, measured `p95 = 0.7100ms`, `budget_pass: true`.

### Regression coverage

N/A — no TC in this manifest carries a `regression-<TC-id>` tag (no prior fix-loop pass on this story).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — all 24 TCs are `automatable: true`.

### Budget-unparseable NFRs

None — `ING-01-NFR-performance`'s budget parsed cleanly.

## Carry-forward candidates (returned to orchestrator — GATE MODE, not self-written)

1. **Advisory, non-blocking** — `ING-01-TC-11` asserts non-Bearer-scheme rejection via a synthetic `credentials=None` input rather than an actual `Authorization: Basic ...` header round-tripped through `HTTPBearer`. Recommend a follow-up pass strengthening it using the same dual-dependency HTTP harness `test_ingest_token_isolation.py` already established. Does not affect this run's verdict.
2. **Already triaged, re-confirmed accurate** — `AF-01` (dev-DB-default-write risk) and `AF-03` (`pnpm` `ERR_PNPM_IGNORED_BUILDS` gate) both re-observed during this run exactly as already recorded in `docs/features/ING-01/FLAGS.md`. No new action needed; noted here only so the orchestrator's carry-forward list reflects this run's independent confirmation.

No `.pending_carry_forward` write was made to `state.json` or `features.json` — both are orchestrator-owned per GATE MODE. No manual-TC pending entries apply (all 24 TCs automatable and all ran).

### Proof of run

```
- session_id:        b1d6b6d2-e1f0-4a8e-9b79-facf55e5b160
- bash_invocations:  42
- test_runner_runs:  3   (1 full-suite `pytest -q`; 1 verbose `-v -s` ING-01-scoped run; 1 `--durations=0` ING-01-scoped run)
- stack_smoke_runs:  1   (fastapi-2: migrate + boot :8099 + /health 200 + teardown)
- duration_seconds:  575
- timestamp:         2026-09-03T06:00:53Z
```
