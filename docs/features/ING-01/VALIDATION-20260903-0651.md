# Validation report — ING-01

- Date: 2026-09-03T06:51:22Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, `/arh-implement` Validate ∥ Review gate, round 3 of 3, content anchor `71196cb3614d`)
- Total: 25   Passed: 25   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~50 min (session start → report write, 2026-09-03T06:00Z sequence continuation → 2026-09-03T06:51:22Z; this agent's own active work ≈ 20 min)

**Headline: both round-2 defects are fixed and independently confirmed. `_parse_program_ids` now guards on `raw is None` (not `if not raw:`), so a *supplied* `--program-ids ""` is a usage error, not allow-all. `docs/requirements/auth.md` § `ingest-token-auth` now matches the built code and cites D-05a. All 25 declared TCs pass; the full 424-test suite, mypy, and ruff all match the orchestrator's cross-check figures exactly.**

## Direct answer to "can any *supplied* `--program-ids` value still mint an unscoped token?"

**No.** Reproduced independently against a disposable DB, going beyond the required table into every adversarial shape suggested plus two more:

| Input | exit | stdout | row written | stored scope |
|---|---|---|---|---|
| `--program-ids ""` | 1 | empty | no | — |
| `--program-ids " "` | 1 | empty | no | — |
| `--program-ids ","` | 1 | empty | no | — |
| `--program-ids "a, b"` | 0 | 1 token | yes | `{a,b}` trimmed |
| `--program-ids "*"` | 0 | 1 token | yes | `{*}` |
| flag omitted | 0 | 1 token | yes | `{}` allow-all |
| `--program-ids ",,"` | 1 | empty | no | — |
| `--program-ids " , "` | 1 | empty | no | — |
| `--program-ids "<tab>"` | 1 | empty | no | — |
| `--program-ids "<newline>"` | 1 | empty | no | — |
| `--program-ids "<tab>,<tab>"` | 1 | empty | no | — |
| `--program-ids=` (equals form, empty) | 1 | empty | no | — |
| `--program-ids "$IDS"` (unset shell var — the realistic CI trigger) | 1 | empty | no | — |

DB cross-check after all 13 invocations (`SELECT label, allowed_program_ids FROM ingest_tokens WHERE label LIKE 'indep-r3-%'`): exactly **3** rows, matching only the 3 positive cases (`indep-r3-valid-ab` → `{a,b}`, `indep-r3-wild` → `{*}`, `indep-r3-omitted` → `{}`). Zero rows for all 10 negative/edge shapes. The omission set is `raw is None` only, confirmed at `services/api/scripts/mint_ingest_token.py:91`. This is the third attempt at closing this path and it now holds under exhaustive adversarial input.

## T-07 / contract-conformance (Phase 3b) — resolved

`docs/requirements/auth.md` § `ingest-token-auth` `mint_surface` (line 83) now reads: *"`allowed_program_ids=[]` (allow-all) is reachable ONLY by omitting `--program-ids` entirely (DECISIONS.md D-04, D-05a) — a supplied value that collapses to zero usable elements after trimming ... is a usage error, never allow-all"*; `authority` (line 85) cites `DECISIONS.md D-01..D-05a`. Both match the shipped code exactly (`_parse_program_ids` docstring and body, `scripts/mint_ingest_token.py:56-105`). **No contract-conformance findings** — grepped `docs/requirements/*.md` for `produced_by: ING-01`, found the one `ingest-token-auth` section, diffed every `shape` field against the live code (`app/core/ingest_auth.py`, unchanged since round 2 and re-verified) and the mint script: all match.

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   9   |   9  |   0  |   0   |   0   |
| Integration  |  19   |  19  |   0  |   0   |   0   |
| Security     |   5   |   5  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

Stack smoke detail (`docs/config/stack-smoke.md`, 9 sections, none configured `stack_smoke: false`):

| Stack | Result | Evidence |
|---|---|---|
| `fastapi-2` (api) | PASS — fresh boot this round | Own disposable Postgres (`ing01-val-r3-pg`, `postgres:16`, host port **15544** — never :5432). `Deps:` substituted (fresh container instead of `docker run -p 5432:5432 ...`, which would collide with the native dev Postgres flagged AF-01). `Migrate:` (`alembic upgrade head`) ran clean against the disposable DB. Server started on isolated port **18000** (not 8000 — PID 21566/55251 pre-existing squatter, not mine). `GET /health` → `200 {"status":"ok"}` within 1s. Clean shutdown (`kill`), port confirmed free after. |
| `fastapi` (generic tag, same process) | PASS | Same evidence as `fastapi-2`. |
| `postgres` | PASS | `ing01-val-r3-pg` container, `pg_isready` OK, answered every migrate/query this session; substituted for `docker compose up postgres` for the same :5432-conflict reason. Torn down (`docker stop`, `--rm` auto-removed) at end of session. |
| `nextjs` (web) | PASS — not re-run this round | Zero `apps/web/**` file changes in this round's diff (`git status` confirms only `apps/web/pnpm-workspace.yaml`, a `pnpm install` regeneration artefact per documented flag AF-03, no real content change). ING-01 ships zero frontend files. Relying on prior rounds' evidence rather than re-booting an unrelated, unchanged service. |
| `next` (generic tag) | PASS | Same as `nextjs`. |
| `typescript` | SKIPPED (n/a — no standalone process) | — |
| `pytest` | SKIPPED (n/a — test runner only) | Exercised transitively via `fastapi-2`'s `test`/`test_unit` commands, see below. |
| `alembic` | PASS (exercised via `fastapi-2 § Migrate`) | `alembic upgrade head` ran clean against the disposable DB, both for the standalone smoke boot and for every `migrated_db`-fixtured pytest test. |
| `pydantic` | SKIPPED (n/a — no standalone process) | Exercised transitively via `fastapi-2`. |

No stack-smoke FAIL.

## Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/core/ingest_auth.py` (get_ingest_token dependency) | ✓ | — | PASS |
| T-02 | `services/api/scripts/mint_ingest_token.py` (mint CLI, D-05a fix at line 91) | ✓ | — | PASS |
| T-03 | `services/api/tests/unit/test_mint_ingest_token.py` (TC-01/02/03/04/05/23/25 + 4 regression_f1 tests) | ✓ | — | PASS |
| T-04 | `services/api/tests/unit/test_ingest_token_auth.py` (TC-06..20, 24) | ✓ | — | PASS |
| T-05 | `services/api/tests/unit/test_ingest_token_isolation.py` (TC-21) | ✓ | — | PASS |
| T-06 | `services/api/tests/perf/test_ingest_token_auth_perf.py` (TC-22) | ✓ | — | PASS |
| T-07 | `docs/requirements/auth.md` (fill `ingest-token-auth` contract shape) | ✓ | ✓ (`mint_surface`/`authority` now cite D-05a and match shipped code — see § T-07 above) | **PASS** |
| T-08 | `services/api/README.md` (new "## Ingest token auth" section) | ✓ | ✓ (`## Ingest token auth` heading present, mint invocation + D-05/D-05a behaviour bullets present) | PASS |

**8/8 tasks PASS.** T-07's round-2 FAIL is resolved: the contract now matches the built code. One documentation-hygiene note, not a verdict-affecting defect: `tasks.json` T-07's `reason` field ("no edit required — already expanded at plan time... every claim matched") is now stale relative to the fact that `auth.md` demonstrably *was* edited during the round-2→3 fix loop — the task record itself wasn't updated to reflect that revisit. Carried forward below.

### Passed (25/25)

| TC id | Title |
|---|---|
| ING-01-TC-01 | Mint prints a raw token matching hrn_pat_ + 64 lowercase hex chars exactly once |
| ING-01-TC-02 | Mint script writes exactly one ingest_tokens row on valid input and exits 0 |
| ING-01-TC-03 | Mint script exits non-zero with no DB write and no token printed on a missing argument |
| ING-01-TC-04 | Minted row persists token_hash/label/user_email/allowed_program_ids, expires_at/revoked_at null |
| ING-01-TC-05 | No ingest_tokens column or emitted log line contains the raw token value |
| ING-01-TC-06 | Empty allowed_program_ids passes the scope check (allow-all) |
| ING-01-TC-07 | Wildcard-array ['*'] passes the scope check |
| ING-01-TC-08 | Program-id membership hit in a specific-ids array passes the scope check |
| ING-01-TC-09 | Program-id membership miss on a non-wildcard array raises 403 |
| ING-01-TC-10 | Missing Authorization header raises 401 reason=missing |
| ING-01-TC-11 | A non-Bearer Authorization scheme raises 401 reason=missing |
| ING-01-TC-12 | An unknown token hash (no matching row) raises 401 reason=unknown |
| ING-01-TC-13 | A revoked token raises 401 reason=revoked |
| ING-01-TC-14 | An expired token (expires_at in the past) raises 401 reason=expired |
| ING-01-TC-15 | expires_at exactly now() classifies as expired; one second future does not |
| ING-01-TC-16 | A valid, active, in-scope token resolves and authorization succeeds |
| ING-01-TC-17 | The scope check uses the caller-supplied program_id, never one read off the token record |
| ING-01-TC-18 | ingest_token_auth_failed payload key set equals exactly {token_id, reason, program_id, timestamp} |
| ING-01-TC-19 | reason takes each of the five literal values across the five denial branches |
| ING-01-TC-20 | No ingest_token_auth_failed event is emitted on a successful auth check |
| ING-01-TC-21 | get_ingest_token and get_current_user resolve independently on a mock dual-dependency route |
| ING-01-TC-22 | Token verification adds under 10ms p95 to request latency |
| ING-01-TC-23 | Two consecutive mints produce cryptographically distinct raw tokens and token_hash values |
| ING-01-TC-24 | Scope membership check compares program_id as an opaque string (UUID or slug) |
| ING-01-TC-25 | Mint exits non-zero, no DB write, no token when --program-ids is supplied as an empty string |

### Performance budget (V3)

**ING-01-TC-22** (NFR-performance): target `p95 < 10ms`; measured **p95 = 0.6250ms** across 100 calls (min=0.3507ms, max=2.9287ms), 1 seeded row, allow-all scope. `budget_pass: true`. No breach.

### Regression coverage (V4)

| Regression test | Guards against | Last attempt | Verdict |
|---|---|---|---|
| ING-01-TC-25 (`regression-f2`, declared TC) | F-2 (CRITICAL, round 2): supplied `--program-ids ""` minting an unscoped, never-expiring token | PASS | **PASS** |
| `test_mint_trims_whitespace_around_program_ids_regression_f1` (extra, not a declared TC) | F-1: untrimmed `"a, b"` under-permitting via a leading-space mismatch | PASS | PASS |
| `test_mint_drops_empty_elements_between_commas_regression_f1` (extra) | F-1: `"a, ,b"` not dropping empty elements | PASS | PASS |
| `test_mint_wildcard_still_produces_single_element_list_regression_f1` (extra) | F-1 fix regressing the `"*"` → `["*"]` path | PASS | PASS |
| `test_mint_program_ids_collapsing_to_empty_after_trim_is_usage_error_regression_f1` (extra) | F-1 fix creating a new fail-open (`" "`/`","` → `[]` allow-all) | PASS | PASS |

No regression-tagged test failed. The F-2 bug (the specific gap that failed round 2) does not re-surface.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — all 25 TCs are `automatable: true`.

### Budget-unparseable NFRs

None.

## Carry-forward (report, do not fix inline — GATE MODE, read-only on source tree)

1. **LOW / cosmetic** — `services/api/README.md` § "Ingest token auth" bullet ("Only omitting the flag reaches the allow-all default, so a typo cannot silently mint an unscoped token") cites `(DECISIONS.md D-05)`. The guarantee it describes — a *supplied* empty/whitespace/punctuation-only value being a usage error — is what **D-05a** fixes; D-05 alone still had the `""` gap. Recommend citing `D-05a` (alongside or instead of D-05) for accuracy. Behavior itself is correct and independently verified; this is a citation-only drift.
2. **LOW / process hygiene** — `docs/features/ING-01/tasks.json` T-07's `reason` field ("no edit required — already expanded at plan time... every claim matched") predates the round-2→3 fix loop that did in fact edit `docs/requirements/auth.md`'s `mint_surface`/`authority` fields. The task record was not updated to reflect that revisit, so a future reader of `tasks.json` alone would not know T-07 was touched twice.

No `.pending_carry_forward` manual-TC entries (none apply — 0 manual TCs). Per GATE MODE, these two items and the verdict below are **returned** to the orchestrator, not written to `state.json`.

## Confirmations

- **No state file written**: `state.json` and `docs/state/features.json` were not touched (`git status` shows `docs/state/features.json` modified from a prior/concurrent process, not by this agent — verified no Write/Edit tool call in this session targeted either path).
- **No source edited**: only `docs/test-cases/ING-01.json` (`last_run` fields) and this report were written by this agent. `git status` confirms zero tracked source files under `services/api/app/`, `services/api/scripts/`, `docs/adr/`, `docs/requirements/`, or `docs/features/ING-01/{REQUIREMENTS,DECISIONS,tasks.json}` were modified this session.
- **DB used**: disposable Docker container `ing01-val-r3-pg` (`postgres:16`), host port **15544** (never 5432), databases `dashboard` + `dashboard_test`; created, migrated, exercised, and torn down (`docker stop`, `--rm` auto-removed) entirely within this session. The native dev Postgres at `localhost:5432/dashboard` and the pre-existing containers `harness-postgres-dev`, `bed01-manual`, `bed03-manual`, `dashboard-initial-postgres-1` were never connected to.

## Hand-off

```
Story:        ING-01
Validation:   25 pass / 0 fail / 0 skipped
Stack smoke:  9 total (6 PASS-executed, 3 SKIPPED-n/a, all PASS-equivalent, 0 FAIL)
Tasks:        8/8 complete
Report:       docs/features/ING-01/VALIDATION-20260903-0651.md
```

### Proof of run

- session_id:        b1d6b6d2-e1f0-4a8e-9b79-facf55e5b160
- bash_invocations:  34
- test_runner_runs:  5 (1× full suite `uv run pytest -q`; 1× ING-01 files `-v --durations=0`; 1× ING-01 files `-v` PASSED/FAILED grep rerun; 1× ING-01 files `-vv --durations=0`; 1× perf file `-s` for measured p95 baseline)
- stack_smoke_runs:  1 (fastapi-2: start → healthcheck → stop)
- duration_seconds:  ~1200 (approximate — session start not machine-logged; based on sequence continuation from round 2's 06:29:30Z end to this report's 06:51:22Z timestamp)
- timestamp:         2026-09-03T06:51:22Z
