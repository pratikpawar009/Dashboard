# Validation report — ING-10

- Date: 2026-09-08T10:55:07Z
- Verdict: PASS
- Mode: full (GATE MODE — report-only; source anchor `49700d5d1c1a37cb`)
- Total: 2   Passed: 2   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~1500s (approximate wall-clock across this session; see Proof of run)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |  599  |  599 |   0  |   0   |   0   |
| Integration  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   0   |   0  |   0  |   0   |   0   |

Unit row = full regression baseline independently re-run this session (500 pytest + 99 vitest, matches the recorded evidence-pass baseline), including the 38 ING-10-specific unit tests across `test_role_map.py`, `test_manifest_auth_scope.py`, `test_manifest_roster_removal.py`, `test_manifest_payload_cap.py`, `test_manifest_identity_survives_rebuild.py` — none of which are TC-tracked (AC-1/AC-2/AC-7/AC-8/AC-9 are accepted-uncovered per REQUIREMENTS.md Approvals) but all green. Integration/Security rows are the 2 TC-tracked cases (`ING-10-TC-01`, `ING-10-TC-02`). Performance: no `type: performance` TC exists — `ING-10-NFR-performance` is a Product-Gate-accepted uncovered id (REQUIREMENTS.md Approvals), not a budget-unparseable finding; no budget was measured or breached.

### Contract conformance (Phase 3b)

`program-manifest-api` and `program-roster-schema` both `produced_by: ING-10`. Checked shipped surface against recorded `shape`:

- **api.md `program-manifest-api`**: live `GET /openapi.json` → `ManifestResponse`/`SectionCounts`/`RosterEntryResult` schemas match the frozen `response` field verbatim (per-section received/valid/rejected + per-email created/updated/skipped/rejected detail). `POST /api/ingest/manifest` request body is untyped (`dict[str, Any]`) by deliberate design (AF-06 — required for row-level validation semantics), not a contract drift.
- **data.md `program-roster-schema`**: shipped `services/api/app/models/roster.py` + migration `003_program_roster.py` match the recorded field list, types, nullability (`removed_at` nullable, all others non-null), and `unique(program_id, email)` constraint exactly.
- **No conformance findings.** Verdict unaffected.

### Known test-case defect (not a new finding — AF-13)

`ING-10-TC-01`'s `expected_results` literally assert `program_roster.role='dev'`/`'arch'` (raw slugs), contradicting the frozen `DATA-DESIGN.md` contract (long-form, post-`role_map`). Verified independently in two ways this session:
1. `tests/unit/test_manifest_ingest.py::test_manifest_ingest_valid_payload_upserts_identity_expands_aliases_maps_roles_tc01` asserts the contract value and passes.
2. Live E2E `POST /api/ingest/manifest` against the real running API + dev Postgres (`program_id=E2E-PROG-100`) returned `program_roster` rows with `role='developer'`/`'architect'`, matching the contract, not the TC JSON's literal text.

Judged PASS per AF-13's instruction (implementation is correct; the TC JSON's expected value is the defect). Not raised as a new finding.

### Task completion verification

Cross-checked all 19 `tasks.json` entries; every task has `status: "done"` (no `blocked`/`skipped` rows).

| Task | Target file(s) | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/migrations/versions/003_program_roster.py` (Alembic rev, table #19) | ✓ | — | PASS |
| T-02 | `services/api/app/models/roster.py`, `app/models/__init__.py` (`ProgramRoster` export) | ✓ | ✓ (`from app.models.roster import ProgramRoster`; `"ProgramRoster"` in `__all__`) | PASS |
| T-03 | `services/api/app/core/role_map.py` (role-slug → persona table) | ✓ | — | PASS |
| T-04 | `services/api/tests/unit/test_role_map.py` (7 slugs + unknown-slug) | ✓ | — | PASS (16 tests green) |
| T-05 | `services/api/app/schemas/manifest.py` (Pydantic request/response) | ✓ | — | PASS |
| T-06 | `services/api/app/services/manifest_ingest.py` (two-tier validation, alias expansion, upsert/soft-delete, PII-safe log) | ✓ | — | PASS |
| T-07 | `services/api/app/api/manifest.py` (router, auth, 413 cap) | ✓ | — | PASS |
| T-08 | `services/api/app/main.py` (register `manifest_router`) | ✓ | ✓ (`from app.api.manifest import router as manifest_router`; `app.include_router(manifest_router)`) | PASS |
| T-09 | `services/api/tests/unit/test_manifest_ingest.py` (TC-01 + TC-02 validation-split) | ✓ | — | PASS (3 tests green) |
| T-10 | `services/api/tests/unit/test_manifest_pii_logging.py` (TC-02 PII half) | ✓ | — | PASS (4 tests green) |
| T-11 | `services/api/tests/unit/test_manifest_auth_scope.py` (401/403 wiring) | ✓ | — | PASS (9 tests green) |
| T-12 | `services/api/tests/unit/test_manifest_roster_removal.py` (soft-delete/un-delete) | ✓ | — | PASS (6 tests green) |
| T-13 | `services/api/tests/unit/test_manifest_payload_cap.py` (413 over 500) | ✓ | — | PASS (5 tests green) |
| T-14 | `services/api/tests/unit/test_manifest_identity_survives_rebuild.py` (AC-9) | ✓ | — | PASS (2 tests green) |
| T-15 | `services/api/tests/test_migrations.py` (`EXPECTED_TABLES` +`program_roster`) | ✓ | ✓ (`"program_roster"` present at line 69) | PASS |
| T-16 | `apps/web/src/lib/programStyle.ts` (Greenfield/Brownfield short-name keys) | ✓ | ✓ (`Greenfield: {...}`, `Brownfield: {...}` present) | PASS |
| T-17 | `apps/web/src/lib/programStyle.test.ts` (new-key + Upgradation regression coverage) | ✓ | ✓ (`Greenfield`/`Brownfield` cases + `"Upgradation has no colour of its own and falls through to Migration's pair"`) | PASS |
| T-18 | `docs/design/tokens.md` (short-name rows + Upgradation gap) | ✓ | ✓ (`Greenfield`/`Brownfield` rows + "`Upgradation` — open gap, not an oversight") | PASS |
| T-19 | `README.md` (add `POST /api/ingest/manifest` to API table) | ✓ | ✓ (route documented in the API table + orientation line) | PASS |

All 19 tasks PASS. `alembic current` on the dev DB independently confirms head = `003_program_roster`, corroborating T-01.

### Passed

| TC id | Title |
|-------|-------|
| ING-10-TC-01 | Valid manifest ingest upserts identity, expands aliases, maps roles, returns per-email response |
| ING-10-TC-02 | Two-tier validation: bad `program.type` aborts whole request; bad `team[]` role rejects only that row; no PII ever logged |

Both re-verified two ways: (a) in-process ASGI test against a real migrated Postgres test DB (`pytest`), and (b) live E2E `curl` against the actually-booted `uvicorn` server + dev Postgres on port 5442, with DB state inspected directly via `psql` and server log lines inspected for PII. Live E2E test data (`E2E-PROG-100`, `E2E-PROG-200`, seeded ingest token) cleaned up after verification.

### Flaky tests

None.

### Performance budget breaches

None — no `type: performance` TC exists this pass. `ING-10-NFR-performance` (p95 < 500ms) is a Product-Gate-accepted uncovered id (REQUIREMENTS.md Approvals), carried forward as risk `R-07-perf-budget-untested` in `state.json.pending_carry_forward`, not a budget-unparseable finding.

### Regression coverage

None — no TC in this manifest carries a `regression-<id>` tag.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — both TCs are `automatable: true`.

### Budget-unparseable NFRs

N/A — no performance TC to parse a budget from (see Performance budget breaches above).

### Carry-forward (returned to orchestrator — GATE MODE, not self-written)

No new carry-forward items from validation. Pre-existing `state.json.pending_carry_forward` risks (`R-03-role-map-persistence`, `R-07-perf-budget-untested`, `R-09-harness-profile-yaml-nonconforming`, `R-10-program-members-enrichment-deferred`) are unaffected by this run; no manual-TC carry-forward applies since both TCs are automatable and have recorded results.

### Proof of run

```
- session_id:        (not exposed to this agent invocation)
- bash_invocations:  ~38
- test_runner_runs:  6  (pytest full suite; vitest full suite; pytest TC-01; pytest TC-02 validation-split; pytest TC-02 PII-logging; pytest 5-file unbacked-coverage batch)
- stack_smoke_runs:  2  (fastapi-2 start+healthcheck; nextjs start+healthcheck)
- duration_seconds:  ~1500 (approximate)
- timestamp:         2026-09-08T10:55:07Z
```
