# Validation report — ING-10 (Round 3, FINAL)

- Date: 2026-09-08T14:05:00Z
- Verdict: PASS
- Mode: full (GATE MODE — report-only; source anchor `1b5907c8fdc14c84`, round 3 — round 2's PASS at `90cc54b541256722` is invalidated by the six-file delta below, per the sequence rule; this is a fresh judgement, not a re-confirmation. Combined round cap (3) reached — this is the final round.)
- Total: 2   Passed: 2   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: see Proof of run

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |  602  |  602 |   0  |   0   |   0   |
| Integration  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   0   |   0  |   0  |   0   |   0   |

Unit row = full regression baseline independently re-run this session: 503 pytest + 99 vitest — identical counts to round 2's baseline, no regression from the six-file delta. Integration/Security rows are the 2 TC-tracked cases (`ING-10-TC-01`, `ING-10-TC-02`). Performance: no `type: performance` TC exists — `ING-10-NFR-performance` remains a Product-Gate-accepted uncovered id.

### The six-file delta since round 2 — independently re-verified

1. **`docs/requirements/api.md#program-manifest-api` `role_mapping`** — was YOUR round-2 carry-forward `ING-10-doc-drift-role-mapping-admin` (CF-09). Re-read: now lists `dev|arch|pm|em|cxo|board_member`, states `admin` was REMOVED 2026-09-08 citing D-10/AF-01 explicitly, and explains why (`admin→cio` bypassed program scoping). No stale `admin` reference remains.
2. **`docs/features/ING-10/REQUIREMENTS.md:73`** — same enumeration corrected: "`dev→developer, arch→architect, pm→product-manager, em→engineering-manager, cxo→cio, board_member→cio`... `admin` is deliberately UNMAPPED". Matches item 1.
3. **`docs/features/ING-10/PLAN.md:118`** — Test Strategy row for `test_role_map.py` now reads "6-slug mapping + unknown-slug behaviour incl. deliberately-unmapped `admin`", replacing round 2's residual "8-slug mapping". All four sources (`PLAN.md`, `test_role_map.py`, `docs/research/ING-10.md`, `tasks.json` T-04) now agree at 6.
4. **`services/api/app/api/manifest.py` module docstring** — re-read in full. No longer asserts "flagged, not fixed here" for AF-09 or "this router never logs anything itself"; now states plainly: "`AF-09` was triaged `accept` on 2026-09-08 and fixed: this router now emits the event itself on its 413 branch... calling the SHARED `log_ingest_manifest_write`". Confirmed docstring-only — diffed the surrounding code against round 2's described behaviour (413 branch calls `log_ingest_manifest_write` with the same field set) and re-ran the live 413 check below: behaviour is byte-identical to round 2, only the prose changed.
5–6. **`tests/unit/test_manifest_roster_removal.py` and `test_manifest_payload_cap.py`** — both now `from tests.conftest import AlembicRunner, repopulate` and call `repopulate(select(...))` at every post-service re-fetch, replacing inline `.execution_options(populate_existing=True)`. Read `conftest.py`'s `repopulate()` (`services/api/tests/conftest.py:125-144`) — it is exactly `stmt.execution_options(populate_existing=True)`, so this is a genuine de-duplication, not a behavioural change. Checked the call sites are still positioned correctly (the AF-14 concern: a session that already loaded a row via a raw Core `ON CONFLICT DO UPDATE` upsert can hand back a stale identity-map object on re-fetch) — both `test_manifest_roster_removal.py`'s `_fetch_roster_rows`/`_count_rows_for_email` and `test_manifest_payload_cap.py`'s `_roster_row_count` wrap the SELECT that runs *after* one or more `ingest_manifest()` calls in the same `test_session`, which is precisely the scenario `populate_existing=True` guards. Re-fetches genuinely re-read: confirmed by re-running the full pytest suite (503 passed, no change) and by independently exercising the same multi-push/re-fetch pattern live against the real DB (TC-01/TC-02 checks below all read back correct, non-stale state after repeated writes to the same session/connection).

### Everything re-verified from round 2 (fresh judgement, not copied)

- **`admin` roster entry still rejected (D-10).** Live POST to `R3-PROG-200` with a `team[]` entry `role: "admin"` → response classified it `rejected`/`"unmapped role slug"`; absent from both `program_roster` and `user_roles` (confirmed via `psql`).
- **TC-01 literal (D-11).** Live POST to `R3-PROG-100`: `program_roster` rows for Alice/Priya carry raw `role='dev'`/`'arch'`; `user_roles` carries mapped `role='developer'`/`'architect'` for the same emails. `services/api/tests/unit/test_manifest_ingest.py:236,240` assertions unchanged (`role == "dev"`/`"arch"`).
- **AF-09 (413 → `ingest_manifest_write`, no PII).** Live POST of a 501-entry raw `team[]` to `R3-PROG-CAP` (wildcard-scoped token, since the entry cap has to be checked before program-scope matters) → `413`, zero writes, and a matching `ingest_manifest_write` log line (`roster_received: 501, identity_written: false`, every other counter `0`). Confirmed unchanged after item 4's docstring rewrite.
- **`repopulate()` re-fetch genuineness (AF-14).** See item 5–6 above — confirmed the call sites guard a real stale-read risk, not decorative wrapping.
- **AF-15 revert still clean.** `pnpm -C apps/web install` → `Already up to date`, exit 0. `grep -i "puppeteer\|pa11y"` across `package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml` → zero hits. `git status` on those three files → clean.
- **All 19 `tasks.json` tasks still done** — see Task completion verification below, independently re-checked.

### Contract conformance (Phase 3b)

`program-manifest-api` and `program-roster-schema`, both `produced_by: ING-10`:

- **api.md `program-manifest-api`**: live `GET /openapi.json` → `/api/ingest/manifest` present; `ManifestResponse`/`SectionCounts`/`RosterEntryResult` schemas match the frozen `response` field verbatim (per-section received/valid/rejected + per-email created/updated/skipped/rejected with reason). Live POSTs this round exercised `200` (valid + partial-reject), `400` (bad `program.type`), `401` (missing bearer), `403` (out-of-scope program), `413` (oversized `team[]`) — every documented status code.
- **data.md `program-roster-schema`**: shipped `services/api/app/models/roster.py` (byte-read) matches the recorded field list, types, nullability (`removed_at` nullable, all others non-null), and `unique(program_id, email)` constraint exactly.
- **No wire-shape conformance findings.** The six-file delta touched prose (items 1–3), a docstring (item 4), and test-file internals (items 5–6) — none of it is a wire contract. Verdict unaffected.
- **CF-09 (round-2 carry-forward `ING-10-doc-drift-role-mapping-admin`) is RESOLVED this round** — see delta items 1–3 above. Recommend the orchestrator mark it `resolved_at` with this report as evidence.

### Task completion verification

Cross-checked all 19 `tasks.json` entries; every task has `status: "done"` (no `blocked`/`skipped` rows). Re-verified independently this round (existence via shell test, section match via grep) — not copied from round 2.

| Task | Target file(s) | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/migrations/versions/003_program_roster.py` | ✓ | — | PASS |
| T-02 | `services/api/app/models/roster.py`, `app/models/__init__.py` (`ProgramRoster` export) | ✓ | ✓ (`from app.models.roster import ProgramRoster`; `"ProgramRoster"` in `__all__`) | PASS |
| T-03 | `services/api/app/core/role_map.py` | ✓ | ✓ (`admin` deliberately absent, AF-01 comment present) | PASS |
| T-04 | `services/api/tests/unit/test_role_map.py` | ✓ | ✓ (6-slug + unknown-slug + admin-unmapped) | PASS |
| T-05 | `services/api/app/schemas/manifest.py` | ✓ | ✓ (`ManifestResponse`/`SectionCounts`/`RosterEntryResult` present) | PASS |
| T-06 | `services/api/app/services/manifest_ingest.py` | ✓ | ✓ | PASS |
| T-07 | `services/api/app/api/manifest.py` | ✓ | ✓ (413 branch calls shared `log_ingest_manifest_write`; docstring now accurate, item 4) | PASS |
| T-08 | `services/api/app/main.py` (register `manifest_router`) | ✓ | ✓ (`from app.api.manifest import router as manifest_router`; `app.include_router(manifest_router)`) | PASS |
| T-09 | `services/api/tests/unit/test_manifest_ingest.py` (TC-01 + TC-02) | ✓ | ✓ (role assertions `"dev"`/`"arch"` at lines 236/240) | PASS |
| T-10 | `services/api/tests/unit/test_manifest_pii_logging.py` | ✓ | — | PASS |
| T-11 | `services/api/tests/unit/test_manifest_auth_scope.py` | ✓ | — | PASS |
| T-12 | `services/api/tests/unit/test_manifest_roster_removal.py` | ✓ | ✓ (now uses shared `repopulate()`, item 5) | PASS |
| T-13 | `services/api/tests/unit/test_manifest_payload_cap.py` | ✓ | ✓ (now uses shared `repopulate()`, item 6) | PASS |
| T-14 | `services/api/tests/unit/test_manifest_identity_survives_rebuild.py` | ✓ | — | PASS |
| T-15 | `services/api/tests/test_migrations.py` (`EXPECTED_TABLES` + `program_roster`) | ✓ | ✓ (`"program_roster"` present; `test_upgrade_head_creates_all_19_tables`) | PASS |
| T-16 | `apps/web/src/lib/programStyle.ts` (Greenfield/Brownfield short-name keys) | ✓ | ✓ (`Greenfield: {...}`, `Brownfield: {...}` present) | PASS |
| T-17 | `apps/web/src/lib/programStyle.test.ts` | ✓ | ✓ (short-name cases + Upgradation-falls-through regression) | PASS |
| T-18 | `docs/design/tokens.md` | ✓ | ✓ (short-name rows + Upgradation gap note) | PASS |
| T-19 | `README.md` (`POST /api/ingest/manifest` in API table) | ✓ | ✓ | PASS |

All 19 tasks PASS. `alembic_version` on the dev DB independently re-confirms head = `003_program_roster` (T-01).

### Passed

| TC id | Title |
|-------|-------|
| ING-10-TC-01 | Valid manifest ingest upserts identity, expands aliases, maps roles, returns per-email response |
| ING-10-TC-02 | Two-tier validation: bad `program.type` aborts whole request; bad `team[]` role rejects only that row; no PII ever logged |

Both re-verified two ways this round: (a) targeted pytest re-run (`test_manifest_ingest.py` + `test_manifest_pii_logging.py`, 7 passed, 1.36s) plus the full 503-test suite for regression safety; (b) live E2E `curl` against the actually-booted `uvicorn` server + dev Postgres on port 5442 (`program_id`s `R3-PROG-100`/`R3-PROG-200`/`R3-PROG-CAP`, freshly minted ingest tokens), with DB state inspected directly via `psql` and every emitted server log line inspected for PII. Live E2E test data and minted tokens cleaned up after verification.

### Flaky tests

None.

### Performance budget breaches

None — no `type: performance` TC exists this pass. `ING-10-NFR-performance` (p95 < 500ms) remains a Product-Gate-accepted uncovered id, unaffected by this round's delta.

### Regression coverage

None — no TC in this manifest carries a `regression-<id>` tag.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — both TCs are `automatable: true`.

### Budget-unparseable NFRs

N/A — no performance TC to parse a budget from.

### Carry-forward (returned to orchestrator — GATE MODE, not self-written)

**Resolutions to apply** (orchestrator writes `state.json`):

```json
{
  "item_id": "CF-09",
  "action": "resolve",
  "resolved_at": "2026-09-08T14:05:00Z",
  "evidence": "docs/features/ING-10/VALIDATION-20260908-1405.md — delta items 1-3: docs/requirements/api.md role_mapping, REQUIREMENTS.md:73, and PLAN.md:118 all now correctly enumerate the 6-slug map with admin deliberately unmapped (D-10/AF-01)."
}
```

Pre-existing `state.json.pending_carry_forward` risks unaffected by this run, still open: `R-03-role-map-persistence`, `R-07-perf-budget-untested`, `R-09-harness-profile-yaml-nonconforming`, `R-10-program-members-enrichment-deferred`, `CF-05`, `CF-06`, `CF-07`, `CF-08`.

**New item this round** (per the run instructions, deliberately not fixed this pass):

```json
{
  "item_id": "ING-10-conftest-hardening-gaps",
  "kind": "finding",
  "reason": "Two pre-existing conftest.py hardening gaps confirmed still present, deliberately not fixed this round: (1) `_ensure_database_exists` (services/api/tests/conftest.py:176) builds `CREATE DATABASE \"{dbname}\"` via an f-string rather than a parameterised/quoted identifier -- dbname is derived from settings.database_url + a test-run suffix, not raw user input, but it is still string-interpolated DDL; (2) the same function's `psycopg.connect(admin, autocommit=True)` call carries no explicit connect timeout, contrary to `.claude/rules/performance-baseline.md` (\"I/O has explicit timeouts\"). Additionally, tasks.json's file_plan has no F-21/F-22 entries for this round's edits to two BED-01-owned files (services/api/tests/conftest.py -- AF-11/AF-14 repopulate()+isolation work; services/api/tests/fixtures/prd_8_4_schema.json -- AF-05's program_roster fixture addition), so ING-10's task-completion table cannot verify those edits against a task row; both files are modified per `git status` but untracked by tasks.json.",
  "owner": "Pratik Pawar",
  "added_at": "2026-09-08T14:05:00Z",
  "added_by": "validate-feature/05-report (round 3, final)",
  "resolved_at": null,
  "evidence": null
}
```

No manual-TC carry-forward applies (both TCs automatable, both PASS).

### Proof of run

```
- session_id:        (not exposed to this agent invocation)
- bash_invocations:  ~45
- test_runner_runs:  4  (pytest full suite x1 [503 passed]; pytest targeted test_manifest_ingest.py+test_manifest_pii_logging.py x1 [7 passed, 1.36s]; vitest full suite x1 [99 passed]; tsc/mypy/eslint/ruff clean re-runs not counted as test-runner invocations)
- stack_smoke_runs:  2  (fastapi-2: uvicorn start + /health 200; nextjs: pnpm dev start + / 307->/overview 200-equivalent reachability)
- duration_seconds:  ~1800 (approximate wall-clock this session)
- timestamp:         2026-09-08T14:05:00Z
```
