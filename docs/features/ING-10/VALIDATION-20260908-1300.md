# Validation report — ING-10 (Round 2)

- Date: 2026-09-08T13:00:00Z
- Verdict: PASS
- Mode: full (GATE MODE — report-only; source anchor `90cc54b541256722`, round 2 — round 1's PASS at `49700d5d1c1a37cb` is invalidated by the nine post-round-1 flag fixes, per the sequence rule; this is a fresh judgement, not a re-confirmation)
- Total: 2   Passed: 2   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~2300s (wall-clock this session; see Proof of run)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |  602  |  602 |   0  |   0   |   0   |
| Integration  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   0   |   0  |   0  |   0   |   0   |

Unit row = full regression baseline independently re-run this session (503 pytest + 99 vitest, +3 pytest over round 1's 500 — matches the round-1 note's prediction: AF-01's `test_map_role_slug_admin_is_deliberately_unmapped_af01` regression guard + AF-09's 413-logging guard). Integration/Security rows are the 2 TC-tracked cases (`ING-10-TC-01`, `ING-10-TC-02`). Performance: no `type: performance` TC exists — `ING-10-NFR-performance` remains a Product-Gate-accepted uncovered id, not a budget-unparseable finding.

### What changed since round 1 — independently re-verified

1. **AF-01/D-10 (security — `admin` deliberately unmapped).** `map_role_slug("admin")` returns `None` (`app/core/role_map.py`, verified by read); `test_map_role_slug_admin_is_deliberately_unmapped_af01` asserts `is None`. Live E2E re-check beyond the base TC-02 payload: a third `team[]` entry with `role: "admin"` (`boss.r2@example.com`) POSTed against the real running API — response classified it `rejected`/`"unmapped role slug"`, and `boss.r2@example.com` is absent from both `program_roster` and `user_roles` (confirmed via `psql`). `admin` grants nothing; confirmed independently, not just re-read from code.
2. **AF-13/D-11 (contract change — `program_roster.role` is now the raw slug).** `manifest_ingest.py`'s roster-row builder writes `team_in.role` (raw), never `mapped_role`; `user_roles` still writes the mapped long-form value. `data.md#program-roster-schema` gained the `role_semantics` field and `DATA-DESIGN.md §1`'s `program_roster` row was updated to match. **TC-01 executed literally this round** (no workaround): live E2E POST against `E2E-R2-PROG-100` returned `program_roster.role = 'dev'/'arch'` (raw) and `user_roles.role = 'developer'/'architect'` (mapped) — exactly TC-01's literal `expected_results`. The backing pytest test's own assertions (`test_manifest_ingest.py:236,240`) were also updated to `role == "dev"`/`"arch"`, so JSON, test file, and live behaviour are now in agreement. `docs/test-cases/ING-10.json` `last_run.failure_reason` for TC-01 is `null` this round (round 1's AF-13 defect note no longer applies).
3. **AF-09 (observability — 413 branch now logs).** Round 1 could only exercise `_log_ingest_manifest_write`'s 413 branch via a direct service-level call (the router's own cap check short-circuited first, over HTTP). This round, `app/api/manifest.py`'s 413 branch now calls the (promoted, no-longer-private) `log_ingest_manifest_write` itself. Verified live: POSTed a raw `team[]` of 501 entries over HTTP against the real running API → `413`, and the server log gained a matching `ingest_manifest_write` line (`roster_received: 501, identity_written: false`, every other counter `0`) — FR-2's allowlist only, no PII. This event did not exist over HTTP in round 1; it does now.
4. **AF-11 + AF-14 (test harness — per-worker DB isolation).** Verified by an independent controlled experiment (not just re-reading `conftest.py`): two concurrent `pytest tests/test_migrations.py` runs with **no** env var set (the historical shared `dashboard_test` path) produced real contention — `1 failed, 10 passed, 1 error` / `1 failed, 8 passed, 4 errors`, `UndefinedObject`/multi-table `ERROR`s consistent with concurrent `downgrade base`/`upgrade head` collisions. The same two runs repeated with distinct `HARNESS_TEST_DB_SUFFIX=r2a` / `r2b` both passed clean (`12 passed` each, `2.2–2.3s`). Default (no env var) behaviour is confirmed unchanged: a subsequent plain serial run of the same file passed `12 passed` against the same `dashboard_test`. Ephemeral `dashboard_test_r2a`/`_r2b` databases dropped after the experiment.
5. **AF-05** — `tests/fixtures/prd_8_4_schema.json` declares `program_roster` (grep-confirmed, note at top of file: "Table count is 19... `program_roster` was added here 2026-09-08 per AF-05"); `test_models.py`'s fixture-driven loop now covers it like the other 18.
6. **AF-04/AF-03/AF-08 (documentation-only)** — `test_migrations.py::test_upgrade_head_creates_all_19_tables` (renamed, confirmed by grep); `test_role_map.py`'s own docstring/assertions correctly say 6 slugs and assert `admin` as unmapped; `PLAN.md`'s Module hierarchy line for `role_map.py`/`manifest.py` now shows the 4-parameter `ingest_manifest(db, program_id, payload, token_label)` signature (grep-confirmed at `PLAN.md:39`). **One residual not caught by AF-03's stated "both stale numbers" claim**: `PLAN.md:118`'s Test Strategy table still reads "8-slug mapping" for `test_role_map.py` — cosmetic (the actual test file, `docs/research/ING-10.md`, and `tasks.json` T-04's title are all correctly "6 slugs"), does not affect verdict, flagged below as a new carry-forward item.

### AF-15 revert — independently confirmed clean

`pnpm -C apps/web install` succeeds (`Already up to date`, exit 0). `git status` on `apps/web/package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml` is clean (nothing to commit). `grep -i "puppeteer\|pa11y"` across all three files: zero hits. The `allowBuilds: unrs-resolver: true` entry (pre-existing, legitimate) is the only entry in `pnpm-workspace.yaml` — no `puppeteer: set this to true or false` placeholder residue. `tsc --noEmit` (114 files clean), `eslint` (clean), `pnpm build` (OK, 8/8 static pages) all independently re-run this round and all pass. Toolchain is genuinely restored, not just claimed.

### Contract conformance (Phase 3b)

`program-manifest-api` and `program-roster-schema` both `produced_by: ING-10`. Checked shipped surface against recorded `shape`:

- **api.md `program-manifest-api`**: live `GET /openapi.json` → `/api/ingest/manifest` present; `ManifestResponse`/`SectionCounts`/`RosterEntryResult` (`app/schemas/manifest.py`) match the frozen `response` field verbatim. Live POSTs this round exercised every documented status: `200` (valid + partial-reject), `400` (bad `program.type`), `413` (oversized `team[]`); `401`/`403` unchanged from round 1 (not re-exercised, no behavioural change flagged for that path this round).
- **data.md `program-roster-schema`**: shipped `services/api/app/models/roster.py` (byte-read this round) matches the recorded field list, types, nullability (`removed_at` nullable, all others non-null) and `unique(program_id, email)` constraint exactly. `role_semantics` field (new, AF-13) accurately describes the shipped raw-slug behaviour — confirmed live (see item 2 above).
- **No wire-shape conformance findings.** Verdict unaffected.
- **Non-blocking documentation-drift note (not a Phase 3b finding — no wire field/type/nullability/status-code is affected):** `docs/requirements/api.md`'s `program-manifest-api.shape.role_mapping` prose and `docs/features/ING-10/REQUIREMENTS.md:73` both still enumerate `admin` as one of the roster slugs that "map... through one shared table" — stale since AF-01/D-10 removed `admin` from the map entirely. `role` itself is still a plain `str` wire field (unaffected), so this is prose drift describing an internal business rule, not a contract-shape defect; recorded as a new carry-forward item below rather than a FAIL.

### Task completion verification

Cross-checked all 19 `tasks.json` entries; every task has `status: "done"` (no `blocked`/`skipped` rows). Re-verified independently this round (existence via `ls`/file read, section match via `grep`), not copied from round 1.

| Task | Target file(s) | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/migrations/versions/003_program_roster.py` | ✓ | — | PASS |
| T-02 | `services/api/app/models/roster.py`, `app/models/__init__.py` (`ProgramRoster` export) | ✓ | ✓ (`from app.models.roster import ProgramRoster`; `"ProgramRoster"` in `__all__`) | PASS |
| T-03 | `services/api/app/core/role_map.py` | ✓ | ✓ (`admin` deliberately absent from `_ROLE_SLUG_TO_PERSONA`, D-10) | PASS |
| T-04 | `services/api/tests/unit/test_role_map.py` (6 slugs + unknown-slug + admin-unmapped guard) | ✓ | ✓ | PASS (11 tests) |
| T-05 | `services/api/app/schemas/manifest.py` | ✓ | ✓ (`ManifestResponse`/`SectionCounts`/`RosterEntryResult` present) | PASS |
| T-06 | `services/api/app/services/manifest_ingest.py` | ✓ | ✓ (`log_ingest_manifest_write` now public, per AF-09) | PASS |
| T-07 | `services/api/app/api/manifest.py` | ✓ | ✓ (413 branch now calls shared `log_ingest_manifest_write`, AF-09) | PASS |
| T-08 | `services/api/app/main.py` (register `manifest_router`) | ✓ | ✓ (`from app.api.manifest import router as manifest_router`; `app.include_router(manifest_router)`) | PASS |
| T-09 | `services/api/tests/unit/test_manifest_ingest.py` (TC-01 + TC-02) | ✓ | ✓ (role assertions now `"dev"`/`"arch"`, AF-13) | PASS |
| T-10 | `services/api/tests/unit/test_manifest_pii_logging.py` | ✓ | — | PASS |
| T-11 | `services/api/tests/unit/test_manifest_auth_scope.py` | ✓ | — | PASS |
| T-12 | `services/api/tests/unit/test_manifest_roster_removal.py` | ✓ | — | PASS |
| T-13 | `services/api/tests/unit/test_manifest_payload_cap.py` | ✓ | — | PASS |
| T-14 | `services/api/tests/unit/test_manifest_identity_survives_rebuild.py` | ✓ | — | PASS |
| T-15 | `services/api/tests/test_migrations.py` (`EXPECTED_TABLES` + `program_roster`; method renamed `..._all_19_tables`, AF-04) | ✓ | ✓ (`"program_roster"` present; `test_upgrade_head_creates_all_19_tables` confirmed by grep) | PASS |
| T-16 | `apps/web/src/lib/programStyle.ts` (Greenfield/Brownfield short-name keys) | ✓ | ✓ (`Greenfield: {...}`, `Brownfield: {...}` present) | PASS |
| T-17 | `apps/web/src/lib/programStyle.test.ts` | ✓ | ✓ (Greenfield/Brownfield cases + Upgradation-falls-through regression) | PASS |
| T-18 | `docs/design/tokens.md` | ✓ | ✓ (short-name rows + Upgradation gap note) | PASS |
| T-19 | `README.md` (`POST /api/ingest/manifest` in API table) | ✓ | ✓ | PASS |

All 19 tasks PASS. `alembic current` on the dev DB independently re-confirms head = `003_program_roster` (T-01).

### Passed

| TC id | Title |
|-------|-------|
| ING-10-TC-01 | Valid manifest ingest upserts identity, expands aliases, maps roles, returns per-email response |
| ING-10-TC-02 | Two-tier validation: bad `program.type` aborts whole request; bad `team[]` role rejects only that row; no PII ever logged |

Both re-verified two ways this round: (a) in-process ASGI test against a real migrated Postgres test DB (`pytest`, full 503-test suite), and (b) live E2E `curl` against the actually-booted `uvicorn` server + dev Postgres on port 5442 (`program_id`s `E2E-R2-PROG-100`/`E2E-R2-PROG-200`, a freshly minted ingest token), with DB state inspected directly via `psql` and every emitted server log line inspected for PII. TC-02's live check this round additionally covered an `admin`-slug entry (D-10) and the AF-09 413-over-HTTP path — both beyond the base TC-02 payload, to independently stress the two security-relevant round-2 changes. Live E2E test data (`E2E-R2-PROG-100`, `E2E-R2-PROG-200`, seeded ingest token) cleaned up after verification.

### Flaky tests

None.

### Performance budget breaches

None — no `type: performance` TC exists this pass. `ING-10-NFR-performance` (p95 < 500ms) is a Product-Gate-accepted uncovered id, carried forward as risk `R-07-perf-budget-untested`, not a budget-unparseable finding.

### Regression coverage

None — no TC in this manifest carries a `regression-<id>` tag. (AF-01/AF-09/AF-13/AF-11/AF-14 are flag-driven fixes verified above via direct re-execution and a controlled concurrency experiment, not via a regression-tagged TC — no TC was ever recorded as failing against the pre-fix code, so the regression-tag protocol does not apply here.)

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — both TCs are `automatable: true`.

### Budget-unparseable NFRs

N/A — no performance TC to parse a budget from.

### Carry-forward (returned to orchestrator — GATE MODE, not self-written)

Pre-existing `state.json.pending_carry_forward` risks (`R-03-role-map-persistence`, `R-07-perf-budget-untested`, `R-09-harness-profile-yaml-nonconforming`, `R-10-program-members-enrichment-deferred`, `CF-05`..`CF-08`) are unaffected by this run.

One **new** item discovered this round:

```json
{
  "item_id": "ING-10-doc-drift-role-mapping-admin",
  "kind": "finding",
  "reason": "D-10/AF-01 (2026-09-08) removed `admin` from the roster-slug -> persona map, but two prose fields were not updated to match: docs/requirements/api.md `program-manifest-api.shape.role_mapping` and docs/features/ING-10/REQUIREMENTS.md:73 both still enumerate `admin` among the mapped slugs. Not a Phase 3b contract-shape finding (the wire field `team[].role` is an untyped str, unaffected) -- pure prose drift describing an internal business rule. PLAN.md:118's Test Strategy table also still reads '8-slug mapping' for test_role_map.py, though AF-03's stated fix ('both stale numbers get corrected in one pass') implies it intended to catch this row too; the test file itself, docs/research/ING-10.md, and tasks.json T-04's title are all already correct at 6.",
  "owner": "Pratik Pawar",
  "added_at": "2026-09-08T13:00:00Z",
  "added_by": "validate-feature/05-report (round 2, doc-drift finding)",
  "resolved_at": null,
  "evidence": null
}
```

No manual-TC carry-forward applies (both TCs automatable, both have recorded PASS results).

### Proof of run

```
- session_id:        (not exposed to this agent invocation)
- bash_invocations:  ~62
- test_runner_runs:  7  (pytest full suite x1 [503 passed]; vitest full suite x1 [99 passed]; pytest tests/test_migrations.py x2 concurrent-no-suffix [contention repro, 1 failed+1 error / 1 failed+4 errors]; pytest tests/test_migrations.py x2 concurrent-distinct-suffix [AF-11/AF-14 fix confirmation, 12 passed each]; pytest tests/test_migrations.py x1 serial sanity re-run post-experiment [12 passed])
- stack_smoke_runs:  2  (fastapi-2 start+migrate+healthcheck; nextjs start+healthcheck)
- duration_seconds:  ~2300 (approximate wall-clock)
- timestamp:         2026-09-08T13:00:00Z
```
