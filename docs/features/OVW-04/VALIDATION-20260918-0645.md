# Validation report — OVW-04 (Round 2 — re-verification of BUG-OVW04-01 fix)

- Date: 2026-09-18T06:45:00Z
- Verdict: **PASS**
- Mode: full re-run (GATE MODE — report-only; branch `feature/OVW-04`, snapshot `a06db803`)
- Total: 12   Passed: 12   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~35 minutes wall clock

## Round 1 → Round 2 delta

Round 1 (`docs/features/OVW-04/VALIDATION-20260918-0630.md`) found ONE blocking defect (`BUG-OVW04-01`): `GET /overview` 500'd for every persona because `ProgramCard.tsx` attached `onClick` to its root `<a>` with no `"use client"` boundary anywhere in its ancestor chain — a Next.js App Router RSC violation ("Event handlers cannot be passed to Client Component props"), invisible to jsdom/Testing-Library component tests.

Fix delta (exactly as described): `ProgramCardLink.tsx` (new, `"use client"` leaf owning the root `<a>` + D-06 `program_drilldown` telemetry, mirroring `ProgramSwitcher.tsx`'s precedent), `ProgramCard.tsx` (now a pure Server Component rendering `<ProgramCardLink>`, no `onClick`), and `apps/web/e2e/overview-program-board.spec.ts` (new Playwright regression spec, tagged `regression-OVW-04-TC-06`, discovery-only per policy).

**Re-verified independently** (real servers, not mocked): started `uv run uvicorn app.main:app --port 8021` against real migrated Postgres on 5442, and `pnpm dev --port 3021` with `NEXT_PUBLIC_API_URL=http://localhost:8021`, minted a CIO session via `/dev-login?role=cio` (backed by real `/auth/dev-bypass`).

- **`GET /overview` → literal HTTP 200** (`GET /overview 200 in 919ms` in the Next.js dev log, no RSC error, no stack trace).
- Response body carries 4 real `ProgramCardLink` `<a>` elements with hrefs `/programs/alpha`, `/programs/beta`, `/programs/gamma`, `/programs/dashboard` — matches the orchestrator's independent baseline exactly.
- 8 occurrences of `"open program detail"` (aria-label affordance) present; zero occurrences of "Event handlers cannot be passed", "Internal Server Error", or "Application error" anywhere in the rendered payload.
- Direct API re-check: CIO dev-bypass token → `GET /api/overview/program-board` → `200`, items include `program_id: "dashboard"`, `metrics: [{label: "Total tokens", value: "3220.9M"}, ...]` — real, non-hardcoded data. Developer dev-bypass token → `403` (RBAC still enforced, unaffected by the fix).
- Servers torn down cleanly after (`kill` on both PIDs, ports 8021/3021 confirmed free); round-1's `tmp/smoke-nextjs.log` / `tmp/overview.html` (FAIL-round evidence) left untouched, my own round-2 scratch files removed.

`BUG-OVW04-01` is **CLOSED**. TC-06/TC-07/TC-12 (previously `verdict: FLAKY`, pending this fix) are re-judged **PASS** — their component-level assertions were always sound; the real page they compose into now renders successfully, so the FLAKY caveat no longer applies.

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Integration  |   7   |   7  |   0  |   0   |   0   |
| Performance  |   2   |   2  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |

Stack-smoke: backend (`fastapi-2`) PASS — real `uvicorn` on idle port 8021, real migrated Postgres on 5442 (already running, `docker` process on `*:5442`), `GET /health` → 200. Frontend (`nextjs`) **PASS this round** — real `next dev --turbopack` on idle port 3021, `GET /login` → 200, `GET /overview` (with a real CIO session) → **200**, program board section fully rendered. This is the round-1 blocker, now closed.

Contract conformance: `program-board-api` (`docs/requirements/api.md`, `produced_by: OVW-04`) — recorded shape (`GET /api/overview/program-board`, `program_summary` ordered `tokens desc`) matches the shipped route exactly. No drift.

E2E: `apps/web/e2e/overview-program-board.spec.ts` (new, `regression-OVW-04-TC-06`) discovered cleanly via `pnpm exec playwright test e2e/overview-program-board.spec.ts --list` → `Total: 1 test in 1 file`. Execution remains deferred per `docs/config/project-commands.yaml` (`test_e2e` execution deferred to this phase) and environment constraint (Playwright browser binaries not installed in this sandbox) — honestly reported as N/A, not a failure. `design_check` remains N/A (deliberately empty per `project-commands.yaml`, no a11y/console-scan tool wired yet).

Full test-runner re-runs this round: `pytest tests/unit/test_program_board.py` (12/12 passed), `pytest -m "not perf"` full backend suite (827 passed, 17 deselected — the 17 deselected are the same pre-existing, unrelated `tests/perf/test_ingest_files_perf.py` ING-10 perf tests flagged in round 1, not re-run since round 1 already carry-forwarded them separately and they are out of OVW-04's `file_plan`), `pnpm test` full frontend suite (43 files / 325 tests, all passed, including `ProgramCard.test.tsx`'s 22 tests and `ProgramLeaderboard.test.tsx`).

## Task completion verification

Unchanged from round 1 — no task files were touched by the fix delta beyond the two component files and the new e2e spec, none of which map to a distinct `tasks.json` row requiring re-verification (T-10's `files_touched` already covered `ProgramCard.tsx`; `ProgramCardLink.tsx` is an implementation-internal split of that same task's scope, not a new task).

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/migrations/versions/009_program_summary_tokens_index.py`, `app/models/rollup.py`, `tests/fixtures/prd_8_4_schema.json` (tokens index) | ✓ | n/a (schema/fixture, verified via full pytest run) | PASS |
| T-02 | `services/api/app/schemas/program_board.py` | ✓ | n/a | PASS |
| T-03 | `services/api/app/services/program_board.py` | ✓ | n/a | PASS |
| T-04 | `services/api/app/api/overview.py` (`GET /program-board` route) | ✓ | ✓ | PASS |
| T-05 | `services/api/tests/unit/test_program_board.py` | ✓ | ✓ (12/12 passing) | PASS |
| T-06 | `docs/design/tokens.md` (MoM flat-chip token) | ✓ | ✓ | PASS |
| T-07 | `apps/web/src/types/programBoard.ts`, `lib/programBoardApi.ts` | ✓ | n/a | PASS |
| T-08 | `apps/web/src/lib/momChipStyle.ts` | ✓ | n/a | PASS |
| T-09 | `apps/web/src/components/ProgramSparkline.tsx` | ✓ | n/a | PASS |
| T-10 | `apps/web/src/components/ProgramCard.tsx`, `.module.css` | ✓ | n/a — file now a clean Server Component; `onClick` moved to the new `ProgramCardLink.tsx` client leaf; real render confirmed 200 | **PASS** (BUG-OVW04-01 closed this round) |
| T-11 | `apps/web/src/components/ProgramLeaderboard.tsx`, `.module.css` | ✓ | n/a | PASS |
| T-12 | `apps/web/src/components/AdoptionOverview.tsx`, `apps/web/src/app/overview/page.tsx` (wiring) | ✓ | ✓ — real render confirmed the wiring produces a working page | PASS |
| T-13 | `apps/web/src/components/ProgramCard.test.tsx`, `ProgramLeaderboard.test.tsx` | ✓ | ✓ (22 + additional tests, all passing) | PASS |
| T-14 | `README.md` (API table row) | ✓ | ✓ | PASS |

All 14 tasks PASS. T-10's round-1 asterisk is removed — the runtime defect it flagged is fixed.

## Passed

| TC id | Title |
|-------|-------|
| OVW-04-TC-01 | Contract shape, ordering, no-inline-css |
| OVW-04-TC-02 | tokens index used, no seq scan, p95 budget |
| OVW-04-TC-03 | null/missing/malformed sparkline → empty points, never 500 |
| OVW-04-TC-04 | MoM 0/1/2+ point edge cases incl. divide-by-zero guard |
| OVW-04-TC-05 | non-CIO 403 no body; rbac_check_org_access logged (re-verified live again this round) |
| OVW-04-TC-06 | Card navigation / program_drilldown — **re-judged PASS** (was FLAKY, blocked on BUG-OVW04-01) |
| OVW-04-TC-07 | No hardcoded/illustrative values — **re-judged PASS** (was FLAKY) |
| OVW-04-TC-08 | empty org → 200 empty items |
| OVW-04-TC-09 | pagination defaults + clamp above 100 |
| OVW-04-TC-10 | page/page_size < 1 → 422 |
| OVW-04-TC-11 | single indexed query, no N+1, p95 budget |
| OVW-04-TC-12 | MoM non-color-reliant encoding — **re-judged PASS** (was FLAKY) |

## Flaky tests

None this round. All three round-1 FLAKY verdicts (TC-06, TC-07, TC-12) resolved to PASS following live re-verification against the fixed real app.

## Performance budget breaches

None. TC-02 and TC-11 both `budget_pass: true` (unchanged from round 1 — the fix did not touch backend query paths).

## Failed (bug blocks)

None.

## Errored

None.

## Manual follow-up

None. All 12 TCs are `automatable: true`.

## Regression coverage

`apps/web/e2e/overview-program-board.spec.ts` is tagged `regression-OVW-04-TC-06` per the fix protocol (guards against BUG-OVW04-01 re-surfacing — asserts `GET /overview` returns 200 and at least one real `ProgramCardLink` `<a>` renders with an `/programs/...` href). Execution is deferred (Playwright browsers not installed in this environment) but discovery confirmed the spec is real and picked up by the runner (`--list` → 1 test, 1 file). This is an honest N/A-with-reason, not a pass/fail claim — the regression guard exists and is wired, but has not yet been machine-executed end-to-end in CI/validation. Flagged below as a carry-forward for whichever environment first has Playwright browsers installed.

| Regression TC | Guards against | Last attempt | Verdict |
|----------------|-----------------|--------------|---------|
| `apps/web/e2e/overview-program-board.spec.ts` (regression-OVW-04-TC-06) | BUG-OVW04-01 (RSC event-handler crash on `/overview`) | Discovery only (`playwright test --list` → 1/1) | N/A — execution deferred, browsers not installed |

## Budget-unparseable NFRs

None.

## Additional live-server verification (beyond the TC manifest)

- Real stack: `uv run uvicorn app.main:app --port 8021` (real migrated Postgres on `:5442`) + `pnpm dev --port 3021` (`NEXT_PUBLIC_API_URL=http://localhost:8021`).
- `GET /health` → 200. `GET /login` → 200.
- CIO session via `/dev-login?role=cio` (local reviewer helper backed by real `/auth/dev-bypass`) → `GET /overview` → **200**, 4 program cards rendered with real hrefs and real API data (`AgentRise Dashboard`, `3220.9M` tokens, etc.).
- Direct API re-check: CIO token → `GET /api/overview/program-board` → 200, ordered items; developer token → `GET /api/overview/program-board` → 403, no data body — RBAC unaffected by the fix, as expected.
- Servers torn down (`kill` both PIDs); ports 8021/3021 confirmed free post-teardown.

## Carry-forward entries (returned to orchestrator — GATE MODE, not self-written)

```json
[
  {
    "item_id": "OVW-04-BUG-01",
    "kind": "defect",
    "reason": "RESOLVED round 2: ProgramCard.tsx no longer attaches onClick to a Server Component <a>; the interactive click + D-06 program_drilldown telemetry now live in the new ProgramCardLink.tsx (\"use client\" leaf). Re-verified live: GET /overview returns 200 with 4 real program cards rendered (was 500 in round 1). Recommend resolving this carry-forward entry.",
    "owner": "OVW-04 implementation owner",
    "added_at": "2026-09-18T06:30:00Z",
    "added_by": "validation-agent/OVW-04 (GATE MODE)",
    "resolved_at": "2026-09-18T06:45:00Z",
    "evidence": "docs/features/OVW-04/VALIDATION-20260918-0645.md#round-1--round-2-delta"
  },
  {
    "item_id": "OVW-04-TC-06-flaky",
    "kind": "test_case",
    "reason": "RESOLVED round 2: OVW-04-TC-06 re-judged PASS (was FLAKY) -- BUG-OVW04-01 fixed and independently re-verified against the real running app. Recommend resolving this carry-forward entry.",
    "owner": "OVW-04 implementation owner",
    "added_at": "2026-09-18T06:30:00Z",
    "added_by": "validation-agent/OVW-04 (GATE MODE)",
    "resolved_at": "2026-09-18T06:45:00Z",
    "evidence": "docs/test-cases/OVW-04.json (OVW-04-TC-06.last_run)"
  },
  {
    "item_id": "OVW-04-TC-07-flaky",
    "kind": "test_case",
    "reason": "RESOLVED round 2: OVW-04-TC-07 re-judged PASS for the same reason as OVW-04-TC-06-flaky.",
    "owner": "OVW-04 implementation owner",
    "added_at": "2026-09-18T06:30:00Z",
    "added_by": "validation-agent/OVW-04 (GATE MODE)",
    "resolved_at": "2026-09-18T06:45:00Z",
    "evidence": "docs/test-cases/OVW-04.json (OVW-04-TC-07.last_run)"
  },
  {
    "item_id": "OVW-04-TC-12-flaky",
    "kind": "test_case",
    "reason": "RESOLVED round 2: OVW-04-TC-12 re-judged PASS for the same reason as OVW-04-TC-06-flaky.",
    "owner": "OVW-04 implementation owner",
    "added_at": "2026-09-18T06:30:00Z",
    "added_by": "validation-agent/OVW-04 (GATE MODE)",
    "resolved_at": "2026-09-18T06:45:00Z",
    "evidence": "docs/test-cases/OVW-04.json (OVW-04-TC-12.last_run)"
  },
  {
    "item_id": "OVW-04-e2e-execution-deferred",
    "kind": "test_case",
    "reason": "apps/web/e2e/overview-program-board.spec.ts (regression-OVW-04-TC-06) exists and is discovered by Playwright (--list confirms 1 test, 1 file), but has never been machine-executed -- browser binaries are not installed in this validation environment, and test_e2e execution is deferred by docs/config/project-commands.yaml policy. Recommend running it for real the first time a Playwright-browser-equipped environment validates this story, to close the loop on the regression guard.",
    "owner": "OVW-04 implementation owner / CI owner",
    "added_at": "2026-09-18T06:45:00Z",
    "added_by": "validation-agent/OVW-04 (GATE MODE)",
    "resolved_at": null,
    "evidence": null
  },
  {
    "item_id": "OVW-04-preexisting-perf-failures",
    "kind": "defect",
    "reason": "Carried forward unchanged from round 1: pre-existing, unrelated to OVW-04 -- tests/perf/test_ingest_files_perf.py (ING-10 area) has 3 known perf failures at 20k/40k/160k accumulated table sizes, not touched by this story's file_plan. Deselected (-m \"not perf\") in this round's full-suite run rather than re-run, since round 1 already isolated and carry-forwarded them under the correct owner.",
    "owner": "ING-10 owner",
    "added_at": "2026-09-18T06:30:00Z",
    "added_by": "validation-agent/OVW-04 (GATE MODE)",
    "resolved_at": null,
    "evidence": "services/api/tests/perf/test_ingest_files_perf.py"
  }
]
```

### Proof of run

- session_id:        45eade80-eb56-42a5-933e-ab78cf26e953
- bash_invocations:  19
- test_runner_runs:  4 (pytest tests/unit/test_program_board.py; pytest -m "not perf" full suite; pnpm test full vitest suite; playwright test --list)
- stack_smoke_runs:  2 (fastapi-2 backend on :8021 — PASS; nextjs frontend on :3021 — PASS, /overview 200 confirmed)
- duration_seconds:  ~2100
- timestamp:         2026-09-18T06:45:00Z
