# Validation report — ING-04

**Story:** ING-04 — MCP server tool exposure (`push_activity` / `push_artifacts`)
**Mode:** GATE MODE — report-only (invoked by `/arh-implement` Step 2 Validate ∥ Review gate, ROUND 2 after fix-pass round 1)
**Source snapshot (asserted by orchestrator at dispatch):** `dd7536287d525e90c8f701e34cd0591d441aa49ef72a759c318d81a72c1b3b12`
**Report generated (UTC):** 2026-09-14T15:00:58Z
**Verdict:** **PARTIAL** — round-1 AF-02 contract-drift resolved and re-verified against the real backend; all runnable TCs pass; stack-smoke green. Only skip is TC-03 (`agentrise-mcp` console script not on caller PATH — integration deferred to CI, identical guard as round 1).

State writes are deferred to the orchestrator per gate-mode directive. This report writes only itself and per-TC `last_run` fields in `docs/test-cases/ING-04.json` (both excluded from the source snapshot).

## Round-1 → round-2 delta

- **AF-02 (HIGH, contract-drift) — RESOLVED.** `push_activity` now consumes `IngestFilesResponse.{received, valid, inserted, updated, rejected, rollup_summaries}` and returns the PRD FR-2 success envelope `{success, files_read, rows_read, batches, inserted, updated, rejected, rollups}`. Round-1 stale keys (`rows_received / rows_upserted / rejections`) are gone from `push_activity.py`, from the three activity-tool unit test files, and from the `docs/requirements/api.md#mcp-tools.tools.push_activity` block (which is the ING-05 consumer contract).
- **AF-03 (open flag, missing-token envelope wording) — RESOLVED.** Both tools now return the PRD FR-6 envelope `{success: false, error: "missing_ingest_token", message: "Set AGENTRISE_INGEST_TOKEN before invoking this tool."}`.
- **Both tool signatures now expose `program_id: str | None = None`** (fix item 2). Verified in `push_activity.py:63`, `push_artifacts.py:85`, and both `@server.tool()` bindings in `server.py:41-55`.
- **Auth-failure envelope aligned with PRD FR-5:** 401 → `{error: "unauthorized", http_status: 401, ...}`; 403 → `{error: "forbidden", http_status: 403, ...}`. Verified in code + tests.
- **Source resolvers realigned to PRD FR-3 semantics** (fix items 6/7/8): `glob_count.py` uses dir `path` + leaf `glob` + `exclude`; `json_key_count.py` counts top-level-object keys minus `exclude`; `json_field_sum.py` sums `len(doc[field])` across every JSON file matching `glob` under `path`.
- **Log event names now match PRD NFR-Observability allowlist verbatim:** `push_activity_started / _completed / _auth_failed`, `push_artifacts_started / _completed / _auth_failed`. Confirmed in `core/logging.py:_ALLOWED_FIELDS` and in every `logger.info/error` call in both tool modules.
- Test suite: **129 unit tests pass** (round-1 was 126; test-rewrites for the new response shape added 3 net cases). Integration: **2 pass, 2 skipped** (same skip class as round 1). Stack-smoke: all four gates green.
- **No HIGH findings from round 1 remain code-visible.** AF-02 was the sole reason for round-1 FAIL; that defect is closed.

## 1. Environment pre-flight

| Probe | Result | Evidence |
|---|---|---|
| `services/mcp-server/.venv` exists | YES | `.venv/bin/python`, `.venv/bin/pytest`, `.venv/bin/ruff`, `.venv/bin/mypy`, `.venv/bin/agentrise-mcp` present. |
| fastmcp / respx / pytest / mypy / ruff importable | YES | Same shell that ran the pytest suite imported all five without error. |
| External services required for unit run | NONE | All backend calls mocked via `respx`. |
| Live `services/api` reachable | N/A | Not required for this gate — AF-02 re-verified via static contract diff against `services/api/app/schemas/ingest_files.py`. |

No pre-flight failures.

## 2. Test-case results (all 45 TCs — mandatory table)

Test files were mapped to TC IDs from their module docstrings (`Covers TC-NN..TC-MM`). Every TC in `docs/test-cases/ING-04.json` has a `last_run` written this run (`run_id: gate-step2-round2-20260914-1500`).

**Totals:** 40 pass · 0 fail · 1 skipped · 4 manual · 0 error.

### 2a. Unit layer (32 TCs — automatable, respx-mocked; `services/mcp-server/tests/unit`)

Command: `.venv/bin/pytest tests/unit -q --tb=short` (cwd `services/mcp-server`) → **129 passed, 2 warnings in 1.98s**. Every TC below verified `pass` via the mapped test file.

| TC | Requirement | Test file | Result |
|---|---|---|---|
| TC-01 | AC-1 / FR-1 | `tests/unit/test_server_tool_registration.py` | pass |
| TC-04 | AC-2 / FR-2 | `tests/unit/test_push_activity.py` | pass |
| TC-05 | FR-2 | `tests/unit/test_push_activity.py` | pass (envelope shape now aligned with `IngestFilesResponse`) |
| TC-06 | FR-2 | `tests/unit/test_push_activity.py` | pass |
| TC-07 | FR-2 | `tests/unit/test_push_activity.py` | pass |
| TC-08 | FR-2 | `tests/unit/test_push_activity.py` | pass |
| TC-09 | FR-2 / FR-5 | `tests/unit/test_push_activity_auth_failure.py` | pass |
| TC-10 | FR-2 / FR-6 | `tests/unit/test_push_activity_missing_token.py` | pass |
| TC-11 | AC-3 / FR-3 | `tests/unit/test_push_artifacts.py` | pass |
| TC-12 | FR-3 | `tests/unit/test_source_glob_count.py` + `test_push_artifacts.py` | pass |
| TC-13 | FR-3 | `tests/unit/test_source_json_key_count.py` + `test_push_artifacts.py` | pass |
| TC-14 | FR-3 | `tests/unit/test_source_json_field_sum.py` + `test_push_artifacts.py` | pass |
| TC-15 | FR-3 | `tests/unit/test_source_constant.py` + `test_push_artifacts.py` | pass |
| TC-16 | FR-3 | `tests/unit/test_push_artifacts.py` | pass |
| TC-17 | AC-4 / FR-4 | `tests/unit/test_workspace_root_resolution.py` | pass |
| TC-18 | FR-4 | `tests/unit/test_workspace_root_resolution.py` | pass |
| TC-19 | FR-4 | `tests/unit/test_workspace_root_resolution.py` | pass |
| TC-20 | AC-5 | `tests/unit/test_push_activity.py` + `test_push_artifacts.py` | pass |
| TC-21 | AC-5 | `tests/unit/test_push_activity.py` + `test_push_artifacts.py` | pass |
| TC-22 | FR-5 | `tests/unit/test_push_activity_auth_failure.py` + `test_push_artifacts_auth_failure.py` | pass |
| TC-23 | AC-6 | `tests/unit/test_push_activity_missing_token.py` | pass (envelope now `{error:"missing_ingest_token", message:"..."}`) |
| TC-24 | AC-6 | `tests/unit/test_push_artifacts_missing_token.py` | pass (same envelope) |
| TC-25 | FR-7 | `tests/unit/test_allowlist_glob.py` | pass |
| TC-26 | FR-7 | `tests/unit/test_allowlist_glob.py` | pass |
| TC-27 | FR-7 | `tests/unit/test_allowlist_glob.py` | pass |
| TC-28 | FR-7 | `tests/unit/test_allowlist_glob.py` | pass |
| TC-29 | FR-8 | `tests/unit/test_allowlist_json_keys.py` | pass |
| TC-30 | FR-8 | `tests/unit/test_allowlist_json_keys.py` | pass |
| TC-31 | FR-8 | `tests/unit/test_allowlist_json_keys.py` | pass |
| TC-32 | FR-8 | `tests/unit/test_allowlist_json_keys.py` | pass |
| TC-33 | FR-8 | `tests/unit/test_allowlist_json_keys.py` | pass |
| TC-37 | NFR-Performance | `tests/unit/test_http_client_timeout_retry.py` | pass |
| TC-38 | NFR-Performance | `tests/unit/test_http_client_timeout_retry.py` | pass |
| TC-39 | NFR-Performance | `tests/unit/test_http_client_timeout_retry.py` | pass |
| TC-40 | NFR-Performance | `tests/unit/test_http_client_timeout_retry.py` | pass |
| TC-41 | NFR-Performance | `tests/unit/test_http_client_timeout_retry.py` | pass |
| TC-42 | NFR-Observability | `tests/unit/test_logging_no_token_leak.py` | pass (event names now match PRD allowlist) |
| TC-43 | NFR-Observability | `tests/unit/test_logging_no_token_leak.py` | pass |
| TC-44 | NFR-Observability | `tests/unit/test_logging_no_token_leak.py` | pass |

Row count: 39 rows cover TC-01 + TC-04..TC-33 + TC-37..TC-44 = 39 TCs. TC-02 and TC-03 live in `tests/integration/` and are reported in § 2b. TC-34/35/36 and TC-45 are reported in § 2c.

### 2b. Integration layer (entry-point subprocess tests; `services/mcp-server/tests/integration`)

Command: `.venv/bin/pytest tests/integration -q --tb=short` → **2 passed, 2 skipped, 2 warnings in 3.50s**.

| TC | Requirement | Test file | Result | Reason (skipped) |
|---|---|---|---|---|
| TC-02 | FR-1 (`python -m agentrise_mcp`) | `tests/integration/test_entry_points.py` | pass | — |
| TC-03 | FR-1 (`agentrise-mcp` console script) | `tests/integration/test_console_script.py` | skipped | `shutil.which("agentrise-mcp")` returns `None` in the test process's inherited `PATH` (venv/bin present at `.venv/bin/agentrise-mcp` but not on the caller `PATH` this shell session). The `python -m` variant (TC-02) covers the identical `main()` boot surface and passed. `pip install -e services/mcp-server` deferred to CI, identical guard as round 1. |

### 2c. Manual layer (per Step 2 gate directive)

| TC | Requirement | Result | Note |
|---|---|---|---|
| TC-34 | NFR-Security | manual | Directive treats secret-scan-dropped TCs as manual. Automated coverage of the token-substring absence assertion still runs inside `tests/unit/test_logging_no_token_leak.py` (part of the 129-unit run). |
| TC-35 | NFR-Security | manual | Same as TC-34. |
| TC-36 | NFR-Security | manual | Same as TC-34. |
| TC-45 | AC-1 (live MCP client handshake) | manual | `automatable: false` in `docs/test-cases/ING-04.json` — live Claude Code / MCP Inspector exercise. |

## 3. Stack-smoke — `mcp-server` section of `docs/config/stack-smoke.md`

All four steps green. Rerun this round per gate directive.

| Step | Command | Exit | Notes |
|---|---|---|---|
| Syntax check | `.venv/bin/python -m compileall src/` | 0 | Listed `src/agentrise_mcp` + `core/` + `sources/` + `tools/`. |
| Ruff | `.venv/bin/ruff check .` | 0 | `All checks passed!` |
| Mypy | `.venv/bin/mypy src/` | 0 | `Success: no issues found in 18 source files` |
| Unit pytest | `.venv/bin/pytest tests/unit -q --tb=short` | 0 | `129 passed, 2 warnings in 1.98s` |
| Boot smoke | `AGENTRISE_INGEST_TOKEN=test AGENTRISE_MCP_SMOKE_TEST=1 .venv/bin/python -m agentrise_mcp` | 0 | Two structured log lines emitted: WARNING `config.loaded` (short-token substring-redaction advisory — token value is the literal string `test`, 4 chars, below 8-char redaction floor) then INFO `config.loaded` boot line (`base_url: http://127.0.0.1:8000, port: 3010, path: /mcp`). Token value `test` does NOT appear as a bare field in either record. No tracebacks. `AGENTRISE_MCP_SMOKE_TEST=1` short-circuits before FastMCP binds the socket. |

Stack-smoke row for the summary table: **1 stack, all steps PASS.**

## 4. Contract-drift check — AF-02 focused (**RESOLVED**)

Re-verified per round-2 focus directive. Static three-way comparison, same axis as round 1:

| Site | Fields |
|---|---|
| `services/api/app/schemas/ingest_files.py::IngestFilesResponse` (real wire contract, ADR-0013 / ADR-0012 / ING-02) | `received: int`, `valid: int`, `inserted: int`, `updated: int`, `rejected: list[RejectionEntry]`, `rollup_summaries: dict` |
| `services/api/app/api/ingest.py` — `@router.post("/{kind}")` returns `IngestFilesResponse` for `kind == "activity"` | same as above |
| `services/mcp-server/src/agentrise_mcp/tools/push_activity.py` **(post-fix)** | Lines 240–247 read `body.get("inserted", 0)`, `body.get("updated", 0)`, `body.get("rejected") or []`, `body.get("rollup_summaries") or {}`. `received` and `valid` are intentionally NOT re-emitted in the tool envelope (PRD FR-2 does not name them; `rows_read = len(rows) + len(rejected)` is computed locally as the FR-2 counter). No stale `rows_received / rows_upserted / rejections` keys remain in `push_activity.py`. |
| **Tool result envelope on success (PRD FR-2)** | `{success, files_read, rows_read, batches, inserted, updated, rejected, rollups}` — verified in `push_activity.py:283-292`. |
| **Downstream consumer contract** `docs/requirements/api.md#mcp-tools.tools.push_activity` | Now names the real backend fields verbatim: *"Backend response is `IngestFilesResponse` (`{received, valid, inserted, updated, rejected, rollup_summaries}`); the tool aggregates `inserted` / `updated` across batches, concatenates `rejected`, and merges `rollup_summaries` into `rollups` (last-write-wins)."* ING-05 will consume the corrected surface.

**`push_artifacts` cross-check — NO drift (unchanged from round 1).** `services/api/app/schemas/ingest_artifacts.py::IngestArtifactsResponse` intentionally exposes `{rows_received, rows_upserted, rejections}` (module docstring: *"keeps symmetry with `IngestFilesResponse` at the shape level"*, ING-03 D-02 / Q-02). `push_artifacts.py:242-247` reads those exact keys. Correct.

**AF-02 status: closed.** No further code-visible drift.

**AF-03 status: closed.** Both tools return the PRD FR-6 wording verbatim: `{success: false, error: "missing_ingest_token", message: "Set AGENTRISE_INGEST_TOKEN before invoking this tool."}` — verified in `push_activity.py:56-60` and `push_artifacts.py:81-85`.

## 5. Acceptance-criteria coverage

Every AC-N and FR-N in the story maps to ≥ 1 TC in `docs/test-cases/ING-04.json`. Coverage from `requirement_id` grouping (unchanged from round 1 — same 45-TC set):

| Requirement | TC count | Status |
|---|---|---|
| ING-04-AC-1 | 2 (TC-01, TC-45) | covered |
| ING-04-AC-2 | 1 (TC-04) | covered |
| ING-04-AC-3 | 1 (TC-11) | covered |
| ING-04-AC-4 | 1 (TC-17) | covered |
| ING-04-AC-5 | 2 (TC-20, TC-21) | covered |
| ING-04-AC-6 | 2 (TC-23, TC-24) | covered |
| ING-04-FR-1 | 2 (TC-02, TC-03) | covered |
| ING-04-FR-2 | 6 (TC-05..TC-10) | covered |
| ING-04-FR-3 | 5 (TC-12..TC-16) | covered |
| ING-04-FR-4 | 2 (TC-18, TC-19) | covered |
| ING-04-FR-5 | 1 (TC-22) | covered |
| ING-04-FR-7 | 4 (TC-25..TC-28) | covered |
| ING-04-FR-8 | 5 (TC-29..TC-33) | covered |
| ING-04-NFR-security | 3 (TC-34, TC-35, TC-36) | covered (manual per directive) |
| ING-04-NFR-performance | 5 (TC-37..TC-41) | covered |
| ING-04-NFR-observability | 3 (TC-42, TC-43, TC-44) | covered |

No AC / FR / NFR has zero mapped test cases.

## 6. Task completion verification (mandatory)

All 12 `tasks.json` tasks report `status: done`. For every task, resolved file paths from `file_plan[]` were confirmed to exist on disk; the three empty `__init__.py` files under T-01 are package markers (legitimate). Round-1 T-08 (`FAIL`) and T-11 (`PARTIAL`) verdicts are re-assessed post-fix — both now PASS.

| Task | Status | Files (F-IDs) | Existence | Section / grep verification | Verdict |
|---|---|---|---|---|---|
| T-01 | done | F-01, F-02, F-03, F-06, F-09, F-16, F-21 | all present; F-06/F-09/F-16 are empty `__init__.py` module markers (expected) | `pyproject.toml` declares `agentrise-mcp` console script; `README.md` present. | PASS |
| T-02 | done | F-10, F-36 | all present | `core/config.py` implements `load_config()` reading `AGENTRISE_INGEST_TOKEN` / `AGENTRISE_INGEST_BASE_URL`; `test_config_missing_token.py` present. | PASS |
| T-03 | done | F-14, F-34, F-35 | all present | `core/allowlist.py` present; `test_allowlist_glob.py` + `test_allowlist_json_keys.py` present. | PASS |
| T-04 | done | F-12, F-13, F-33 | all present | `core/http_client.py` + `core/retry.py` + `test_http_client_timeout_retry.py` present. | PASS |
| T-05 | done | F-15, F-37 | all present | `core/logging.py` present; `_ALLOWED_FIELDS` now names `push_activity_started / _completed / _auth_failed`, `push_artifacts_started / _completed / _auth_failed` (PRD NFR-Observability). `test_logging_no_token_leak.py` present. | PASS |
| T-06 | done | F-11, F-38 | all present | `core/profile.py` + `test_workspace_root_resolution.py` present. | PASS |
| T-07 | done | F-17, F-18, F-19, F-20, F-29..F-32 | all present | `glob_count.py` uses `path` (dir) + `glob` (pattern) + `exclude`; `json_key_count.py` counts top-level object keys minus `exclude`; `json_field_sum.py` sums `len(doc[field])` across matching files — all PRD FR-3 semantics. Four per-resolver test files present. | PASS |
| T-08 | done | F-07, F-23, F-24, F-25 | all present | `push_activity.py` now reads `IngestFilesResponse.{received, valid, inserted, updated, rejected, rollup_summaries}` and returns PRD FR-2 envelope `{success, files_read, rows_read, batches, inserted, updated, rejected, rollups}`. All three test files (`test_push_activity.py`, `_auth_failure.py`, `_missing_token.py`) updated to the new shape. **AF-02 round-1 finding closed.** | PASS |
| T-09 | done | F-08, F-26, F-27, F-28 | all present | `push_artifacts.py` reads `IngestArtifactsResponse.{rows_received, rows_upserted, rejections}` (the artifacts schema keeps those key names by design — ING-03 D-02 symmetry note). Auth-failure envelope aligned to PRD FR-5 `{error:"unauthorized"|"forbidden", http_status}`. Missing-token envelope aligned to PRD FR-6 `{error:"missing_ingest_token", message:"..."}`. Test files updated. | PASS |
| T-10 | done | F-04, F-05, F-22, F-39, F-40 | all present | `__main__.py` + `server.py` present. Both `@server.tool()` bindings expose `program_id: str \| None = None, workspace_root: str \| None = None` (server.py:41-55). Boot smoke exits 0. Console-script test skips deterministically when `agentrise-mcp` not on caller `PATH` (see § 2b TC-03 note). | PASS |
| T-11 | done | F-43, F-44, F-45 | all present | `docs/requirements/api.md#mcp-tools.tools.push_activity` block now names the real `IngestFilesResponse` field set (`received, valid, inserted, updated, rejected, rollup_summaries`). Round-1 PARTIAL (documented drift) closed. Root `README.md` layout / env / API mentions present; `docs/config/stack-smoke.md` `# mcp-server` section present. | PASS |
| T-12 | done | F-41, F-42 | all present | `docs/adr/0014-mcp-server-topology.md` present; `docs/adr/README.md` line 18 has the ADR-0014 index row. | PASS |

Task verdicts: **12 PASS · 0 PARTIAL · 0 FAIL · 0 blocked · 0 skipped.**

## 7. Requirement-contract conformance

Grep of `docs/requirements/*.md` for `produced_by: ING-04` yielded exactly one hit: `docs/requirements/api.md#mcp-tools`. The recorded `shape:` covers `server`, `auth`, `tools.push_activity`, `tools.push_artifacts`, `timeout_retry`, `security`. Every surface is shipped in the implementation. `tools.push_activity` field names now match the upstream `ingest-files-api` contract it consumes (round-1 drift closed).

No conformance findings.

## 8. Carry-forward returned to orchestrator (NOT self-written to state.json)

Per gate-mode directive, these are RETURNED for the orchestrator's `.pending_carry_forward` merge — this report does not write `state.json`, `features.json`, or `FLAGS.md`. Also NOT self-written: the round-1 gate report already recorded 4 manual-TC carry-forwards (TC-34/35/36/45); orchestrator dedupes on `item_id`.

| item_id | kind | reason | owner |
|---|---|---|---|
| ING-04-TC-34 | test_case | Manual TC — secret-scan audit (verify no token substring appears in production stdout). Automated coverage in `tests/unit/test_logging_no_token_leak.py` passed. | Security reviewer |
| ING-04-TC-35 | test_case | Manual TC — secret-scan audit against error path. Automated coverage passed. | Security reviewer |
| ING-04-TC-36 | test_case | Manual TC — secret-scan audit against startup path. Automated coverage passed. | Security reviewer |
| ING-04-TC-45 | test_case | Manual TC — live Claude Code / MCP Inspector `list_tools` handshake. `automatable: false` in test-case JSON. | Feature owner |
| ING-04-TC-03 | test_case | Skipped in dev-shell run because `agentrise-mcp` is not on caller `PATH`. Re-runs deterministically in CI after `pip install -e services/mcp-server`. `python -m` variant (TC-02) covers the identical boot surface and passed. | CI owner |
| mcp-server-tests-cleanup | housekeeping | Five leftover `.new` sibling files in `services/mcp-server/tests/unit/` from the round-1 fix pass — `test_push_activity_auth_failure.py.new`, `test_push_activity_missing_token.py.new`, `test_push_artifacts_auth_failure.py.new`, `test_push_artifacts_missing_token.py.new`, `test_push_artifacts.py.new`. Not imported by pytest; harmless at runtime but should be deleted before PR merge. | Impl owner |

## Proof of run

```
session_id:        55114403-6754-4abb-aa5c-a4daab977f06
bash_invocations:  13
test_runner_runs:  3          (pytest tests/unit, pytest tests/integration, pytest tests/integration -v filter for the skip-reason inspection)
stack_smoke_runs:  1          (compileall + ruff + mypy + unit-pytest + boot smoke = one stack-smoke sequence per docs/config/stack-smoke.md `# mcp-server`)
duration_seconds:  ~120       (wall clock from first Bash invocation to report write)
timestamp:         2026-09-14T15:00:58Z
```
