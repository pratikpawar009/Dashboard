# Validation report — SHP-01

- Date: 2026-09-03T15:23:21Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, Validate ∥ Review gate, round 1/3)
- Total: 4   Passed: 4   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~600s (estimate — no explicit session-start timestamp captured; span observed from internal tool timestamps)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   1  |   0  |   0   |   0   |
| Unit         |   2   |   2  |   0  |   0   |   0   |
| Integration  |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

Stack smoke note: `nextjs` was independently re-booted this round (see Proof of run) — real `next dev --turbopack --port 3021`, polled to `200` on `GET /`. `fastapi-2` is counted but not independently re-run this round: SHP-01 touches zero files under `services/api/` (confirmed by diff below), so its surface is unchanged since Step 1's `impl_evidence.checks.runtime` (PASS, `GET /health` → 200, 2026-09-03T21:10:00Z) — re-running its Postgres+migration bring-up would exercise a stack this story's diff cannot have broken. Not scored FAIL/PASS in this round's own totals; carried from Step 1 evidence per the invocation's "read it, do NOT regenerate it" instruction on `impl_evidence`.

Contract conformance (Phase 3b): the one contract SHP-01 produces — `### persona-shell` in `docs/requirements/api.md` — was checked field-by-field against the shipped `PersonaDashboardShell.tsx`/`formatPersonaTag.ts`/`programStyle.ts`/`deriveInitials.ts`. Every prop shape, derived-value function, and state description matches the shipped code exactly, including the four tag/subtitle literals (byte-exact, incl. lowercase "m" in "Engineering manager overview") and both `docs/design/tokens.md` color tables (persona colors and program-type colors, cross-checked hex-for-hex against the code's `PERSONA_DISPLAY`/`PROGRAM_TYPE_COLORS` maps — zero mismatches). **No conformance finding.** Note: the contract text names `getProgramStyle()`'s two keys (`avatarStyle`, `typeChip`) but never pins a type for them, so D-06's `string`→`CSSProperties` deviation from PLAN.md does not contradict this contract — it's a PLAN-vs-code decision, not a contract-vs-code drift.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `apps/web/src/types/persona.ts` (FR-1 field-name isolation adapter) | ✓ | n/a | PASS |
| T-02 | `docs/design/tokens.md` (add "Persona colors" + "Program type colors" tables) | ✓ | ✓ (`### Persona colors` line 39, `### Program type colors` line 52; hex values cross-checked byte-exact against code) | PASS |
| T-03 | `apps/web/src/lib/formatPersonaTag.ts` (formatPersonaTag + PersonaTagError) | ✓ | n/a | PASS |
| T-04 | `apps/web/src/lib/formatPersonaTag.test.ts` (unit test, covers TC-01) | ✓ | n/a — verified by test execution (6/6 sub-cases pass) | PASS |
| T-05 | `apps/web/src/lib/deriveInitials.ts` | ✓ | n/a | PASS |
| T-06 | `apps/web/src/lib/programStyle.ts` (getProgramStyle) | ✓ | n/a | PASS |
| T-07 | `apps/web/src/components/PersonaHeader.tsx` + `.module.css` | ✓ | n/a | PASS |
| T-08 | `apps/web/src/components/ProgramContext.tsx` + `.module.css` | ✓ | n/a | PASS |
| T-09 | `apps/web/src/components/ProgramContext.test.tsx` (covers TC-03) | ✓ | n/a — verified by test execution (2/2 pass) | PASS |
| T-10 | `apps/web/src/components/PersonaDashboardShell.tsx` + `.module.css` | ✓ | n/a | PASS |
| T-11 | `apps/web/src/components/PersonaDashboardShell.test.tsx` (covers TC-02) | ✓ | n/a — verified by test execution (4/4 pass) | PASS |
| T-12 | `apps/web/src/components/PersonaDashboardShell.perf.test.tsx` (covers TC-04) | ✓ | n/a — verified by test execution (1/1 pass, budget met) | PASS |

12/12 PASS. All `tasks.json` entries carry `status: "done"` — none `blocked`, none `skipped`. All 15 planned files (`F-01`..`F-15`) exist, non-empty; `git status --porcelain -uall -- apps/web` lists exactly the 14 planned `create` files under `apps/web/src/` and nothing else — zero scope creep. `services/api/` has zero diff, confirming the story's own "frontend-only" claim.

### Passed

| TC id        | Title                                                                                   |
|--------------|------------------------------------------------------------------------------------------|
| SHP-01-TC-01 | formatPersonaTag resolves all four persona literals; cio/invalid throw                   |
| SHP-01-TC-02 | Shell suppresses gated regions while loading; neutral error state; identity-bar fallback |
| SHP-01-TC-03 | Program-context passes data through unchanged; composes style itself, ignores caller CSS |
| SHP-01-TC-04 | Shell render completes within the 200ms p95 budget                                       |

### Flaky tests

None — every TC passed on its first attempt (0 retries triggered).

### Performance budget breaches

None. `SHP-01-TC-04` (`SHP-01-NFR-1`): target `p95 < 200ms` @ 30 iterations, measured `p95 = 5.083ms` (nearest-rank, n=30, captured verbatim from the test's own `console.log`) — `budget_pass: true`. 39x margin under budget.

### Regression coverage

None — no TC in this manifest carries a `regression-<id>` tag (no prior fix-loop round exists for SHP-01 yet).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — all 4 TCs are `automatable: true`; no manual TCs exist in this manifest.

### Budget-unparseable NFRs

None — `SHP-01-NFR-1`'s budget string (`≤200ms p95`) parsed cleanly.

### Known manifest-vs-implementation mismatches — judged per invocation guidance

Both are pre-existing, orchestrator-triaged flags (`docs/features/SHP-01/FLAGS.md` AF-06; open, pending `/arh-human-review`), not new findings and not scored as failures:

1. **AF-06 — `SHP-01-TC-03.test_data` cites `type: "infrastructure"`/`"product"`**, which don't exist in `docs/design/tokens.md` § Program type colors (only `Migration`/`Greenfield feature development`/`Brownfield feature development`/`Maintenance` exist, byte-exact against the EMD mockup's `tMap`). Verified: `apps/web/src/components/ProgramContext.test.tsx` actually exercises `Migration` and `Maintenance` — real tokens.md-backed types, per `tasks.json` T-09's explicit direction to substitute "type='infrastructure'-analog from tokens.md's 4 real types". The substance (verbatim data passthrough + caller-CSS-ignored) is genuinely tested, more strictly than the literal `test_data` would have been. **Agree the manifest should be reconciled** — `docs/test-cases/SHP-01.json` TC-03's `test_data`/steps should be updated to name `Migration`/`Maintenance` (or another real pair) so a future reader doesn't see the test as diverging from its own spec. Not actioned here (GATE MODE is read-only on state artifacts other than this report + test-case `last_run`; `test_data` literal rewrite is a manifest-content change, not a `last_run` patch).
2. **TC-02's `test_data` speaks of `session: null` and a "PersonaNotFoundError-raising persona-resolver stub"** — verified the shell has no `session` prop and no resolver dependency (`PersonaDashboardShell.tsx` props are `signedInUser`/`persona`/`program` only). `PersonaDashboardShell.test.tsx`'s own header comment documents the exact vocabulary translation (`persona={undefined}` for unresolved, a sentinel string in `persona` for a caught resolver error) and the four scenarios it actually runs match TC-02's four `expected_results` one-for-one. Translation is faithful; no substance gap.

### Deliberately uncovered (carry-forward, not a failure)

`SHP-01-FR-1` and `SHP-01-FR-3` are uncovered by design (`docs/test-cases/SHP-01.json` `coverage_audit.uncovered`, approved at the PRD Product Gate) — both depend on the unlanded AUTH-01 session-contract amendment. Returning as a carry-forward entry for the orchestrator (GATE MODE — not self-written):

```json
{
  "item_id": "SHP-01-FR-1-FR-3-uncovered",
  "kind": "test_case",
  "reason": "SHP-01-FR-1 (signedInUser populated-identity render) and SHP-01-FR-3 (deriveInitials coverage) are deliberately uncovered under the approved <=4 TC cap — both depend on the unlanded AUTH-01 session-contract amendment (field names may still change). Revisit docs/test-cases/SHP-01.json once that amendment lands.",
  "owner": "SHP-01 owner / whoever revisits AUTH-01 session-contract landing",
  "added_at": "2026-09-03T15:23:21Z",
  "added_by": "validation-agent (SHP-01 gate round 1, report-only mode)",
  "resolved_at": null,
  "evidence": null
}
```

Pre-existing carry-forward items already in `docs/features/SHP-01/state.json` (`R-01-session-seam-unowned`, `R-02-persona-resolver-callsite-unowned`, `NFR-3-contrast-check-unowned`) were reviewed and remain accurate — not duplicated here.

### Independent verification notes (beyond the 4 TCs)

- **D-06 deviation confirmed as implemented**: `apps/web/src/lib/programStyle.ts` `getProgramStyle()` returns `{ avatarStyle: CSSProperties; typeChip: CSSProperties }`, not `string` — matches `DECISIONS.md` D-06 exactly, including the "geometry stays in `.module.css`, only `{color, background}` crosses via `style`" rule (verified against all three `.module.css` files — no hardcoded persona/program hex outside the two lookup files, no `dangerouslySetInnerHTML` anywhere).
- **AF-03 mitigation confirmed as implemented**: `PersonaDashboardShell.tsx`'s `avatarColorStyle()` catches only `PersonaTagError` and falls back to the documented neutral pair (`#5b6472`/`#e4e7ec`) when `signedInUser` is defined but `persona` is invalid — matches the flag's stated resolution.
- **Color tables byte-exact**: both `docs/design/tokens.md` § Persona colors and § Program type colors hex values cross-checked 1:1 against `PERSONA_DISPLAY` (`formatPersonaTag.ts`) and `PROGRAM_TYPE_COLORS` (`programStyle.ts`) — zero mismatches.
- **NFR-2 (security) spot-check**: `SignedInUser` carries only `name`/`jobTitle` — no raw IdP `role` field is ever read or rendered by the shell; no `console.log`/analytics call touches `signedInUser` anywhere in the 4 component files (only the perf test logs a timing number, not PII).
- **Backend regression safety net**: `docs/config/project-commands.yaml`'s `test_unit` also runs `uv run pytest tests/` — executed as part of this round (`431 passed, 0 failed`, pre-existing suite, zero SHP-01-owned files in it). Confirms this frontend-only story introduced no backend regression; not counted in the TC summary table since none of the 431 are SHP-01 test cases.

### Proof of run

```
### Proof of run

- session_id:        not exposed to this agent's tool interface
- bash_invocations:  36
- test_runner_runs:  4  (pnpm/vitest run x3 — default, --reporter=verbose, --reporter=json; uv run pytest tests/ x1)
- stack_smoke_runs:  1  (nextjs: start on :3021, poll GET / -> 200, teardown; fastapi-2 not re-run this round, see Stack smoke note above)
- duration_seconds:  ~600 (estimate)
- timestamp:         2026-09-03T15:23:21Z
```
