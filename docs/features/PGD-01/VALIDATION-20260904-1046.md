# Validation report — PGD-01

- Date: 2026-09-04T04:46:00Z
- Verdict: PASS
- Mode: full (gate mode — report-only, inside `/arh-implement` Validate ∥ Review; snapshot `ee5ad949ab357738`)
- Total: 4   Passed: 4   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~2100s (this validation session; excludes the separately-timestamped Step 1 evidence pass)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |  54   |  54  |   0  |   0   |   0   |
| Integration  |   3   |   3  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

Stack-smoke row: both stacks (`fastapi-2`/api, `nextjs`/web) are counted PASS on the strength of Step 1's evidence packet (`runtime-api.log`, `runtime-web.log` — clean boots, `/health` 200, real SSR HTML for `/programs/prog-smoke-01`) rather than an independent re-boot in this gate pass; this validation instead re-ran the full test suites (below) against real Alembic-migrated Postgres test fixtures (`migrated_db`/`test_engine`, `services/api/tests/conftest.py`), which is the equivalent real-database check at the test layer for TC-01/02/04. Unit row (54) is `apps/web` vitest total (includes the 5 co-located component unit suites: `BackToProgramBoard`, `ProgramDetailHeader`, `ProgramSwitcher`, `ProgramSummaryCards`, `ProgramDetailErrorPanel`, plus pre-existing suites); backend `pytest tests/` unit total is 434 passed (not itemized here — only the PGD-01-owned/affected files are broken out: `test_overview.py` under Contract+Integration, `test_overview_perf.py` under Performance, `test_programs.py`/`test_auth_cors.py` re-run clean under the AUTH-04 href fix and D-07 CORS header addition).

Contract conformance (Phase 3b): `docs/requirements/api.md#program-detail-api` (`produced_by: PGD-01`) checked field-by-field against the shipped `app/schemas/program_detail.py` / `app/api/overview.py` — endpoint path, `header`/`summary` shapes, card order, `format_number()`/ratio-string split, byte-identical invariant, RBAC call shape, 404 envelope, and `program_drilldown`/`program_switch` observability shapes all match verbatim. No drift found.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/schemas/program_detail.py` (ProgramDetailResponse schema) | ✓ | n/a | PASS |
| T-02 | `services/api/app/api/overview.py` (GET router) | ✓ | n/a | PASS |
| T-03 | `services/api/app/main.py` (router wiring + CORS `allow_headers`) | ✓ | ✓ (`X-Program-Switch-From` present in `allow_headers`, verified by `test_auth_cors.py`) | PASS |
| T-04 | `services/api/app/api/programs.py`, `tests/unit/test_programs.py` (AUTH-04 href fix) | ✓ | ✓ (`href=f"/programs/{row.program_id}"`; TC-07 assertion updated to `/programs/prog-1`, test passes) | PASS |
| T-05 | `services/api/tests/unit/test_overview.py` (TC-01/TC-02) | ✓ | n/a | PASS |
| T-06 | `services/api/tests/perf/test_overview_perf.py` (TC-04) | ✓ | n/a | PASS |
| T-07 | `apps/web/src/lib/apiConfig.ts`, `types/programDetail.ts`, `lib/programDetailApi.ts` | ✓ | n/a | PASS |
| T-08 | `apps/web/src/lib/routes.ts`, `components/BackToProgramBoard.{tsx,module.css,test.tsx}` | ✓ | n/a | PASS |
| T-09 | `apps/web/src/components/ProgramDetailHeader.{tsx,module.css,test.tsx}` | ✓ | n/a | PASS |
| T-10 | `apps/web/src/components/ProgramSwitcher.{tsx,module.css,test.tsx}` | ✓ | n/a | PASS |
| T-11 | `apps/web/src/components/ProgramSummaryCards.*`, `ProgramDetailErrorPanel.*` | ✓ | n/a | PASS |
| T-12 | `apps/web/src/components/ProgramDetailView.{tsx,module.css,test.tsx}` | ✓ | n/a | PASS |
| T-13 | `apps/web/src/app/programs/[program_id]/page.tsx` | ✓ | n/a | PASS |
| T-14 | `README.md` (API table row + env var row), `apps/web/.env.example` | ✓ | ✓ (grep confirms `GET /api/overview/program-detail/{program_id}` row and `NEXT_PUBLIC_API_URL` row both present, `README.md:37,56`) | PASS |
| T-15 | `docs/config/stack-smoke.md` (NEXT_PUBLIC_API_URL note under nextjs/next `Run:`) | ✓ | ✓ (grep confirms the note under both sections, lines 31 and 63) | PASS |

15/15 tasks verified present and correct. No `blocked` or `skipped` tasks in `tasks.json` — none exempted.

### Passed

| TC id | Title |
|-------|-------|
| PGD-01-TC-01 | Program Detail endpoint: bearer-JWT required, RBAC-open, header + 7-card contract, byte-identical across personas |
| PGD-01-TC-02 | Unknown program_id returns the standard 404 JSON error envelope |
| PGD-01-TC-03 | Program Detail page: switcher reloads in place, back-link navigates, error state on unknown program, keyboard/aria affordances |
| PGD-01-TC-04 | Program Detail request stays within its single-query performance budget and emits the program_drilldown observability event |

Each TC's assertions were read against its `expected_results[]` in `docs/test-cases/PGD-01.json`, not just its pass/fail status:

- **TC-01**: `test_overview.py::test_program_detail_contract_bearer_required_rbac_open_byte_identical_tc01` — asserts 401 with no bearer, 200 for both `cio` and `engineering-manager` tokens, `cio_resp.content == em_resp.content` (byte-for-byte, not a field-subset compare), exact header field values, all 7 cards' glyph/value/label against the seeded `format_number()`/ratio expectations in mockup order, exact top-level key set `{header, summary}` (no `avatarStyle`/`typeChip`/`session.programs` leakage), and an **AST walk** of `app/api/overview.py` asserting no `current_user.role`/`current_user.persona` attribute access exists anywhere in the module — a materially stronger check than a source-text grep (immune to false positives from the module's own docstring prose, and a true negative on any renamed/refactored branch).
- **TC-02**: `test_overview.py::test_unknown_program_id_returns_standard_404_envelope_tc02` — asserts status 404, exact key set `{"error"}` → `{"code","message","details"}`, `code == "http_404"`, `message == "program not found"`, `details is None` — the exact envelope shape, not a loose "some 404 happened" check.
- **TC-03**: `ProgramDetailView.test.tsx` (6 assertions) — `fetchProgramDetail` called with `("prog-099", {switchedFrom: "prog-042"})`; `router.replace("/programs/prog-099")` called and `router.push` never called and `window.location.href` unchanged (proves no hard navigation); header/cards re-render to fixture B's values in place; back-link `href` equals the imported `ADOPTION_OVERVIEW_ROUTE` constant AND is nested inside the header's sticky wrapper (AF-05 regression guard, asserted via DOM ancestry, not just presence); a `{status:"not_found"}` initial result renders the distinct error panel text and never calls `fetchPrograms`; switcher trigger has `aria-haspopup="true"`, `aria-expanded` toggling correctly, and responds to a real Enter/Space keydown+click sequence.
- **TC-04**: `test_overview_perf.py::test_program_detail_single_select_budget_and_program_drilldown_event_tc04` — a `before_cursor_execute` SQL-statement counter (not a mock) asserts exactly 1 SELECT against `program_summary`; measured handler duration 11.98ms against the pinned 500ms budget (comfortable margin, honestly reported — the test's own `print()` states the measured number regardless of outcome); exactly one `program_drilldown` log record with `{timestamp, level, logger, message, program_id}` and an explicit negative-assertion loop confirming no PII key (`email`/`groups`/`user_id`/`name`/`description`/`path`) is present.

No test's assertions were weakened relative to `docs/test-cases/PGD-01.json` — each production test file's own docstring cross-references its TC id and the story's ACs, and the assertions read match the TC's `expected_results[]` verbatim in substance.

### Flaky tests

None. All 4 TCs passed on the first attempt in every run (targeted run, full-suite run, and a `--durations=0`/`-s` re-run for timing evidence — 3 independent pytest passes and 2 independent vitest passes over these files in this session, zero flakes observed).

### Performance budget breaches

None. TC-04's only budget (`handler_duration_budget_ms: 500`, `query_count_budget: 1`) passed with wide margin: measured 11.98ms / 1 SELECT.

### Regression coverage

N/A — no TC in `docs/test-cases/PGD-01.json` carries a `regression-<id>` tag (first-implementation story, no prior fix-loop).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — all 4 TCs are `automatable: true`; no manual TC exists in this manifest.

### Anti-suppression sweep

Grepped every PGD-01-touched file (backend + frontend, source + tests) for `# noqa`, `# type: ignore`, `eslint-disable`, `@ts-ignore`, `.skip(`/`xfail`/`it.skip`/`describe.skip`. **Zero matches.** No assertion was found weakened to pass (each TC's assertions match `expected_results[]` in substance, confirmed by direct read of every test file body, not just pass/fail status).

### AUTH-04 / AF-flag cross-check

- **T-04 (AUTH-04 href fix, D-04)**: `services/api/app/api/programs.py:132` now emits `href=f"/programs/{row.program_id}"`; `test_programs.py::test_href_routes_to_program_detail_api_path_tc07` updated to assert `/programs/prog-1` and passes. `programs-api`'s AUTH-04 CF-05 carry-forward is resolvable once this lands (owned by AUTH-04's own state, not mutated here).
- **AF-01 (T-09/T-10 import-order)**: resolved by execution order, not by re-verification here — `ProgramDetailHeader.tsx` imports `ProgramSwitcher` and both typecheck/build/test clean, confirming no stale-import breakage shipped.
- **AF-03 (`test_auth_cors.py` outside `file_plan`)**: confirmed correct — `allow_headers` now includes `X-Program-Switch-From`, and `test_auth_cors.py`'s 10 assertions (including the exact-kwargs and no-wildcard checks) all pass.
- **AF-05 (back-link nesting)**: confirmed fixed and regression-guarded — `ProgramDetailHeader.tsx` renders `<BackToProgramBoard />` as the first child of its sticky wrapper in every state (populated/loading/error), and `ProgramDetailView.test.tsx` has a dedicated assertion (`link.parentElement?.contains(headerName)`) that would fail if it regressed to a sibling-above placement.
- **AF-06/AF-07 (design_check / render_check N/A)**: no tooling exists to close either gap (`design_check:` empty in `project-commands.yaml`; no Playwright/Cypress installed) — confirmed still N/A, not a regression introduced by this story. Not scored against this validation's PASS verdict (both are pre-existing, disclosed infrastructure gaps, not PGD-01 defects).
- **AF-02 (ProgramSwitcher open-border color default)**: cosmetic, non-blocking; not re-litigated here — carried forward as-is (see CARRY-FORWARD below).

### Budget-unparseable NFRs

None — TC-04's budget parsed cleanly from its own `test_data`.

### Proof of run

```
### Proof of run

- session_id:        a8fcf321-763b-4cab-9c5b-cf84ac26e157
- bash_invocations:  25
- test_runner_runs:  7  (4x pytest: targeted TC-01/02/04, full `pytest -q` [434 passed], `--durations=0` timing pass, `-s` TC-04 print-capture pass; 3x vitest: targeted 6-file component run, full `vitest run` [54 passed], `--reporter=json` TC-03 timing pass)
- stack_smoke_runs:  0  (relied on Step 1 evidence's runtime-api.log/runtime-web.log for the live-boot check; this pass verified the equivalent real-database path via `migrated_db`/`test_engine` Alembic-backed pytest fixtures instead of re-booting the dev servers)
- duration_seconds:  ~2100
- timestamp:         2026-09-04T04:46:00Z
```
