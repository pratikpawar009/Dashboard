# Validation report — AUTH-02 (round 2 — re-validation after fix pass)

- Date: 2026-08-28T10:35:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, invoked from `/arh-implement` Validate ∥ Review gate)
- Total: 18 automatable TC executions (15 manifest TCs + 3 new TC-16 pytest cases) Passed: 18 Failed: 0 Errored: 0 Flaky: 0 Skipped manual: 0
- Duration: ~25 min (preflight + full suite + mypy/ruff + 3-stack smoke)

Verdict justification: the round-1 FAIL (`contract-conformance` finding — `persona_mapping_loaded` never carried `tier3_latency_ms` on a Tier-3 hit, despite the `persona-resolver` contract, `REQUIREMENTS.md` § C-4, and `tasks.json` T-05's own note all requiring it) is **closed**. `_resolve_uncached`/`_log_resolution` now measure and emit `tier3_latency_ms` on a fresh Tier-3 query only; `docs/requirements/auth.md`'s `persona-resolver` shape, `REQUIREMENTS.md`, `DECISIONS.md` D-11, and `services/api/README.md`'s runbook all now agree with the shipped code. Three new pytest cases (informally `TC-16` a/b/c) directly assert the fixed behaviour and all pass. Task completion is 10/10 PASS (T-05 was the only round-1 FAIL, now closed). Stack smoke 3/3 PASS. No regression-tagged TC failed.

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   3   |   3  |   0  |   0   |   0   |
| Unit         |  16   |  16  |   0  |   0   |   0   |
| Performance  |   2   |   2  |   0  |   0   |   0   |

Stack smoke row = 3 distinct runnable stacks (`fastapi-2`, `nextjs`, `postgres`), same dedup rule as round 1 (`fastapi`≡`fastapi-2`, `next`≡`nextjs`; `typescript`/`pytest`/`alembic`/`pydantic` are `(n/a — ...)` for both `Run:`/`Docker:`).

"Unit" row = 13 original `AUTH-02-TC-01..11,14,15` + 3 new `TC-16` cases (`test_fresh_tier3_hit_carries_tier3_latency_ms_tc16`, `test_tier1_and_tier2_hits_omit_tier3_latency_ms_tc16`, `test_warm_hit_reusing_tier3_omits_tier3_latency_ms_tc16`).

Contract-conformance (Phase 3b): **no findings this round** — re-checked `docs/requirements/auth.md#persona-resolver` (the only contract `produced_by: AUTH-02`) field-by-field against the shipped `PersonaResolver`/`_log_resolution`: `mechanism`, `interface`, `input`, `output`, `cache` (300s TTL matches `_CACHE_TTL_SECONDS`), `errors` (`PersonaResolutionError`/`PersonaNotFoundError` match), and `observability` (base 4-field event + `tier3_latency_ms` on a tier-3 hit) all match the shipped code.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/pyproject.toml` (add `pyyaml>=6.0` dependency pin) | ✓ | ✓ (`pyyaml>=6.0` present) | PASS |
| T-02 | `docs/config/project-commands.yaml` (add pyyaml smoke-import to preflight) | ✓ | ✓ (`import yaml` line present, ran green this round) | PASS |
| T-03 | `services/api/app/core/config.py` (extend Settings with `persona_role_map`/`persona_config_file`) | ✓ | ✓ (`persona_role_map`, `persona_config_file`, `_parse_persona_role_map` validator present) | PASS |
| T-04 | `services/api/config/persona_role_map.yaml` (ship Tier-2 YAML stub) | ✓ | ✓ (`{}` + schema comment naming all 5 personas + no-hot-reload note) | PASS |
| T-05 | `services/api/app/core/persona_resolver.py` (implement PersonaResolver incl. `tier3_latency_ms` on Tier-3 hits) | ✓ | ✓ (`grep -rn tier3_latency_ms app/` now matches `persona_resolver.py:132,144,150,159,175,177,198,203,222,223` — measured via `time.perf_counter()` in `_resolve_uncached`, threaded through `_log_resolution`, emitted only on a fresh Tier-3 query) | **PASS** (round-1 FAIL closed) |
| T-06 | `services/api/app/main.py` (wire PersonaResolver into `create_app()`) | ✓ | ✓ (`app.state.persona_resolver = PersonaResolver(cfg)` present; confirmed live via stack-smoke boot, no startup exception) | PASS |
| T-07 | `services/api/tests/unit/test_persona_resolver.py` (unit suite, TC-01..11,14,15 + new TC-16 x3) | ✓ | ✓ (16/16 tests pass, all TC ids present incl. 3 new TC-16 functions) | PASS |
| T-08 | `services/api/tests/perf/test_persona_resolver_perf.py` (perf suite, TC-12,13) | ✓ | ✓ (2/2 tests pass) | PASS |
| T-09 | `services/api/.env.example`, `README.md` (document `PERSONA_ROLE_MAP`) | ✓ | ✓ (both files carry `PERSONA_ROLE_MAP`/`PERSONA_CONFIG_FILE` documentation) | PASS |
| T-10 | `services/api/README.md` (add "Persona resolution" ops-runbook section) | ✓ | ✓ (`## Persona resolution` heading present; "Reading the logs" subsection updated this fix pass to name `tier3_latency_ms` as the field to alert on and the fresh-query/cache-hit distinguisher) | PASS |

10/10 PASS. No task-completion drop this round.

### Passed

| TC id            | Title                                                              |
|------------------|---------------------------------------------------------------------|
| AUTH-02-TC-01 | Tier-1 env-JSON source resolves role to persona without consulting Tier-2/3 |
| AUTH-02-TC-02 | Tier-2 YAML fallback when role not in Tier-1                       |
| AUTH-02-TC-03 | Tier-3 Postgres fallback when role not in Tier-1 or Tier-2         |
| AUTH-02-TC-04 | All sources empty raises PersonaNotFoundError                      |
| AUTH-02-TC-05 | Warm cache hit returns cached persona without re-reading sources   |
| AUTH-02-TC-06 | Cache expiry after 5 minutes re-reads sources                      |
| AUTH-02-TC-07 | Data-driven custom role mapping (executive role to cio)            |
| AUTH-02-TC-08 | Tier-1 unparseable JSON logs warning and treats as empty           |
| AUTH-02-TC-09 | Missing Tier-2 YAML file causes startup error                      |
| AUTH-02-TC-10 | Malformed Tier-2 YAML causes startup error                         |
| AUTH-02-TC-11 | Tier-3 query timeout raises PersonaResolutionError                 |
| AUTH-02-TC-12 | Warm cache hit latency <1ms p99                                    |
| AUTH-02-TC-13 | Cold Tier-3 hit latency <100ms p95                                 |
| AUTH-02-TC-14 | Concurrency: N=10 concurrent requests result in 1 Tier-3 query     |
| AUTH-02-TC-15 | persona_mapping_loaded event contains no PII (Tier-1 event, exact 7-key allowlist unchanged) |
| TC-16a (new) | `test_fresh_tier3_hit_carries_tier3_latency_ms_tc16` — fresh Tier-3 hit carries `tier3_latency_ms` and exactly one field beyond TC-15's allowlist |
| TC-16b (new) | `test_tier1_and_tier2_hits_omit_tier3_latency_ms_tc16` — Tier-1/Tier-2 hits omit `tier3_latency_ms` |
| TC-16c (new) | `test_warm_hit_reusing_tier3_omits_tier3_latency_ms_tc16` — a warm hit reusing `tier-3-postgres` omits `tier3_latency_ms` (only 1 Tier-3 query ran across 2 resolves) |

All 18 automatable executions passed on the first attempt — no retries triggered.

### Flaky tests

**AUTH-02 scope**: N/A — `test_warm_cache_hit_latency_baseline_p99_under_1ms` (AF-03) passed cleanly this round, both standalone and combined with the full suite, with stdout redirected to a file (non-tty). Consistent with the orchestrator's brief: the 1.1175ms breach is terminal-attached-stdout-only, which production never has (Dockerfile pipes uvicorn stdout to the log collector).

**Out-of-scope, pre-existing (not AUTH-02)**: `tests/perf/test_auth_jwks_perf.py::test_jwt_validation_latency_cold_then_warm_within_budget` (AUTH-01-owned) failed once in the full-suite run (`0.1238s` vs `<0.1s` budget), then passed cleanly on isolated re-run. Matches `FLAGS.md` AF-03's documented repeat-offender list (this exact test, 1/6 historical failure rate) — a pre-existing, repo-wide load-sensitivity pattern, not introduced or affected by this fix. Not part of AUTH-02's TC set or file plan; does not affect this verdict.

### Performance budget breaches

N/A — both AUTH-02 performance TCs met budget this round.

- **AUTH-02-TC-12** (warm cache hit): target `p99 < 1ms` @ 100 iterations; assertion passed (stdout to file, non-tty) — `budget_pass: true`.
- **AUTH-02-TC-13** (cold Tier-3 hit): target `p95 < 100ms` @ 10 iterations against live Postgres (`localhost:5443/dashboard_test`); assertion passed — `budget_pass: true`.

### Regression coverage

The round-1 finding was a **contract-conformance** FAIL (Phase 3b), not a failing TC — all 15 manifest TCs passed in round 1, so there is no pre-existing `AUTH-02-TC-NN` this fix formally regresses against per the `regression-<original-TC-id>` tag convention. The fix loop instead added 3 new pytest functions (informal `TC-16` a/b/c, per their own file comment header) directly targeting the missing-field behaviour; all 3 pass.

**Manifest gap (flagged, not verdict-blocking)**: `docs/test-cases/AUTH-02.json` was **not** updated with formal `AUTH-02-TC-16` entries (id/title/requirement_id/steps/etc.) — only the 3 pytest functions were added to `test_persona_resolver.py`. `coverage_audit.functional_requirements` for `AUTH-02-FR-5` still lists only `["AUTH-02-TC-15"]`; a future `/arh-trace` or coverage audit will not see the TC-16 cases. This is a process gap against the regression-tag protocol ("A fix-pass without a regression-tagged TC is rejected by Step 3"), not a functional defect — the underlying bug is genuinely fixed and independently verified in this round. Returned as a carry-forward item below (Gate Mode: I do not write `state.json`).

### Failed (bug blocks)

N/A — no TC, stack-smoke, or contract-conformance failures this round.

### Errored

N/A.

### Manual follow-up

N/A — all TCs are `automatable: true`; zero manual TCs in `docs/test-cases/AUTH-02.json`.

### Budget-unparseable NFRs

N/A — both performance TCs' budgets parsed and evaluated.

### Static checks (evidence for task-completion / contract confidence, not separate TCs)

- `mypy` (`services/api`): `Success: no issues found in 75 source files`.
- `ruff check` (`services/api`): `All checks passed!`.
- Full backend suite (`uv run pytest tests/`): `1 failed, 334 passed` on first pass — the 1 failure is the out-of-scope AUTH-01 perf flake above, re-run green in isolation. Matches the orchestrator's stated "334 passed" baseline.

### Carry-forward (returned to orchestrator — Gate Mode, not self-written)

```json
[
  {
    "item_id": "AUTH-02-TC-16-manifest-gap",
    "kind": "test_case",
    "reason": "Fix pass for round-1 tier3_latency_ms contract-conformance FAIL added 3 new passing pytest functions (test_fresh_tier3_hit_carries_tier3_latency_ms_tc16, test_tier1_and_tier2_hits_omit_tier3_latency_ms_tc16, test_warm_hit_reusing_tier3_omits_tier3_latency_ms_tc16) to services/api/tests/unit/test_persona_resolver.py, but did not add corresponding AUTH-02-TC-16 entries to docs/test-cases/AUTH-02.json per the regression-tag protocol. coverage_audit.functional_requirements[AUTH-02-FR-5].covered_by still lists only AUTH-02-TC-15. Functional fix is verified independently in this validation round; this is a manifest/traceability completeness gap, not a code defect.",
    "owner": "AUTH-02 implementation-agent (next touch of docs/test-cases/AUTH-02.json)",
    "added_at": "2026-08-28T10:35:00Z",
    "added_by": "validation-agent/round-2 (GATE MODE report-only)",
    "resolved_at": null,
    "evidence": "docs/features/AUTH-02/VALIDATION-20260828-1035.md § Regression coverage"
  }
]
```

### Known findings re-confirmed (`docs/features/AUTH-02/FLAGS.md`)

- **AF-01** (uv.lock wording) — unchanged, documentation-only, no verdict impact.
- **AF-02** (TEST_DATABASE_URL override) — confirmed again this round; `postgresql+psycopg://postgres:postgres@localhost:5443/dashboard_test` used for all live-DB TCs and fastapi-2 stack-smoke migration.
- **AF-03** (`tests/perf/` load sensitivity) — AUTH-02's own case did not reproduce this round (stdout-to-file); the AUTH-01-owned `test_jwt_validation_latency_cold_then_warm_within_budget` did reproduce once in the full-suite run and passed clean in isolation, consistent with AF-03's characterization. No action needed for AUTH-02.
- **AF-04** (`design_check` N/A) — confirmed, still no `apps/web` files in AUTH-02's file plan.

### Environment note

`fastapi-2`/`nextjs` stack smoke this round ran on non-default ports (`8021`/`3021`) — the machine's default ports `8000`/`3000` were already bound by unrelated `uvicorn`/`next-server` processes running out of `/Users/pratik.pawar/Desktop/dashboard/services/api` (the **main** checkout, not this `auth-02` worktree). Per `validation-execution`'s "never silently reuse whatever is already listening" rule, those processes were left untouched and the worktree's own build was smoke-tested on alternate ports instead — confirmed a clean boot (`app.state.persona_resolver` construction succeeded, `/health` → 200, `/openapi.json` lists all 4 `/auth/*` routes) and torn down after.

### Proof of run

```
### Proof of run

- session_id:        not exposed to agent context (see orchestrator Task-dispatch log for this invocation)
- bash_invocations:  32
- test_runner_runs:  6 (pytest: unit-only -v, perf-only, perf -v -s, unit+perf --durations=0, full tests/ -q, isolated re-run of the flaky AUTH-01 perf test) + mypy + ruff
- stack_smoke_runs:  2 (fastapi-2 start+healthcheck on :8021; nextjs start+healthcheck on :3021) + 1 connectivity-only check (postgres pg_isready via bed03-manual container on :5443)
- duration_seconds:  ~1500 (approximate, wall-clock across this invocation)
- timestamp:         2026-08-28T10:35:00Z
```
