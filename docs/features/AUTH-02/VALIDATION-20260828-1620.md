# Validation report — AUTH-02 (round 3 — doc-consistency re-check, no behaviour change)

- Date: 2026-08-28T16:20:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, invoked from `/arh-implement` Validate ∥ Review gate)
- Total: 18 automatable TC executions Passed: 18 Failed: 0 Errored: 0 Flaky: 0 Skipped manual: 0
- Duration: ~20 min (preflight + full suite + mypy/ruff + 3-stack smoke + perf isolation re-run)

Verdict justification: round 2 was already GREEN (validation PASS + review PASS WITH WARNINGS). This round exists only because 3 documentation-consistency fixes were closed post-gate: (1) round-2's own carry-forward `AUTH-02-TC-16-manifest-gap` — `docs/test-cases/AUTH-02.json` now carries formal `AUTH-02-TC-16a/b/c` entries with correct `implemented_by` pointers, manifest is 18 cases, `FR-5.covered_by` includes all three; (2) `docs/requirements/auth.md`'s `persona-resolver` contract now states the fresh-vs-warm `tier3_latency_ms` distinction the code implements; (3) `REQUIREMENTS.md` FR-5 now frames "nothing else" as a PII allowlist, naming `tier3_latency_ms` as the one permitted non-PII addition, cross-referencing D-11. The only source-code delta is a corrected docstring comment in `persona_resolver.py` (no behaviour change) — confirmed by full-suite pass counts matching round 2/preflight exactly and a clean app boot. No regression-tagged TC failed. Task completion 10/10 PASS. Stack smoke 3/3 PASS.

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   3   |   3  |   0  |   0   |   0   |
| Unit         |  16   |  16  |   0  |   0   |   0   |
| Performance  |   2   |   2  |   0  |   0   |   0   |

Stack smoke = 3 distinct runnable stacks per `docs/config/stack-smoke.md` (`fastapi-2`, `nextjs`, `postgres`; `fastapi`≡`fastapi-2`, `next`≡`nextjs` are the same dedup as prior rounds; `typescript`/`pytest`/`alembic`/`pydantic` are `(n/a — ...)` for both `Run:`/`Docker:`).

"Unit" row = 13 original `AUTH-02-TC-01..11,14,15` + 3 `TC-16` cases (`test_fresh_tier3_hit_carries_tier3_latency_ms_tc16`, `test_tier1_and_tier2_hits_omit_tier3_latency_ms_tc16`, `test_warm_hit_reusing_tier3_omits_tier3_latency_ms_tc16`) — all 16 re-run this round via `pytest tests/unit/test_persona_resolver.py -v`, 16/16 PASS.

**Contract conformance (Phase 3b)**: re-checked `docs/requirements/auth.md#persona-resolver` (the only contract `produced_by: AUTH-02`) field-by-field against the shipped `PersonaResolver`/`_log_resolution`: `mechanism`, `interface`, `input`, `output`, `cache` (300s TTL matches `_CACHE_TTL_SECONDS`), `errors`, and `observability` (base 4-field event + `tier3_latency_ms` on a *fresh* Tier-3 hit only, warm reuse omits it) all match the shipped code. **No findings.** This round's `observability` shape text is more precise than round 2's (now states the fresh-vs-warm distinction explicitly) but was not previously a mismatch — round 2 already verified the underlying behaviour, only the contract's wording was terser.

### Manifest coherence check (this round's specific charge)

Verified each `AUTH-02-TC-16x` entry's `implemented_by` resolves to a real, passing pytest function in `services/api/tests/unit/test_persona_resolver.py`:

| TC id | `implemented_by` function | Function exists | Assertion matches TC intent |
|-------|---------------------------|:---:|:---:|
| AUTH-02-TC-16a | `test_fresh_tier3_hit_carries_tier3_latency_ms_tc16` (line 630) | ✓ | ✓ — asserts `tier == "tier-3-postgres"`, `tier3_latency_ms` numeric ≥0, key set = TC-15's 7 keys + 1 |
| AUTH-02-TC-16b | `test_tier1_and_tier2_hits_omit_tier3_latency_ms_tc16` (line 663) | ✓ | ✓ — asserts neither Tier-1 nor Tier-2 event carries `tier3_latency_ms` |
| AUTH-02-TC-16c | `test_warm_hit_reusing_tier3_omits_tier3_latency_ms_tc16` (line 681) | ✓ | ✓ — asserts exactly 1 Tier-3 query across 2 resolves, cold event carries the field, warm event omits it |

`coverage_audit.functional_requirements[AUTH-02-FR-5].covered_by` = `["AUTH-02-TC-15", "AUTH-02-TC-16a", "AUTH-02-TC-16b", "AUTH-02-TC-16c"]` — resolves correctly, `coverage_audit.uncovered` is `[]`. Manifest gap from round 2's carry-forward is closed.

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/pyproject.toml` (add `pyyaml>=6.0` dependency pin) | ✓ | ✓ (`pyyaml>=6.0` present) | PASS |
| T-02 | `docs/config/project-commands.yaml` (add pyyaml smoke-import to preflight) | ✓ | ✓ (`import yaml` preflight line ran green) | PASS |
| T-03 | `services/api/app/core/config.py` (extend Settings with `persona_role_map`/`persona_config_file`) | ✓ | ✓ (`persona_role_map`, `persona_config_file`, `_parse_persona_role_map` validator present) | PASS |
| T-04 | `services/api/config/persona_role_map.yaml` (ship Tier-2 YAML stub) | ✓ | ✓ (file present) | PASS |
| T-05 | `services/api/app/core/persona_resolver.py` (implement PersonaResolver incl. `tier3_latency_ms` on Tier-3 hits) | ✓ | ✓ (docstring's PII-invariant paragraph now correctly names `tier3_latency_ms` as the one permitted addition, consistent with `_log_resolution`'s own docstring below it — the round-3 fix) | PASS |
| T-06 | `services/api/app/main.py` (wire PersonaResolver into `create_app()`) | ✓ | ✓ (`app.state.persona_resolver = PersonaResolver(cfg)` present; confirmed live via stack-smoke boot on :8010, `/health` → 200) | PASS |
| T-07 | `services/api/tests/unit/test_persona_resolver.py` (unit suite, TC-01..11,14,15 + TC-16 x3) | ✓ | ✓ (16/16 pass, all TC ids present) | PASS |
| T-08 | `services/api/tests/perf/test_persona_resolver_perf.py` (perf suite, TC-12,13) | ✓ | ✓ (2/2 pass in isolation and in full perf-suite re-run) | PASS |
| T-09 | `services/api/.env.example`, `README.md` (document `PERSONA_ROLE_MAP`) | ✓ | ✓ (both carry `PERSONA_ROLE_MAP`/`PERSONA_CONFIG_FILE` documentation) | PASS |
| T-10 | `services/api/README.md` (add "Persona resolution" ops-runbook section) | ✓ | ✓ (`## Persona resolution` heading present, "fresh"/`tier3_latency_ms` distinguisher documented) | PASS |

10/10 PASS. No task-completion drop this round.

### Passed

| TC id            | Title                                                              |
|------------------|---------------------------------------------------------------------|
| AUTH-02-TC-01 | Tier-1 env-JSON source resolves role to persona without consulting Tier-2/3 |
| AUTH-02-TC-02 | Tier-2 YAML fallback when role not in Tier-1                       |
| AUTH-02-TC-03 | Tier-3 Postgres fallback when role not in Tier-1 or Tier-2         |
| AUTH-02-TC-04 | All sources empty raises PersonaNotFoundError                      |
| AUTH-02-TC-05 | Warm cache hit returns cached persona without re-reading sources   |
| AUTH-02-TC-06 | Cache expiry after 5 minutes re-reads sources                      |
| AUTH-02-TC-07 | Data-driven custom role mapping (executive role to cio)            |
| AUTH-02-TC-08 | Tier-1 unparseable JSON logs warning and treats as empty           |
| AUTH-02-TC-09 | Missing Tier-2 YAML file causes startup error                      |
| AUTH-02-TC-10 | Malformed Tier-2 YAML causes startup error                         |
| AUTH-02-TC-11 | Tier-3 query timeout raises PersonaResolutionError                 |
| AUTH-02-TC-12 | Warm cache hit latency <1ms p99                                    |
| AUTH-02-TC-13 | Cold Tier-3 hit latency <100ms p95                                 |
| AUTH-02-TC-14 | Concurrency: N=10 concurrent requests result in 1 Tier-3 query     |
| AUTH-02-TC-15 | persona_mapping_loaded event contains no PII                       |
| AUTH-02-TC-16a | Fresh Tier-3 resolution emits tier3_latency_ms                    |
| AUTH-02-TC-16b | Tier-1 and Tier-2 resolutions omit tier3_latency_ms                |
| AUTH-02-TC-16c | Warm hit reusing tier-3-postgres omits tier3_latency_ms            |

All 18 automatable executions passed on the first attempt — no retries triggered.

### Flaky tests

**AUTH-02 scope**: `test_warm_cache_hit_latency_baseline_p99_under_1ms` (AF-03) FAILed once when run as part of the combined `tests/perf` batch (14 tests together: p99 = 1.4965ms vs 1.0ms budget), stdout still redirected to a file (non-tty) — a slightly different trigger condition than the terminal-attached-stdout case AF-03 previously documented. Per AF-03 protocol, re-ran in isolation: 3/3 clean passes (`0.34s`, `0.37s`, `0.26s`, p99 well under budget each time), and a full second run of the entire `tests/perf` directory came back 14/14 clean. Treated as batch-contention load-sensitivity (GC pause / scheduler jitter when 14 perf tests share one process), not a regression — consistent with "the only code delta this round is a docstring comment," which cannot affect measured latency. `budget_pass: true` stands for AUTH-02-TC-13's isolated and batch-clean runs.

**Out-of-scope**: none observed this round (no AUTH-01 perf flake reproduced in either full-suite run).

### Performance budget breaches

N/A — both AUTH-02 performance TCs met budget this round (isolated re-run after the one batch-contention flake above).

- **AUTH-02-TC-12** (warm cache hit): target `p99 < 1ms` @ 100 iterations; `budget_pass: true`.
- **AUTH-02-TC-13** (cold Tier-3 hit): target `p95 < 100ms` @ 10 iterations against live Postgres (`localhost:5443/dashboard_test`); `budget_pass: true`.

### Regression coverage

No `regression-<id>`-tagged TC exists for AUTH-02 (round 1's finding was a contract-conformance FAIL, not a failing TC, per round 2's report). N/A.

### Failed (bug blocks)

N/A — no TC, stack-smoke, or contract-conformance failures this round.

### Errored

N/A.

### Manual follow-up

N/A — all 18 TCs are `automatable: true`.

### Budget-unparseable NFRs

N/A — both performance TCs' budgets parsed and evaluated.

### Static checks (evidence for task-completion / contract confidence)

- `mypy` (`services/api`): `Success: no issues found in 75 source files`.
- `ruff check` (`services/api`): `All checks passed!`.
- Full backend suite (`uv run pytest --ignore=tests/perf -q`): `321 passed`.
- Perf suite (`uv run pytest tests/perf -q`): `1 failed, 13 passed` on first combined run (AUTH-02's `test_warm_cache_hit_latency_baseline_p99_under_1ms`, see Flaky tests above); isolated re-run 3/3 PASS; full `tests/perf` re-run `14 passed`. Combined total 321 + 14 = **335 passed**, matching the orchestrator's stated preflight baseline exactly.
- `services/api/app/core/persona_resolver.py`: confirmed via inspection (module docstring only) and `git status` (untracked file, no committed baseline to diff against in this worktree) that the only round-3 edit is the PII-invariant docstring paragraph — `_log_resolution`, `resolve()`, `_resolve_uncached` bodies unchanged from round 2's passing state, corroborated by identical pass counts and identical `implemented_by` line numbers.

### Carry-forward (returned to orchestrator — Gate Mode, not self-written)

Round 2's `AUTH-02-TC-16-manifest-gap` carry-forward is **resolved** this round — evidence: `docs/test-cases/AUTH-02.json` now has 18 test cases including formal `AUTH-02-TC-16a/b/c` entries with correct `implemented_by` pointers, and `FR-5.covered_by` lists all three. No new carry-forward items from this round.

```json
[]
```

Orchestrator should mark `AUTH-02-TC-16-manifest-gap` `resolved_at` using this report as `evidence`.

### Known findings re-confirmed (`docs/features/AUTH-02/FLAGS.md`)

- **AF-01** (uv.lock wording) — unchanged, documentation-only, no verdict impact.
- **AF-02** (TEST_DATABASE_URL override) — confirmed again; `postgresql+psycopg://postgres:postgres@localhost:5443/dashboard_test` used for all live-DB TCs and fastapi-2 stack-smoke.
- **AF-03** (`tests/perf/` load sensitivity) — reproduced once this round under a new trigger (batch of 14, stdout-to-file), not the previously-documented terminal-attached-stdout case. Isolated re-run clean; treated as environmental, not a regression. Recommend widening AF-03's characterization to include batch-contention, not just tty-attachment, next time this file is touched.
- **AF-04** (`design_check` N/A) — confirmed, no `apps/web` files in AUTH-02's file plan.

### Proof of run

```
### Proof of run

- session_id:        not exposed to agent context (see orchestrator Task-dispatch log for this invocation)
- bash_invocations:  24
- test_runner_runs:  8 (pytest: preflight smoke-imports x4, mypy, ruff, full suite minus perf -q, perf -q combined, perf test -k tc16 -v, isolated flaky-test re-run x3, full perf re-run) + unit-only -v run
- stack_smoke_runs:  2 (fastapi-2 start+healthcheck on :8010; nextjs start+healthcheck on :3011) + 1 connectivity-only check (postgres pg_isready via bed03-manual container on :5443)
- duration_seconds:  ~1200 (approximate, wall-clock across this invocation)
- timestamp:         2026-08-28T16:20:00Z
```
