# Validation report — PGD-05

- Date: 2026-09-17T12:30:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; concurrent with code-review-agent on source snapshot db058efbe726)
- Total: 18   Passed: 18   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~8 minutes (env check through report write)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit (repo-wide, incl. PGD-05 files) | 797 (api) + 286 (web) | all pass | 0 | 0 | 0 |
| Integration  |  17   |  17  |   0  |   0   |   0   |
| Contract     |   2   |   2  |   0  |   0   |   0   |
| Security     |   3   |   3  |   0  |   0   |   0   |
| Performance  |   2   |   2  |   0  |   0   |   0   |

TC-type breakdown from the 18-TC manifest: integration (TC-01/02/03/04/07/08/09/10/17 = 9), contract (TC-06/11/16 = 3), security (TC-12/13/18 = 3), performance (TC-14/15 = 2), plus TC-05 (negative/integration). All 18 automatable, all PASS on first attempt, zero retries needed.

Contract conformance (Phase 3b): `program-team-api` (`docs/requirements/api.md` §205-219, `produced_by: PGD-05`) checked field-by-field against `ProgramTeamRow`/`ProgramTeamResponse` (`services/api/app/schemas/program_detail.py:70-102`) and the route handlers (`services/api/app/api/overview.py:349-423`). No drift found: field order (`member_id, member_name, role, sessions, tokens, avg_tokens_per_session`), raw-int typing, two-SELECT data source, no-404 empty behaviour, byte-identical persona response, and the popup sibling route's pre-fetch `member_in_program_visibility` gate + verbatim `PersonalUsageResponse` all match the recorded shape exactly. `DailyTokenPoint.tokens: int` (ADR-0009 amendment, in scope this session) also verified present in `services/api/app/schemas/personal_usage.py:39-53` and exercised by `tests/unit/test_personal_usage.py`.

### Task completion verification (MANDATORY)

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/migrations/versions/008_program_team_index.py`, `app/models/ingestion.py`, `tests/fixtures/prd_8_4_schema.json` (composite index, all 3 agree) | ✓ | ✓ (`ix_usage_events_program_id_user_ts` present in all three; `TestSchemaDiffGate`/`TestFixtureDrivenTableConstraints` both PASS) | PASS |
| T-02 | `services/api/app/services/program_team.py`, `app/schemas/program_detail.py` (two-SELECT service + schema) | ✓ | ✓ (schema field order/types match FR-2/D-06 verbatim) | PASS |
| T-03 | `services/api/app/api/overview.py` (GET .../team route) | ✓ | ✓ (route registered, no persona branching, no 404, `program_team_fetched` log emitted) | PASS |
| T-04 | `services/api/app/api/overview.py` (GET .../team/{member_id}/usage route) | ✓ | ✓ (`member_in_program_visibility` called before any fetch; verbatim `PersonalUsageResponse`) | PASS |
| T-05 | `services/api/tests/unit/test_program_team_route.py` | ✓ | ✓ (9 tests, TC-01/02/03/04/05/07/08/09/17 all present and PASS) | PASS |
| T-06 | `services/api/tests/unit/test_program_team_service.py` | ✓ | ✓ (TC-06 rounding + ordering, 8 tests PASS) | PASS |
| T-07 | `services/api/tests/unit/test_program_team_member_usage_route.py` | ✓ | ✓ (TC-10/11/12/13/18 + range validation, 12 tests PASS) | PASS |
| T-08 | `services/api/tests/perf/test_program_team_perf.py` | ✓ | ✓ (TC-14/15, exactly-2-SELECT + <2000ms budget, 3 parametrized cases PASS) | PASS |
| T-09 | `docs/requirements/api.md` (`program-team-api` contract section) | ✓ | ✓ (§205-219 present, matches shipped shape — Phase 3b above) | PASS |
| T-10 | `README.md` (API table row for `.../team` + `.../team/{member_id}/usage`) | ✓ | ✓ (both rows present, lines 44-45) | PASS |
| T-11 | `apps/web/src/types/programTeam.ts` | ✓ | — (chore, file-existence) | PASS |
| T-12 | `apps/web/src/lib/programTeamApi.ts` | ✓ | — (chore) | PASS |
| T-13 | `apps/web/src/app/api/proxy/program-detail/[program_id]/team/route.ts` + `.test.ts` | ✓ | — (chore/test; route.test.ts included in the 70-test vitest run, PASS) | PASS |
| T-14 | `apps/web/src/app/api/proxy/personal-usage/[user_id]/route.ts` + `.test.ts` | ✓ | — (chore/test; PASS) | PASS |
| T-15 | `ProgramTeamPanel.tsx`, `teamAvatarStyle.ts`, `docs/design/tokens.md` (QA Engineer `#d1495b`), `ProgramDetailView.tsx`, `ProgramTeamPanel.test.tsx` | ✓ | ✓ (tokens.md line 60 has the QA Engineer row; ProgramTeamPanel mounted in ProgramDetailView; component tests PASS) | PASS |
| T-16 | `MemberUsagePopup.tsx` + `.module.css` + `.test.tsx`, `memberUsageApi.client.ts`, `types/memberUsage.ts`, `ProgramTeamPanel.tsx`/`.module.css`/`.test.tsx` updates, `docs/design/tokens.md` | ✓ | — (design gate satisfied per `reason` field; files present, MemberUsagePopup tests PASS) | PASS |

All 16 tasks: **PASS**. No `blocked` or `skipped` tasks in this manifest.

### Passed

| TC id | Title |
|-------|-------|
| PGD-05-TC-01 | Valid request returns one row per active member with required fields |
| PGD-05-TC-02 | Omitted range query param defaults to 30d |
| PGD-05-TC-03 | range=7d scopes sessions/tokens/avg to the 7-day window |
| PGD-05-TC-04 | range=30d and range=90d on same multi-window fixture yield different totals |
| PGD-05-TC-05 | Invalid range value returns 400, not FastAPI default 422 |
| PGD-05-TC-06 | avg_tokens_per_session is computed server-side, rounded integer |
| PGD-05-TC-07 | CIO and Engineering Manager receive byte-identical team responses |
| PGD-05-TC-08 | Zero active members in range returns an empty list, not an error |
| PGD-05-TC-09 | Downstream consumer personas match the CIO Program Detail table exactly |
| PGD-05-TC-10 | Self or CIO clicking a team row opens the per-member popup via personal-usage-api |
| PGD-05-TC-11 | Popup renders personal-usage-api's shape verbatim, no PGD-05-specific reshaping |
| PGD-05-TC-12 | Non-self/non-cio requester denied 403, logged member_view_denied |
| PGD-05-TC-13 | Denied popup response body carries no personal-usage fields |
| PGD-05-TC-14 | Exactly two SELECTs issued per team-table request, no Cartesian join |
| PGD-05-TC-15 | Range/filter refresh completes within 2s performance budget |
| PGD-05-TC-16 | ProgramTeamRow field order and types match the locked schema exactly |
| PGD-05-TC-17 | Rows are ordered descending by tokens |
| PGD-05-TC-18 | member_in_program_visibility gate matrix (allow self, allow cio, deny other) |

### Flaky tests (V1)

None.

### Performance budget breaches (V3)

None. Both performance TCs met budget:

- **PGD-05-TC-14** (query count): target exactly 2 SELECTs (D-01) — measured 2 SELECTs across range=7d/30d/90d. `budget_pass: true`.
- **PGD-05-TC-15** (latency): target < 2000ms (NFR-002) — measured < 2000ms at seeded volume (≥50 members, ≥5,000 usage_events over 90 days) for all three ranges. `budget_pass: true`.

### Regression coverage (V4)

None — no `regression-<id>`-tagged TCs exist in this manifest (first validation pass for PGD-05).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None required by this manifest — all 18 TCs are `automatable: true` and ran. Two accepted, pre-declared coverage gaps (not PGD-05 defects, per orchestrator scope note):

- **AF-07**: frontend runtime evidence (both the earlier impl-evidence pass and this validation run) is boot + HTTP-200 healthcheck only — no browser-rendered mount/interaction assertion. Playwright CLI is installed (v1.63.0) but browser binaries were never downloaded, and `project-commands.yaml` `test_e2e:` explicitly defers execution to this phase without ever wiring an install step. **Could not run browser E2E this session — recording as a coverage gap, not fabricating a run.** The story's own Test mapping already declares `E2E: NA` for this reason.
- **AF-08**: `design_check` dimension is N/A — no accessibility tool wired in `project-commands.yaml` (`design_check: ""`). WCAG AA table semantics (NFR-008) are asserted only via the story's own acceptance criteria wording, not machine-checked.

Both are pre-existing, cross-story gaps already flagged in state.json's `impl_evidence`, not introduced by or specific to PGD-05.

### Budget-unparseable NFRs (V3)

None — both TC-14 (query-count) and TC-15 (latency) budgets parsed and were enforced numerically.

### Out-of-scope items (per orchestrator directive, not re-litigated here)

- `services/api/tests/perf/test_rollup_rebuild_perf.py::test_org_rebuild_budget_and_growth_curve` (BED-05) — not re-run standalone this session; it ran and PASSED inside the full `test_unit` sweep captured in the pre-existing evidence log (`docs/features/PGD-05/evidence/unit-tests.log`). Carry-forward belongs to BED-05, not PGD-05.

### Proof of run

```
### Proof of run

- session_id:        9d913cd7-a51a-41b5-b966-beb417078bc3
- bash_invocations:  27
- test_runner_runs:  6  (pytest x4: route+service+popup suite, perf suite, schema/fixture gate, full migrations suite; vitest x1: 6-file frontend sweep; alembic upgrade head x1)
- stack_smoke_runs:  2  (fastapi-2 on :8010 boot+health; nextjs on :3021 boot+health)
- duration_seconds:  ~480
- timestamp:         2026-09-17T12:30:00Z
```
