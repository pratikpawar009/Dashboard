# Validation report — AUTH-04

- Date: 2026-08-31T12:32:49Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; invoked from `/arh-implement` Step 2 Validate ∥ Review gate)
- Total: 18   Passed: 18   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~900s (approximate — no dedicated wall-clock timer instrumented; spans initial context load through report write)

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   9   |  3*  |   0  |   0   |   0   |
| Unit         |   7   |   7  |   0  |   0   |   0   |
| Security     |   7   |   7  |   0  |   0   |   0   |
| Contract     |   3   |   3  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |

`*` Stack smoke: 3 PASS (`fastapi-2`, `fastapi`, `postgres` — the stacks this backend-only story actually exercises) + 6 SKIPPED (`typescript`, `pytest`, `alembic`, `pydantic` — both `Run:`/`Docker:` are `(n/a — ...)` in `docs/config/stack-smoke.md`, PASS-equivalent per protocol; `next`, `nextjs` — real Run/Docker commands exist but were not booted, see note below). Not all-SKIPPED (3 genuine PASS), so `stack-smoke-not-configured` does not apply.

**Stack smoke detail:**
- `fastapi-2` / `fastapi` (same backend process, generic + concrete tags): **PASS**. A live `uvicorn app.main:app` server was pre-provisioned by the orchestrator at `http://127.0.0.1:8123`; `GET /health` → `200 {"status":"ok"}` confirmed at session start. Schema confirmed migrated (`program_summary` columns match `BED-01` migration exactly). Full backend suite (`uv run pytest`, `services/api`) — **395 passed**, 0 failed, run against `TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5442/dashboard_test`. `ruff check` and `mypy` clean on every AUTH-04-touched file.
- `postgres`: **PASS**. Container `bed01-manual` (host port 5442, db `dashboard`) reachable via `docker exec … psql`; `dashboard_test` sibling DB present and used for the pytest run. (`docs/config/stack-smoke.md`'s literal `Check:` command, `pg_isready -h 127.0.0.1 -p 5432`, targets the generic local-compose default port, not this gate's provisioned container on 5442 — verified by direct `psql` connection instead, functionally equivalent.)
- `next` / `nextjs`: **not booted** — AUTH-04's `file_plan` (`tasks.json`) touches zero `apps/web/**` files; `REQUIREMENTS.md` § Visual spec is explicitly `N/A — design = n/a, backend-only feature`; no TC in this manifest exercises the frontend. Booting an unrelated dev server would add no validation signal for this story. Not counted as a FAIL.
- `typescript` / `pytest` / `alembic` / `pydantic`: **SKIPPED** — both `Run:`/`Docker:` bullets are `(n/a — ...)` in `docs/config/stack-smoke.md`.

**Preflight note (Phase 1, non-blocking):** Backend preflight (`uv sync`, `import app.main`, `authlib`/`respx`/`yaml` smoke-imports) — all **PASS**. Frontend preflight (`pnpm -C apps/web install`) exits non-zero on a pre-existing `ERR_PNPM_IGNORED_BUILDS` gate (`unrs-resolver` build script not yet approved). This is a documented, pre-existing repo condition unrelated to AUTH-04 (zero `apps/web` files in this story's `file_plan`; the orchestrator's own `impl_evidence.checks.typecheck` note in `docs/features/AUTH-04/state.json` already flags the same gate as AF-01, worked around via a direct binary call). Reported here for transparency; does not drop the verdict for a backend-only story where the actually-relevant (backend) preflight is fully clean. Recommend `pnpm approve-builds` be run once, outside this validation run, to clear it project-wide.

### Contract conformance (Phase 3b)

`docs/requirements/api.md` § `programs-api` (`produced_by: AUTH-04`) checked against the live endpoint:

| Field | Recorded (api.md) | Shipped (`GET /openapi.json` + live response) | Match |
|---|---|---|---|
| endpoint | `GET /api/programs` | `GET /api/programs` | ✓ |
| fields | `{program_id, label, href, dotStyle}` | `{program_id, label, href, dotStyle}` (exact key set, live + OpenAPI schema) | ✓ |
| excluded | `type, description` | absent from `ProgramEntry` schema and every live response | ✓ |
| scoping | cio sees all; else scoped to session-derived program list | confirmed live (TC-01/02/05/18) | ✓ |
| client_derived | `current, rowStyle` | absent — not this endpoint's concern | ✓ |

No conformance findings. ADR-0005 (`docs/adr/0005-programs-api-switcher-shape.md`) is satisfied exactly — `dotStyle` field name (not `dot_style`), no `type`/`description`, `href` server-derived. Story `docs/stories/AUTH-04.md` AC-5's stale field set (superseded per ADR-0005 and the PRD's explicit carry-forward note) was correctly NOT implemented — confirmed absent live (TC-06).

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/core/persona_resolver.py` (`get_persona_resolver` dependency) | ✓ | ✓ (function present, line 229) | PASS |
| T-02 | `services/api/app/utils/format.py` (`dot_style_for_program`) | ✓ | ✓ (function present, line 132, deterministic sha256-based palette) | PASS |
| T-03 | `services/api/app/schemas/programs.py` (ProgramEntry/ProgramsListResponse) | ✓ | ✓ (`dotStyle` alias, exact ADR-0005 field set) | PASS |
| T-04 | `services/api/app/api/programs.py` (GET /api/programs route) | ✓ | ✓ (veto-gate, fail-closed persona resolution, WHERE-clause scoping, FR-4 WARN, all present and behaviorally confirmed) | PASS |
| T-05 | `services/api/app/main.py` (router registration) | ✓ | ✓ (`app.include_router(programs_router)`, line 82) | PASS |
| T-06 | `services/api/tests/unit/test_programs.py` (TC-01/02/05) | ✓ | ✓ (all 3 tests present and PASS) | PASS |
| T-07 | `services/api/tests/unit/test_programs.py` (TC-03/04/09/18) | ✓ | ✓ (all 4 tests present and PASS) | PASS |
| T-08 | `services/api/tests/unit/test_programs.py` (TC-06/07/08/11) | ✓ | ✓ (all 4 tests present and PASS) | PASS |
| T-09 | `services/api/tests/unit/test_programs.py` (TC-10) | ✓ | ✓ (key-set-equality log assertion present and PASS) | PASS |
| T-10 | `services/api/tests/unit/test_programs.py` (TC-12/13/14) | ✓ | ✓ (all 3 tests present and PASS) | PASS |
| T-11 | `services/api/tests/unit/test_programs.py` (TC-15/16) | ✓ | ✓ (both tests present and PASS) | PASS |
| T-12 | `services/api/tests/perf/test_programs_perf.py` (TC-17) | ✓ | ✓ (p95 assertion present, PASS, p95=6.78ms) | PASS |
| T-13 | `README.md` (root API table, `GET /api/programs` row) | ✓ | ✓ (`grep "api/programs" README.md` → row present at line 36, matches ADR-0005 shape + veto-gate note) | PASS |

13/13 tasks genuinely complete — no task claims `done` without matching, verified on-disk evidence.

### Passed

| TC id | Title | Evidence |
|---|---|---|
| AUTH-04-TC-01 | cio persona receives every program_summary row regardless of session.groups | pytest PASS + live: `GET /api/programs` as cio, groups=[] → 200, all 5 seeded programs returned |
| AUTH-04-TC-02 | non-cio persona sees only programs whose slug is in session.programs | pytest PASS + live: developer scoped to [prog-2,prog-4] → 200, exactly those 2 |
| AUTH-04-TC-03 | missing bearer token rejected 401 before scoping logic runs | pytest PASS (spy-verified) + live: no Authorization header → 401 `invalid_token` |
| AUTH-04-TC-04 | malformed/invalid bearer token rejected 401 before scoping logic runs | pytest PASS (spy-verified) + live: garbage token → 401 `invalid_token` |
| AUTH-04-TC-05 | non-cio, zero matching programs → 200 empty list, not an error | pytest PASS + live: programs=[] → 200 `{"programs":[]}` |
| AUTH-04-TC-06 | program entry key set == exactly {program_id, label, href, dotStyle} | pytest PASS + live + OpenAPI schema: no `type`/`description`/`name`/`icon` present |
| AUTH-04-TC-07 | href routes to program-detail-api's path for the entry's program_id | pytest PASS + live: `href == "/api/overview/program-detail/prog-1"` |
| AUTH-04-TC-08 | dotStyle arrives pre-formatted CSS | pytest PASS + live: `"background-color: #0f1a2e;"` — bindable, non-empty |
| AUTH-04-TC-09 | no per-program 403 — program_visibility is open-aggregate, not per-row | pytest PASS + live: both cio and non-cio scenarios single 200, no per-item errors |
| AUTH-04-TC-10 | programs_list_returned payload key set == exactly {user_id, persona, returned_count, timestamp} | pytest PASS + live log inspection (`/private/tmp/gate-api.log`): JSON payload keys = `{timestamp, level, logger, message, user_id, persona, returned_count}` exactly, no email/groups/path |
| AUTH-04-TC-11 | program_visibility called at most once, never once per program | pytest PASS (spy count == 1 for 5-program scope) |
| AUTH-04-TC-12 | PersonaNotFoundError fails closed 403 + WARN, never 500 | pytest PASS + live: unmapped role → 403 `Access denied`, live log shows exactly one WARNING `programs_persona_resolution_failed` |
| AUTH-04-TC-13 | PersonaResolutionError fails closed 403 + WARN, never 500 | pytest PASS (mocked) |
| AUTH-04-TC-14 | mocked persona_resolver timeout fails closed 403, no hang | pytest PASS (mocked, wall-clock bounded) |
| AUTH-04-TC-15 | session program_id absent from program_summary silently filtered | pytest PASS + live: programs=[prog-1,prog-2,prog-99] → 200, exactly {prog-1,prog-2}, prog-99 silently absent |
| AUTH-04-TC-16 | discrepancy WARN, distinct from programs_list_returned, on returned_count < len(programs) | pytest PASS + live log: `programs_missing_from_summary` WARNING (expected_count=3, returned_count=2) alongside a separate `programs_list_returned` INFO |
| AUTH-04-TC-17 | p95 < 300ms @ 100 seeded / 50 scoped | pytest PASS: p95=6.78ms (min=3.64ms, max=31.25ms) across 50 calls — well under budget |
| AUTH-04-TC-18 | client-supplied program filter query param never widens scope | pytest PASS + live: `?programs=prog-1,...,prog-5` with session scoped to [prog-1] → 200, exactly {prog-1} |

### Flaky tests

None. Every TC passed on its first attempt (no retries triggered).

### Performance budget breaches

None. AUTH-04-TC-17: target `p95 < 300ms @ 100 seeded / 50 scoped`, measured `p95 = 6.78ms` — `budget_pass: true`.

### Regression coverage

N/A — no `regression-<id>`-tagged TCs exist in this manifest (first validation pass for AUTH-04; no prior fix loop).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None. All 18 TCs are `automatable: true`; no `.pending_carry_forward` manual-TC entries generated.

### Carry-forward (returned to orchestrator — GATE MODE, not self-written)

1. **`item_id`**: `AUTH-04-preflight-pnpm-gate` — **kind**: `environment` — **reason**: `apps/web` preflight (`pnpm -C apps/web install`) fails on a pre-existing `ERR_PNPM_IGNORED_BUILDS` gate (`unrs-resolver` build script unapproved). Unrelated to AUTH-04 (zero `apps/web` files touched); already flagged once as `AF-01` in this feature's own `impl_evidence`. Recommend a one-time `pnpm approve-builds` (or an explicit allow-list) at the repo level so future preflight runs across all stories are clean. **owner**: platform/tooling — **added_by**: `validation-agent (gate mode)`.

No TC-level or task-level carry-forward entries — all 18 TCs and all 13 tasks are clean PASS.

### Budget-unparseable NFRs

None. TC-17's NFR budget string (`p95 < 300ms @ 100-program/50-scoped baseline`) parsed and enforced successfully.

### Proof of run

```
### Proof of run

- session_id:        07813870-65e2-4e40-b33e-277ef4c30351
- bash_invocations:  52
- test_runner_runs:  4  (tests/unit/test_programs.py; tests/perf/test_programs_perf.py; full services/api suite [395 passed]; --durations timing re-run)
- stack_smoke_runs:  2  (backend /health confirmation against the pre-provisioned live server; postgres connectivity confirmation via docker exec psql)
- duration_seconds:  ~900 (approximate)
- timestamp:         2026-08-31T12:32:49Z
```
