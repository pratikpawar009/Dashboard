# Validation report — BED-05

- Date: 2026-09-09T11:35:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; invoked by `/arh-implement` Validate ∥ Review gate against source snapshot `f4faa6ab`)
- Total: 2 (manifest TCs)   Passed: 2   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Total (BED-05-scoped task-level tests, `tasks.json` T-01..T-13): 26 passed / 26 run, 0 failed
- Full repo regression: 507 passed / 507 run, 0 failed (`uv run pytest -q`, 151.99s)
- Duration: ~1400s wall-clock (this validation session)

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit         |  19   |  19  |   0  |   0   |   0   |
| Integration  |   1   |   1  |   0  |   0   |   0   |
| Performance  |   3   |   3  |   0  |   0   |   0   |

Unit (19) = `test_rollup_rebuild_transaction.py` (3) + `test_rollup_rebuild_contract.py` (10) + `test_rollup_rebuild_query_plan.py` (2) + `test_migrations.py::TestRollupQueryIndexRevision` (1) + `test_rollup_rebuild_golden_snapshot.py` (3, parametrized 20k/40k/160k — AC-6, highest-risk). Integration (1) = manifest `BED-05-TC-01`. Performance (3) = manifest `BED-05-TC-02`'s file, 3 parametrized budget/growth-curve tests.

Contract conformance (Phase 3b): **N/A — no contract produced by BED-05.** Grepped `docs/requirements/*.md` for `produced_by: BED-05` — zero matches. `rollup-rebuild`'s `produced_by` stays `BED-03` by explicit RTM decision (2026-09-09): BED-05 rewrites the implementation behind the contract, is recorded as `consumed_by`, and adds no new interface. Confirmed no drift is possible to check against.

Stack smoke: both stacks PASS. `fastapi-2`: `Deps` (Postgres 16, disposable DB `dashboard_val_bed05` on the pre-existing container `dashboard-dev5442`, port 5442) → `Migrate` (`alembic upgrade head`, reached `004_rollup_query_indexes`, 1.08s) → `Run` (uvicorn :8000) → `Check` (`GET /health` → `200 {"status":"ok"}`). `nextjs`: `Run` (pnpm dev :3000) → `Check` (`GET /` → `307` redirect, expected — root is not a health endpoint for this stack). `/overview` returns `404` as documented: pre-existing, zero OVW-01..04 routes exist, BED-05 touched zero frontend files — **not a BED-05 regression**, confirmed by `git status` showing no `apps/web/**` changes on this branch.

## Task completion verification

| Task | Target file(s) | Existence | Section match | Verdict |
|------|-----------------|-----------|----------------|---------|
| T-01 | `services/api/scripts/generate_rollup_golden_snapshots.py`; `tests/fixtures/rollup_golden_{20k,40k,160k}.json` | ✓ (all 4 non-empty; JSON fixtures valid, contain real row data: e.g. `user_sessions` 20000/40000/160000 rows) | — | PASS |
| T-02 | `services/api/tests/conftest.py` | ✓ (857 lines; `four_program_concurrent_sessions` fixture confirmed present) | — | PASS |
| T-03 | `services/api/app/services/rollup_rebuild.py` | ✓ (671 lines; all 9 `_build_*` confirmed SQL `.group_by()`/`func.*`; `func.min(UsageEvent.user)` confirmed for `_build_user_sessions`; `ON CONFLICT` confirmed for org-scope writes; `contention_wait_ms` confirmed in `rollup_rebuild_completed` `extra={}`; zero remaining `select(UsageEvent)` bare full-row query) | — | PASS |
| T-04 | `services/api/migrations/versions/004_rollup_query_indexes.py` | ✓ (88 lines; additive, `revises=003_program_roster`, `downgrade()` drops exactly `ix_usage_events_program_id_covering`) | — | PASS |
| T-05 | `services/api/tests/test_migrations.py` (`TestRollupQueryIndexRevision`) | ✓ | test targets the real shipped index name (`ix_usage_events_program_id_covering`, not D-04's superseded `ix_usage_events_ts`) — confirmed by reading assertions | PASS |
| T-06 | (measurement-only, no `files[]`) | N/A — no target file by design | Consistency check: T-07's pinned constants (`_STATEMENT_TIMEOUT_MS=5000`, budgets in `test_rollup_rebuild_perf.py`) match T-06's reported measurement exactly | PASS |
| T-07 | `app/core/db.py`; `app/models/ingestion.py`; `tests/fixtures/prd_8_4_schema.json` | ✓ all 3 — `db.py` has `_STATEMENT_TIMEOUT_MS=5_000`/`_CONNECT_TIMEOUT_S=10` (independently re-derived below); `ingestion.py` diff adds `Index("ix_usage_events_program_id_covering", ...)`; fixture diff adds `"index(program_id)"` entry (confirmed via `git diff`, not pre-existing) | — | PASS |
| T-08 | `services/api/tests/perf/test_rollup_rebuild_perf.py` | ✓ (281 lines; 3/3 tests pass; `EVENT_COUNT`/`BUDGET_SECONDS` constants absent, confirmed by grep) | — | PASS |
| T-09 | `services/api/tests/unit/test_rollup_rebuild_golden_snapshot.py` | ✓ (296 lines; 3/3 parametrized pass; scrutinized non-vacuous — keyed row-by-row/column-by-column diff, real 20k/40k/160k seed sizes, correct `EXCLUDED_COLUMNS` set) | — | PASS |
| T-10 | `services/api/tests/unit/test_rollup_rebuild_concurrency.py` | ✓ (367 lines; 1/1 pass; anti-vacuous-pass overlap guard confirmed present and asserted) | — | PASS |
| T-11 | `services/api/tests/unit/test_rollup_rebuild_transaction.py` | ✓ (3/3 pass; git diff shows a 7-line docstring-only addition, no logic change — consistent with the task's own "no modification was warranted" claim, not a discrepancy) | — | PASS |
| T-12 | `services/api/tests/unit/test_rollup_rebuild_contract.py` | ✓ (10/10 pass, incl. `test_ac3_call_site_is_byte_identical_to_pre_bed_05_commit` for both frozen paths and `test_decisions_md_records_the_d05_caller_ordering_write_up`) | `DECISIONS.md` D-05 caller-ordering write-up confirmed present (read directly) | PASS |
| T-13 | `services/api/tests/unit/test_rollup_rebuild_query_plan.py` | ✓ (489 lines; 2/2 pass; asserts no full-row materialisation + bounded query count, superseding BED-03's invariant) | — | PASS |

13/13 tasks `done`. Zero `blocked`, zero `skipped` — no exemption rows needed.

## Passed

| TC id            | Title                                  |
|------------------|----------------------------------------|
| BED-05-TC-01 | Four concurrent ingest-triggered rebuilds on distinct programs succeed with zero IntegrityError and match serial output |
| BED-05-TC-02 | Rebuild duration holds flat against table size across ≥2 populated usage_events sizes (growth-curve regression guard) |

## Flaky tests

None — every TC and every task-level test passed on its first attempt; no retries triggered.

## Performance budget breaches

None. `BED-05-TC-02`'s file (`tests/perf/test_rollup_rebuild_perf.py`) ran all 3 parametrized tests (org budget+growth-curve, program budget by in-program size, program-vs-total-history independence) and all passed within their pinned constants in this environment: org p95 ≤0.5s@20k / ≤1.5s@160k (ratio ceiling 8.0x); program p95 ≤1.5s@5k-in-program / ≤7.0s@40k-in-program; total-independence ratio ≤1.5x. Full run: 64.99s wall.

## Regression coverage

N/A — no TC in this manifest carries a `regression-<id>` tag.

## Failed (bug blocks)

None.

## Errored

None.

## Manual follow-up

None — both manifest TCs are `automatable: true`; no `.pending_carry_forward` entries generated for manual TCs.

## Budget-unparseable NFRs

None — `BED-05-NFR-performance`'s budget resolved to concrete, parseable per-size constants (T-06 measured → T-07 pinned).

## Per-AC verification (9/9 story ACs)

| AC | Claim | Verification | Result |
|----|-------|--------------|--------|
| AC-1 | 4 concurrent rebuilds on distinct programs, zero `IntegrityError`, output = serial rebuild | `BED-05-TC-01` ran (0.90s): 0 exceptions, `contention_wait_ms` non-negative ints on all 4 org-scope calls, org rows match serial baseline, genuine wall-clock overlap proven (anti-vacuous guard) | **PASS** |
| AC-2 | Scope-wide rollback on injected failure | `test_rollup_rebuild_transaction.py` 3/3 pass; monkeypatch fires after all 7 program DELETEs, full rollback asserted | **PASS** |
| AC-3 | No call-site edit; caller ordering documented | `test_rollup_rebuild_contract.py`'s byte-identical git-blob diff test passes for both `app/api/ingest.py` and `app/services/manifest_ingest.py`; independently corroborated — neither file appears in `git status` as modified on this branch. `DECISIONS.md` D-05 write-up present (read directly) | **PASS** |
| AC-4 | `rebuild_org_rollups` at 160k rows: SQL `GROUP BY`, no bare `select(UsageEvent)`, budget met | Grepped `rollup_rebuild.py` — zero `select(UsageEvent)` matches remain; `test_org_rebuild_budget_and_growth_curve` passed | **PASS** |
| AC-5 | `rebuild_program_rollups` budget met, not tracking total history | `test_program_rebuild_budget_by_in_program_size` + `test_program_rebuild_does_not_track_total_history` both pass (ratio ≤1.5x for 8x total growth at fixed in-program size) | **PASS** |
| AC-6 | Output identical to golden snapshot at 20k/40k/160k, excl. id/clock cols | 3/3 parametrized pass (42.84s). **Scrutinized hardest per instruction**: keyed (not positional) row/column diff, streamed via server-side cursor, real seeded sizes (20000/40000/160000 `user_sessions` rows confirmed in fixtures), correct `EXCLUDED_COLUMNS = {id, as_of_timestamp, created_at, updated_at}` applied as a set difference, loud `assert not all_diffs` with diff detail on failure — not vacuous | **PASS** |
| AC-7 | Explicit statement/connect timeout on shared engine | Read `db.py`: `_STATEMENT_TIMEOUT_MS=5_000`, `_CONNECT_TIMEOUT_S=10`. **Independently re-derived** (not trusted): connected via the real `engine`, `SHOW statement_timeout` → `5s`; `SELECT pg_sleep(8)` raised `psycopg.errors.QueryCanceled` as expected | **PASS** |
| AC-8 | Perf test replaces empty-table constants with table-size-indexed budget + growth-curve assertion | `EVENT_COUNT=5000`/`BUDGET_SECONDS=2.0` confirmed absent (grep); 3 named per-size budget constants + 2 growth-ratio ceilings present and enforced; 3/3 tests pass | **PASS** |
| AC-9 | Additive, index-only revision revising `003_program_roster`; downgrade drops exactly what it added | Read `004_rollup_query_indexes.py` in full: `down_revision="003_program_roster"`, `upgrade()` adds exactly one index, `downgrade()` drops exactly that index, no column/constraint/data change. `TestRollupQueryIndexRevision` + `TestSchemaDiffGate` + `TestFixtureDrivenTableConstraints[usage_events]` all pass; model (`ingestion.py`) and fixture (`prd_8_4_schema.json`) diffs both confirmed genuinely new (via `git diff`), not pre-existing | **PASS** |

**9/9 ACs PASS.**

## Coverage-gap note (accepted, not a failure)

Per instruction: `AC-2, AC-3, AC-6, AC-7, AC-9, FR-1, FR-2, FR-3, NFR-security, NFR-accessibility` are `uncovered` in the manifest's `coverage_audit` by explicit, approved Product Gate decision (2-TC cap). All 8 of the non-N/A ids are independently exercised by task-level tests (table above) — none is silently unverified. `NFR-accessibility` stays legitimately N/A (backend-only, no UI surface, `design: n/a`).

## Findings vs. `tasks.json` completion claims

No contradictions found. Every measured/asserted claim in `tasks.json` `reason` fields reproduced under independent re-run in this environment:

- T-07's `_STATEMENT_TIMEOUT_MS=5000`/`_CONNECT_TIMEOUT_S=10` — confirmed in source and by live `SHOW`/`pg_sleep(8)` probe.
- T-04/D-06's finding that `ix_usage_events_ts` was superseded by `ix_usage_events_program_id_covering` — confirmed shipped index matches D-06, not the original D-04 name.
- T-11's "no modification was warranted" — confirmed: `git diff` shows only a 7-line explanatory docstring addition, zero logic change.
- T-12's byte-identical AC-3 diff and D-05 write-up — both independently reproduced.
- Full-suite count moved from T-07's preflight snapshot of "505 passed" to 507 passed in this run — **not a contradiction**: T-07's preflight ran before T-08/T-09/T-10 added their new test files to the suite; a net increase is expected as later tasks land new tests, and 0 failures either way.

One pre-existing, explicitly out-of-scope item independently reconfirmed, not a BED-05 defect: web root `/` → 307 → `/overview` → 404 (no OVW-01..04 routes implemented; zero `apps/web/**` files touched by this feature).

## Proof of run

```
### Proof of run

- session_id:        0c26fdcc-d9a3-4106-b486-3b51ee89af95
- bash_invocations:  ~52
- test_runner_runs:  7 (pytest: concurrency, perf, transaction+contract+query_plan+migrations, golden-snapshot, full-suite, SchemaDiffGate, TestFixtureDrivenTableConstraints)
- stack_smoke_runs:  2 (fastapi-2: migrate+start+healthcheck; nextjs: start+check)
- duration_seconds:  ~1400
- timestamp:         2026-09-09T11:35:00Z
```

## Environment notes

- Live Postgres 16 confirmed on host port **5442** (container `dashboard-dev5442`), not 5432 — `.env.example`/`docker-compose.yml`'s documented default is stale for this environment, per prior validation-agent memory.
- Disposable validation database `dashboard_val_bed05` created, migrated to head (`004_rollup_query_indexes`), used for all test runs, and **dropped** at the end of this session. The real `dashboard` DB (116 rows) was never connected to.
- Ports 8000/3000 were free at the start and end of this session; both stacks started, checked, and were stopped cleanly (no leftover processes).
- Tool paths used throughout: `/Users/pratik.pawar/.local/bin/uv`, `/usr/local/bin/pnpm` (neither on the non-interactive `PATH`).
