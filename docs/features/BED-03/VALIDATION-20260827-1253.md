# Validation report — BED-03

- Date: 2026-08-27T12:53:17Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; invoked by `/arh-implement` Step 2 Validate ∥ Review gate, round 1)
- Total: 17   Passed: 17   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~480s (approximate — wall-clock start not separately timestamped; session ran Phases 1–5 continuously)

Source snapshot validated: hash `ca5acbc6e148ffa6` (7 modified + 8 new files, all backend-only — `services/api/**`, `docs/requirements/data.md`).

Test environment: dedicated container `bed03-manual` on port **5443** (`TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5443/dashboard_test`), per orchestrator instruction — default port 5432 is occupied by an unrelated native Postgres instance rejecting these credentials; this is a local-environment fact, not a feature defect.

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   9   |   1  |   0  |   0   |   0   |
| Integration  |  14   |  14  |   0  |   0   |   0   |
| Contract     |   1   |   1  |   0  |   0   |   0   |
| Performance  |   1   |   1  |   0  |   0   |   0   |
| Security     |   1   |   1  |   0  |   0   |   0   |

Stack smoke: 9 sections total in `docs/config/stack-smoke.md` — `fastapi-2` (backend) ran and PASSED (migrate + boot + `/health` 200); `nextjs` SKIPPED (zero `apps/web` changes in this snapshot — BED-03 is backend-only, nothing to smoke on that stack for this story); `fastapi`, `typescript`, `next`, `postgres`, `pytest`, `alembic`, `pydantic` SKIPPED as duplicate/no-op sections (`Run:`/`Docker:` both `(n/a — ...)` or explicitly deferring to `fastapi-2`). Not all-SKIPPED (1 real PASS), so no `stack-smoke-not-configured` PARTIAL trigger.

No `unit`-typed TCs exist in this manifest by design (`test-case-generation` skill § Scope — unit tests are enumerated in `tasks.json`, not here). As a regression backstop, the full backend suite (`cd services/api && uv run pytest tests/`, includes BED-01/BED-02's pre-existing tests) was also run: **144 passed, 0 failed** — no regression introduced by this story's changes.

### Phase 1 — Environment check

- Preflight (`docs/config/project-commands.yaml`): `pnpm -C apps/web install` (0 exit), `cd services/api && uv sync` (0 exit, lockfile up to date), `uv run python -c "import app.main"` (0 exit, no output). All 3 commands passed.
- Env vars: `TEST_DATABASE_URL` set per orchestrator instruction; no other required var missing.

### Phase 2b — Stack smoke (fastapi-2)

1. Deps: dedicated container `bed03-manual` (postgres:16, port 5443) already running — substituted for the generic `Deps:` bullet's `docker run ... -p 5432:5432` per orchestrator's mandatory port override; not treated as a defect.
2. Migrate: `uv run alembic upgrade head` against port 5443 → `Running upgrade -> 001_initial_schema` (0 exit).
3. Start: `uv run uvicorn app.main:app --port 8000` (background, `DATABASE_URL` pointed at the same test DB).
4. Wait-for-boot / Check: `GET http://127.0.0.1:8000/health` → `HTTP 200` within 3s.
5. Stop: process killed cleanly, port 8000 confirmed free afterward.

Verdict: PASS, sub_step: none, log_tail: null.

### Phase 3b — Contract conformance

One contract produced by BED-03 in `docs/requirements/*.md`: `docs/requirements/data.md#rollup-rebuild` (`produced_by: BED-03`). Compared its recorded `shape.contract` block against the shipped `services/api/app/services/rollup_rebuild.py`:

| Field | Recorded (data.md) | Shipped (rollup_rebuild.py) | Match |
|---|---|---|---|
| `RebuildResult` fields | `scope`, `program_id`, `duration_ms`, `event_count` (frozen dataclass) | identical, `@dataclass(frozen=True)` | ✓ |
| `rebuild_program_rollups` signature | `async def rebuild_program_rollups(session: AsyncSession, program_id: str) -> RebuildResult` | identical | ✓ |
| `rebuild_org_rollups` signature | `async def rebuild_org_rollups(session: AsyncSession) -> RebuildResult` | identical | ✓ |

No drift found. Contract conformance: **PASS**.

### Task completion verification (MANDATORY)

Source: `docs/features/BED-03/tasks.json` `tasks[]` (15 tasks, all `status: done`), resolved through `file_plan`.

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/core/db.py` (engine/SessionLocal/`get_db()`) | ✓ | ✓ (all 3 exports present) | PASS |
| T-02 | `services/api/app/main.py` (module-level `engine` import + shutdown disposal) | ✓ | ✓ (`from app.core.db import engine`; `@app.on_event("shutdown")` disposes it) | PASS |
| T-03 | `services/api/app/services/rollup_rebuild.py` (`RebuildResult`, `rebuild_program_rollups`, `rebuild_org_rollups`) | ✓ | ✓ (all present, matches FR-1..FR-5/D-01..D-08) | PASS |
| T-04 | `services/api/app/services/__init__.py` (barrel export) | ✓ | ✓ (`RebuildResult`, `rebuild_org_rollups`, `rebuild_program_rollups` added to `__all__`) | PASS |
| T-05 | `services/api/tests/unit/test_rollup_rebuild_program.py` | ✓ | ✓ (2 tests, TC-01/TC-02 both PASS) | PASS |
| T-06 | `services/api/tests/unit/test_rollup_rebuild_org.py` | ✓ | ✓ (2 tests, TC-03/TC-04 both PASS) | PASS |
| T-07 | `services/api/tests/unit/test_rollup_rebuild_idempotency.py` | ✓ | ✓ (2 tests, TC-05/TC-06 both PASS) | PASS |
| T-08 | `services/api/tests/unit/test_rollup_rebuild_isolation.py` | ✓ | ✓ (2 tests, TC-07/TC-08 both PASS) | PASS |
| T-09 | `services/api/tests/unit/test_rollup_rebuild_contract.py` | ✓ | ✓ (7 tests, TC-09/TC-16 both PASS) | PASS |
| T-10 | `services/api/tests/unit/test_rollup_rebuild_transaction.py` | ✓ | ✓ (3 tests, TC-10 PASS on both `session.begin()` and savepoint paths) | PASS |
| T-11 | `services/api/tests/unit/test_rollup_rebuild_query_plan.py` | ✓ | ✓ (2 tests, TC-11/TC-12 both PASS) | PASS |
| T-12 | `services/api/tests/unit/test_rollup_rebuild_observability.py` | ✓ | ✓ (3 tests, TC-13/TC-14/TC-17 all PASS) | PASS |
| T-13 | `services/api/tests/perf/test_rollup_rebuild_perf.py` | ✓ | ✓ (1 test, TC-15 PASS, budget_pass=true) | PASS |
| T-14 | `services/api/README.md` (add "Session factory" / "Rollup rebuild" sections) | ✓ | ✓ (`## Session factory`, `## Rollup rebuild` headings present, documenting both contracts per PRD § Documentation requirements) | PASS |
| T-15 | `docs/requirements/data.md` (fill `rollup-rebuild` contract with concrete signatures) | ✓ | ✓ (decomposition-time sketch replaced with the exact `RebuildResult`/function signatures — verified byte-for-byte against shipped code in Phase 3b above) | PASS |

15/15 PASS. No `blocked` or `skipped` tasks in `tasks.json`.

### Passed

| TC id | Title |
|---|---|
| BED-03-TC-01 | rebuild_program_rollups fully replaces all 7 program-scoped rollup tables from usage_events alone |
| BED-03-TC-02 | Program-scoped rebuild deletes stale rows for events removed since the prior rebuild (DELETE+INSERT, not UPDATE/upsert) |
| BED-03-TC-03 | rebuild_org_rollups fully replaces all 3 org-scoped rollup tables from usage_events across all programs |
| BED-03-TC-04 | Org-scoped rebuild deletes stale rows for a program's events removed since the prior rebuild |
| BED-03-TC-05 | Rebuild run twice on an unchanged event set produces byte-identical rollup rows |
| BED-03-TC-06 | Duplicate ingest payload is rejected by the unique constraint and the second rebuild is identical to the first (FR-4 mechanics) |
| BED-03-TC-07 | rebuild_program_rollups(P) changes only program P's rollup rows |
| BED-03-TC-08 | Program-scope isolation holds under a wide fan-out of unrelated programs |
| BED-03-TC-09 | rollup_rebuild.py exports the documented signatures, RebuildResult shape, and never constructs its own session |
| BED-03-TC-10 | A failure mid-rebuild rolls back the entire transaction — no partial rebuild visible to a concurrent reader |
| BED-03-TC-11 | rebuild_program_rollups issues exactly one filtered read of usage_events, not one scan per rollup table |
| BED-03-TC-12 | rebuild_org_rollups issues exactly one grouped read across all programs, not one scan per org-scoped table |
| BED-03-TC-13 | Program-scope rebuild emits exactly one rollup_rebuild_completed log line with scope/program_id/duration_ms/event_count |
| BED-03-TC-14 | Org-scope rebuild emits exactly one rollup_rebuild_completed log line with program_id omitted/None |
| BED-03-TC-15 | rebuild_program_rollups completes within 2 seconds for a program with 5,000 usage_events rows |
| BED-03-TC-16 | Rebuild functions log no PII and expose no HTTP route |
| BED-03-TC-17 | rollup_rebuild_completed is emitted end-to-end via the real JSONFormatter seam for both program and org scope |

### Flaky tests

None. Every TC passed on its first attempt (no retries triggered).

### Performance budget breaches

None. TC-15 (`BED-03-NFR-performance`):

- Target: `rebuild_program_rollups()` wall-clock ≤2s for a program with ≤5,000 `usage_events` rows.
- Measured: assertion passed (`elapsed <= 2.000s`, timed around the rebuild call only, excluding the 5,000-row seed insert). Full pytest call-phase (incl. seed insert) = 1.040s.
- `budget_pass: true`.

### Regression coverage

N/A — no TC in `docs/test-cases/BED-03.json` carries a `regression-<id>` tag (this is BED-03's first validation pass).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None — all 17 TCs are `automatable: true`; no `.pending_carry_forward` manual-TC entries generated.

### Budget-unparseable NFRs

None — TC-15's budget parsed cleanly (see above).

### Independently confirmed, already-triaged items (no new action)

- `@app.on_event("shutdown")` DeprecationWarning (`app/main.py:22`) — confirmed present in test output, matches AF-01/D-02, triaged-accepted. Not re-raised.
- `_rebuild_transaction()` savepoint fallback (D-08) — both code paths (`session.begin()` idle-session path and `begin_nested()` savepoint path) independently exercised and PASS in `test_rollup_rebuild_transaction.py`. Matches AF-02, triaged-accepted.
- `mau_series.developer` bucketing (D-07) — confirmed correct in `test_rollup_rebuild_org.py` (`_MAU_ROLE_BUCKET` constant). The stale "flagged for confirmation" docstring wording at `rollup_rebuild.py:388-389` and `test_rollup_rebuild_org.py:15-21` is still present (D-07 settles the decision; the docstrings were not updated to say so) — matches the concurrent code-review's LOW carry-forward finding. Confirmed independently; not re-raised as a new item.
- `design_check` N/A (AF-03) and web render check N/A (AF-04) — both out of scope for this backend-only story; not applicable to this validation run.

### Proof of run

```
### Proof of run

- session_id:        (not exposed to this agent's context)
- bash_invocations:  29
- test_runner_runs:  7  (pytest: unit suite x2, perf suite x3, full-suite regression x1, isolated duration-capture pass x1)
- stack_smoke_runs:  1  (fastapi-2: migrate + start + /health check + clean shutdown)
- duration_seconds:  ~480 (approximate)
- timestamp:         2026-08-27T12:53:17Z
```
