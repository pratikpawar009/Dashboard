# Validation report — SHP-04

- Date: 2026-09-18T10:30:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; invoked from `/arh-implement` Validate ∥ Review gate)
- Total: 11   Passed: 11   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~35 minutes wall-clock

Test cases were NOT pushed as GitHub issues for this story (`push_test_cases: false`); referenced by TC id only, per invocation instructions.

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit/Integ (backend pytest, full suite) | 854 | 854 | 0 | 0 | 0 |
| Unit/Integ (frontend vitest, full suite) | 341 | 341 | 0 | 0 | 0 |
| Contract    |   1   |   1  |   0  |   0   |   0   |
| Integration (SHP-04 TCs) | 6 | 6 | 0 | 0 | 0 |
| Security (SHP-04 TCs) | 3 | 3 | 0 | 0 | 0 |
| Performance (SHP-04 TCs) | 1 | 1 | 0 | 0 | 0 |
| Frontend integration (SHP-04 TCs, Testing-Library) | 2 | 2 | 0 | 0 | 0 |

The 854/341 rows are the full repo suites (`services/api` full `pytest`, `apps/web` full `vitest run`), not SHP-04-specific — included as evidence the story didn't regress anything else. 17 backend tests were `deselected` (Playwright-tagged e2e — deferred per `docs/config/project-commands.yaml` `test_e2e` policy, not a failure).

No contract-conformance findings — `docs/requirements/api.md`'s `artifacts-api` section (bg/color hex, 5-field lock, `count: int`, no-404 behavior, RBAC gate description) matches the live response byte-for-byte, verified against a real running server.

### Task completion verification (MANDATORY)

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/schemas/artifacts.py` (ArtifactItem/ArtifactsResponse schemas) | ✓ | n/a | PASS |
| T-02 | `services/api/app/services/artifacts.py` (fetch_program_artifacts + zero-fill) | ✓ | n/a | PASS |
| T-03 | `services/api/app/api/artifacts.py` (route, governance_visibility gate) | ✓ | n/a | PASS |
| T-04 | `services/api/app/main.py` (router wiring) | ✓ | grep confirms `artifacts_router` include | PASS |
| T-05 | `services/api/tests/unit/test_artifacts_route.py` (TC-01..08) | ✓ | 546 lines, 8 tests, all pass | PASS |
| T-06 | `services/api/tests/unit/test_artifacts_performance.py` (TC-11) | ✓ | 198 lines, 2 tests, all pass | PASS |
| T-07 | `apps/web/src/types/artifacts.ts` | ✓ | n/a | PASS |
| T-08 | `apps/web/src/lib/artifactsApi.ts` (server fetch) | ✓ | n/a | PASS |
| T-09 | `apps/web/src/lib/artifactsApi.client.ts` (client fetch) | ✓ | n/a | PASS |
| T-10 | `apps/web/src/app/api/proxy/artifacts/[program_id]/route.ts` | ✓ | live-verified: compiles, 401 with no cookie, 200-path wired to fetchProgramArtifacts() | PASS |
| T-11 | `apps/web/src/app/api/proxy/artifacts/[program_id]/route.test.ts` | ✓ | status-mapping tests pass | PASS |
| T-12 | `apps/web/src/components/ArtifactsPanel.tsx` + `.module.css` | ✓ | n/a | PASS |
| T-13 | `apps/web/src/components/ArtifactsPanel.test.tsx` (TC-09, TC-10) | ✓ | 177 lines, tests pass | PASS |
| T-14 | `README.md` (docs: add `GET /api/artifacts/{program_id}` row) | ✓ | grep confirms row present with request/response/403/zero-fill cases | PASS |
| T-15 | `docs/design/tokens.md` (add `#fef4e6`, `9px` radius reuse, type steps, persona/artifact shared-ramp note) | ✓ | grep confirms `#fef4e6`, `9px tag-chip use added 2026-09-18 (SHP-04...)`, `13.5px/600` step all present | PASS |

All 15/15 tasks verified PASS. No blocked, no skipped.

### Passed

| TC id | Title |
|-------|-------|
| SHP-04-TC-01 | Fixed 5-row order, server-owned constants, raw-int count |
| SHP-04-TC-02 | architect/product-manager/developer each 200, byte-identical |
| SHP-04-TC-03 | cio AND engineering-manager both 403, no data body |
| SHP-04-TC-04 | governance_visibility logs outcome on both paths, no obsolete event |
| SHP-04-TC-05 | Persona-resolution failure → 403, fail-closed |
| SHP-04-TC-06 | Unknown program_id → 200 all-zero, never 404 |
| SHP-04-TC-07 | Empty program → 200 all-zero, 5 entries |
| SHP-04-TC-08 | Real zero-count row vs missing row render identically |
| SHP-04-TC-09 | ArtifactsPanel non-color-reliant rendering |
| SHP-04-TC-10 | ArtifactsPanel byte-identical across 3 mounts |
| SHP-04-TC-11 | Single indexed query, no N+1, budget met |

### Empirical RBAC verification — the highest-value check (live server, real dev-bypass tokens)

Server: fresh `uvicorn` on port 8010 (idle port, avoided the pre-existing stale server on 8000 which had no `/api/artifacts` route at all — confirmed via `/openapi.json` diff before choosing to boot a fresh instance). Real Postgres on port 5442 (already at Alembic head `009_program_summary_tokens_index` — no migration write was needed or attempted after confirming `alembic current == alembic heads`). Real seeded data used: `program_id='dashboard'` → `prd=4, user_story=45, test_case=274, arch_diagram=0, api_spec=0`, matching documented live values exactly.

| Persona | HTTP status observed | Body |
|---|---|---|
| architect | **200** | full 5-item payload, `count` raw ints |
| product-manager | **200** | byte-identical to architect's body (`diff` confirmed) |
| developer | **200** | byte-identical to architect's body (`diff` confirmed) |
| cio | **403** | `{"error":{"code":"http_403","message":"Forbidden","details":null}}` — no `items` key |
| engineering-manager | **403** | `{"error":{"code":"http_403","message":"Forbidden","details":null}}` — no `items` key |

Unknown `program_id` (`GET /api/artifacts/nonexistent-program-zzz-shp04`, architect token) → **200**, all 5 canonical types at `count: 0`. Never 404.

Log capture (`grep` over the live server's stdout JSON logs) confirmed exactly the FR-2 contract:
```
rbac_check_governance_visibility outcome=authorized persona=architect
rbac_check_governance_visibility outcome=authorized persona=product-manager
rbac_check_governance_visibility outcome=authorized persona=developer
rbac_check_governance_visibility outcome=denied    persona=cio
rbac_check_governance_visibility outcome=denied    persona=engineering-manager
```
Zero occurrences of the obsolete `governance_view_denied` event name across the entire log.

### Stack smoke

| Stack | Sub-step | Status | Notes |
|---|---|---|---|
| fastapi-2 (backend) | migrate → start → healthcheck | **PASS** | Fresh `uvicorn` on port 8010 (avoided colliding with the pre-existing stale server on 8000). `/health` → `{"status":"ok"}` within 1s. `/openapi.json` confirms `/api/artifacts/{program_id}` registered. |
| nextjs (frontend) | start → healthcheck | **PASS** (after one port-collision retry) | First attempt on port 3010 hit a **pre-existing IPv4/IPv6 port collision**: another process (`python3.1`, foreign to this session) was also bound to `:3010`, and `127.0.0.1:3010` resolved to it instead of the Next.js dev server, producing spurious 404s with `server: uvicorn` in the response header — a false-FAIL signature this codebase's own `stack-smoke.md` explicitly warns about. Killed only the Next.js process I had started on 3010 (left the foreign process alone, per instructions to tear down only what I start) and restarted clean on port 3020. `/login` rendered correctly (200, real markup: "AgentRise Harness", "Sign in with SSO"). `/api/proxy/artifacts/dashboard` compiled and responded 401 (no session cookie) — correctly wired to `fetchProgramArtifacts()` against the backend on 8010. |

Both stacks PASS. This is **not** a defect in SHP-04 — it's an environment collision from a concurrent/leftover process on port 3010, unrelated to this story's code, resolved by picking an idle port.

### Flaky tests

None.

### Performance budget breaches

None. SHP-04-TC-11's `budget_pass: true` — a query counter confirmed exactly one `SELECT` against `program_artifacts` per call (no N+1 fan-out); the pytest-level p95 latency is well under the 3000ms panel-render budget. Note: this smoke run measured query-count and in-process latency via the existing test suite (`test_artifacts_performance.py`), not a separate load-test harness against the live server — no k6/locust script exists for this route, matching the story's own test-mapping (TC-11 → pytest, not a dedicated perf tool).

### Regression coverage

N/A — no TC in this manifest carries a `regression-<id>` tag (SHP-04 is new-feature, no prior fix-loop history).

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None. All 11 TCs are `automatable: true`.

### Budget-unparseable NFRs

None.

### Bug-blocks / carry-forward (RETURNED to orchestrator — GATE MODE, not self-written to state.json)

No defects found in SHP-04's own code. One environment observation worth carrying forward as a process note, not a code defect:

```json
{
  "item_id": "SHP-04-ENV-01",
  "kind": "environment_note",
  "reason": "Port 3010 had a foreign process (unrelated python3.1/uvicorn) already bound, causing IPv4 127.0.0.1:3010 to resolve to it instead of a freshly started Next.js dev server -- produced false-404s that looked like a routing defect until traced via response Server header. Recommend validation runs default to higher, less-likely-collided port ranges (302x/801x) rather than the stack-smoke.md default ports when 3000/8000 are already occupied by prior manual sessions.",
  "owner": "validation-agent",
  "added_at": "2026-09-18T10:30:00Z",
  "added_by": "validation-agent/SHP-04 (gate-mode)",
  "resolved_at": null,
  "evidence": "tmp/smoke-web.log (first attempt, port 3010) vs tmp/smoke-web2.log (second attempt, port 3020)"
}
```

No `.pending_carry_forward` entries requiring resolution — this is informational only, does not block PASS verdict, and is returned (not self-written) per GATE MODE rules.

### Proof of run

```
### Proof of run

- session_id:        45eade80-eb56-42a5-933e-ab78cf26e953
- bash_invocations:  ~35
- test_runner_runs:  4 (pytest x2 [targeted SHP-04 + full suite], vitest x3 [full suite + targeted ArtifactsPanel/proxy])
- stack_smoke_runs:  2 (backend :8010 start+healthcheck; frontend :3010 failed-collision retry + :3020 start+healthcheck)
- duration_seconds:  ~2100
- timestamp:         2026-09-18T10:30:00Z
```
