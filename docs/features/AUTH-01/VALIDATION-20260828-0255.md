# Validation report — AUTH-01

- Date: 2026-08-28T02:55:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only, `/arh-implement` Step 2 Validate ∥ Review gate, round 1)
- Source snapshot under judgement: `1a95ab2fa34c30de` (branch `feature/AUTH-01`, nothing committed)
- Total: 41   Passed: 41   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~570s (02:44:50Z → 02:55:00Z)

## Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   3   |   3  |   0  |   0   |   0   |
| Unit         |   1   |   1  |   0  |   0   |   0   |
| Integration  |  26   |  26  |   0  |   0   |   0   |
| E2E          |   0   |   0  |   0  |   0   |   0   |
| Security     |   7   |   7  |   0  |   0   |   0   |
| Contract     |   3   |   3  |   0  |   0   |   0   |
| Performance  |   4   |   4  |   0  |   0   |   0   |

No E2E-typed TCs exist by design (D-03: mock-based integration strategy, `docs/config/project-commands.yaml` `test_e2e: ""` unchanged).

Stack-smoke: 3 stacks exercised for real — `api-development` (:8151, :8152), `api-production` (:8153); each booted the real `create_app()`, hit `/health` (200) and `/auth/*` routes directly (curl), then was killed. `nextjs`/`next` stack verified via `build:` (compile row below), not a served boot (no route change in `apps/web` this story). `postgres` — `bed03-manual` container already running on :5443, used as the pytest `TEST_DATABASE_URL` backing store; not a fresh boot but confirmed live (`docker ps`).

No contract-conformance findings (Phase 3b): the `session` contract (`docs/requirements/auth.md`, `produced_by: AUTH-01`) matches the shipped surface exactly — routes (`GET /auth/login`, `GET /auth/callback`, `POST /auth/refresh`, `POST /auth/dev-bypass`), response shape (`{access_token, refresh_token, expires_in}`, no `Set-Cookie`), claim mapping (`user_id=sub, email, role, groups`), CORS kwargs (`allow_credentials=False`, `allow_methods=[GET,POST,OPTIONS]`, `allow_headers=[Authorization,Content-Type]`), config-completeness gate (501), and dev-bypass allow-list gating (404 outside the allow-list) all independently re-verified against running code, not re-read from docs.

## Task completion verification (MANDATORY)

All 29 tasks in `docs/features/AUTH-01/tasks.json` are `status: done`. Every resolved `files_touched` path exists on disk and its content matches the task's `notes`.

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/pyproject.toml`, `uv.lock` (authlib/respx deps) | ✓ | ✓ (`authlib>=0.15,<1.0` in `[project].dependencies`; `respx>=0.20` in dev group) | PASS |
| T-02 | `services/api/tests/conftest.py` (respx fixtures, JWT builder, spy) | ✓ | ✓ (`KeycloakMock`, `RSATestKeypair`, `build_access_token`, `KeycloakCallSpy` all present, 615 lines) | PASS |
| T-03 | `services/api/app/core/config.py` (OIDC fields + env allow-list) | ✓ | ✓ (`NON_PRODUCTION_ENVIRONMENTS`, `_normalize_environment`, `dev_bypass_enabled`) | PASS |
| T-04 | `services/api/app/auth/__init__.py`, `app/schemas/auth.py` | ✓ | ✓ (`TokenResponse`, `DevBypassRequest`) | PASS |
| T-05 | `services/api/app/auth/jwks.py` (fetch-once cache) | ✓ | ✓ (`JwksCache`, lock-guarded fetch-once) | PASS |
| T-06 | `services/api/app/core/auth.py` (JWT validation dependency) | ✓ | ✓ (`get_current_user`, `_parse_role`, `_parse_programs`) | PASS |
| T-07 | `services/api/app/auth/oidc.py` (login/callback/refresh) | ✓ | ✓ (`/login`, `/callback`, `/refresh` routes present) | PASS |
| T-08 | `services/api/app/auth/dev_bypass.py` | ✓ | ✓ (`_issue_dev_token`, zero Keycloak calls, no dashboard_login) | PASS |
| T-09 | `services/api/app/main.py` (CORS + router wiring) | ✓ | ✓ (`CORSMiddleware`, conditional `dev_bypass_router` include) | PASS |
| T-10 | `services/api/tests/unit/test_auth_config.py` | ✓ | ✓ (TC-12 present, passing) | PASS |
| T-11 | `services/api/tests/unit/test_auth_oidc_login.py` | ✓ | ✓ (TC-01/02/13/14/15 present, passing) | PASS |
| T-12 | `services/api/tests/unit/test_auth_callback.py` | ✓ | ✓ (TC-03/16/26/36 present, passing) | PASS |
| T-13 | `services/api/tests/unit/test_auth_jwt_validation.py` | ✓ | ✓ (TC-04/17/33 present, passing) | PASS |
| T-14 | `services/api/tests/unit/test_auth_groups.py` | ✓ | ✓ (TC-05/18/19/20 present, passing) | PASS |
| T-15 | `services/api/tests/unit/test_auth_refresh.py` | ✓ | ✓ (TC-06/07/21/27/31 present, passing) | PASS |
| T-16 | `services/api/tests/unit/test_auth_dev_bypass.py` | ✓ | ✓ (TC-08/09/10/22/23/24/37/38/39 present, passing) | PASS |
| T-17 | `services/api/tests/unit/test_auth_cors.py` | ✓ | ✓ (TC-11/25 present, passing) | PASS |
| T-18 | `services/api/tests/unit/test_auth_logging_security.py` | ✓ | ✓ (TC-32/35 present, passing) | PASS |
| T-19 | `services/api/tests/unit/test_auth_accessibility_scope.py` | ✓ | ✓ (TC-34 present, passing) | PASS |
| T-20 | `services/api/tests/perf/test_auth_jwks_perf.py` | ✓ | ✓ (TC-28/29 present, passing) | PASS |
| T-21 | `services/api/tests/perf/test_auth_retry_perf.py` | ✓ | ✓ (TC-30 present, passing) | PASS |
| T-22 | `docs/config/project-commands.yaml` (preflight smoke-imports) | ✓ | ✓ (authlib + respx import lines present; re-ran, both resolve: authlib 0.15.6, respx 0.23.1) | PASS |
| T-23 | `README.md` (API section + env-vars table), `services/api/.env.example` | ✓ | ✓ (`/auth/login`, `/auth/callback`, `/auth/refresh`, `/auth/dev-bypass` route rows; `OIDC_CLIENT_ID`/`CORS_ORIGINS` env rows present) | PASS |
| T-24 | `services/api/README.md` (Auth subsection) | ✓ | ✓ (`## Auth`, `### Booting with real Keycloak auth`, `### Signing in locally via dev-bypass`) | PASS |
| T-25 | `docs/how-to/dev-bypass-auth.md` | ✓ | ✓ (100-line walkthrough, § 1–3 present) | PASS |
| T-26 | `services/api/app/core/auth.py` (D-09/D-10) | ✓ | ✓ (`_is_keycloak_system_role`, zero-length-remainder drop in `_parse_programs`) | PASS |
| T-27 | `services/api/app/auth/jwks.py`, `app/auth/dev_bypass.py` (D-08) | ✓ | ✓ (`DEV_BYPASS_KID`, `get_dev_signing_key`, resolved only when `dev_bypass_enabled`) | PASS |
| T-28 | `services/api/tests/unit/test_auth_jwt_validation.py`, `test_auth_groups.py` (D-09/D-10 test flip) | ✓ | ✓ (`test_bare_prefix_group_is_dropped_from_programs_but_kept_in_groups` present, passing) | PASS |
| T-29 | `services/api/app/core/config.py`, `app/auth/oidc.py`, `test_auth_config.py`, `test_auth_oidc_login.py` (D-11) | ✓ | ✓ (`oidc_redirect_uri`, `_resolve_redirect_uri` used at both the authorize-redirect and token-exchange call sites) | PASS |

29/29 tasks verified PASS. No blocked or skipped tasks in this set.

### Passed

| TC id | Title |
|-------|-------|
| AUTH-01-TC-01 | OIDC route registered and reachable when all three OIDC config vars are set |
| AUTH-01-TC-02 | All three OIDC config vars unset at startup -> auth route responds 501, app boots |
| AUTH-01-TC-03 | Callback returns Keycloak's token response as JSON, no Set-Cookie |
| AUTH-01-TC-04 | Valid Bearer JWT verified against JWKS, derives user_id/email/role/groups |
| AUTH-01-TC-05 | groups claim 'program-alpha' parses to 'alpha' |
| AUTH-01-TC-06 | Token refresh with a valid refresh_token returns a new access/refresh pair |
| AUTH-01-TC-07 | Expired/revoked refresh_token returns an error causing re-auth redirect |
| AUTH-01-TC-08 | Dev-bypass issues a bearer token without contacting Keycloak, ENVIRONMENT != production |
| AUTH-01-TC-09 | Dev-bypass unreachable (404) when ENVIRONMENT == 'production' |
| AUTH-01-TC-10 | No dashboard_login (or audit-log) event emitted for a served dev-bypass request |
| AUTH-01-TC-11 | CORS allow-lists configured frontend origin without credentials |
| AUTH-01-TC-12 | Settings schema exposes full OIDC field list with documented types/defaults |
| AUTH-01-TC-13 | oidc_client_id missing alone -> 501, startup does not raise |
| AUTH-01-TC-14 | oidc_client_secret missing alone -> 501, startup does not raise |
| AUTH-01-TC-15 | oidc_issuer missing alone -> 501, startup does not raise |
| AUTH-01-TC-16 | No Set-Cookie header on callback, success or failure |
| AUTH-01-TC-17 | JWT claim-to-field mapping; no server-side persistence |
| AUTH-01-TC-18 | Non-matching groups dropped from program-membership list |
| AUTH-01-TC-19 | Empty groups claim yields empty program-membership list |
| AUTH-01-TC-20 | Missing groups claim yields empty program-membership list |
| AUTH-01-TC-21 | Any non-2xx Keycloak refresh response maps to 401 |
| AUTH-01-TC-22 | ENVIRONMENT='PRODUCTION' (uppercase) gates dev-bypass identically |
| AUTH-01-TC-23 | ENVIRONMENT='Prod' not allow-listed, dev-bypass unreachable |
| AUTH-01-TC-24 | Dev-bypass path never calls dashboard_login (call-count, white-box) |
| AUTH-01-TC-25 | CORSMiddleware registered with exact configured kwargs |
| AUTH-01-TC-26 | Successful callback emits exactly one dashboard_login event (user_id only) |
| AUTH-01-TC-27 | Successful refresh emits exactly one dashboard_login event (user_id only) |
| AUTH-01-TC-28 | JWT validation latency under budget (<10ms warm, <100ms cold) |
| AUTH-01-TC-29 | Unrecognized kid triggers exactly one fresh JWKS fetch |
| AUTH-01-TC-30 | Transient failure retries ≤2x, exponential backoff+jitter, 5s timeout |
| AUTH-01-TC-31 | 4xx response from Keycloak never triggers a retry |
| AUTH-01-TC-32 | Token values never appear in log output; only user_id logged |
| AUTH-01-TC-33 | Forged X-Role/X-Groups header ignored; role/groups from JWT only |
| AUTH-01-TC-34 | No HTML sign-in surface at the backend layer (a11y scope boundary) |
| AUTH-01-TC-35 | All /auth/* routes emit structlog JSON |
| AUTH-01-TC-36 | Callback's expires_in passes through Keycloak's real value unmodified |
| AUTH-01-TC-37 | ENVIRONMENT='staging' not allow-listed, dev-bypass unreachable |
| AUTH-01-TC-38 | Unrecognized ENVIRONMENT typo fails closed |
| AUTH-01-TC-39 | Every allow-listed ENVIRONMENT value keeps dev-bypass reachable |
| AUTH-01-TC-40 | Dev-bypass token accepted by a get_current_user-protected route; rejected in production (regression-AF-05) |
| AUTH-01-TC-41 | Expired access token rejected with 401 on a protected route (regression-AF-11) |

### Flaky tests

None.

### Performance budget breaches

None of the 4 `type: performance` TCs breached a stated numeric budget.

- **AUTH-01-TC-28** — target: cold JWKS fetch < 100ms, mean of 20 warm calls < 10ms. Measured: both internal assertions passed (cold+20-warm call window totalled ~20ms wall-clock in this run, well inside budget). `budget_pass: true`.
- **AUTH-01-TC-30** — target: ≤2 retries, 5s timeout/attempt, ~250ms exponential backoff base with jitter. Measured: exactly 3 total attempts (1 initial + 2 retries), each attempt's timeout confirmed at 5s, backoff sequence confirmed at 0.25s/0.5s (exponential, per pinned formula). `budget_pass: true`.
- **AUTH-01-TC-29** — flagged budget-unparseable below (not a breach).

### Budget-unparseable NFRs

- **AUTH-01-TC-29** (`AUTH-01-NFR-performance`): the TC's `then` clause pins an architectural invariant ("an unrecognized kid triggers exactly one fresh JWKS fetch before the request fails"), not a numeric latency/throughput budget. No `budget_pass` value can be computed against a time/RPS target — recorded as `budget_pass: null`. The TC's own functional assertions (exactly 1 fetch call, request fails 401, no retry loop) passed. Not a coverage gap or regression; worth a note for whoever authors the next NFR-performance TC that this one is really a cache-invalidation-policy check riding on the performance NFR id rather than a latency SLA.

### Regression coverage

Two regression-tagged TCs, both from AF-05 (dev-bypass tokens rejected by every protected route) and AF-11 (two behaviours implemented but unpinned), added by `/arh-implement` during this same implementation round:

| Regression TC | Guards against | Last attempt | Verdict |
|---------------|-----------------|--------------|---------|
| AUTH-01-TC-40 | AF-05 (dev-bypass token 401s on every protected route because it wasn't signed with a JWKS-servable key) | PASS | PASS |
| AUTH-01-TC-41 | AF-11 (no TC exercised an expired ACCESS token on a protected route; TC-07 only covers an expired refresh token on a different route) | PASS | PASS |

Both pass. No re-surfaced regression.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up

None. Every TC in `docs/test-cases/AUTH-01.json` is `automatable: true` — confirmed by direct read of all 41 entries, matching the invocation's stated expectation.

## Acceptance criteria coverage (AUTH-01-AC-1..11)

| AC | Covered by | Verdict |
|----|-----------|---------|
| AC-1 (OIDC route registered/reachable when config complete) | TC-01 | PASS |
| AC-2 (missing config -> 501, no startup crash) | TC-02 | PASS |
| AC-3 (callback returns JSON token body, no Set-Cookie) | TC-03, TC-16 | PASS |
| AC-4 (stateless per-request JWT verification, claim derivation) | TC-04, TC-17 | PASS |
| AC-5 (program-group prefix parsing) | TC-05, TC-18, TC-19, TC-20 | PASS |
| AC-6 (refresh grant success) | TC-06 | PASS |
| AC-7 (refresh failure -> re-auth signal) | TC-07, TC-21 | PASS |
| AC-8 (dev-bypass issues usable token, non-production) | TC-08, **TC-40** (end-to-end against a guarded route — closes AF-05) | PASS |
| AC-9 (dev-bypass unreachable in production) | TC-09, TC-22, TC-23, TC-37, TC-38, **TC-40** (same token 401s under production settings) | PASS |
| AC-10 (no audit-log event for dev-bypass) | TC-10, TC-24 | PASS |
| AC-11 (CORS allow-lists origin, no credentials) | TC-11, TC-25 | PASS |

11/11 ACs covered by passing tests. AC-8/AC-9 (the security-critical pair) were independently re-verified live against the running app (see Runtime confirmation below), not just re-read from the test suite.

## Runtime confirmation (independent of the test suite)

Booted the real `create_app()` three times, hit endpoints with curl / a standalone script, killed each process afterward:

1. **Development** (`ENVIRONMENT=development`, :8151/:8152): `GET /health` → 200; `GET /auth/login` → 501 (no OIDC config, matches config-completeness gate); `POST /auth/dev-bypass` → 200 with a well-formed `{access_token, refresh_token, expires_in}`. With `CORS_ORIGINS=http://localhost:3000` set (:8152), an `OPTIONS` preflight to `/auth/login` with `Origin: http://localhost:3000` returned `Access-Control-Allow-Origin: http://localhost:3000` (echoed, not `*`) and no `Access-Control-Allow-Credentials` header — matches FR-9/AC-11 exactly.
2. **Production** (`ENVIRONMENT=production`, :8153): `GET /health` → 200; `POST /auth/dev-bypass` → 404 (router never registered — D-01 fail-closed); `GET /auth/login` → 501 (OIDC still unconfigured in this run).
3. **AC-8/AC-9/AC-40 end-to-end, not test-suite-mediated**: a standalone script (`create_app(settings_override=...)`, no test fixtures) minted a dev-bypass token under `development` settings, mounted a throwaway `Depends(get_current_user)`-guarded route on that same app instance, and confirmed: the token authenticates (200, `role='qa'`, `programs=['alpha']`, derived only from the token — no Keycloak call made); the identical token presented to a **separately-constructed** `production`-settings app 401s (`invalid_token`) on the same guarded route, and `POST /auth/dev-bypass` on that app 404s. This independently confirms D-08's process-local ephemeral signing key is served by the JWKS cache only under the allow-list, exactly as AF-05's resolution and TC-40 claim — not re-reading TC-40's own mocked assertions, a fresh construction against real code.

No stack-smoke or runtime discrepancy from what the test suite and `state.json .impl_evidence` already claimed.

## Environment

- Preflight: 5/5 commands passed — `pnpm -C apps/web install`, `uv sync`, `import app.main`, `import authlib` (0.15.6), `import respx` (0.23.1).
- `uv run ruff check .` — all checks passed (re-run, independent of evidence packet).
- `uv run mypy .` — Success: no issues found in 70 source files (re-run).
- `pnpm -C apps/web build` — Next.js production build succeeded (re-run; AUTH-01 touches no frontend file, included per anti-gaming rule since `build:` is non-empty repo-wide).
- `pytest` full suite: `TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5443/dashboard_test uv run pytest -q` → **293 passed, 0 failed**, 162 pre-existing `on_event` DeprecationWarnings (expected, predate AUTH-01). Matches the invocation's stated expected result exactly.
- `bed03-manual` Postgres container confirmed live on :5443 before the run (`docker ps`); ports 8151/8152/8153 confirmed free before each boot, killed after.

## Deferred/triaged items (not re-reported as new findings)

Per `docs/features/AUTH-01/state.json .pending_carry_forward` and `FLAGS.md`, already triaged and correctly reflected in the shipped code — spot-checked, no drift found:

- **AF-07** (`OAuth state generated but never verified`, D-12 deferred to pre-pilot) — confirmed `state` is minted in `oidc.py::login` and genuinely unchecked in `callback`; matches the flag's description exactly. Still deferred, not a merge blocker per D-12.
- **AF-09** (`_peek_kid` duplicated between `oidc.py` and `core/auth.py`) — confirmed both copies exist as described; cleanup, no behavioural impact.
- **AF-10** (`_RefreshRequest` lives in `oidc.py`, not `schemas/auth.py`) — confirmed as described; cleanup, no behavioural impact.
- **R-09** (no local Keycloak in docker-compose, E2E deferred) — confirmed no `keycloak` service in `docker-compose.yml`; consistent with D-03's mock-based strategy.

None of these were reported as new validation failures. No evidence the deferrals were applied incorrectly.

## Proof of run

- session_id: not exposed to this agent invocation (no session-id value available in the tool-call context to cite honestly)
- bash_invocations: 46
- test_runner_runs: 5 (1 full-suite `pytest -q`; 2 auth-subset `pytest -v` runs — count + FAILED/ERROR grep; 1 `--junitxml` run; 1 `--durations=0` run)
- stack_smoke_runs: 3 (development :8151, development+CORS :8152, production :8153 — each a real `uvicorn`/`create_app()` boot + curl healthcheck, killed after)
- duration_seconds: ~600
- timestamp: 2026-08-28T02:55:00Z
