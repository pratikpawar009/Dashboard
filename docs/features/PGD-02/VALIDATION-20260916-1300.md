# Validation report — PGD-02 (round 2)

- Date: 2026-09-16T13:00:00Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; no state.json write)
- Total: 18   Passed: 17 (+1 partially deferred, unchanged from round 1)   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~14 minutes wall-clock

## Why this round exists

Round 1 (`docs/features/PGD-02/VALIDATION-20260916-1001.md`) returned **PASS**. Since then `.mcp.json` was
reverted to `origin/main` (it held a literal credential that blocked the review gate) — confirmed via
`git diff origin/main -- .mcp.json` (empty) and `git status` (file not listed as modified). No PGD-02 source
file changed between rounds; `git status` shows the same PGD-02 file set as round 1 (modified: `README.md`,
`apps/web/package.json`, `apps/web/pnpm-lock.yaml`, `ProgramDetailView.{tsx,authFlow.test.tsx}`,
`docs/config/project-commands.yaml`, `docs/requirements/api.md`, `services/api/app/api/overview.py`,
`services/api/app/schemas/program_detail.py`, `services/api/tests/unit/test_format.py`; new files per
`tasks.json` `file_plan`). This run re-executes every phase on the current tree rather than trusting the
prior report.

### Summary

| Layer        | Total | Pass | Fail | Error | Flaky |
|--------------|-------|------|------|-------|-------|
| Stack smoke  |   2   |   2  |   0  |   0   |   0   |
| Unit (FE)    |  22   |  22  |   0  |   0   |   0   |
| Unit/Integration (BE) |  16  |  16  |   0  |   0   |   0   |
| Performance  |   2   |   2  |   0  |   0   |   0   |
| E2E          |   1   |  0 (deferred, author-time verified) | 0 | 0 | 0 |
| Contract conformance |  1  |  1  |  0  |  0  |  0  |

Backend `718 passed, 5 deselected` (identical count to round 1). Frontend `206 passed (206)` across 33 files
(identical count to round 1). Both dev servers (already running from an earlier manual check) reused: API
`/health` → 200, web `/` → 307 redirect to `/overview` (healthy). No new servers started, none torn down.

No contract-conformance findings (Phase 3b).

### Task completion verification

| Task | Target file | Existence | Section match | Verdict |
|------|-------------|-----------|---------------|---------|
| T-01 | `services/api/app/schemas/program_detail.py` (ProgramTokenPoint + ProgramTokenTrendResponse) | ✓ | n/a (code task) | PASS |
| T-02 | `services/api/app/services/program_detail_token_trend.py` (daily series + zero-pad + totals) | ✓ | n/a | PASS |
| T-03 | `services/api/app/api/overview.py` (GET token-trend route) | ✓ | verified live at `/openapi.json` | PASS |
| T-04 | `services/api/tests/unit/test_program_detail_token_trend.py` | ✓ | in backend 718-pass run | PASS |
| T-05 | `services/api/tests/unit/test_program_token_trend_route.py` | ✓ | in backend 718-pass run | PASS |
| T-06 | `services/api/tests/perf/test_program_token_trend_perf.py` | ✓ | 2/2 passing | PASS |
| T-07 | `apps/web/src/types/programTokenTrend.ts` | ✓ | n/a | PASS |
| T-08 | `apps/web/src/lib/programTokenTrendApi.ts` | ✓ | n/a | PASS |
| T-09 | `apps/web/src/lib/programTokenTrendApi.client.ts` | ✓ | n/a | PASS |
| T-10 | `apps/web/src/app/api/proxy/program-detail/[program_id]/token-trend/route.ts` | ✓ | n/a | PASS |
| T-11 | `docs/requirements/api.md` (`### program-token-trend-api` shape filled) | ✓ | heading + concrete fields present, re-verified vs live endpoint this round | PASS |
| T-12 | `apps/web/src/components/DailyTokenTrendChart.tsx` + `.module.css` | ✓ | n/a | PASS |
| T-13 | `apps/web/src/components/ProgramDetailView.tsx` (mount `<DailyTokenTrendChart>`) | ✓ | component mounted; authFlow test still passing (in 206-pass run) | PASS |
| T-14 | `apps/web/src/components/DailyTokenTrendChart.test.tsx` | ✓ | in frontend 206-pass run | PASS |
| T-15 | `.../token-trend/route.test.ts` | ✓ | in frontend 206-pass run | PASS |
| T-16 | `apps/web/playwright.config.ts`, `package.json`, `project-commands.yaml` | ✓ | `test_e2e` still wired | PASS |
| T-17 | `apps/web/e2e/program-token-trend.spec.ts` | ✓ | `playwright test --list` → 1 test collected (re-run this round) | PASS |
| T-18 | `README.md` (token-trend API row) | ✓ | row present, grepped this round | PASS |

All 18/18 tasks PASS. No blocked/skipped tasks.

### Passed

| TC id | Title |
|-------|-------|
| PGD-02-TC-01 | No range param defaults to 30d (integration half PASS; e2e half deferred, see below) |
| PGD-02-TC-02 | range=7d returns exactly 7 points |
| PGD-02-TC-03 | range=30d returns exactly 30 points |
| PGD-02-TC-04 | range=90d returns exactly 90 points |
| PGD-02-TC-05 | Zero-padding: partial-data gaps filled |
| PGD-02-TC-06 | All-empty program returns full zero-padded series |
| PGD-02-TC-07 | Window boundaries inclusive of start/end day |
| PGD-02-TC-08 | period_total/avg_per_day raw ints, not formatted strings |
| PGD-02-TC-09 | avg_per_day = round(period_total / fixed day-count) |
| PGD-02-TC-10 | Range-toggle refresh budget p95 < 2000ms |
| PGD-02-TC-11 | Initial render budget p95 < 3000ms |
| PGD-02-TC-12 | range=15d → explicit 400, not 422 |
| PGD-02-TC-13 | range='' → explicit 400 |
| PGD-02-TC-14 | Byte-identical response across personas (open-aggregate RBAC) |
| PGD-02-TC-15 | Access granted regardless of session.programs membership |
| PGD-02-TC-16 | Missing bearer token → 401 |
| PGD-02-TC-17 | Invalid bearer token → 401 |
| PGD-02-TC-18 | No duplicate dates, exact {date, tokens} shape |

### Flaky tests

None.

### Performance budgets (V3)

### PGD-02-TC-10 — Range toggle refresh (NFR-002)
- Target: p95 < 2000ms
- Measured: PASS (`tests/perf/test_program_token_trend_perf.py::test_range_toggle_refresh_90d_budget_tc10`)
- budget_pass: true

### PGD-02-TC-11 — Initial render (NFR-001)
- Target: p95 < 3000ms
- Measured: PASS (`tests/perf/test_program_token_trend_perf.py::test_initial_render_default_30d_budget_tc11`)
- budget_pass: true

### Regression coverage

None — no `regression-<id>`-tagged TCs in this manifest.

### Failed (bug blocks)

None.

### Errored

None.

### Manual follow-up / deferred

**PGD-02-TC-01 — e2e half deferred (not failed), unchanged from round 1.**

`apps/web/e2e/program-token-trend.spec.ts` (TC-01's e2e half) is NOT executed against a live seeded backend
in this gate-mode run (PLAN.md §7). Author-time re-verification this round:

```
$ pnpm exec playwright test --list
Listing tests:
  program-token-trend.spec.ts:101:7 › Program Detail — daily token consumption range toggle (TC-01) › defaults to 30D on load, then toggles to 7D and 90D
Total: 1 test in 1 file
```

1 test collected, matching round 1. Integration half of TC-01 (backend default-range=30d) fully covered and
PASSING in `test_program_token_trend_route.py`, and independently re-verified live below. No
`.pending_carry_forward` entry needed — plan-level deferred-execution decision, not an unrecorded manual TC.

### Contract conformance (Phase 3b)

Contract: `program-token-trend-api` (`docs/requirements/api.md:154-169`), `produced_by: PGD-02`.

Live endpoint re-verified against the recorded `shape` on the current snapshot:

| Recorded shape | Verified live (this round) |
|---|---|
| `GET /api/overview/program-detail/{program_id}/token-trend?range=` sibling route on `overview` router | Confirmed via `/openapi.json`: path present, `range` query param default `"30d"` |
| `points: list[{date: str, tokens: int}]`, zero-padded to exact 7/30/90 | Confirmed: unknown `program_id` → 200, 30 points, all `tokens: 0` |
| `period_total: int`, `avg_per_day: int`, raw (not formatted) | Confirmed: Python types `int`/`int` on live response |
| `rbac: program_visibility open-aggregate, byte-identical across personas` | Confirmed: developer-token response == cio-token response (byte-diff, this round) |
| `errors: 400 invalid_range (never 422); 401` | Confirmed: `range=15d` → 400; no bearer → 401 |
| `no 404 for unknown program_id` | Confirmed: unknown `program_id` → 200 with all-zero series |

**No conformance findings.** Record and shipped code agree in full.

### Budget-unparseable NFRs

None.

### AC verification (story-level, re-cross-checked against shipped code this round)

| AC | Verified how | Result |
|----|---------------|--------|
| AC-1 (default range=30d) | Live curl, no `range` param → 30 points | PASS |
| AC-2 (exact 7/30/90 counts, `{date,tokens}` shape) | `test_program_token_trend_route.py` (in 718-pass run) + live curl | PASS |
| AC-3 (zero-padding, all-empty → full series) | `test_program_detail_token_trend.py` (in 718-pass run) + live curl on nonexistent program | PASS |
| AC-4 (avg_per_day divides by FIXED range day-count) | unit test + code inspection of `fetch_program_token_trend()` (unchanged since round 1) | PASS |
| AC-5 (range-toggle refresh ≤2s / render ≤3s) | perf tests, both PASS this round | PASS |
| AC-6 (out-of-range → explicit 400, never 422) | `test_program_token_trend_route.py` + live curl `range=15d` → 400 | PASS |
| AC-7 (open-aggregate RBAC, no per-program gating) | live cross-persona byte-diff this round | PASS |

All 7 story ACs re-confirmed against the running stack on the current snapshot.

### Known findings re-confirmed (not re-litigated)

1. **`test_rollup_rebuild_perf.py::test_org_rebuild_budget_and_growth_curve`** — included in this round's
   full `718 passed` run (not deselected); no failure. Pre-existing flake risk noted in orchestrator
   instructions did not reproduce.
2. **FLAGS.md F-01** (proxy collapses backend 400→502) — accepted, not fixed; AC-6 tests target the FastAPI
   route directly, unaffected.
3. **FLAGS.md F-02** (authFlow test mock) — fixed in-scope, confirmed still green this round (206/206).
4. **`design_check`** empty by design — N/A, not a failure.
5. **`.mcp.json`** — confirmed reverted to `origin/main` (byte-identical diff), not part of PGD-02's
   implementation; no PGD-02 source file changed between round 1 and round 2.

## Full command log (verbatim results, round 2)

```
$ git diff origin/main -- .mcp.json                              → (empty — file matches origin/main)
$ git status --short                                              → same PGD-02 file set as round 1

$ curl /health                                                    → 200 {"status":"ok"}
$ curl http://127.0.0.1:3000/                                     → 307 (healthy, redirects to /overview)

$ cd services/api && uv run pytest -q                              → 718 passed, 5 deselected, 202.06s
$ uv run mypy .                                                    → Success: no issues found in 158 source files
$ uv run ruff check .                                              → All checks passed!

$ pnpm -C apps/web test                                            → 33 files, 206 tests passed, 9.96s
$ pnpm exec tsc --noEmit                                           → exit 0, no output
$ pnpm exec eslint .                                                → exit 0, no output
$ pnpm exec playwright test --list                                  → 1 test in 1 file

$ curl POST /auth/dev-bypass (role=developer)                        → token issued
$ curl POST /auth/dev-bypass (role=cio)                              → token issued
$ curl GET .../token-trend (no range, unknown program)                → 200, 30 zero points, period_total=0 int, avg_per_day=0 int
$ curl GET .../token-trend?range=15d                                  → 400
$ curl GET .../token-trend (no auth)                                  → 401
$ curl GET .../token-trend (developer token) vs (cio token)            → byte-identical
$ curl GET /openapi.json | grep token-trend path                       → path present, range default 30d

$ grep "### program-token-trend-api" docs/requirements/api.md            → present, shape unchanged from round 1
$ for f in <11 PGD-02 task target files>; do test -s "$f"; done         → all 11 exist and non-empty
```

### Proof of run

```
### Proof of run

- session_id:        4a6a2c96-fe42-44ce-b06d-b1034a0c7ffe
- bash_invocations:  16
- test_runner_runs:  7 (backend pytest x1, frontend vitest x1, tsc x1, eslint x1, mypy x1, ruff x1, playwright --list x1)
- stack_smoke_runs:  2 (fastapi reused-server healthcheck, nextjs reused-server healthcheck — servers already running, not freshly started)
- duration_seconds:  ~840
- timestamp:         2026-09-16T13:00:00Z
```
