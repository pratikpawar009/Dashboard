# Validation report — OVW-05

- Date: 2026-09-11T07:40:16Z
- Verdict: **PASS**
- Mode: full (GATE MODE — report-only; invoked by `/arh-implement` Validate ∥ Review gate against frozen snapshot `4fe4d04d`)
- Total (automatable TCs): 2   Passed: 2   Failed: 0   Errored: 0   Flaky: 0   Skipped manual: 0
- Duration: ~660s (this validation pass)

## Summary

| Layer                          | Total | Pass | Fail | Error | Flaky |
|--------------------------------|-------|------|------|-------|-------|
| Stack smoke                    |   3   |  3   |  0   |   0   |   0   |
| Unit — web (`pnpm -C apps/web test`, vitest, full repo suite) | 167 | 167 | 0 | 0 | 0 |
| Unit — api (`uv run pytest`, full repo suite) | 590 | 590 | 0 | 0 | 0 |
| Integration (TC-typed — `docs/test-cases/OVW-05.json`) | 2 | 2 | 0 | 0 | 0 |
| E2E                             |   0   |  0   |  0   |   0   |   0   | ← `test_e2e` empty, no framework configured (ADR-0001); legitimately unavailable, flagged AF-03/AF-04 |
| Performance                     |   0   |  0   |  0   |   0   |   0   | ← no `type: performance` TC in this story |

**Static checks (supplementary, not TC layers):** `tsc --noEmit` clean · `eslint .` clean · `mypy .` clean (126 files) · `ruff check .` clean. Backend suite (590 tests) is unaffected by this story (zero `services/api/**` files in `file_plan`) and is run to confirm no cross-cutting regression; it grew from the 507-test baseline recorded at BED-05 (2026-09-09), consistent with intervening merges (AUTH-07, OVW-01), not a validation artefact of this run.

Stack smoke detail: `nextjs` (Run: `pnpm dev --port 3000`, Check: `http://127.0.0.1:3000/`) — PASS; `fastapi-2` (Deps: Postgres on `dashboard-dev5442:5442`, Migrate: `alembic upgrade head` against a fresh ephemeral DB, Run: `uvicorn --port 8000`, Check: `/health`) — PASS; `postgres` connectivity — PASS. Six other `docs/config/stack-smoke.md` sections (`fastapi`, `typescript`, `next`, `pytest`, `alembic`, `pydantic`) are generic-tag duplicates of the two concrete stacks above or `(n/a — ...)` library-only entries — not double-counted, not FAIL. `docs/config/stack-smoke.md`'s hardcoded `5432` in its `Deps:`/`Check:` lines is stale (real port is `5442`, filed AUTH-06 AF-18, open) — worked around by pointing `DATABASE_URL` at the correct port directly; not an OVW-05 defect.

## Task completion verification

| Task | Target file(s) | Existence | Section match | Verdict |
|------|-----------------|-----------|----------------|---------|
| T-01 | `apps/web/src/types/persona.ts`, `formatPersonaTag.ts`, `formatPersonaTag.test.ts` (`cio` fifth persona + `jobTitle` field) | ✓ | ✓ (`cio` in `VALID_PERSONAS`; `PERSONA_DISPLAY.cio` w/ `jobTitle`; test's `it.each` includes `cio` as valid, not throw) | PASS |
| T-02 | `PersonaDashboardShell.tsx`, `.module.css` (org-header variant + sign-out control) | ✓ | ✓ (`.headerRegionOrg`, `.orgTitleRow`, `.pageTitle`, `.orgPill`, `.orgSubtitle`, `.signOutButton` all present; sign-out `<form>` is a sibling of the `signedInUser` ternary, gated only by `!isLoading`) | PASS |
| T-03 | `PersonaDashboardShell.test.tsx` (org-header + sign-out coverage, incl. AC-12 both-branch guard) | ✓ | ✓ (explicit "renders with signedInUser defined" + "renders with signedInUser undefined … AC-12 branch-placement guard" cases, both pass) | PASS |
| T-04 | `types/me.ts`, `lib/meApi.ts`, `meApi.test.ts` | ✓ | ✓ (status-mapping mirrors `overviewApi.ts`: 403→forbidden, 401→unauthorized, else→error) | PASS |
| T-05 | `lib/composeSignedInUser.ts`, `.test.ts` | ✓ | ✓ (decoy-`jobTitle`-key test present and passing — see § Points verified below) | PASS |
| T-06 | `AdoptionOverview.tsx` (forwards persona/signedInUser/pageTitle) | ✓ | ✓ | PASS |
| T-07 | `AdoptionOverview.test.tsx` | ✓ | ✓ ("forwards persona, signedInUser and pageTitle to PersonaDashboardShell unchanged" test present, passing) | PASS |
| T-08 | `app/overview/page.tsx` (wires `GET /api/me`, `Promise.all`, D-03 sentinel) | ✓ | ✓ (no `persona="cio"` literal; existing `redirect("/login")` line unedited) | PASS |
| T-09 | `app/overview/page.test.tsx` (OVW-05-TC-01 harness + redirect regression) | ✓ | ✓ | PASS |
| T-10 | `login/start/route.ts` (+ test); old `login/route.ts` (+ test) deleted | ✓ (new files present; old files confirmed absent) | ✓ | PASS |
| T-11 | `login/page.tsx`, `page.module.css` (branded sign-in + pass-through) | ✓ | ✓ (card `max-width: 400px` per D-06) | PASS |
| T-12 | `login/page.test.tsx` (AC-16..19 + OVW-05-TC-02) | ✓ | ✓ | PASS |
| T-13 | `login/redirectCallSites.test.ts` (5 verified call sites unchanged) | ✓ | ✓ (test passes; also asserts old file gone / new file present) | PASS |
| T-14 | `README.md` (`/login` route-table split) | ✓ | ✓ (line 48 describes the branded page; line 50 describes `/login/start` as the relocated relay) | PASS |
| T-15 | `docs/requirements/api.md` (`persona-shell` contract: `page_title_prop` + `sign_out_control` keys) | ✓ | ✓ (both keys present, additive per D-04 precedent) | PASS |

All 15 tasks: **PASS**. No `blocked` or `skipped` task in `tasks.json` — nothing to exempt.

## Points verified directly (per invocation instructions)

1. **OVW-05-TC-01's composition-not-pass-through invariant** — CONFIRMED structurally: `composeSignedInUser.ts` reads `me.persona` (never `me.jobTitle`) and derives `jobTitle` solely via `formatPersonaTag(me.persona).jobTitle`; the type contract (`types/me.ts::MeData`) has no `jobTitle` field. `composeSignedInUser.test.ts`'s decoy-key test (`jobTitle: "Chief Backend Engineer"` cast through `unknown`) asserts the composed result is `"Chief Information Officer"` — PASS (35ms, isolated run). `overview/page.test.tsx`'s OVW-05-TC-01 case re-proves it at the full page-composition level — PASS (35ms, isolated run). Grepped `AdoptionOverview.tsx`/`page.tsx`: neither reads a `jobTitle` field off any response.
2. **Condition C-2(a)** — `login/redirectCallSites.test.ts` passes; source-scanned (not mocked) against the 5 real files, and independently reconfirmed by this run's full 167-test vitest pass with zero failures across `programs/[program_id]/page.tsx`, `overview/page.tsx`, `callback/route.ts` (×2), `ProgramDetailView.tsx`. `login/route.ts` confirmed absent, `login/start/route.ts` confirmed present.
3. **AC-12 branch-placement guard** — `PersonaDashboardShell.test.tsx` has two explicit cases: `signedInUser` defined and `signedInUser` undefined, both asserting the "Sign out" button renders; both pass. Source confirms the `<form>` sits outside the `signedInUser ? … : …` ternary, gated only by `!isLoading`.
4. **D-03 sentinel** — confirmed structurally: `result` (fed to `OverviewErrorPanel`) is derived only from the `overview-summary` call; the `meResult` branch only sets `persona`/`signedInUser`. Test "renders the summary content normally while the shell shows the neutral 'Persona unavailable' badge, and no OverviewErrorPanel" passes (9ms).
5. **NFR-Security, no hardcoded `persona="cio"`** — grepped `overview/page.tsx`: no such literal exists (`persona` is always `meResult.data.persona` or the `PERSONA_RESOLUTION_ERROR` sentinel). Test "passes through whatever persona GET /api/me resolves — never a hardcoded persona='cio' literal (NFR-Security)" passes (15ms).

## Contract conformance (Phase 3b)

`persona-shell` is the only contract in `docs/requirements/api.md` with `produced_by` containing `OVW-05` (co-producer with `SHP-01`). Checked shipped `PersonaDashboardShell.tsx` against the contract's `page_title_prop` and `sign_out_control` shape entries (added by T-15):

- `page_title_prop`: selection guard is `!isLoading && pageTitle !== undefined && program === undefined` in source — matches recorded shape exactly; no `persona`-literal conditional found anywhere in the file (grepped).
- `sign_out_control`: shipped `<form action="/logout" method="get"><button type="submit" aria-label="Sign out">Sign out</button></form>`, sibling of the `signedInUser` ternary — byte-matches the recorded shape.

Also spot-checked `session-identity-api` (produced_by `AUTH-07`, consumed by OVW-05): shipped `services/api/app/schemas/me.py::MeResponse` (`{name: str | None, persona: str}`, `extra="forbid"`) matches `apps/web/src/types/me.ts::MeData` exactly.

**No conformance findings.**

## Passed

| TC id         | Title                                                                 |
|---------------|------------------------------------------------------------------------|
| OVW-05-TC-01  | jobTitle composed frontend-side from PERSONA_DISPLAY, never passed through from GET /api/me |
| OVW-05-TC-02  | Already-authenticated visitor at /login redirected server-side to /overview, no sign-in action shown |

## Flaky tests

None.

## Performance budget breaches

N/A — no `type: performance` TC in `docs/test-cases/OVW-05.json`.

## Regression coverage

N/A — no `regression-<id>`-tagged TC exists yet for this story (first validation pass).

## Failed (bug blocks)

None.

## Errored

None.

## Manual follow-up

None triggered by this run — both TCs are `automatable: true` with recorded PASS results. Two **pre-existing, already-tracked** items are restated here (not new findings, not blocking this verdict) so they surface at this gate rather than only in `FLAGS.md`:

- **AF-04 (open)** — no browser/E2E tool is installed, so component tests prove DOM structure but not real-browser paint. This run performed a live stack-smoke `curl` against the running `/login` page and confirmed the raw HTML contains "AgentRise Harness", "AI SDLC Governance", and "Sign in with SSO" — partial mitigation at the HTTP-response level, not a substitute for an authenticated visual check of `/overview`'s identity block + org header + sign-out control (no dev-bypass session was established in this run). Recommend a human eyeball those before merge, per AF-04's own recommendation.
- **AF-02 (open)** — `PersonaHeader.tsx`'s docstring still frames `cio` reaching it as an invariant violation; stale since T-01 admitted `cio`. Comment-only, no behavioural defect, not in this story's `file_plan`, carried forward to whichever story next touches that file.
- **DQA-1 (recorded in `DESIGN.md`/`REQUIREMENTS.md`)** — inherited `#8a93a1` @ 3.10:1 contrast failure (fails AA) now also reaches `/login`, a pre-auth surface, for the first time. Fix has global-token blast radius; recorded, not fixed, per the story's own Product Gate exception.

## Budget-unparseable NFRs

N/A — no performance NFR budget in scope for this story's TCs.

## Coverage gap (recorded exception, not a validation failure)

`docs/test-cases/OVW-05.json` carries 2 test cases against 26 requirement ids (AC-6, AC-18) — **24 uncovered, 7.7% coverage**. This is a user-directed hard cap recorded in the manifest's own `_note`/`coverage_audit.note` and approved at the Product Gate (`REQUIREMENTS.md` § "Approved with a recorded exception") — **not treated as a defect of this implementation**. Both shipped TCs target the story's highest-risk surfaces (the sole HIGH research risk, and the `/login` routing split) and both PASS. The 24 uncovered ids (AC-1..5,7-17,19; FR-1..3; all 4 NFR topics) are exercised only by the unit/component test layer in `tasks.json` (T-01, T-03, T-04, T-06/T-07, T-08/T-09 partial, T-12, T-13), which this run's full 167-test vitest pass also confirms green, but which are not TC-enumerated per this skill's unit/behavioural split. Two specific defects the PRD itself flagged as "shipping untested" (AC-12 branch placement) were separately re-verified directly above and found correctly implemented and now test-covered by T-03 — so the highest-named risk in the coverage gap is closed even though it isn't reflected as a dedicated TC-manifest entry.

## Proof of run

```
### Proof of run

- session_id:        unavailable — no Claude Code session id is exposed to this agent's tool environment
- bash_invocations:  ~79
- test_runner_runs:  7  (6 × vitest: full suite, isolated OVW-05-TC-01/composeSignedInUser, isolated OVW-05-TC-02, 9-file targeted, overview/page verbose, login/page verbose; 1 × pytest full suite — 590 passed in 162.03s)
- stack_smoke_runs:  3  (postgres connectivity; fastapi-2 migrate+start+healthcheck; nextjs start+healthcheck)
- duration_seconds:  ~660
- timestamp:         2026-09-11T07:40:16Z
```
